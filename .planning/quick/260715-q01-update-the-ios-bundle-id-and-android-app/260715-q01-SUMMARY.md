---
phase: 260715-q01
plan: 01
subsystem: infra
tags: [ios, android, xcodeproj, gradle, kotlin, app-groups, bundle-id, share-extension]

# Dependency graph
requires:
  - phase: share-extension-setup
    provides: App Group entitlements (Runner + ShareExtension) keyed on the example bundle id
provides:
  - iOS Runner/RunnerTests bundle id renamed to com.openwanderer.wanderer
  - Both entitlements files' App Group renamed to group.com.openwanderer.wanderer
  - Android applicationId + namespace renamed to com.openwanderer.wanderer
  - Kotlin MainActivity relocated to com/openwanderer/wanderer package
affects: [ios-share-extension-manual-xcode-setup, android-release-signing, app-store-listing]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - app/ios/Runner.xcodeproj/project.pbxproj
    - app/ios/Runner/Runner.entitlements
    - "app/ios/Share Extension/ShareExtension.entitlements"
    - app/ios/SHARE_EXTENSION_SETUP.md
    - app/android/app/build.gradle.kts
    - app/android/app/src/main/kotlin/com/openwanderer/wanderer/MainActivity.kt

key-decisions:
  - "Pure find/replace rename, no Dart/SvelteKit changes - scope stayed exactly as planned"

patterns-established: []

requirements-completed: [q01-bundle-rename]

# Metrics
duration: ~10min
completed: 2026-07-15
---

# Quick Task 260715-q01: Update iOS bundle id and Android app id Summary

**Renamed the mobile app's native identity from `com.example.wanderer` to `com.openwanderer.wanderer` across iOS pbxproj/entitlements and Android Gradle/Kotlin package, keeping host-app and Share Extension App Group ids in sync.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-07-15T16:38:00Z
- **Completed:** 2026-07-15T16:48:04Z
- **Tasks:** 2 completed
- **Files modified:** 6 (5 modified + 1 renamed)

## Accomplishments
- iOS `Runner` and `RunnerTests` targets now build under `com.openwanderer.wanderer` (6 `PRODUCT_BUNDLE_IDENTIFIER` lines across build configs, `.RunnerTests` suffix preserved)
- Both `Runner.entitlements` and `ShareExtension.entitlements` share the App Group `group.com.openwanderer.wanderer`, so the host app and Share Extension can share their container once the extension target is registered in Xcode
- `SHARE_EXTENSION_SETUP.md` instructional text updated to reference the new identifiers
- Android `applicationId`/`namespace` renamed to `com.openwanderer.wanderer`; `MainActivity.kt` moved via `git mv` to `kotlin/com/openwanderer/wanderer/MainActivity.kt` with matching `package` declaration; old `com/example` tree removed
- Repo-wide sweep confirms zero remaining `com.example.wanderer` occurrences under tracked `app/` source (generated, gitignored `app/build/` excluded)

## Task Commits

Each task was committed atomically:

1. **Task 1: Rename iOS bundle id and App Group ids** - `40cf3a69` (feat)
2. **Task 2: Rename Android applicationId + namespace, relocate Kotlin package, and final sweep** - `8022ae97` (feat)

_Note: `AndroidManifest.xml` was left untouched per plan instructions — `android:name=".MainActivity"` resolves relative to the updated `namespace` automatically. It was already modified by unrelated share-sheet intent-filter work pre-dating this task and was not staged._

## Files Created/Modified
- `app/ios/Runner.xcodeproj/project.pbxproj` - 6 `PRODUCT_BUNDLE_IDENTIFIER` lines renamed (3 Runner, 3 RunnerTests with suffix preserved)
- `app/ios/Runner/Runner.entitlements` - App Group renamed to `group.com.openwanderer.wanderer`
- `app/ios/Share Extension/ShareExtension.entitlements` - App Group renamed to `group.com.openwanderer.wanderer`
- `app/ios/SHARE_EXTENSION_SETUP.md` - instructional bundle id / App Group text updated
- `app/android/app/build.gradle.kts` - `namespace` and `applicationId` renamed to `com.openwanderer.wanderer`
- `app/android/app/src/main/kotlin/com/openwanderer/wanderer/MainActivity.kt` - relocated (via `git mv`) from `com/example/wanderer`, `package` declaration updated

## Decisions Made
None - plan executed exactly as written; pure identifier rename, no architectural changes.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required. Note: the Share Extension target itself is still not registered in Xcode (per `SHARE_EXTENSION_SETUP.md`, a pre-existing manual step unrelated to this rename); once registered, it will pick up the already-renamed `com.openwanderer.wanderer` App Group automatically.

## Next Phase Readiness
- Bundle id / App Group rename is complete and internally consistent between host app and Share Extension entitlements.
- No blockers. Existing unrelated uncommitted work (Podfile, Info.plist, AndroidManifest.xml share-sheet intent filters, main.dart, trail_import_util.dart, settings_account_screen.dart, trail_source_select_screen.dart, pubspec.yaml/lock) is untouched by this quick task and remains as it was found.

---
*Phase: 260715-q01*
*Completed: 2026-07-15*

## Self-Check: PASSED

All created/modified files verified present on disk; both task commits (`40cf3a69`, `8022ae97`) verified present in git log.
