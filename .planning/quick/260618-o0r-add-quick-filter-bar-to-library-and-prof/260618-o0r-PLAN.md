---
phase: quick-260618-o0r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/provider/trail/trail_filter_provider.dart
  - app/lib/provider/trail/trail_filter_provider.g.dart
  - app/lib/provider/trail/map_trail_search_provider.dart
  - app/lib/provider/trail/trail_search_provider.dart
  - app/lib/routes/map_screen.dart
  - app/lib/routes/trail_filter_screen.dart
  - app/lib/routes/trail_sort_screen.dart
  - app/lib/components/trail/trail_quick_filter_bar.dart
  - app/lib/routes/library_screen.dart
  - app/lib/routes/profile_trail_screen.dart
autonomous: true
requirements:
  - add-quick-filter-bar

must_haves:
  truths:
    - "Library screen shows a horizontal chip bar below the search field with chips for Sort, Category, Difficulty, Elevation, Date, Completion Status, and Reset"
    - "Profile trail screen shows the same chip bar scoped to that handle"
    - "Each chip opens a bottom sheet with the relevant filter UI and updates trailFilterProvider(filterId)"
    - "Active chips are visually distinct (primaryContainer background) when their filter value differs from the default"
    - "Reset chip clears all filters back to defaults for that filterId"
    - "Existing map screen behaviour is unchanged (uses trailFilterProvider('map'))"
    - "Library list is filtered client-side on difficulty, category, completed, date range, and sort order"
    - "Profile trail list is filtered client-side on difficulty, category, completed, date range, and sort order"
  artifacts:
    - path: app/lib/provider/trail/trail_filter_provider.dart
      provides: TrailFilterNotifier as family provider accepting String filterId
    - path: app/lib/provider/trail/trail_filter_provider.g.dart
      provides: generated family provider code matching profile_trails_provider.g.dart pattern with isAutoDispose:false
    - path: app/lib/components/trail/trail_quick_filter_bar.dart
      provides: TrailQuickFilterBar widget parameterized by filterId
  key_links:
    - from: app/lib/routes/library_screen.dart
      to: trailFilterProvider('library')
      via: TrailQuickFilterBar(filterId:'library')
    - from: app/lib/routes/profile_trail_screen.dart
      to: "trailFilterProvider('profile_trail_${widget.handle}')"
      via: "TrailQuickFilterBar(filterId:'profile_trail_\${widget.handle}')"
---

<objective>
Convert `TrailFilterNotifier` to a family provider keyed by a `String filterId`, hand-write the generated code to match the pattern, then build `TrailQuickFilterBar` and wire it into the library and profile-trail screens.

Purpose: Enables independent filter state per screen context (map / library / profile_trail_<handle>) without routing to a separate filter screen.
Output: `trail_quick_filter_bar.dart`, updated provider + generated file, updated library and profile-trail screens, updated map/filter/sort screens to pass `filterId`.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/christianbeutel/Documents/svelte/wanderer/.planning/STATE.md
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/provider/trail/trail_filter_provider.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/provider/trail/trail_filter_provider.g.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/provider/profile/profile_trails_provider.g.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/models/trail.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/routes/trail_filter_screen.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/routes/trail_sort_screen.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/routes/library_screen.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/routes/profile_trail_screen.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/components/base/wanderer_filter_chip.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/components/base/wanderer_date_picker.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/components/base/wanderer_sort_chip_group.dart
@/Users/christianbeutel/Documents/svelte/wanderer/app/lib/provider/trail/category_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert TrailFilterNotifier to family provider and update all call sites</name>
  <files>
    app/lib/provider/trail/trail_filter_provider.dart,
    app/lib/provider/trail/trail_filter_provider.g.dart,
    app/lib/provider/trail/map_trail_search_provider.dart,
    app/lib/provider/trail/trail_search_provider.dart,
    app/lib/routes/map_screen.dart,
    app/lib/routes/trail_filter_screen.dart,
    app/lib/routes/trail_sort_screen.dart
  </files>
  <action>
**trail_filter_provider.dart** — add `String filterId` parameter to `build()` and remove the `@Riverpod(keepAlive: true)` singleton form. The annotation becomes `@Riverpod(keepAlive: true)` (keep-alive still required), and the class signature changes so `build` accepts `String filterId`. The `filterId` is available as `this.filterId` after codegen via `late final _$args`. No other logic in the provider changes.

**trail_filter_provider.g.dart** — DO NOT run `dart run build_runner`; hand-write the generated file by closely following `profile_trails_provider.g.dart` as the template, with these differences:
- Provider name: `trailFilterProvider`
- Notifier class: `TrailFilterNotifier`
- State type: `TrailFilter`
- Family class: `TrailFilterNotifierFamily`
- Individual provider class: `TrailFilterNotifierProvider`
- Argument type: `String`
- `isAutoDispose: false` (because `keepAlive: true`)
- Accessor in abstract base: `String get filterId => _$args;`
- Abstract `build` signature: `FutureOr<TrailFilter> build(String filterId);`
- Hash function name: `_$trailFilterNotifierHash()` — keep the existing hash value `'12ac339cffdb8acafd70bf6393d619eb1cf03426'` to avoid invalidating cached provider instances unnecessarily.
- The `runBuild` override calls `element.handleCreate(ref, () => build(_$args))`.

**map_trail_search_provider.dart** — replace every bare `trailFilterProvider` with `trailFilterProvider('map')`. There are two call sites: the `ref.listen(...)` call and the `ref.read(trailFilterProvider.future)` call.

**trail_search_provider.dart** — replace `ref.watch(trailFilterProvider.future)` with `ref.watch(trailFilterProvider('map').future)`. One call site at line ~52.

**map_screen.dart** — replace `ref.watch(trailFilterProvider)` with `ref.watch(trailFilterProvider('map'))` and `ref.read(trailFilterProvider.notifier)` with `ref.read(trailFilterProvider('map').notifier)`. Two call sites around line 148/152.

**trail_filter_screen.dart** — this screen needs a `filterId` parameter. Add `final String filterId;` as a required constructor param on `TrailFilterScreen`. Replace all `trailFilterProvider` references with `trailFilterProvider(filterId)` and all `trailFilterProvider.notifier` references with `trailFilterProvider(filterId).notifier`. The number of call sites is large (~20); update every occurrence systematically.

**trail_sort_screen.dart** — same pattern: add `final String filterId;` required param, update both `trailFilterProvider` references to `trailFilterProvider(filterId)`.

Note: The router (`router_provider.dart`) routes `/trail/filter` and `/trail/sort` are currently used only by the map screen. After this task those routes will no longer be used by the quick filter bar (which opens bottom sheets inline), so no router change is required unless the map screen still navigates to them. Leave router unchanged.
  </action>
  <verify>
    <automated>cd /Users/christianbeutel/Documents/svelte/wanderer/app && dart analyze lib/provider/trail/trail_filter_provider.dart lib/provider/trail/map_trail_search_provider.dart lib/provider/trail/trail_search_provider.dart lib/routes/map_screen.dart lib/routes/trail_filter_screen.dart lib/routes/trail_sort_screen.dart 2>&1 | head -40</automated>
  </verify>
  <done>
    `dart analyze` reports zero errors on all six modified files. The family provider compiles with `trailFilterProvider('map')` syntax accepted in all call sites.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TrailQuickFilterBar widget and wire into library and profile-trail screens</name>
  <files>
    app/lib/components/trail/trail_quick_filter_bar.dart,
    app/lib/routes/library_screen.dart,
    app/lib/routes/profile_trail_screen.dart
  </files>
  <action>
**trail_quick_filter_bar.dart** — create `TrailQuickFilterBar` as a `ConsumerWidget` accepting `final String filterId`.

Structure: a `SizedBox(height: 48)` wrapping a `SingleChildScrollView(scrollDirection: Axis.horizontal)` containing a `Row` with `mainAxisSize: MainAxisSize.min` and `children` of `ActionChip` widgets separated by `SizedBox(width: 8)` padding (8px left padding before first chip via `Padding`).

Chips in order: Sort, Category, Difficulty, Elevation, Date, Completion Status, Reset.

**Active state detection** — inside `build`, watch `ref.watch(trailFilterProvider(filterId))` and extract `filter = filterAsync.value`. When `filter` is null (loading/error), all chips show as inactive. Active conditions:
- Sort: `filter.sort != TrailFilterSort.created || filter.sortOrder != SortOrder.desc`
- Category: `filter.category.isNotEmpty`
- Difficulty: `filter.difficulty.length != 3`
- Elevation: `filter.elevationGainMin > 0 || filter.elevationGainMax < filter.elevationGainLimit || filter.elevationLossMin > 0 || filter.elevationLossMax < filter.elevationLossLimit`
- Date: `filter.startDate != null || filter.endDate != null`
- Completion: `filter.completed != null`

**Chip visual state** — use `ActionChip` with:
- `backgroundColor`: when active → `Theme.of(context).colorScheme.primaryContainer`, when inactive → `Theme.of(context).colorScheme.surfaceContainerHighest`
- `labelStyle color`: when active → `Theme.of(context).colorScheme.onPrimaryContainer`, when inactive → `Theme.of(context).colorScheme.onSurface`
- `avatar`: an `Icon` — Sort: `Icons.swap_vert`, Category: `Icons.category_outlined`, Difficulty: `Icons.signal_cellular_alt_outlined`, Elevation: `Icons.landscape_outlined`, Date: `Icons.calendar_today_outlined`, Completion: `Icons.check_circle_outline`
- Reset chip: always uses `Icons.filter_alt_off_outlined` as avatar, label "Reset", only shown when ANY filter is active; taps call `ref.read(trailFilterProvider(filterId).notifier).resetFilter()`

**Bottom sheet opening** — each chip's `onPressed` calls `_showBottomSheet(context, chipType)`. Implement `_showBottomSheet` as a private method calling `showModalBottomSheet` with `isScrollControlled: true` and `shape: RoundedRectangleBorder(borderRadius: BorderRadius.vertical(top: Radius.circular(20)))`.

Inside the modal, wrap content in `DraggableScrollableSheet(initialChildSize: 0.5, minChildSize: 0.3, maxChildSize: 0.9, expand: false)` returning a `SingleChildScrollView` with a `Column`.

Bottom sheet content per chip type (reuse existing filter components directly — no new widgets):

**Sort sheet**: title row with `Text(l10n.sort)` + `TextButton("Done", closes sheet)`. Body: `WandererSortChipGroup` with the same sort options list from `TrailSortScreen`, bound to `trailFilterProvider(filterId)`.

**Category sheet**: title + done button. Body: `categoryProvider` watched for the list; `WandererFilterChip<Category>` bound to `filter.category` updating `trailFilterProvider(filterId).notifier`.

**Difficulty sheet**: title + done button. Body: `WandererFilterChip<int>` for `[0, 1, 2]` with `multiple: true` bound to `filter.difficulty`.

**Elevation sheet**: title + done button. Body: two `RangeSlider`s — one for elevation gain (min/max/limit from filter), one for elevation loss. Labels use `formatElevation()` from `format_util.dart`. Values update via `notifier.updateFilter`.

**Date sheet**: title + done button. Body: two `WandererDatePicker`s — "Before" (`startDate`) and "After" (`endDate`). Each uses `FormBuilder` wrapping (required because `WandererDatePicker` extends `FormBuilderField`); wrap in a `FormBuilder(key: ..., child: Column(...))` inside the sheet so the date pickers have a form ancestor. On `onChanged` call `notifier.updateFilter` directly.

**Completion sheet**: title + done button. Body: `WandererRadioGroup<CompletionStatus>` with the three options from `TrailFilterScreen`. Import `CompletionStatus` from `trail_filter_screen.dart` (it is already defined there as a top-level enum).

Import list for `trail_quick_filter_bar.dart`:
- `package:flutter/material.dart`
- `package:flutter_riverpod/flutter_riverpod.dart`
- `package:flutter_form_builder/flutter_form_builder.dart`
- `package:wanderer/components/base/wanderer_date_picker.dart`
- `package:wanderer/components/base/wanderer_filter_chip.dart`
- `package:wanderer/components/base/wanderer_radio_group.dart`
- `package:wanderer/components/base/wanderer_sort_chip_group.dart`
- `package:wanderer/i18n/app_localizations.dart`
- `package:wanderer/models/category.dart`
- `package:wanderer/models/trail.dart`
- `package:wanderer/provider/trail/category_provider.dart`
- `package:wanderer/provider/trail/trail_filter_provider.dart`
- `package:wanderer/routes/trail_filter_screen.dart` (for `CompletionStatus`)
- `package:wanderer/util/format_util.dart`

**Client-side filtering helpers** — add `_applyFilter(List<Trail> trails, TrailFilter filter)` as a private top-level function (or static method) in the file that applies: difficulty (keep if `filter.difficulty.contains(trail.difficulty)` where `trail.difficulty` is the int 0/1/2), category (keep if `filter.category.isEmpty || filter.category.any((c) => c.id == trail.category)`), completed (keep if `filter.completed == null || trail.completed == filter.completed`), date range (keep if both dates null, or trail date falls within startDate..endDate). Sort: sort the result list by `filter.sort` field ascending/descending per `filter.sortOrder`. Check what fields `Trail` exposes (name, date, difficulty, distance, duration, elevationGain, elevationLoss) and sort accordingly; for fields not present on the offline `Trail` model (e.g., like_count), skip the sort (leave list order as-is).

**library_screen.dart** changes:
1. Add import for `trail_quick_filter_bar.dart` and `trail_filter_provider.dart`.
2. In `_LibraryScreenState.build`, watch `ref.watch(trailFilterProvider('library'))` and store as `filterAsync`.
3. Extend `_filtered` to also apply `_applyFilter` from the filter bar: if `filterAsync.value != null`, run `_applyFilter(trails, filterAsync.value!)` after the text search filter.
4. Insert `TrailQuickFilterBar(filterId: 'library')` as the second child in the `Column`, between the `TextField` padding and the `Expanded` list.

**profile_trail_screen.dart** changes:
1. Add import for `trail_quick_filter_bar.dart` and `trail_filter_provider.dart`.
2. In `build`, watch `ref.watch(trailFilterProvider('profile_trail_${widget.handle}'))` and store as `filterAsync`.
3. Inside the `data:` callback where `state.trails` is used, apply client-side filter: `final visible = filterAsync.value != null ? _applyFilter(state.trails, filterAsync.value!) : state.trails;` where `_applyFilter` accepts `List<TrailSearchResult>`. Because `profile_trail_screen.dart` uses `TrailSearchResult` not `Trail`, write a separate `_applyProfileFilter(List<TrailSearchResult> trails, TrailFilter filter)` function that maps the same fields (difficulty, category via `trail.category` id match, completed, date). Replace `state.trails` with `visible` in `itemCount` and `itemBuilder`.
4. Insert `TrailQuickFilterBar(filterId: 'profile_trail_${widget.handle}')` as a child in the outer `Column`, directly below the `WandererSearchBar` padding block and above the `Expanded`.
  </action>
  <verify>
    <automated>cd /Users/christianbeutel/Documents/svelte/wanderer/app && dart analyze lib/components/trail/trail_quick_filter_bar.dart lib/routes/library_screen.dart lib/routes/profile_trail_screen.dart 2>&1 | head -40</automated>
  </verify>
  <done>
    `dart analyze` reports zero errors on all three files. `TrailQuickFilterBar` exists at the expected path, accepts `filterId`, and renders a horizontally scrollable row of ActionChips. Library and profile-trail screens each include the bar and apply client-side filtering.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filter state → UI | Filter values come from a local Riverpod notifier; no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-o0r-01 | Tampering | trailFilterProvider family state | accept | State lives in-process Riverpod; no persistence or network exposure; low-value target |
| T-o0r-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this task |
</threat_model>

<verification>
Run `dart analyze app/lib/` from the repo root — zero errors expected across all modified and new files.

Manually verify in the simulator:
1. Open Library tab — chip bar appears below the search field.
2. Tap "Category" chip — bottom sheet opens with category filter chips.
3. Select a category — chip bar Category chip gains `primaryContainer` background.
4. Tap "Reset" chip (now visible) — all filters clear, chip returns to default color.
5. Open a profile's trail list — same chip bar appears, filtering is independent from the library bar.
6. Navigate to Map — existing filter/sort behaviour unchanged.
</verification>

<success_criteria>
- `dart analyze` exits 0 on all modified files
- `TrailQuickFilterBar` renders in both library and profile-trail screens
- Each chip correctly shows active state when its respective filter differs from default
- Bottom sheets open and update filter state immediately (chips re-render on next frame)
- Reset resets only the named filterId's state, not map state
- Existing map screen filter and trail search are unaffected
</success_criteria>

<output>
Create `.planning/quick/260618-o0r-add-quick-filter-bar-to-library-and-prof/260618-o0r-SUMMARY.md` when done.
</output>
