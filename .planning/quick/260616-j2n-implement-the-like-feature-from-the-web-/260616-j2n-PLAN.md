---
phase: quick-260616-j2n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/provider/trail/trail_provider.dart
  - app/lib/components/trail/like_button.dart
  - app/lib/components/trail/trail_panel.dart
autonomous: true
requirements: [LIKE-01]

must_haves:
  truths:
    - "User sees a heart-shaped like button in the trail panel with the total like count beneath it"
    - "Tapping the heart when the trail is not yet liked creates a like; the count increments and the heart fills red"
    - "Tapping the heart when the trail is already liked removes the like; the count decrements and the heart returns to its unfilled state"
    - "The heart shows a red fill when the current user has liked the trail"
  artifacts:
    - path: "app/lib/provider/trail/trail_provider.dart"
      provides: "like() and unlike() methods on TrailNotifier that call the trail-like API and update state"
      contains: "Future<void> like"
    - path: "app/lib/components/trail/like_button.dart"
      provides: "LikeButton ConsumerWidget rendering heart icon + count and toggling like state"
      contains: "class LikeButton"
    - path: "app/lib/components/trail/trail_panel.dart"
      provides: "LikeButton mounted in the trail panel stats area"
      contains: "LikeButton("
  key_links:
    - from: "app/lib/components/trail/like_button.dart"
      to: "trailProvider(trail.id).notifier"
      via: "ref.read(...).like()/.unlike()"
      pattern: "trailProvider\\(.*\\)\\.notifier"
    - from: "app/lib/provider/trail/trail_provider.dart"
      to: "/trail-like and /trail-like/delete"
      via: "dio api.put / api.post"
      pattern: "trail-like"
---

<objective>
Add the web "like" feature to the Flutter trail panel: a heart-shaped button showing the total like count, which toggles the current user's like for the trail and fills red when liked.

Purpose: Reach feature parity with the web app so a hiker can like trails from mobile.
Output: like()/unlike() methods on TrailNotifier, a new LikeButton widget, and the button wired into the trail panel.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@app/lib/provider/trail/trail_provider.dart
@app/lib/components/trail/trail_panel.dart
@app/lib/components/trail/stat_chip.dart
@app/lib/models/trail.dart
@app/lib/models/trail_like.dart
@app/lib/provider/api_provider.dart
@app/lib/provider/auth_provider.dart

# Existing facts (no changes needed to these):
# - Trail has `likeCount` (int, @JsonKey 'like_count') and `expand.trailLikeViaTrail` (List<TrailLike>?).
# - TrailLike has `id?`, `actor`, `trail`, `expand?`.
# - TrailNotifier.build already fetches `trail_like_via_trail` in the expand query and the
#   generated provider is `trailProvider(id)` (see trail_detail_screen.dart line 59).
# - authProvider value (UserEntity) exposes `actorId` (String).
# - dio client is `ref.watch(apiProvider)` / `ref.read(apiProvider)`.
# - API: PUT /trail-like {actor, trail} creates; POST /trail-like/delete {actor, trail} removes.
# - `package:collection` is NOT a dependency — do not use firstWhereOrNull; use a where(...).isEmpty/isNotEmpty
#   check or a plain loop to find the current user's like.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add like()/unlike() methods to TrailNotifier</name>
  <files>app/lib/provider/trail/trail_provider.dart</files>
  <action>
Add two public methods to the existing `TrailNotifier` class (the one whose `build` returns `FutureOr<Trail>`): `Future<void> like(String actorId)` and `Future<void> unlike(String actorId)`.

For `like`: read the current resolved trail from `state.valueOrNull`; if null, return. Call `ref.read(apiProvider)` and `api.put("/trail-like", data: {"actor": actorId, "trail": trail.id})`. On success, update `state` to `AsyncData` of a copy of the trail with `likeCount` incremented by 1 and a new `TrailLike(actor: actorId, trail: trail.id)` appended to `expand.trailLikeViaTrail`. Build the updated expand with `(trail.expand ?? const TrailExpand()).copyWith(trailLikeViaTrail: [...(trail.expand?.trailLikeViaTrail ?? []), newLike])`, mirroring the existing `copyWith(expand: ...)` pattern already used in `build` for the gpx field.

For `unlike`: read the current trail from `state.valueOrNull`; if null, return. Call `api.post("/trail-like/delete", data: {"actor": actorId, "trail": trail.id})`. On success, update `state` with `likeCount` decremented by 1 (clamp at 0) and `trailLikeViaTrail` filtered to remove entries where `l.actor == actorId` using `(trail.expand?.trailLikeViaTrail ?? []).where((l) => l.actor != actorId).toList()`.

Wrap each API call in try/catch; on error, leave `state` unchanged (do not rethrow into an AsyncError that would blank the screen) and let it fail silently per the existing graceful-degradation convention. Do NOT use `firstWhereOrNull` (package:collection is not a dependency). Do NOT run build_runner — the executor handles codegen separately.
  </action>
  <verify>
    <automated>cd app && grep -q "Future<void> like" lib/provider/trail/trail_provider.dart && grep -q "Future<void> unlike" lib/provider/trail/trail_provider.dart && grep -q "trail-like" lib/provider/trail/trail_provider.dart && grep -q "trail-like/delete" lib/provider/trail/trail_provider.dart && echo OK</automated>
  </verify>
  <done>TrailNotifier exposes like(actorId) and unlike(actorId) that hit PUT /trail-like and POST /trail-like/delete and optimistically update state (likeCount + trailLikeViaTrail).</done>
</task>

<task type="auto">
  <name>Task 2: Create LikeButton widget and mount it in the trail panel</name>
  <files>app/lib/components/trail/like_button.dart, app/lib/components/trail/trail_panel.dart</files>
  <action>
Create `app/lib/components/trail/like_button.dart` as a `LikeButton extends ConsumerWidget` taking `required this.trail` (a `Trail`). Imports: flutter/material.dart, flutter_riverpod, font_awesome_flutter, the Trail model, trail_provider.dart, and auth_provider.dart.

In `build`: read the current actor id via `ref.watch(authProvider).requireValue!.actorId` (matches the existing pattern in trail_panel.dart line 34). Determine liked state by scanning `trail.expand?.trailLikeViaTrail ?? []` for an entry whose `actor == actorId` using `.where((l) => l.actor == actorId).isNotEmpty` (NOT firstWhereOrNull). Read the count from `trail.likeCount`.

Render a tappable vertical layout (Column, mainAxisSize.min) inside an `InkWell` (or `GestureDetector`): a heart `FaIcon(FontAwesomeIcons.solidHeart)` when liked / `FaIcon(FontAwesomeIcons.heart)` when not liked, colored `Colors.red` when liked and `Colors.blueGrey` (matching StatChip icon color) when not, size 16; then a small `SizedBox(height: 4)`; then a `Text("$count", style: Theme.of(context).textTheme.bodySmall)`. Match StatChip's visual padding/look so it sits naturally beside the other stat chips.

onTap: call `final notifier = ref.read(trailProvider(trail.id).notifier);` then if currently liked call `notifier.unlike(actorId)` else `notifier.like(actorId)`. (Provider name is `trailProvider` per trail_detail_screen.dart line 59.)

Then edit `app/lib/components/trail/trail_panel.dart`: add the import for like_button.dart, and add `LikeButton(trail: trail)` as the last child of the existing stats `Wrap` widget (the one starting around line 144 containing the StatChip entries), so the heart appears alongside distance/duration/elevation. Do not remove or restructure the existing StatChips.

Do NOT run build_runner.
  </action>
  <verify>
    <automated>cd app && test -f lib/components/trail/like_button.dart && grep -q "class LikeButton" lib/components/trail/like_button.dart && grep -q "solidHeart" lib/components/trail/like_button.dart && grep -q "trailProvider(trail.id).notifier" lib/components/trail/like_button.dart && grep -q "LikeButton(trail: trail)" lib/components/trail/trail_panel.dart && echo OK</automated>
  </verify>
  <done>LikeButton renders a heart + count, fills red when the current actor has liked the trail, toggles like/unlike on tap, and is mounted in the trail panel stats Wrap. flutter analyze passes once codegen runs.</done>
</task>

</tasks>

<verification>
- `cd app && dart format --output=none --set-exit-if-changed lib/components/trail/like_button.dart lib/provider/trail/trail_provider.dart lib/components/trail/trail_panel.dart` (formatting clean)
- After the executor runs build_runner: `cd app && flutter analyze lib/components/trail/like_button.dart lib/provider/trail/trail_provider.dart lib/components/trail/trail_panel.dart` reports no errors.
- Manual: open a trail detail screen, confirm the heart + count render; tap to like (count +1, heart red) and tap again to unlike (count -1, heart unfilled).
</verification>

<success_criteria>
- A heart-shaped like button with the total like count appears in the trail panel.
- Tapping likes/unlikes the trail via the trail-like API.
- The heart fills red when the current user has liked the trail; the count reflects the change immediately.
- Existing trail panel stats, tabs, and layout are unaffected.
</success_criteria>

<output>
Create `.planning/quick/260616-j2n-implement-the-like-feature-from-the-web-/260616-j2n-SUMMARY.md` when done.
</output>
