---
phase: quick-260627-p67
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/util/safe_fetch.go
  - db/util/network.go
  - db/routes/federation_admin.go
autonomous: true
requirements: [SSRF-REFACTOR]

must_haves:
  truths:
    - "NewSafeURLClient is exported from db/util/safe_fetch.go and returns util.HTTPDoer"
    - "FetchPublicURL calls NewSafeURLClient instead of building the safeurl config inline"
    - "federation_admin.go has no import of github.com/doyensec/safeurl"
    - "federation_admin.go has no newDiscoveryClient() function"
    - "federation_admin.go's httpDoer type is deleted; fetchNodeInfo21URL uses util.HTTPDoer"
    - "isPrivateOrReservedIP in network.go also rejects special-purpose IPs (CGNAT, TEST-NET, etc.)"
    - "IsSpecialPurposeIP is exported from safe_fetch.go; the local unexported isSpecialPurposeIP is removed"
    - "go build ./... passes with no errors"
  artifacts:
    - path: db/util/safe_fetch.go
      provides: "NewSafeURLClient, HTTPDoer interface, IsSpecialPurposeIP"
      exports: ["HTTPDoer", "NewSafeURLClient", "IsSpecialPurposeIP"]
    - path: db/util/network.go
      provides: "isPrivateOrReservedIP calling IsSpecialPurposeIP"
      contains: "IsSpecialPurposeIP"
    - path: db/routes/federation_admin.go
      provides: "FederationDiscover calling util.NewSafeURLClient; fetchNodeInfo21URL accepting util.HTTPDoer"
  key_links:
    - from: db/util/safe_fetch.go
      to: db/util/network.go
      via: "IsSpecialPurposeIP exported call"
      pattern: "IsSpecialPurposeIP"
    - from: db/routes/federation_admin.go
      to: db/util/safe_fetch.go
      via: "util.NewSafeURLClient(10*time.Second, nil)"
      pattern: "util\\.NewSafeURLClient"
---

<objective>
Refactor the HTTP SSRF-protection layer across two concrete changes:

1. Extract safeurl client construction into `util.NewSafeURLClient` + `util.HTTPDoer`, eliminating the duplicate `newDiscoveryClient()` in federation_admin.go and the inline build inside `FetchPublicURL`. federation_admin.go stops importing safeurl directly.

2. Upgrade `isPrivateOrReservedIP` in network.go to also reject special-purpose addresses (CGNAT 100.64/10, TEST-NET ranges, benchmarking ranges, etc.) by calling `IsSpecialPurposeIP` exported from safe_fetch.go, removing the local unexported copy.

Purpose: Consolidate SSRF defence so the full special-purpose prefix table is enforced everywhere a raw net.IP is checked, and safeurl configuration lives in exactly one place.
Output: Three modified Go files; `go build ./...` green.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/christianbeutel/Documents/svelte/wanderer/.planning/STATE.md
@/Users/christianbeutel/Documents/svelte/wanderer/db/util/safe_fetch.go
@/Users/christianbeutel/Documents/svelte/wanderer/db/util/network.go
@/Users/christianbeutel/Documents/svelte/wanderer/db/routes/federation_admin.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export IsSpecialPurposeIP and add NewSafeURLClient + HTTPDoer to safe_fetch.go</name>
  <files>db/util/safe_fetch.go</files>
  <action>
Make three targeted edits to db/util/safe_fetch.go:

1. Add exported interface HTTPDoer immediately before FetchPublicURL (before line 37):

    type HTTPDoer interface {
        Do(req *http.Request) (*http.Response, error)
    }

2. Add exported constructor NewSafeURLClient immediately after HTTPDoer. It accepts a timeout and an optional checkRedirect func (nil is valid). It returns HTTPDoer (not *safeurl.WrappedClient) so callers depend only on the util package:

    func NewSafeURLClient(timeout time.Duration, checkRedirect func(*http.Request, []*http.Request) error) HTTPDoer {
        b := safeurl.GetConfigBuilder().
            SetTimeout(timeout).
            SetAllowedSchemes("http", "https").
            SetAllowedPorts(80, 443).
            EnableIPv6(true).
            AllowSendingCredentials(false)
        if checkRedirect != nil {
            b = b.SetCheckRedirect(checkRedirect)
        }
        return safeurl.Client(b.Build())
    }

3. Replace the inline safeurl config block inside FetchPublicURL (lines 48-56 of the original — the block from `config := safeurl.GetConfigBuilder()` through `client := safeurl.Client(config)`) with:

    client := NewSafeURLClient(60*time.Second, publicMediaRedirectPolicy)

4. Rename isSpecialPurposeIP to IsSpecialPurposeIP (exported). Update the one call site in connectorIPAllowed (same file) to use the new name IsSpecialPurposeIP(addr).

No other logic changes. Do not touch mustPrefixes, specialPurposePrefixes, publicMediaRedirectPolicy, ConnectorHTTPClient, or ReadBoundedForPlugin.
  </action>
  <verify>
    <automated>cd /Users/christianbeutel/Documents/svelte/wanderer/db && go build ./util/ 2>&amp;1</automated>
  </verify>
  <done>
`go build ./util/` exits 0. safe_fetch.go exports HTTPDoer, NewSafeURLClient, IsSpecialPurposeIP. FetchPublicURL contains no inline safeurl.GetConfigBuilder() call. connectorIPAllowed calls IsSpecialPurposeIP (capital I).
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade isPrivateOrReservedIP in network.go to call IsSpecialPurposeIP</name>
  <files>db/util/network.go</files>
  <action>
Edit db/util/network.go — add "net/netip" to the import block (it is not currently imported there), then extend isPrivateOrReservedIP to also check special-purpose prefixes.

The current isPrivateOrReservedIP receives a net.IP and returns bool. Append a special-purpose check at the end of the function body, before the final `return false`:

    // Convert to netip.Addr so IsSpecialPurposeIP can check the prefix table.
    if addr, ok := netip.AddrFromSlice(ip); ok {
        a := addr
        if a.Is4In6() {
            a = a.Unmap()
        }
        if IsSpecialPurposeIP(a) {
            return true
        }
    }
    return false

This replaces the bare `return false` that currently closes the function. The special-purpose check catches CGNAT (100.64.0.0/10), TEST-NET (192.0.2.0/24, 198.51.100.0/24, 203.0.113.0/24), benchmarking (198.18.0.0/15), and the other ranges already in specialPurposePrefixes. IsSpecialPurposeIP is defined in the same package (safe_fetch.go) — no import required beyond net/netip.

Do not alter SafeHTTPClient, safeTransport, RoundTrip, GetSafeActorContext, or the rate-limiter logic.
  </action>
  <verify>
    <automated>cd /Users/christianbeutel/Documents/svelte/wanderer/db && go build ./util/ 2>&amp;1</automated>
  </verify>
  <done>
`go build ./util/` exits 0. isPrivateOrReservedIP contains a call to IsSpecialPurposeIP. network.go imports "net/netip".
  </done>
</task>

<task type="auto">
  <name>Task 3: Collapse newDiscoveryClient and httpDoer in federation_admin.go onto util package</name>
  <files>db/routes/federation_admin.go</files>
  <action>
Edit db/routes/federation_admin.go — three deletions and two replacements:

1. Delete the import "github.com/doyensec/safeurl" from the import block.

2. Delete the httpDoer interface declaration (lines 59-61 in the original):
    type httpDoer interface {
        Do(req *http.Request) (*http.Response, error)
    }

3. Delete the newDiscoveryClient function (lines 74-83 in the original) along with its doc comment block starting at line 67.

4. In fetchNodeInfo21URL's signature, change the parameter type from `httpDoer` to `util.HTTPDoer`:
    func fetchNodeInfo21URL(client util.HTTPDoer, rawURL string) (string, error) {

5. In FederationDiscover, replace:
    client := newDiscoveryClient()
   with:
    client := util.NewSafeURLClient(10*time.Second, nil)

No other logic changes. All remaining calls to client.Do and fetchNodeInfo21URL are unchanged. The "time" import is already present in the file.
  </action>
  <verify>
    <automated>cd /Users/christianbeutel/Documents/svelte/wanderer/db && go build ./... 2>&amp;1</automated>
  </verify>
  <done>
`go build ./...` exits 0. federation_admin.go contains no import of safeurl, no newDiscoveryClient function, no local httpDoer type. fetchNodeInfo21URL accepts util.HTTPDoer. FederationDiscover calls util.NewSafeURLClient(10*time.Second, nil).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| util → routes | HTTPDoer interface is the only type crossing the boundary; safeurl concrete type stays inside util |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p67-01 | Spoofing | isPrivateOrReservedIP | mitigate | Adding IsSpecialPurposeIP call closes the CGNAT/TEST-NET gap; addresses in those ranges can no longer be accessed via SafeHTTPClient's DialContext |
| T-p67-02 | Tampering | NewSafeURLClient return type | accept | Returning HTTPDoer (interface) rather than *safeurl.WrappedClient prevents callers from accessing safeurl internals and mutating config post-construction |
| T-p67-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this refactor; safeurl dependency already present |
</threat_model>

<verification>
1. `cd /Users/christianbeutel/Documents/svelte/wanderer/db && go build ./...` exits 0
2. `grep -r "doyensec/safeurl" db/routes/` returns no output (safeurl import removed from routes)
3. `grep "NewSafeURLClient" db/util/safe_fetch.go` matches (exported constructor present)
4. `grep "HTTPDoer" db/util/safe_fetch.go` matches (exported interface present)
5. `grep "IsSpecialPurposeIP" db/util/network.go` matches (upgraded predicate calls exported func)
6. `grep "newDiscoveryClient" db/routes/federation_admin.go` returns no output (deleted)
7. `grep "httpDoer" db/routes/federation_admin.go` returns no output (local interface deleted)
8. `grep "isSpecialPurposeIP" db/util/safe_fetch.go` returns no output (renamed to exported form)
</verification>

<success_criteria>
- go build ./... exits 0 with no errors or warnings
- safeurl is imported only in db/util/safe_fetch.go, not in db/routes/
- isPrivateOrReservedIP (network.go) rejects 100.64.0.1 (CGNAT) and 192.0.2.1 (TEST-NET) via IsSpecialPurposeIP
- FetchPublicURL and FederationDiscover both build their safeurl client through NewSafeURLClient
- No functional behaviour changes outside the expanded IP block list
</success_criteria>

<output>
Create `.planning/quick/260627-p67-refactor-http-client-ssrf-layer-extract-/260627-p67-SUMMARY.md` when done
</output>
