---
phase: 38.1-downloaded-trail-blocker-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/routes/library_detail_screen.dart
  - app/lib/provider/router_provider.dart
  - app/lib/models/trail.dart
  - app/lib/components/trail/trail_panel.dart
  - app/lib/entities/trail_entity.dart
  - app/test/components/trail/trail_panel_sync_badge_test.dart
  - .planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md
autonomous: true
requirements: [DL-01]
must_haves:
  truths:
    - "No second trail-detail surface exists that a future destructive-action guard would have to be re-applied to"
    - "Trail.isLocal's doc comment no longer tells a future reader that a destructive action may be routed by it"
    - "Phase 36's D-10 no longer asserts the false mutual-exclusivity premise in the artifact future phases read"
    - "No comment in app/lib or app/test outside trail_dropdown.dart still asserts the retracted premise"
  artifacts:
    - path: "app/lib/provider/router_provider.dart"
      provides: "Router with no /library/:id nested route"
      absent_pattern: "LibraryDetailScreen"
    - path: "app/lib/models/trail.dart"
      provides: "isLocal doc naming only the two thumbnail consumers plus an explicit prohibition"
      contains: "trail_list_item.dart"
    - path: "app/lib/components/trail/trail_panel.dart"
      provides: "Offline-badge comment no longer asserting the retracted premise"
      absent_pattern: "mutually exclusive"
    - path: ".planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md"
      provides: "D-10 corrected in place with a pointer to the correction note"
      contains: "unsynced-and-downloaded-are-not-mutually-exclusive.md"
  key_links:
    - from: ".planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md"
      to: ".planning/notes/unsynced-and-downloaded-are-not-mutually-exclusive.md"
      via: "in-place RETRACTED annotation on D-10"
      pattern: "unsynced-and-downloaded-are-not-mutually-exclusive"
---

<objective>
Close the three items of Phase 38's review that are independent of the three blockers: delete the
unreachable `LibraryDetailScreen` and its route (WR-07 / D-15), correct `Trail.isLocal`'s doc
comment so it stops inviting the exact reintroduction Phase 38 removed (WR-08 / D-16), and correct
Phase 36's D-10 in place so the false premise stops propagating into future phases (D-18).

Purpose: this phase exists because a false premise ("unsynced and downloaded are mutually exclusive
by construction") propagated from a planning artifact into three destructive code paths. Two of the
three items here are the *propagation* fixes — a dead duplicate detail surface and a doc comment
that both re-arm the class of error being closed. Fixing them first means the executors of plans
02-05 never read the wrong story.

Output: one deleted screen, one deleted route, one rewritten field doc, one corrected planning
decision.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-downloaded-trail-blocker-closure/38.1-CONTEXT.md
@.planning/phases/38.1-downloaded-trail-blocker-closure/38.1-PATTERNS.md
@.planning/notes/unsynced-and-downloaded-are-not-mutually-exclusive.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete LibraryDetailScreen and the /library/:id route (WR-07 / D-15)</name>
  <files>app/lib/routes/library_detail_screen.dart (deleted), app/lib/provider/router_provider.dart</files>
  <read_first>
    - `app/lib/routes/library_detail_screen.dart` — the whole file (52 lines); note the
      `firstWhere((t) => t.id == id)` at line 13 with no `orElse`, and the `availableOffline: true`
      comment at lines 39-44 that asserts a live surface that does not exist.
    - `app/lib/provider/router_provider.dart` lines 169-195 — the `/library` `GoRoute` and its
      nested `':id'` child at lines 180-188, plus the import at line 17.
    - `app/lib/components/base/wanderer_layout.dart` lines 40-50 and 115-122 — the only two places
      `/library` is referenced (`router.go('/library')` and `path.startsWith('/library')`). Both
      must keep working; only the NESTED route is removed.
    - `.planning/phases/38-downloaded-trails-as-state-not-objects/38-REVIEW.md` § WR-07 — the
      evidence that nothing pushes `/library/:id`.
  </read_first>
  <action>
Delete the file `app/lib/routes/library_detail_screen.dart` outright (D-15 chose deletion over
wiring it up: nothing pushes `/library/:id`, library cards push `trailDetailLocation(trail)`, and a
diverging duplicate detail surface is exactly where CR-03 came from).

In `app/lib/provider/router_provider.dart`: remove the import
`package:wanderer/routes/library_detail_screen.dart` (line 17) and remove the entire nested
`routes: [ GoRoute(path: ':id', builder: ... ) ]` block from the `/library` `GoRoute` (lines
180-188), leaving `GoRoute(path: '/library', builder: (context, state) => LibraryScreen())` with no
`routes:` argument at all. Do NOT touch the `/library` route itself, and do not touch
`wanderer_layout.dart` — the bottom-nav `router.go('/library')` and the `startsWith('/library')`
tab-index test both stay.

Leave a one-line comment above the `/library` route recording why there is deliberately no nested
detail route: library cards navigate to `trailDetailLocation(trail)` (`/trail/<id>`), so a second
detail surface would need every 38.1 destructive-action guard re-applied to it.
  </action>
  <verify>
    <automated>cd app &amp;&amp; grep -rn "LibraryDetailScreen\|library_detail_screen" lib test | grep -v '^$' ; test $(grep -rn "LibraryDetailScreen\|library_detail_screen" lib test | wc -l) -eq 0 &amp;&amp; flutter analyze lib/provider/router_provider.dart</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cd app && grep -rn "LibraryDetailScreen\|library_detail_screen" lib test` returns zero
      lines.
    - CLI: `cd app && test ! -f lib/routes/library_detail_screen.dart` succeeds.
    - Source: `app/lib/provider/router_provider.dart` contains `path: '/library',` exactly once and
      contains no `path: ':id',` anywhere inside the `/library` route.
    - Source: `app/lib/components/base/wanderer_layout.dart` still contains `router.go('/library')`
      and `path.startsWith('/library')` — unchanged.
    - CLI: `cd app && flutter analyze lib/provider/router_provider.dart` reports no issues.
  </acceptance_criteria>
  <done>`LibraryDetailScreen` and `/library/:id` no longer exist anywhere in `app/lib` or `app/test`; the `/library` tab route and its bottom-nav wiring still work and analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Correct every doc comment that still carries the retracted premise (WR-08 / D-16 / D-18)</name>
  <files>app/lib/models/trail.dart, app/lib/components/trail/trail_panel.dart, app/lib/entities/trail_entity.dart, app/test/components/trail/trail_panel_sync_badge_test.dart</files>
  <read_first>
    - `app/lib/models/trail.dart` lines 100-140 — the current `isLocal` field doc (lines 106-125),
      especially the "Live consumers" paragraph at 118-124 which still names three consumers Phase
      38 deleted and calls one of them "load-bearing".
    - `app/lib/components/trail/trail_card.dart` line 48 and
      `app/lib/components/trail/trail_list_item.dart` line 38 — the two, and only two, surviving
      consumers (`if (trail.isLocal && localPath != null)`), both thumbnail-path selection.
    - `app/lib/entities/trail_entity.dart` lines 348-362 — `toModel()` hardcoding `isLocal: true`
      (line 355, which is why the flag can never gate anything) and the `localPhotos` fallback
      comment at 356-359 that justifies itself with "D-10: mutually exclusive by construction".
    - `app/lib/components/trail/trail_panel.dart` lines 194-208 — the offline-badge comment whose
      last sentence asserts "Phase 36's D-03/D-10 guarantee unsynced and downloaded are mutually
      exclusive (a blank-id unsynced trail is never a library member), so this single badge can
      never render for one."
    - `app/test/components/trail/trail_panel_sync_badge_test.dart` lines 1-45 — the file header,
      which asserts the premise in prose (WR-12's second half).
    - `.planning/notes/unsynced-and-downloaded-are-not-mutually-exclusive.md` — the replacement
      rule to cite in every rewritten comment.
  </read_first>
  <action>
Four comment-only edits. No behaviour changes in this task, no code lines added or removed, no test
bodies touched — `flutter analyze` and the existing suites must be unaffected.

**1. `app/lib/models/trail.dart` — `Trail.isLocal` (WR-08 / D-16).** Replace only the "Live
consumers" paragraph (currently lines 118-124). Keep the first two paragraphs (the "set only by
`TrailEntity.toModel`" line and the "**Provenance, not connectivity.**" paragraph including the
`OFFUI-01` history) exactly as they are — they are still accurate. The replacement paragraph must
state three things concretely:
  1. The only live consumers are thumbnail-path selection in `trail_card.dart` and
     `trail_list_item.dart` — name both files.
  2. An explicit prohibition, phrased so a future reader cannot mistake it for advice: never gate a
     destructive action, a badge, or tab visibility on this flag, because `TrailEntity.toModel()`
     hardcodes it `true` for every cached row (phase 38, D-01).
  3. A pointer to the rule that supersedes it: destructive-action availability derives from library
     membership and authorship; destructive-action *scoping* derives from `owner`/account for local
     capture state and from `savedByUserIds` membership for a download (phase 38.1, D-02). Reference
     `.planning/notes/unsynced-and-downloaded-are-not-mutually-exclusive.md` by filename.
Do not change the field's type, default, or annotations. Do not touch `Trail.mock()` or any other
member of this file.

**2. `app/lib/components/trail/trail_panel.dart` (D-18 propagation).** Rewrite only the final
sentence of the offline-badge comment — the one citing "Phase 36's D-03/D-10 guarantee ... mutually
exclusive". Keep the whole preceding argument (the badge derives from `trailLibraryProvider`
membership and never from the cache-provenance flag) verbatim; it is correct and load-bearing.
Replace the guarantee with the truth: unsynced-ness and library membership are independent axes and
a row can be both (38.1 D-01), which is exactly why this badge is derived from membership alone —
membership is a fact about this account, so the badge stays correct on an overlap row instead of
depending on a premise that does not hold. Cite the correction note by filename.

**3. `app/lib/entities/trail_entity.dart` — the `localPhotos` fallback comment.** The CODE stays
byte-identical (`localPhotos: localPhotos.isNotEmpty ? localPhotos : photos`). Replace only its
justification: drop "D-10: mutually exclusive by construction" and state the actual rule the
expression implements — a row's own `localPhotos` win when present (an overlap row's capture copies
are the ones the drain still needs), and a plain downloaded row has an empty `localPhotos`, so the
fallback to `photos` keeps every downloaded-trail reader working. Note that the correctness of this
expression never depended on the two states being exclusive.

**4. `app/test/components/trail/trail_panel_sync_badge_test.dart` — the file header.** Correct the
prose that asserts the premise (WR-12's second half). Do not change a single test body or fixture in
this task — the missing fixture is D-17's job and lands in plan 05. State instead that the suite's
fixtures deliberately cover the membership axis and that the overlap fixture
(`availableOffline: true` with a non-synced `syncState`) lives in
`trail_dropdown_menu_test.dart`, added by 38.1 plan 05.

Leave `app/lib/components/trail/trail_dropdown.dart` alone in this plan — both of its
premise-asserting comments sit on code that plan 05 rewrites, and correcting them here would only
create churn.
  </action>
  <verify>
    <automated>cd app &amp;&amp; flutter analyze lib/models/trail.dart lib/components/trail/trail_panel.dart lib/entities/trail_entity.dart &amp;&amp; flutter test test/components/trail/trail_panel_sync_badge_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: the `isLocal` doc in `app/lib/models/trail.dart` contains the substrings
      `trail_card.dart` and `trail_list_item.dart`.
    - Source: the `isLocal` doc contains the substring `unsynced-and-downloaded-are-not-mutually-exclusive.md`.
    - Source (negative): `grep -n "load-bearing" app/lib/models/trail.dart` returns no line inside
      the `isLocal` doc block; the words `trail_panel.dart` and `trail_dropdown.dart` no longer
      appear anywhere in that doc block.
    - CLI (negative): `cd app && grep -rn "mutually exclusive" lib test | grep -v trail_dropdown.dart`
      returns zero lines — every remaining occurrence in the repo is in `trail_dropdown.dart`, which
      plan 05 rewrites.
    - Source: `app/lib/entities/trail_entity.dart` still contains
      `localPhotos: localPhotos.isNotEmpty ? localPhotos : photos,` byte-identical.
    - CLI: `cd app && git diff --stat app/lib/entities/trail_entity.dart app/lib/components/trail/trail_panel.dart`
      shows only comment lines changed (no `+`/`-` on a line ending in `;`, `{`, `}` or `,` that is
      not a comment).
    - Test: `cd app && flutter test test/components/trail/trail_panel_sync_badge_test.dart` exits 0
      with no test-body edits.
    - CLI: `cd app && flutter analyze lib/models/trail.dart lib/components/trail/trail_panel.dart lib/entities/trail_entity.dart`
      reports no issues.
  </acceptance_criteria>
  <done>No comment in `app/lib` or `app/test` outside `trail_dropdown.dart` still asserts that unsynced and downloaded are mutually exclusive, and `Trail.isLocal` explicitly forbids gating a destructive action on it.</done>
</task>

<task type="auto">
  <name>Task 3: Correct Phase 36's D-10 in place (D-18)</name>
  <files>.planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md` lines 70-95 — D-10
      as written (line 77) plus its neighbours D-09 and D-11, so the edit does not disturb them.
    - `.planning/notes/unsynced-and-downloaded-are-not-mutually-exclusive.md` — the full correction:
      what the claim was, what the codebase already said (`trail_download_service.dart:210-215`,
      `shouldDeleteUploadedRow`'s doc), why the window is unbounded (Phase 36's own D-07), and the
      rule that replaces it.
    - `.planning/phases/38.1-downloaded-trail-blocker-closure/38.1-CONTEXT.md` § D-01/D-02/D-03 and
      § D-18 — the exact scope of the retraction.
  </read_first>
  <action>
Edit D-10 in `36-CONTEXT.md` in place. Do not delete it and do not renumber any decision — future
artifacts cite `D-10` by number and a hole would be worse than a correction.

Rewrite D-10's body so it reads as a RETRACTED decision:
- Prefix the bolded lead with a retraction marker, e.g. `**D-10 (RETRACTED 2026-08-04):**`.
- Keep the original claim visible but clearly marked as false: the premise "unsynced and downloaded
  are mutually exclusive by construction" does not hold, because `TrailDownloadService.downloadTrail`
  (`app/lib/services/trail_download_service.dart:210-215`) carries `owner`, `localId`, `syncState`,
  `syncAttempts` and `syncNextAttemptAt` forward when it writes into an existing row, and
  `TrailEntity.id` is `@Unique(onConflict: replace)` so both writers target one row.
- State what survives: the two BADGES D-10 justified are still fine (they are cosmetic), and the
  consequence clause "unsynced trails must NOT be expressed by adding the user to `savedByUserIds`"
  still holds.
- State what does not survive: any reasoning of the form "an `isUnsyncedState` check cannot fire on
  a downloaded row", and any use of D-10 to justify destructive-action gating or scoping.
- Point at both the correction note (`.planning/notes/unsynced-and-downloaded-are-not-mutually-exclusive.md`)
  and the remediation phase (`.planning/phases/38.1-downloaded-trail-blocker-closure/`), and name
  the replacement rule (38.1 D-02): any destructive action must be scoped by the identity it
  actually destroys — local capture state by `owner`/account, download removal by `savedByUserIds`
  membership, never by a flag read off the shared row.
- Note that Phase 36's own D-07 (a repeatedly-failing upload parks in `failed` with no scheduled
  retry) is what makes the overlap window unbounded rather than a brief race.

Leave every other decision in `36-CONTEXT.md` byte-identical.
  </action>
  <verify>
    <automated>grep -c "RETRACTED" .planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md &amp;&amp; grep -c "unsynced-and-downloaded-are-not-mutually-exclusive" .planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `36-CONTEXT.md` still contains exactly one line beginning `- **D-10` and still
      contains `- **D-09:**` and `- **D-11:**` unchanged.
    - Source: the D-10 entry contains the substring `RETRACTED`.
    - Source: the D-10 entry contains the substring
      `unsynced-and-downloaded-are-not-mutually-exclusive.md`.
    - Source: the D-10 entry contains the substring `trail_download_service.dart` (the file:line
      evidence) and the substring `savedByUserIds` (the surviving consequence clause).
    - CLI: `git diff --stat .planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md`
      shows exactly one file changed and no other planning artifact touched by this task.
  </acceptance_criteria>
  <done>A future planner reading `36-CONTEXT.md` sees D-10 marked retracted, learns which half survives, and is routed to the correction note and the replacement rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → future implementer | A decision recorded as true is copied forward into code without re-derivation. Phase 38's D-03 and both blocker-adjacent doc comments are the proof this boundary is real. |
| go_router path parameter → widget | `/library/:id` fed an attacker-influenceable (deep-link, back-stack) id straight into a `firstWhere` with no `orElse`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-01 | Tampering | `_allowDelete` + `unsyncedTrailPhotoDir` (cross-account destruction of another account's unsynced capture and photo dir) | mitigate | Not fixed here. This plan removes the two artifacts that re-arm it: `Trail.isLocal`'s "load-bearing" doc (Task 2) and a second detail surface that would need the guard re-applied (Task 1). Code fix lands in plans 02/03/05. |
| T-38.1-03 | Denial of Service | Second trail-detail surface (`LibraryDetailScreen`) offering destructive actions with no guard | mitigate | Task 1 deletes the surface entirely, so the CR-03 guard has exactly two call sites to cover (`trail_dropdown.dart`, `library_screen.dart`) rather than three. |
| T-38.1-05 | Information Disclosure | `LibraryDetailScreen`'s `firstWhere((t) => t.id == id)` with no `orElse` — an unhandled `StateError` from a route parameter, with no error boundary above it | mitigate | Task 1 deletes the route and the widget; no `orElse` needs adding because no reachable caller remains. |
| T-38.1-06 | Repudiation | Phase 36 D-10 asserting a false invariant that future phases cite as settled | mitigate | Task 3 corrects it in place with a retraction marker, the file:line counter-evidence, and a pointer to the replacement rule — so a future planner cannot cite it without seeing the retraction. |
| T-38.1-SC | Tampering | npm/pub/pip/cargo installs | accept | No package-manager installs in this phase. `app/pubspec.yaml` and `app/pubspec.lock` are not in any plan's `files_modified`. If an executor finds it needs a new dependency, stop and escalate — the package legitimacy gate has not been run for this phase. |
</threat_model>

<verification>
- `cd app && flutter analyze` reports no issues for `lib/provider/router_provider.dart` and
  `lib/models/trail.dart`.
- `cd app && grep -rn "LibraryDetailScreen\|library_detail_screen" lib test` returns nothing.
- `cd app && flutter test test/routes/ test/components/trail/` exits 0 (no route or widget test
  referenced the deleted screen; no test body was edited).
- `cd app && grep -rn "mutually exclusive" lib test | grep -v trail_dropdown.dart` returns nothing.
- `git diff --name-only` lists exactly: `app/lib/provider/router_provider.dart`,
  `app/lib/models/trail.dart`, `app/lib/components/trail/trail_panel.dart`,
  `app/lib/entities/trail_entity.dart`,
  `app/test/components/trail/trail_panel_sync_badge_test.dart`,
  `.planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md`, plus the deletion of
  `app/lib/routes/library_detail_screen.dart`.
</verification>

<success_criteria>
- `LibraryDetailScreen`, its file, its import and its `/library/:id` route are gone; the `/library`
  tab still resolves and analyzes clean.
- `Trail.isLocal`'s doc names exactly two consumers (`trail_card.dart`, `trail_list_item.dart`) and
  carries an explicit prohibition against gating a destructive action, a badge or tab visibility on
  it.
- Every other comment in `app/lib` and `app/test` that asserted the retracted mutual-exclusivity
  premise is corrected; the only surviving occurrences are in `trail_dropdown.dart`, whose code plan
  05 rewrites.
- Phase 36's D-10 is marked RETRACTED in place, carries the counter-evidence, and points at both
  the correction note and 38.1's replacement rule (D-02).
- No behavioural change to any shipping surface: `flutter test test/routes/ test/components/trail/`
  is green with no test edits.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 01 share)

**Deleted:**
- `app/lib/routes/library_detail_screen.dart` (file)
- `LibraryDetailScreen` (class)
- the nested `GoRoute(path: ':id', ...)` under `/library` in `app/lib/provider/router_provider.dart`
- the import `package:wanderer/routes/library_detail_screen.dart`

**Rewritten (no new symbols, comments only):**
- the "Live consumers" paragraph of `Trail.isLocal`'s doc comment in `app/lib/models/trail.dart`
- the offline-badge premise sentence in `app/lib/components/trail/trail_panel.dart`
- the `localPhotos` fallback justification in `app/lib/entities/trail_entity.dart` (code unchanged)
- the file header of `app/test/components/trail/trail_panel_sync_badge_test.dart` (no test bodies)
- decision `D-10` in `.planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md`

**New symbols:** none.
</artifacts_produced>

<output>
Create `.planning/phases/38.1-downloaded-trail-blocker-closure/38.1-01-SUMMARY.md` when done.
</output>
