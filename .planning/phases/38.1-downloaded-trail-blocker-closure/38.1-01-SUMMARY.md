---
phase: 38.1-downloaded-trail-blocker-closure
plan: 01
subsystem: mobile-app-cleanup
tags: [flutter, dart, go_router, documentation, planning-artifact-correction]

# Dependency graph
requires:
  - phase: 38-downloaded-trails-as-state-not-objects
    provides: the code review (38-REVIEW.md) whose WR-07/WR-08 items this plan closes, and the false D-03 premise this plan's D-18 traces back to Phase 36
provides:
  - LibraryDetailScreen and its /library/:id route deleted (no duplicate destructive-action surface remains)
  - Trail.isLocal's doc comment corrected to name only its two real consumers and explicitly forbid gating destructive actions/badges/tabs on it
  - Every non-trail_dropdown.dart comment asserting "unsynced and downloaded are mutually exclusive" corrected to state the true independent-axes rule
  - Phase 36's D-10 marked RETRACTED in place with counter-evidence and pointers to the correction note and the replacement rule
affects: [38.1-02, 38.1-03, 38.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Retract-in-place, don't delete/renumber a planning decision that later proved false — keep the number, strike the false claim, state what survives, point to the correction."

key-files:
  created: []
  modified:
    - app/lib/routes/library_detail_screen.dart (deleted)
    - app/lib/provider/router_provider.dart
    - app/lib/models/trail.dart
    - app/lib/components/trail/trail_panel.dart
    - app/lib/entities/trail_entity.dart
    - app/test/components/trail/trail_panel_sync_badge_test.dart
    - .planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md

key-decisions:
  - "D-15: delete LibraryDetailScreen and its nested route outright rather than wire it up — nothing pushes /library/:id and a second detail surface is exactly where CR-03 (a later plan's blocker) came from."
  - "D-16: Trail.isLocal's doc now names its only two live consumers (trail_card.dart, trail_list_item.dart) and explicitly forbids using it for destructive-action gating, badges, or tab visibility."
  - "D-18: Phase 36's D-10 corrected in place (not deleted/renumbered) as RETRACTED, since future artifacts cite it by number; the two cosmetic badges it justified still hold, but any destructive-action gating/scoping derived from it does not."

patterns-established:
  - "RETRACTED marker convention for planning decisions: strike the false claim, keep it visible, state what survives vs. what doesn't, point to a correction note and replacement rule."

requirements-completed: [DL-01]

# Metrics
duration: ~12min
completed: 2026-08-04
---

# Phase 38.1 Plan 01: Blocker-Independent Cleanup Summary

**Deleted the unreachable LibraryDetailScreen/route, rewrote four doc comments (Trail.isLocal, trail_panel.dart's offline badge, trail_entity.dart's localPhotos fallback, and a test file header) to drop the false "unsynced and downloaded are mutually exclusive" premise, and retracted Phase 36's D-10 in place with counter-evidence.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-08-04T18:00:00Z
- **Completed:** 2026-08-04T18:06:53Z
- **Tasks:** 3
- **Files modified:** 6 modified + 1 deleted

## Accomplishments
- Removed a second, un-guarded trail-detail surface (`LibraryDetailScreen`) and its `/library/:id` route before any of the later blocker-fix plans would otherwise need to re-apply their guards to it
- Corrected `Trail.isLocal`'s doc comment so it no longer names three deleted consumers as "load-bearing" and instead states an explicit prohibition against using the flag for destructive-action gating
- Corrected every other comment in `app/lib`/`app/test` (outside `trail_dropdown.dart`, which plan 05 rewrites) that asserted the retracted mutual-exclusivity premise
- Retracted Phase 36's D-10 in place in `36-CONTEXT.md`, with file:line counter-evidence, a statement of what survives (the cosmetic badges) and what doesn't (destructive-action gating/scoping), and pointers to the correction note and the replacement rule (38.1 D-02)

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete LibraryDetailScreen and the /library/:id route** - `244a40c6` (feat)
2. **Task 2: Correct every doc comment that still carries the retracted premise** - `bc4786ea` (docs)
3. **Task 3: Correct Phase 36's D-10 in place** - `621c4ad6` (docs)

**Plan metadata:** commit pending (see final commit step)

## Files Created/Modified
- `app/lib/routes/library_detail_screen.dart` - deleted; nothing pushed `/library/:id`, and it duplicated the real trail-detail screen with a `firstWhere` and no `orElse`
- `app/lib/provider/router_provider.dart` - removed the `LibraryDetailScreen` import and the nested `GoRoute(path: ':id', ...)` under `/library`; left a comment recording why there's deliberately no nested detail route
- `app/lib/models/trail.dart` - rewrote `Trail.isLocal`'s "Live consumers" doc paragraph
- `app/lib/components/trail/trail_panel.dart` - rewrote the offline-badge comment's closing sentence
- `app/lib/entities/trail_entity.dart` - rewrote the `localPhotos` fallback justification comment (code byte-identical)
- `app/test/components/trail/trail_panel_sync_badge_test.dart` - rewrote the file header to state the suite covers the membership axis only, and that the overlap fixture lands in `trail_dropdown_menu_test.dart` via plan 05
- `.planning/phases/36-local-first-recording-automatic-upload/36-CONTEXT.md` - D-10 marked `RETRACTED 2026-08-04` in place, D-09/D-11 untouched

## Decisions Made
- Followed the plan's D-15/D-16/D-18 exactly as specified; no deviation from the prescribed comment content.
- Chose phrasing for the two negative-grep-sensitive rewrites (trail_entity.dart's localPhotos comment and the test file header) that conveys the same "independent axes, can co-occur" meaning without reusing the literal substring "mutually exclusive" — both files are covered by the plan's repo-wide negative grep (`grep -rn "mutually exclusive" lib test | grep -v trail_dropdown.dart` must return zero lines), and an initial draft of both rewrites momentarily reintroduced the literal phrase before being corrected in the same task.

## Deviations from Plan

None - plan executed exactly as written. All four acceptance-criteria greps, `flutter analyze`, and `flutter test test/routes/ test/components/trail/` pass; `git diff --stat` confirms only comment lines changed in `trail_entity.dart` and `trail_panel.dart`.

## Issues Encountered

The plan's frontmatter lists `requirements: [DL-01]`, and plans 02-05 all list DL-01 too (it is
re-closed across the whole phase, not by any single plan). Since this plan closes only the
premise-propagation items (WR-07/WR-08/D-18) and not the CR-01 defect itself (DL-01's actual
violation — destructive-action availability still needs the owner-scoped guard landing in plans
02/03), `requirements.mark-complete DL-01` was run per protocol but its checkbox flip was reverted
in `.planning/REQUIREMENTS.md` before the final commit — DL-01 stays unchecked, matching the
traceability table's still-accurate "Re-opened (CR-01)" status, until the plan that actually fixes
CR-01 lands.

While drafting Task 2's rewrites, the first draft of both the `trail_entity.dart` localPhotos comment and the `trail_panel_sync_badge_test.dart` file header inadvertently reused the literal substring "mutually exclusive" (in the negated form "not mutually exclusive" / "being mutually exclusive"), which would have failed the plan's own repo-wide negative grep. Caught by re-running the grep before committing; both comments were reworded to convey the same meaning ("independent axes," "excluding one another") without the literal substring. Fixed within Task 2's single commit — no separate deviation commit needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plans 02/03/05 (CR-01, CR-02/CR-03, WR-12 fixture) can now proceed without inheriting the false D-10 premise from either the code comments or the planning artifact.
- `trail_dropdown.dart` still carries two "mutually exclusive" comments (lines 65 and 383) by design — plan 05 rewrites the code they annotate, so correcting the comments here would only create churn.
- No blockers.

---
*Phase: 38.1-downloaded-trail-blocker-closure*
*Completed: 2026-08-04*

## Self-Check: PASSED
