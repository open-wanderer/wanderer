---
phase: 20-route-planner-views-waypoint-list-elevation-location-search
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - app/lib/components/trail/elevation_profile.dart
  - app/lib/components/route_planner/elevation_tab.dart
autonomous: true
requirements: [PLANUI-02]
must_haves:
  truths:
    - "ElevationProfile renders from a Gpx with a null trail — stats header falls back to gpx.getTotals(), no route-anchor icons appear on the chart (D-12)"
    - "The elevation tab fetches /valhalla/height only while it is the visible tab and debounces further route edits (~500ms) (D-11)"
    - "With fewer than 2 anchors the elevation tab shows the empty-state message instead of calling Valhalla (D-13)"
  artifacts:
    - path: "app/lib/components/trail/elevation_profile.dart"
      provides: "ElevationProfile with an optional trail param (Trail?) — reusable for a trail-less planned route"
      contains: "Trail? trail"
    - path: "app/lib/components/route_planner/elevation_tab.dart"
      provides: "ElevationTab — tab-gated debounced height fetch, ele-merged local Gpx, empty state (PLANUI-02)"
      contains: "class ElevationTab"
  key_links:
    - from: "app/lib/components/route_planner/elevation_tab.dart"
      to: "plannedGpxProvider"
      via: "ref.watch(plannedGpxProvider(travelProfile))"
      pattern: "plannedGpxProvider"
    - from: "app/lib/components/route_planner/elevation_tab.dart"
      to: "/valhalla/height"
      via: "api.post('/valhalla/height', {shape: buildNavShape(points)}) gated on TabController"
      pattern: "valhalla/height"
    - from: "app/lib/components/route_planner/elevation_tab.dart"
      to: "ElevationProfile"
      via: "renders ElevationProfile(trail: null, gpx: eleMergedGpx)"
      pattern: "ElevationProfile\\("
---

<objective>
Build the sheet's Elevation tab (PLANUI-02, D-09..D-13): adapt the existing
`ElevationProfile` widget to accept a null `trail` (so it renders from a
route-planner `Gpx` with no bound `Trail`), and create `ElevationTab`, which
watches `plannedGpxProvider`, fetches `/valhalla/height` only while it is the
active tab (D-11), merges the returned elevations into a local `Gpx` copy, and
shows an empty-state message under 2 anchors (D-13).

Depends on Plan 01's `plannedGpxProvider` (the pre-elevation `Gpx` source) and
the existing `buildNavShape` downsampler. Runs in Wave 2 in parallel with the
route anchor list tab (Plan 04).

Purpose: A live elevation profile that updates as the route changes, reusing
~500 lines of proven `fl_chart` config rather than rebuilding it.
Output: `ElevationProfile.trail` becomes `Trail?`; a new `ElevationTab` widget.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-route-planner-views-waypoint-list-elevation-location-search/20-CONTEXT.md
@.planning/phases/20-route-planner-views-waypoint-list-elevation-location-search/20-RESEARCH.md
@.planning/phases/20-route-planner-views-waypoint-list-elevation-location-search/20-UI-SPEC.md
</context>

<artifacts_produced>
Symbols this plan creates (exclude from source-grounding drift checks):
- `ElevationProfile.trail` changes type from `Trail` to `Trail?` (constructor `required this.trail` → `this.trail`)
- `ElevationTab` — new `ConsumerStatefulWidget` in `app/lib/components/route_planner/elevation_tab.dart`
- New file: `app/lib/components/route_planner/elevation_tab.dart`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Make ElevationProfile.trail optional (Trail?) with a gpx-derived stats fallback (D-12)</name>
  <files>app/lib/components/trail/elevation_profile.dart</files>
  <read_first>
    - app/lib/components/trail/elevation_profile.dart (the file being modified — lines 16-33 constructor, lines 134-191 stats header, lines 222-234 + 397-439 `_buildChart` waypoint overlay, lines 670-698 `_EmptyState`)
    - app/lib/util/gpx_util.dart (GpxStats + `GpxMappingUtils.getTotals()` — returns `totalElevationGain` and `totalElevationloss` (note the existing lowercase-`l` typo — match it exactly))
    - .planning/phases/20-route-planner-views-waypoint-list-elevation-location-search/20-RESEARCH.md (Pattern 6 + Pitfall 5 — the exact lines that read `widget.trail` unguarded)
  </read_first>
  <action>
    Change `final Trail trail;` to `final Trail? trail;` and the constructor `required this.trail` to `this.trail` (keep it a named param, now optional). Then fix the two sites that read `trail` non-null-safely:

    1. Stats header non-scrub branch (currently reads `widget.trail.elevationGain` and `widget.trail.elevationLoss`): when `widget.trail == null`, source the elevation gain/loss from `widget.gpx.getTotals()` — use `GpxStats.totalElevationGain` and `GpxStats.totalElevationloss` (match the lowercase-`l` spelling exactly). When `widget.trail != null`, keep reading `widget.trail!.elevationGain`/`elevationLoss` as before. Compute the `getTotals()` result once and branch, so the Font Awesome trend-up/trend-down stat cells still render.

    2. `_buildChart` waypoint overlay (currently `widget.trail.expand?.waypointsViaTrail ?? []`): change to `widget.trail?.expand?.waypointsViaTrail ?? []` so a null trail yields an empty waypoint list — this makes both the `extraLinesData` vertical lines AND the positioned icon markers render nothing when there is no trail (D-12: route anchors must NOT appear as icons on the elevation curve). Re-read the surrounding block after the nullable change to confirm no other `widget.trail.` (unguarded) access remains — `dart analyze` will flag any missed one as a compile error.

    Do not otherwise alter the chart, smoothing, gradient, or scrub logic. Leave `_EmptyState` here (Plan will reuse its pattern from the elevation tab, with a caption override).
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/components/trail/elevation_profile.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Trail? trail" app/lib/components/trail/elevation_profile.dart` returns one match (nullable field).
    - `grep -c "widget.trail\." app/lib/components/trail/elevation_profile.dart` shows every remaining direct access is null-safe (`widget.trail?.` or `widget.trail!` guarded by a null check) — no unguarded `widget.trail.` access on the nullable field.
    - `grep -n "getTotals()" app/lib/components/trail/elevation_profile.dart` returns a match (the trail-null stats fallback).
    - `cd app && flutter analyze lib/components/trail/elevation_profile.dart` reports no errors (this is the primary Pitfall 5 gate — an unguarded nullable access is a compile error).
  </acceptance_criteria>
  <done>ElevationProfile accepts a null trail, derives its stats from gpx.getTotals() in that case, and renders no anchor icons on the chart.</done>
</task>

<task type="auto">
  <name>Task 2: Create ElevationTab — tab-gated debounced height fetch + empty state (PLANUI-02, D-10/D-11/D-13)</name>
  <files>app/lib/components/route_planner/elevation_tab.dart</files>
  <read_first>
    - app/lib/provider/planned_gpx_provider.dart (Plan 01 output — `plannedGpxProvider(travelProfile)` supplies the pre-elevation Gpx this tab reads)
    - app/lib/util/gpx_util.dart (`buildNavShape(points)` — reuse to downsample to <=500 points before POSTing to /height; `GpxMappingUtils.allPoints`)
    - app/lib/provider/search/global_search_provider.dart (the `Timer(const Duration(milliseconds: 500), ...)` debounce idiom to copy)
    - app/lib/provider/api_provider.dart (`apiProvider` / `api.post(...)` — the Dio client used for the /valhalla/height call)
    - app/lib/components/trail/elevation_profile.dart (Task 1 output — construct `ElevationProfile(trail: null, gpx: ...)`; reuse its `_EmptyState` visual pattern via a local equivalent for D-13)
    - web/src/routes/api/v1/valhalla/height/+server.ts (the pass-through proxy — request body `{shape: [{lat, lon}, ...]}`, response `{height: [...]}` a flat array in shape order)
    - .planning/phases/20-route-planner-views-waypoint-list-elevation-location-search/20-RESEARCH.md (Pattern 5 height fetch + Pitfall 2 tab-visibility gating; Assumptions Log A1 on the /height point cap)
    - .planning/phases/20-route-planner-views-waypoint-list-elevation-location-search/20-UI-SPEC.md (Elevation Tab Contract + Copywriting: empty-state copy)
  </read_first>
  <action>
    Create `ElevationTab` as a `ConsumerStatefulWidget` with a required `String travelProfile`. It watches `ref.watch(plannedGpxProvider(travelProfile))` for the live pre-elevation `Gpx`.

    Empty state (D-13): when the planned Gpx has fewer than 2 points (i.e. fewer than 2 anchors' worth of geometry), render an empty-state container matching `ElevationProfile._EmptyState`'s look (200px height, `colorScheme.secondaryContainer` background, `borderRadius: 16`, centered `Icons.terrain` size 48 + Body-14 caption) with the caption copy `'Add at least 2 anchors to see the elevation profile.'`. Do NOT call `/valhalla/height` in this case.

    Tab-visibility gating (D-11, Pitfall 2): in `initState`, obtain the `TabController` via `DefaultTabController.of(context)` (defer to `didChangeDependencies` if `of(context)` is null in initState) and add a listener; treat the tab as "visible" when `controller.index == 1 && !controller.indexIsChanging`. Trigger a height fetch when the tab becomes visible AND whenever the watched Gpx changes while the tab is visible — never on mere widget build/mount (TabBarView keeps both tabs built simultaneously, so build presence != visibility).

    Debounced fetch (D-11): hold a `Timer? _debounce`; on a route change while visible, `_debounce?.cancel()` then `_debounce = Timer(const Duration(milliseconds: 500), _fetchHeights)`. `_fetchHeights` reads the current planned Gpx's ordered points (`allPoints`), guards on `>= 2` points, POSTs `{'shape': buildNavShape(points)}` to `/valhalla/height` via `ref.read(apiProvider)`, reads `response.data['height'] as List` cast to num, and builds a LOCAL copy of the Gpx with `ele` populated per point by index (same order as the request shape). Hold that ele-merged Gpx in local state and pass it to `ElevationProfile(trail: null, gpx: eleMergedGpx)`. Do NOT write `ele` back into `plannedGpxProvider` (D-10 — it stays pre-elevation and is Phase 21's handoff artifact). Cancel the timer and any pending work in `dispose`; guard `setState` with `mounted`.

    Note the shape/point-index alignment: `buildNavShape` may downsample >500 points, so when merging heights back, merge against the SAME downsampled shape you sent (build the ele-merged Gpx from the downsampled points + returned heights), not against the full original point list.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/components/route_planner/elevation_tab.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ElevationTab" app/lib/components/route_planner/elevation_tab.dart` returns one match.
    - `grep -n "plannedGpxProvider" app/lib/components/route_planner/elevation_tab.dart` returns a match (watches the derived Gpx).
    - `grep -n "valhalla/height" app/lib/components/route_planner/elevation_tab.dart` returns a match; `grep -n "buildNavShape" app/lib/components/route_planner/elevation_tab.dart` returns a match (downsampled shape sent).
    - `grep -n "indexIsChanging\|_tabController.index\|controller.index" app/lib/components/route_planner/elevation_tab.dart` returns a match (visibility gated on TabController, not build lifecycle — Pitfall 2).
    - `grep -n "Timer(const Duration(milliseconds: 500)" app/lib/components/route_planner/elevation_tab.dart` returns a match (500ms debounce).
    - `grep -n "trail: null" app/lib/components/route_planner/elevation_tab.dart` returns a match (renders the trail-less ElevationProfile).
    - `cd app && flutter analyze lib/components/route_planner/elevation_tab.dart` reports no errors.
  </acceptance_criteria>
  <done>ElevationTab renders a live elevation profile from the planned route, fetches height only while visible with a 500ms debounce, shows the empty state under 2 anchors, and never writes ele back into the derived provider.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthesized route points → `/valhalla/height` | Client-derived shape crosses to the existing SvelteKit pass-through proxy (unchanged endpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Denial of Service | oversized /valhalla/height payload from a long route | mitigate | Reuse `buildNavShape()` (<=500-point downsampler) before POSTing (RESEARCH.md A1) |
| T-20-03-02 | Denial of Service | height refetch on every route edit | mitigate | 500ms debounce + tab-visibility gate (D-11) so fetches fire only while the Elevation tab is open and settle |
| T-20-03-03 | Tampering | height array index alignment | accept | Points sent are Phase-19-validated anchor coordinates; heights merged by index against the exact shape sent — no unvalidated external index source |
| T-20-03-SC | Tampering | package installs | accept | No package installs — reuses `dio`, `fl_chart`, `gpx`, `flutter_riverpod` already in pubspec (RESEARCH.md audit: N/A) |
</threat_model>

<verification>
- `cd app && flutter analyze lib/components/trail/elevation_profile.dart lib/components/route_planner/elevation_tab.dart` reports no errors.
- HUMAN (end-of-phase): open the Elevation tab on a >=2-anchor route and confirm the profile renders and updates as anchors are added/moved; confirm the <2-anchor empty state; confirm the height call does not fire while the Route Anchors tab is showing.
</verification>

<success_criteria>
- `ElevationProfile.trail` is `Trail?` and renders correctly with a null trail (no anchor icons, gpx-derived stats).
- `ElevationTab` fetches `/valhalla/height` only while visible, debounced, and shows the empty state under 2 anchors.
- No `ele` is written back into `plannedGpxProvider`.
</success_criteria>

<output>
Create `.planning/phases/20-route-planner-views-waypoint-list-elevation-location-search/20-03-SUMMARY.md` when done.
</output>
