---
phase: 24-settings-offline-maps-regions-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/services/tile_repository_manager.dart
  - app/lib/provider/region/tile_repository_provider.dart
  - app/lib/provider/region/region_provider.dart
  - app/lib/provider/region/region_provider.g.dart
  - app/lib/util/byte_format_util.dart
  - app/lib/util/region_disk_usage_util.dart
  - app/test/util/byte_format_util_test.dart
  - app/test/util/region_disk_usage_util_test.dart
  - app/lib/i18n/app_en.arb
autonomous: true
requirements: [SETUI-01, SETUI-02, SETUI-04, SETUI-05]
must_haves:
  truths:
    - "TileRepositoryManager can delete ONLY a region's DEM package, leaving its vector package and vector file intact (D-01)"
    - "The region list is readable as a synchronous ObjectBox snapshot sorted A-Z by name (D-09)"
    - "A region's real current on-disk byte usage — final files AND partial .part files — can be summed for the disk-usage summary (D-06, Pitfall 1)"
    - "Byte counts render as human-readable one-decimal KB/MB/GB strings"
    - "Every English UI string this phase needs exists in app_en.arb"
  artifacts:
    - path: "app/lib/services/tile_repository_manager.dart"
      provides: "deleteDemPackage(String regionId) — DEM-only cascade delete"
      contains: "Future<void> deleteDemPackage"
    - path: "app/lib/provider/region/tile_repository_provider.dart"
      provides: "TileRepositoryStatus.deleteDemPackage notifier method"
      contains: "deleteDemPackage"
    - path: "app/lib/provider/region/region_provider.dart"
      provides: "RegionListNotifier synchronous snapshot provider"
      contains: "class RegionListNotifier"
    - path: "app/lib/util/byte_format_util.dart"
      provides: "formatBytes(int) -> '45 MB' / '2.4 GB'"
      contains: "String formatBytes"
    - path: "app/lib/util/region_disk_usage_util.dart"
      provides: "regionDiskUsageBytes + totalRegionDiskUsageBytes"
      contains: "regionDiskUsageBytes"
    - path: "app/lib/i18n/app_en.arb"
      provides: "Phase 24 English l10n keys"
      contains: "settings_offline_regions_title"
  key_links:
    - from: "app/lib/services/tile_repository_manager.dart"
      to: "app/lib/util/region_file_path.dart"
      via: "regionDemPath / assertValidRegionId (never string-concat a path)"
      pattern: "regionDemPath\\("
    - from: "app/lib/util/region_disk_usage_util.dart"
      to: "app/lib/util/region_file_path.dart"
      via: "regionVectorPath / regionDemPath"
      pattern: "regionVectorPath\\(|regionDemPath\\("
    - from: "app/lib/provider/region/region_provider.dart"
      to: "objectBoxProvider"
      via: "store.box<RegionEntity>().getAll()"
      pattern: "box<RegionEntity>"
---

<objective>
Build the non-UI foundation Phase 24's Offline Maps/Regions screen consumes: the one net-new download-engine method (D-01 DEM-only delete), a synchronous region-list snapshot provider, two pure utilities (byte formatting + real-on-disk usage aggregation), and every English l10n string the screen renders. This plan touches no UI — it defines the contracts the screen (Plan 02) is written against, interface-first.

Purpose: Deliver the four gaps RESEARCH.md flagged as "not yet built anywhere" — `deleteDemPackage` (SETUI-04's missing piece), `RegionListNotifier` (SETUI-01/02 data source), `regionDiskUsageBytes` (SETUI-05, correcting Pitfall 1), and `formatBytes` — plus the string contract, so Plan 02 wires a screen to symbols that already exist rather than exploring for them.
Output: `deleteDemPackage` on `TileRepositoryManager` + `TileRepositoryStatus`; `RegionListNotifier`/`regionListNotifierProvider`; `byte_format_util.dart`; `region_disk_usage_util.dart` (+ unit tests); new keys in `app_en.arb`.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-settings-offline-maps-regions-ui/24-CONTEXT.md
@.planning/phases/24-settings-offline-maps-regions-ui/24-RESEARCH.md
@.planning/phases/24-settings-offline-maps-regions-ui/24-PATTERNS.md
@.planning/phases/24-settings-offline-maps-regions-ui/24-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
New symbols introduced across Phase 24 (both plans) — the source-grounding pass must treat these as newly-created, not drift:

- `TileRepositoryManager.deleteDemPackage(String regionId)` — method (Plan 01)
- `TileRepositoryStatus.deleteDemPackage(String regionId)` — Riverpod notifier method (Plan 01)
- `RegionListNotifier` class + generated `regionListNotifierProvider` — new `@riverpod` provider in `region_provider.dart` (Plan 01)
- `formatBytes(int bytes)` — function in new `app/lib/util/byte_format_util.dart` (Plan 01)
- `regionDiskUsageBytes(String root, RegionEntity region)` and `totalRegionDiskUsageBytes(Iterable<RegionEntity> regions)` — functions in new `app/lib/util/region_disk_usage_util.dart` (Plan 01)
- `SettingsOfflineRegionsScreen` widget + `_SettingsOfflineRegionsScreenState` — new `app/lib/routes/settings_offline_regions_screen.dart` (Plan 02)
- New route path `/settings/regions` and its `GoRoute` in `router_provider.dart` (Plan 02)
- New l10n keys in `app_en.arb` (Plan 01): `settings_offline_regions_title`, `settings_offline_regions_tile`, `regions_search_hint`, `regions_size_breakdown`, `regions_dem_toggle_label`, `regions_dem_toggle_caption`, `regions_update_available`, `regions_update_action`, `regions_retry`, `regions_not_yet_available`, `regions_build_failed`, `regions_delete_confirm_title`, `regions_delete_confirm_body`, `regions_delete_confirm_action`, `regions_disk_usage_summary`, `regions_empty_search_title`, `regions_empty_search_body`, `regions_empty_catalog_title`, `regions_empty_catalog_body` (final key set at executor discretion; the screen in Plan 02 must reference only keys added here)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: DEM-only delete on TileRepositoryManager + TileRepositoryStatus (D-01)</name>
  <files>app/lib/services/tile_repository_manager.dart, app/lib/provider/region/tile_repository_provider.dart</files>
  <read_first>
    - app/lib/services/tile_repository_manager.dart — read `deleteRegion` (lines ~319-366) as the exact shape to narrow, and `pauseRegion` (lines ~259-270) for the selective `'$id:dem'` cancel idiom; note `_regionById`, `_activeCancelTokens`, and the best-effort file-deletion block
    - app/lib/provider/region/tile_repository_provider.dart — read the existing `delete` (lines ~138-141) and `downloadDem` (lines ~69-97) notifier methods for the try/finally ephemeral-state shape
    - app/lib/util/region_file_path.dart — `assertValidRegionId`, `regionDemPath`, `regionStorageDir` (the only sanctioned path builders)
    - app/lib/entities/region_entity.dart — `demPackage`/`vectorPackage` ToOne fields
    - app/lib/entities/downloaded_tile_package_entity.dart — the box row `deleteDemPackage` removes
  </read_first>
  <action>
    Per D-01, add `Future<void> deleteDemPackage(String regionId)` to `TileRepositoryManager`, modeled on `deleteRegion` but narrowed to the DEM package only. It MUST: (1) `assertValidRegionId(regionId)` first, then cancel ONLY the `'$id:dem'` entry in `_activeCancelTokens` (never `'$id:vector'` — mirror the selective-cancel loop but drop the vector clause); (2) resolve the region via `_regionById(id)`, no-op return if null (match `deleteRegion`'s unknown-region no-op); (3) inside a single `_store.runInTransaction(TxMode.write, ...)`, remove the `region.demPackage.target` row from `box<DownloadedTilePackageEntity>()` if non-null, set `region.demPackage.target = null`, and `put` the region — leave `region.vectorPackage`, its target row, and `region.lastDownloadedVersion` UNTOUCHED (D-01's explicit asymmetry vs `deleteRegion`); (4) best-effort outside the transaction, delete ONLY `regionDemPath(root, id)` and `'${regionDemPath(root, id)}.part'` (never `regionVectorPath`); do NOT remove the region storage dir (the vector file may still live there). Then add a matching `deleteDemPackage(String regionId)` to the `TileRepositoryStatus` notifier: call `ref.read(tileRepositoryManagerProvider).deleteDemPackage(regionId)` inside a try/finally that clears the region's ephemeral map entry (`state = {...state}..remove(regionId)`), mirroring the existing `delete` method — no progress tracking. Reuse `_getOrCreatePackage`/`_updatePackageStatus`? No — this is a pure removal, not a status transition; do not create a package row.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/services/tile_repository_manager.dart lib/provider/region/tile_repository_provider.dart && grep -q "Future<void> deleteDemPackage" lib/services/tile_repository_manager.dart && grep -q "deleteDemPackage" lib/provider/region/tile_repository_provider.dart && grep -q "regionDemPath" lib/services/tile_repository_manager.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` passes clean on both edited files
    - `deleteDemPackage` in `tile_repository_manager.dart` cancels a `'$id:dem'` token but contains NO `'$id:vector'` cancel and NO reference to `regionVectorPath` (grep: `grep -A25 'Future<void> deleteDemPackage' lib/services/tile_repository_manager.dart | grep -c "vector"` returns 0 for cancel/path lines — a doc-comment mention is acceptable, a `region.vectorPackage.target = null` or `regionVectorPath(` is NOT)
    - The method removes `region.demPackage.target` and sets `region.demPackage.target = null` inside a `runInTransaction(TxMode.write` block
    - The method deletes `regionDemPath(root, id)` and its `.part` sibling; it does NOT delete `regionStorageDir`
    - `TileRepositoryStatus.deleteDemPackage` calls `tileRepositoryManagerProvider`'s `deleteDemPackage` and clears the region's ephemeral state entry in a `finally`
    - Every filesystem path routes through `assertValidRegionId`/`regionDemPath` — no string-concatenated path literal (T-24-01)
  </acceptance_criteria>
  <done>Toggling a region's DEM off can remove only the DEM row + file, leaving the vector package downloaded and usable; the vector lifecycle is provably untouched by DEM deletion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: regionListNotifierProvider snapshot + byte-format + disk-usage utilities</name>
  <files>app/lib/provider/region/region_provider.dart, app/lib/provider/region/region_provider.g.dart, app/lib/util/byte_format_util.dart, app/lib/util/region_disk_usage_util.dart, app/test/util/byte_format_util_test.dart, app/test/util/region_disk_usage_util_test.dart</files>
  <read_first>
    - app/lib/provider/trail/trail_library_provider.dart — verbatim structural precedent for the synchronous ObjectBox snapshot provider (`@riverpod class ... build() => box.getAll()...`)
    - app/lib/provider/region/region_provider.dart — the file the new provider is appended to (keeps `regionRepository` provider intact)
    - app/lib/entities/region_entity.dart — `RegionEntity.name`, `vectorPackage`/`demPackage` ToOne targets, `status` getter
    - app/lib/util/region_file_path.dart — `assertValidRegionId`, `regionVectorPath`, `regionDemPath` (the disk-usage util MUST route through these)
    - app/lib/util/disk_space_util.dart — sibling byte-oriented util for directory/naming convention
    - app/lib/provider/objectbox_store_provider.dart — `objectBoxProvider`
    - app/test/util/region_file_path_test.dart — test-file conventions (import style, group/test structure, temp-dir usage)
  </read_first>
  <behavior>
    byte_format_util (`formatBytes(int bytes)`):
    - Test: `formatBytes(0)` == "0 B"
    - Test: `formatBytes(512)` == "512 B"
    - Test: `formatBytes(1024)` == "1.0 KB"
    - Test: `formatBytes(1536)` == "1.5 KB"
    - Test: `formatBytes(1024 * 1024)` == "1.0 MB"
    - Test: `formatBytes((2.4 * 1024 * 1024 * 1024).round())` == "2.4 GB"
    region_disk_usage_util (`regionDiskUsageBytes(String root, RegionEntity region)`):
    - Test: a region whose vector final file exists under `regionVectorPath(root, id)` with N bytes contributes N (write a temp file)
    - Test: a region with only a `${regionVectorPath(root,id)}.part` partial file of M bytes contributes M (final absent, .part counted — Pitfall 1)
    - Test: a region with neither vector nor DEM file on disk contributes 0
    - Test: vector final (N) + DEM final (K) both present sums to N+K
    - Test: an invalid region id (e.g. "../etc") throws ArgumentError via `assertValidRegionId`
  </behavior>
  <action>
    Add `RegionListNotifier` to `region_provider.dart` mirroring `TrailLibraryNotifier` verbatim: `@riverpod class RegionListNotifier extends _$RegionListNotifier { @override List<RegionEntity> build() { final store = ref.watch(objectBoxProvider); final regions = store.box<RegionEntity>().getAll(); regions.sort((a, b) => a.name.toLowerCase().compareTo(b.name.toLowerCase())); return regions; } }` — alphabetical A-Z by `name` per D-09 (NOT by created, NOT downloaded-first). No mutation methods (all mutations flow through `TileRepositoryStatus`; callers `ref.invalidate(regionListNotifierProvider)` after each per Pitfall 2). The generated provider symbol is `regionListNotifierProvider` (Plan 02's screen watches exactly this). Run `dart run build_runner build --delete-conflicting-outputs` so `region_provider.g.dart` gains it. Create `byte_format_util.dart` with `String formatBytes(int bytes)` using 1024-based steps (const kb/mb/gb), one decimal at KB/MB/GB via `toStringAsFixed(1)`, bare `'$bytes B'` below 1 KB — exactly per RESEARCH.md's Code Examples snippet and UI-SPEC's "one decimal place, unit steps at KB/MB/GB" convention. Create `region_disk_usage_util.dart` with a pure `int regionDiskUsageBytes(String root, RegionEntity region)` that, for both `regionVectorPath(root, region.id)` and `regionDemPath(root, region.id)`, adds the final file's `lengthSync()` if it exists, else the `'$path.part'` file's `lengthSync()` if it exists, else 0 (D-06 / Pitfall 1 — read real on-disk bytes, never trust `sizeBytesOnDisk`); it MUST call `assertValidRegionId(region.id)` and build every path via `regionVectorPath`/`regionDemPath` (T-24-01), never string-concatenate. Add a thin `Future<int> totalRegionDiskUsageBytes(Iterable<RegionEntity> regions)` that resolves the root once via `getApplicationDocumentsDirectory()` and sums `regionDiskUsageBytes(root, r)` across regions. Write the two unit-test files first (RED) covering the behaviors above using temp directories, then implement to green.
  </action>
  <verify>
    <automated>cd app && dart run build_runner build --delete-conflicting-outputs && flutter analyze lib/provider/region/region_provider.dart lib/util/byte_format_util.dart lib/util/region_disk_usage_util.dart && flutter test test/util/byte_format_util_test.dart test/util/region_disk_usage_util_test.dart && grep -q "regionListNotifierProvider" lib/provider/region/region_provider.g.dart</automated>
  </verify>
  <acceptance_criteria>
    - `region_provider.g.dart` defines `regionListNotifierProvider` after build_runner
    - `RegionListNotifier.build()` sorts by `name` case-insensitively ascending (grep: `name.toLowerCase().compareTo`)
    - `flutter test test/util/byte_format_util_test.dart` passes all listed behaviors incl. "1.0 KB", "2.4 GB"
    - `flutter test test/util/region_disk_usage_util_test.dart` passes incl. the .part-only case and the ArgumentError-on-invalid-id case
    - `region_disk_usage_util.dart` contains `regionVectorPath(` and `regionDemPath(` and `assertValidRegionId(` and contains NO string-concatenated path (no `'/regions/'` literal, no `p.join` bypassing the builders)
    - `formatBytes` uses `toStringAsFixed(1)` and 1024/1048576/1073741824 thresholds
  </acceptance_criteria>
  <done>The screen can render an A-Z region list from local ObjectBox, format any byte count, and show a disk-usage total that correctly includes in-progress/paused partial files.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 24 English l10n keys</name>
  <files>app/lib/i18n/app_en.arb</files>
  <read_first>
    - app/lib/i18n/app_en.arb — existing key style (JSON object, snake_case keys, `@key` ICU metadata blocks for placeholder-bearing keys like `settings_categories_confirm_disable_body`)
    - app/lib/i18n/l10n.yaml is not present at app root; config is `arb-dir: lib/i18n`, `template-arb-file: app_en.arb`, `output-localization-file: app_localizations.dart` (confirmed) — English is the template locale, so keys added here generate getters; other locales fall back to English until translated via crowdin (established, per quick-260720-s7m)
    - .planning/phases/24-settings-offline-maps-regions-ui/24-UI-SPEC.md — the "Copywriting Contract" table is the source of truth for every string's exact copy
  </read_first>
  <action>
    Add the Phase 24 English strings to `app_en.arb`, copy sourced verbatim from UI-SPEC's Copywriting Contract. Required keys and values: `settings_offline_regions_title` = "Offline Maps/Regions" (AppBar + Settings tile label, D per UI-SPEC); `settings_offline_regions_tile` may reuse the same title string (executor may collapse to one key if identical); `regions_search_hint` = "Search regions" (D-08); `regions_dem_toggle_label` = "Download elevation data (DEM)"; `regions_dem_toggle_caption` = "Adds hillshading; increases download size" (SETUI-04 optional/adds-size framing); `regions_update_available` = "Update available" (D-05 banner); `regions_update_action` = "Update"; `regions_retry` = "Retry" (D-03); `regions_not_yet_available` = "Not yet available" (D-09 building catalog status); `regions_build_failed` = "Build failed" (D-09 error catalog status); `regions_delete_confirm_title` = "Delete {name}?" with an `@regions_delete_confirm_title` ICU metadata block declaring the `name` String placeholder (D-02); `regions_delete_confirm_body` = "This removes the downloaded map and elevation data for this region. You'll need to download it again to use it offline."; `regions_delete_confirm_action` = "Delete"; `regions_disk_usage_summary` = "{size} used across {count} downloaded region(s)" with an `@regions_disk_usage_summary` block declaring `size` (String) and `count` (num, plural-capable) placeholders (SETUI-05 / D-06); `regions_empty_search_title` = "No matching regions"; `regions_empty_search_body` = "Try a different search term."; `regions_empty_catalog_title` = "No offline regions available"; `regions_empty_catalog_body` = "Ask your Wanderer instance administrator to configure downloadable regions." (REGN-01 fresh-instance empty state). Reuse existing `download`, `cancel`, `error_saving_settings` keys where the screen needs them — do NOT duplicate them. Follow the ICU-metadata and double-quote-around-interpolated-values conventions established by quick-260720-s7m (single quotes are ICU escape chars). Run `flutter gen-l10n` so `AppLocalizations` gains the getters.
  </action>
  <verify>
    <automated>cd app && flutter gen-l10n && grep -q "settings_offline_regions_title" lib/i18n/app_en.arb && grep -q "regions_delete_confirm_title" lib/i18n/app_en.arb && grep -q "regions_disk_usage_summary" lib/i18n/app_en.arb && grep -q "settingsOfflineRegionsTitle\|settings_offline_regions_title" lib/i18n/app_localizations.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter gen-l10n` succeeds with no ICU/parse errors (guards against the single-quote-escape pitfall)
    - `app_en.arb` is valid JSON and contains every key listed in the action
    - `regions_delete_confirm_title` and `regions_disk_usage_summary` each have a corresponding `@`-prefixed metadata block declaring their placeholders
    - The generated `app_localizations.dart` exposes a getter/method for `settings_offline_regions_title` and `regions_delete_confirm_title`
  </acceptance_criteria>
  <done>Every string Plan 02's screen renders resolves through `AppLocalizations`; no hard-coded UI literals will be needed in the screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ObjectBox catalog row → filesystem path | A region's `id` (persisted from a fetched catalog) is turned into an on-disk path by `deleteDemPackage` and `region_disk_usage_util` |
| npm/pub dependency set | This plan adds ZERO new packages (confirmed against `app/pubspec.yaml` and RESEARCH.md's "Package Legitimacy Audit: Not applicable") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `deleteDemPackage`, `region_disk_usage_util` region-id→path | mitigate | Every path routes through `assertValidRegionId` + `regionDemPath`/`regionVectorPath` (regex allow-list `^[a-z0-9][a-z0-9_-]*$`, throws `ArgumentError`); never string-concatenate an id into a path (T-23-01 continuity) |
| T-24-02 | Tampering | `deleteDemPackage` deleting the wrong package | mitigate | Cancels only `'$id:dem'` token, removes only `demPackage` row + `regionDemPath` file, provably leaves `vectorPackage`/`regionVectorPath` untouched (D-01); unit-verified by grep acceptance criterion |
| T-24-SC | Tampering | Supply chain (pub installs) | accept | No package-manager install task in this plan or phase; RESEARCH.md audit confirms zero new dependencies — legitimacy gate N/A |
</threat_model>

<verification>
- `cd app && flutter analyze lib/services/tile_repository_manager.dart lib/provider/region/tile_repository_provider.dart lib/provider/region/region_provider.dart lib/util/byte_format_util.dart lib/util/region_disk_usage_util.dart` passes clean
- `cd app && flutter test test/util/byte_format_util_test.dart test/util/region_disk_usage_util_test.dart` all green
- `dart run build_runner build --delete-conflicting-outputs` succeeds; `region_provider.g.dart` contains `regionListNotifierProvider`
- `flutter gen-l10n` succeeds; new keys present in `app_en.arb` and generated `app_localizations.dart`
</verification>

<success_criteria>
- `TileRepositoryManager.deleteDemPackage` and `TileRepositoryStatus.deleteDemPackage` exist and are DEM-only (D-01)
- `regionListNotifierProvider` returns an A-Z-by-name `List<RegionEntity>` snapshot (D-09)
- `formatBytes` and `regionDiskUsageBytes`/`totalRegionDiskUsageBytes` exist, unit-tested, reading real on-disk bytes incl. `.part` files (D-06, SETUI-05)
- All Phase 24 English l10n keys exist and generate cleanly
- No new pub dependencies introduced
</success_criteria>

<output>
Create `.planning/phases/24-settings-offline-maps-regions-ui/24-01-SUMMARY.md` when done
</output>
