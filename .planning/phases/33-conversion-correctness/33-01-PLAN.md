---
phase: 33-conversion-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/models/gpx/gpx.ts
  - web/src/lib/models/gpx/gpx.test.ts
autonomous: true
requirements: [CONV-01, CONV-02]
must_haves:
  truths:
    - "D-03: the `getTotals()` accumulation loop starts at index 0, so a track segment's first point contributes to distance, bounding box, and centroid (CONV-01)"
    - "CONV-02: the centroid divides by exactly the number of points the loop summed, never by a larger `allPoints.length`"
    - "A 2-point track segment whose endpoints are ~134.6 m apart reports ~134.6 m instead of 0"
    - "A 2-segment route (route-planner leg shape, shared anchor point) reports the full polyline length across both legs instead of dropping the opening hop"
    - "D-04: a Vitest fixture suite exists for `GPX.getTotals()` covering the 2-point segment, the extreme-first-point centroid/bbox case, the multi-leg planner route, and the zero-point regression guard"
    - "D-05: every fixture in the new suite is built inline through the real `GPX`/`Track`/`TrackSegment`/`Waypoint` constructors — no disk-read `.gpx` files, no mocks"
    - "The zero-point track keeps its pre-existing sentinel behavior (`centroid` NaN, `boundingBox` Infinity/-Infinity, `distance` 0) — this phase does not silently change it"
  artifacts:
    - path: "web/src/lib/models/gpx/gpx.ts"
      provides: "Corrected getTotals() accumulation loop and centroid divisor"
      contains: "summedPointCount"
    - path: "web/src/lib/models/gpx/gpx.test.ts"
      provides: "Vitest fixture suite for GPX.getTotals() CONV-01/CONV-02"
      min_lines: 60
  key_links:
    - from: "web/src/lib/models/gpx/gpx.ts"
      to: "web/src/lib/models/gpx/gpx-metrics-computation.ts"
      via: "metrics.addAndFilter(point) called once per point, starting at index 0"
      pattern: "for \\(let i = 0; i < pointLength; i\\+\\+\\)"
    - from: "web/src/lib/models/gpx/gpx.test.ts"
      to: "web/src/lib/models/gpx/gpx.ts"
      via: "relative import of the class under test"
      pattern: "from \"\\./gpx\""
---

<objective>
Fix the off-by-one accumulation loop in `GPX.getTotals()` (D-03) — the single root cause behind
CONV-01, CONV-02, and the route planner's dropped opening hop — and pin the corrected behavior
with the first Vitest fixture suite this domain has ever had (D-04, D-05).

Purpose: this is the root-cause fix the rest of the phase builds on. D-03 locks it to land
**first**: plan 33-03's `cumulativeDistance` rebuild is only index-aligned if `addAndFilter()`
is already being called once per point starting at index 0.

Output: a corrected `web/src/lib/models/gpx/gpx.ts` and a new
`web/src/lib/models/gpx/gpx.test.ts`.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-conversion-correctness/33-CONTEXT.md
@.planning/phases/33-conversion-correctness/33-RESEARCH.md
@.planning/phases/33-conversion-correctness/33-PATTERNS.md
@CLAUDE.md
</context>

## Source Coverage Audit (phase-level, all four artifact types)

```
SOURCE    | ID      | Feature/Requirement                                              | Plan  | Status  | Notes
--------- | ------- | ---------------------------------------------------------------- | ----- | ------- | -----
GOAL      | —       | Every GPX converted anywhere reports correct distance/elevation/duration | 01-03 | COVERED | Duration path is untouched by the four defects; loop fix restores it implicitly (segment start/end times already read points[0]/points[n-1] outside the buggy loop)
REQ       | CONV-01 | First track point included in distance/bbox/centroid; planned route stops dropping the opening hop | 01 | COVERED | Task 1
REQ       | CONV-02 | Centroid divides by the same point count it summed                | 01    | COVERED | Task 1 (`summedPointCount`)
REQ       | CONV-03 | Points with no elevation excluded from gain/loss, not treated as 0 m | 02  | COVERED | 33-02 Task 1
REQ       | CONV-04 | Elevation gain/loss sampled independently of the horizontal threshold | 02 | COVERED | 33-02 Task 2
REQ       | CONV-05 | Distance from the smoothed accumulator; misaligned `cumulativeDistance` replaced | 03 | COVERED | 33-03 Tasks 1-2
RESEARCH  | —       | Fix loop bound first (root cause of CONV-01/02 + planner symptom) | 01    | COVERED | Wave ordering 1 -> 2 -> 3
RESEARCH  | —       | Do NOT reset `metrics` anchors at segment boundaries (Pitfall 2) | 01    | COVERED | Task 1 negative acceptance criterion
RESEARCH  | —       | `point.ele` is genuinely `undefined` when `<ele>` absent (Pitfall 3) | 02  | COVERED | 33-02 Task 1
RESEARCH  | —       | Keep `cumulativeDistance` raw/per-point, not rescaled to smoothed (Anti-Pattern 2) | 03 | COVERED | 33-03 Task 1
RESEARCH  | —       | Do NOT remove the horizontal threshold from distance smoothing (Anti-Pattern 3) | 02 | COVERED | 33-02 Task 2 negative acceptance criterion
RESEARCH  | —       | Zero-point/empty-segment edge case: assert unchanged, do not "fix" (Pitfall 4, Open Q2) | 01 | COVERED | Task 2 regression guard
RESEARCH  | —       | No new npm packages; Package Legitimacy Gate not applicable      | 01-03 | COVERED | No install tasks anywhere in this phase
RESEARCH  | —       | ASVS V5: no NaN/Infinity may reach trail.distance/elevation_*     | 01-03 | COVERED | `Number.isFinite` guards in 33-02/33-03; threat models in all three plans
RESEARCH  | —       | `ElevationProfileControl` computes its own distance array — out of blast radius | —  | N/A     | Explicitly out of scope per RESEARCH.md
CONTEXT   | D-01    | Repair `cumulativeDistance` in place as raw index-aligned array   | 03    | COVERED | 33-03 Task 1
CONTEXT   | D-02    | Crop slider rescales to the raw array's own total                 | 03    | COVERED | 33-03 Task 2
CONTEXT   | D-03    | Loop-bound fix first; cumulativeDistance rebuild last             | 01    | COVERED | This plan is wave 1; 33-03 is wave 3
CONTEXT   | D-04    | Vitest fixture suite, one fixture per success criterion           | 01-03 | COVERED | 33-01 Task 2, 33-02 Task 3, 33-03 Task 3
CONTEXT   | D-05    | Inline fixtures (object graphs for computation, XML strings for the parse path) | 01-03 | COVERED | Enforced in every test task's acceptance criteria
CONTEXT   | —       | CONV-06 (moving time) deferred to Phase 34                        | —     | N/A     | Deferred Ideas
CONTEXT   | —       | CONV-F01 migration of already-stored trails deferred              | —     | N/A     | Deferred Ideas — surfaced as a known risk below, no task
```

All rows COVERED or N/A. No unplanned items.

## Known risks carried by this phase (no task — surfaced deliberately)

1. **CONV-F01 (deferred, REQUIREMENTS.md):** trails already persisted with the buggy metrics keep
   their wrong `distance`/`elevation_gain`/`elevation_loss` until re-saved or re-imported. After
   this phase, a re-saved trail's numbers will visibly differ from its stored numbers. No
   migration or backfill task exists in this phase by explicit product decision. Do not add one.
2. **`web/src/lib/components/trail/trail_anchor_list.svelte` carries an independent copy of the
   same `i = 1` loop bug** (line ~242, `for (let i = 1; i < points.length; i++)`) and reads
   `metrics.totalDistance` (raw) at line ~221 for its per-anchor segment readout. It is **outside
   this phase's locked scope** (the ROADMAP scope note names three files; D-01/D-02 admit only
   `trail/edit/[id]/+page.svelte`). Consequence after 33-03 lands: the anchor list's per-segment
   distances will be raw-sum based and will not add up to the route's reported smoothed distance.
   RESEARCH.md's blast-radius map did not find this file. Flag for a follow-up phase or quick task;
   do not edit it here.

## Artifacts this phase produces

New symbols and files created across plans 33-01 / 33-02 / 33-03 (all three plans list the same
set so the source-grounding pass can exclude them):

| Artifact | Kind | Created in |
|----------|------|------------|
| `web/src/lib/models/gpx/gpx.test.ts` | new file (Vitest suite) | 33-01 Task 2 (extended in 33-03 Task 3) |
| `web/src/lib/models/gpx/gpx-metrics-computation.test.ts` | new file (Vitest suite) | 33-02 Task 3 |
| `summedPointCount` | new local in `GPX.getTotals()` (`gpx.ts`) | 33-01 Task 1 |
| `parseElevation` | new **named export** in `gpx-metrics-computation.ts` | 33-02 Task 1 |
| `waypointAt` | new test helper in `gpx.test.ts` | 33-01 Task 2 |
| `gpxFromSegments` | new test helper in `gpx.test.ts` | 33-01 Task 2 |
| `hopMetres` | new test helper in `gpx.test.ts` | 33-01 Task 2 |
| `gpxXml` | new test helper in `gpx-metrics-computation.test.ts` | 33-02 Task 3 |
| `trkptXml` | new test helper in `gpx-metrics-computation.test.ts` | 33-02 Task 3 |
| `cumulativeRoute` | new local in `updateCropMarkers()` (`trail/edit/[id]/+page.svelte`) | 33-03 Task 2 |
| `rawRouteTotal` | new local in `updateCropMarkers()` (`trail/edit/[id]/+page.svelte`) | 33-03 Task 2 |

No new exported types. `GPXFeature.cumulativeDistance` keeps its declared `number[]` type; only
its semantics change (33-03).

<tasks>

<task type="auto">
  <name>Task 1: Start the getTotals() accumulation loop at index 0 and divide the centroid by the summed point count</name>
  <files>web/src/lib/models/gpx/gpx.ts</files>
  <read_first>
    - `web/src/lib/models/gpx/gpx.ts` — the file being modified; read `getTotals()` (lines 97-157) and `flatten()` (lines 159-171) in full before editing.
    - `web/src/lib/models/gpx/gpx-metrics-computation.ts` — read `addAndFilter()` to confirm the first-ever-call initialization branch (lines 24-31) and that `lastPointXY` is instance state shared across all segments.
    - `.planning/phases/33-conversion-correctness/33-RESEARCH.md` — "Common Pitfalls / Pitfall 2" (cross-segment continuity must NOT be reset) and the system architecture diagram's "DEFECT A" annotation.
    - `.planning/phases/33-conversion-correctness/33-PATTERNS.md` — the `gpx.ts` section: nullish-coalescing idiom (`?? 0` / `?? Infinity`, never `||`), metre-denominated units convention.
  </read_first>
  <action>
    Two edits inside `GPX.getTotals()` in `web/src/lib/models/gpx/gpx.ts`, implementing D-03.

    Edit 1 (CONV-01) — the point loop currently at line 125 reads
    `for (let i = 1; i < pointLength; i++)`. Change the initializer to `i = 0` so every point of
    every `trkseg` is passed to `metrics.addAndFilter(point)` and contributes to `totalLat`,
    `totalLon`, `minLat`, `maxLat`, `minLon`, `maxLon`. Change nothing else inside the loop body —
    the existing `point.$.lat ?? 0` / `?? Infinity` / `?? -Infinity` guards stay exactly as they
    are.

    Edit 2 (CONV-02) — declare a new local `let summedPointCount = 0;` next to the existing
    `let totalLat = 0` / `let totalLon = 0` declarations (currently lines 102-103). Increment it
    once per iteration inside the same loop body that accumulates `totalLat`/`totalLon`. Then
    change the `centroid` construction (currently line 145) so both components divide by
    `summedPointCount` instead of `allPoints.length`. This makes CONV-02 structural: the divisor is
    by construction the same count the numerator summed, independent of how `allPoints` is
    populated.

    Do NOT change: the `allPoints.push(...points)` line (it still feeds `generateMinHash`), the
    per-segment duration block (lines 115-122), the `boundingBox` sentinel initializers at line
    107, or any line in `gpx-metrics-computation.ts`.

    Critical negative constraint (33-RESEARCH.md Pitfall 2): do NOT add any reset of the `metrics`
    instance, `lastPointXY`, or `lastFilteredPointXY` at segment boundaries, and do NOT construct a
    second `GpxMetricsComputation`. The single instance declared at line 105 must stay shared across
    all tracks and segments — that cross-segment continuity is what makes a multi-leg planner route
    measure through its anchors instead of restarting at each leg.

    Zero-point behavior is deliberately preserved: with no points, `summedPointCount` is 0 and
    `0 / 0` yields the same `NaN` centroid the code produces today (33-RESEARCH.md Pitfall 4 /
    Open Question 2). Do not add a guard for it.
  </action>
  <acceptance_criteria>
    - `web/src/lib/models/gpx/gpx.ts` contains the literal string `for (let i = 0; i < pointLength; i++)` and zero occurrences of `for (let i = 1; i < pointLength; i++)`.
    - `web/src/lib/models/gpx/gpx.ts` declares `let summedPointCount = 0;` and the returned `centroid` object divides `totalLat` and `totalLon` by `summedPointCount`; `grep -c "allPoints.length" web/src/lib/models/gpx/gpx.ts` returns `0`.
    - `grep -n "new GpxMetricsComputation" web/src/lib/models/gpx/gpx.ts` returns exactly one line (line ~105) — no per-segment instance was introduced.
    - `grep -nE "lastPointXY|lastFilteredPointXY" web/src/lib/models/gpx/gpx.ts` returns no matches — no anchor reset was added.
    - `git diff --stat` shows `web/src/lib/models/gpx/gpx.ts` as the only changed file for this task.
    - `npx svelte-check --tsconfig ./tsconfig.json` (run from `web/`) reports no new errors in `src/lib/models/gpx/gpx.ts` relative to the pre-task baseline.
    - Behavior spot-check (run from `web/`, any scratch script or the Task 2 suite): parsing
      `<trk><trkseg><trkpt lat="47.0" lon="11.0"/><trkpt lat="47.001" lon="11.001"/></trkseg></trk>`
      yields `features.distance` ≈ 134.59 m (pre-fix value: exactly `0`) and
      `features.boundingBox.minLat === 47.0` (pre-fix value: `47.001`).
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -c "for (let i = 0; i < pointLength; i++)" src/lib/models/gpx/gpx.ts && grep -c "summedPointCount" src/lib/models/gpx/gpx.ts && test "$(grep -c 'allPoints.length' src/lib/models/gpx/gpx.ts)" = "0"</automated>
  </verify>
  <done>`getTotals()` iterates from index 0, the centroid divides by `summedPointCount`, no metrics-anchor reset was introduced, and a 2-point segment reports ~134.59 m instead of 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the GPX.getTotals() Vitest fixture suite for CONV-01 and CONV-02</name>
  <files>web/src/lib/models/gpx/gpx.test.ts</files>
  <behavior>
    - 2-point segment ~134.59 m apart reports `features.distance` within 0.5 m of 134.592 (pre-fix: exactly 0).
    - 3-point segment whose first point is the geographic extreme reports `boundingBox.minLat === 40.0` and `boundingBox.minLon === 10.0` (pre-fix: 47.0 / 11.0), and `centroid.lat` ≈ 45.0, `centroid.lon` ≈ 11.0 (pre-fix: 31.334333 / 7.334333).
    - Centroid divisor invariant: for a single 3-point segment, `centroid.lat` equals the plain mean of the three fixture latitudes to 6 decimal places.
    - Two-segment planner route (each `TrackSegment` a routed leg, the shared anchor point repeated as leg 2's first point) reports `features.distance` within 0.5 m of 444.780 m — the full polyline across both legs (pre-fix: 333.585).
    - Zero-point regression guard: a GPX whose single `Track` has one empty `TrackSegment` still yields `centroid.lat` NaN, `boundingBox.minLat === Infinity`, `boundingBox.maxLat === -Infinity`, `features.distance === 0`, and `flatten()` length 0.
  </behavior>
  <read_first>
    - `web/src/lib/models/trail.test.ts` — the single Vitest analog in this codebase. Copy its structure verbatim: `import { describe, expect, it } from "vitest";`, one top-level `describe` per behavior, plain-English `it(...)` sentences, no `beforeEach`, builder functions declared *below* the describes, real constructors instead of mocks.
    - `web/src/lib/models/gpx/gpx.ts` — the class under test (post-Task-1 state).
    - `web/src/lib/models/gpx/waypoint.ts` — the `Waypoint` constructor: it requires a `$` object; `$.lat`/`$.lon` fall back to `-1` on missing input, `ele` has no fallback.
    - `web/src/lib/models/gpx/track.ts` and `web/src/lib/models/gpx/track-segment.ts` — constructor shapes for building fixtures.
    - `web/src/lib/models/gpx/utils.ts` — `haversineDistance(lat1, lon1, lat2, lon2)` returns metres; import it to express expected distances as computed values rather than hand-copied magic numbers where that reads better.
    - `web/vite.config.ts` — confirms `test.include` is `['src/**/*.{test,spec}.{js,ts}']`, so a co-located `gpx.test.ts` is picked up with no config change.
    - `.planning/phases/33-conversion-correctness/33-CONTEXT.md` — D-04 (fixture coverage) and D-05 (inline fixtures only).
  </read_first>
  <action>
    Create `web/src/lib/models/gpx/gpx.test.ts`.

    Per D-05, these are computation-path tests, so build fixtures as inline object graphs through
    the real constructors — never as `.gpx` files on disk, never with `vi.fn()` mocks. Declare two
    local builder helpers below the describes, following `trail.test.ts`'s `originalTrail()`
    placement convention:
    - `waypointAt(lat: number, lon: number, ele?: number): Waypoint` returning
      `new Waypoint({ $: { lat, lon }, ele })`.
    - `gpxFromSegments(segments: Waypoint[][]): GPX` returning a `GPX` whose single `Track` holds
      one `TrackSegment` per input array.
    - `hopMetres(a: Waypoint, b: Waypoint): number` wrapping `haversineDistance` so expected
      totals can be expressed as sums of real hops.

    Write four `describe` blocks:

    1. `describe("GPX.getTotals — CONV-01 first point of a segment")` with the 2-point fixture at
       (47.0, 11.0) and (47.001, 11.001). Assert `features.distance` is close to 134.592 with a
       0.5 m tolerance (`toBeCloseTo(134.592, 1)` or an explicit tolerance assertion). Add an
       explicit comment recording the pre-fix value `0` so a future regression is unambiguous.
       Both fixture points are >5 m apart on purpose, so the raw and smoothed accumulators agree
       and this assertion survives plan 33-03's distance-source swap.

    2. `describe("GPX.getTotals — CONV-01/CONV-02 centroid and bounding box")` with the 3-point
       fixture at (40.0, 10.0), (47.0, 11.0), (48.0, 12.0). Assert `boundingBox.minLat === 40.0`,
       `boundingBox.minLon === 10.0`, `boundingBox.maxLat === 48.0`, `boundingBox.maxLon === 12.0`,
       and `centroid.lat` / `centroid.lon` close to the plain arithmetic means (45.0 and 11.0) to
       6 decimal places. Add a second `it(...)` that states the CONV-02 invariant directly: the
       centroid equals the mean of every fixture point, i.e. the divisor matches the summed count.

    3. `describe("GPX.getTotals — CONV-01 multi-leg planner route")` with two `TrackSegment`s
       shaped like `valhalla_store.svelte.ts`'s `insertIntoRoute()` output: leg 1 =
       (47.000, 11.0) → (47.001, 11.0) → (47.002, 11.0); leg 2 = (47.002, 11.0) → (47.003, 11.0)
       → (47.004, 11.0), with the shared anchor at 47.002 deliberately repeated as leg 2's first
       point (Valhalla leg shapes include both endpoints). Assert `features.distance` is within
       0.5 m of 444.780 m — four ~111.195 m hops, the full polyline through the anchor. Record the
       pre-fix value 333.585 in a comment. Add a second `it(...)` asserting
       `boundingBox.minLat === 47.0`, proving leg 1's opening point is no longer dropped from the
       bounds.

    4. `describe("GPX.getTotals — zero-point regression guard")` with a GPX whose `Track` has a
       single `TrackSegment` carrying no points. Assert the sentinel behavior is UNCHANGED:
       `Number.isNaN(features.centroid.lat)` is true, `features.boundingBox.minLat === Infinity`,
       `features.boundingBox.maxLat === -Infinity`, `features.distance === 0`, and
       `gpx.flatten()` has length 0. Comment this describe as a deliberate
       assert-current-behavior guard per 33-RESEARCH.md Open Question 2 — it is NOT a fix.

    Do NOT assert on `features.cumulativeDistance` in this file yet. That array is still misaligned
    until plan 33-03 rebuilds it (D-01/D-03 sequencing); its assertions belong to 33-03 Task 3.
    Do NOT assert on `features.elevationGain` / `elevationLoss` here — plan 33-02 owns those.

    Follow CLAUDE.md conventions: 2-space indentation is the repo norm for `src/lib/models/gpx/`
    (note `trail.test.ts` itself uses 4 — match the directory being written into, i.e. the
    `gpx/` files' 2-space style), explicit types on helper signatures, camelCase function names,
    named exports not required for test-local helpers.
  </action>
  <acceptance_criteria>
    - `web/src/lib/models/gpx/gpx.test.ts` exists and `npx vitest run src/lib/models/gpx/gpx.test.ts` (from `web/`) exits 0 with at least 6 passing tests and 0 failures.
    - The file imports `{ describe, expect, it }` from `"vitest"` and imports `GPX`, `Track`, `TrackSegment`, `Waypoint` via relative `./` paths — `grep -c "\$lib/" web/src/lib/models/gpx/gpx.test.ts` returns `0`.
    - `grep -cE "readFileSync|import\.meta\.url|\.gpx\"|vi\.fn" web/src/lib/models/gpx/gpx.test.ts` returns `0` (D-05: inline fixtures, no disk reads, no mocks).
    - The file defines `waypointAt`, `gpxFromSegments`, and `hopMetres`.
    - `grep -c "cumulativeDistance" web/src/lib/models/gpx/gpx.test.ts` returns `0` (deferred to 33-03).
    - `grep -cE "elevationGain|elevationLoss" web/src/lib/models/gpx/gpx.test.ts` returns `0` (owned by 33-02).
    - Reverting Task 1's loop-bound change makes at least the 2-point-distance test and the bounding-box test fail (confirm once via `git stash` / re-run / `git stash pop`, or by reasoning against the recorded pre-fix values 0 and 47.001 — do not leave the revert in place).
    - `npx vitest run` (from `web/`, whole unit suite) exits 0 — `src/lib/models/trail.test.ts` and both `server.test.ts` files still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vitest run src/lib/models/gpx/gpx.test.ts && npx vitest run</automated>
  </verify>
  <done>`gpx.test.ts` exists with the four described blocks, all pass, the whole unit suite is green, and no assertion touches `cumulativeDistance` or elevation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → `PUT /api/v1/trail/upload` / `POST /api/v1/trail/convert` | Untrusted, user-supplied GPX XML reaches `GPX.parse()` and then `getTotals()`. Coordinates are attacker-controlled numeric strings coerced by `attrValueProcessors`. |
| Browser file picker / route planner → `GPX` constructor | Same code path, client side. Values originate from an imported file or from the Valhalla proxy response. |
| `getTotals()` output → `gpx2trail()` → PocketBase numeric fields + Meilisearch index | Computed `distance`, `elevation_gain`, `elevation_loss`, `min_lat`/`max_lat`/`min_lon`/`max_lon` are persisted without server-side recomputation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering (data integrity) | `GPX.getTotals()` centroid divisor | mitigate | `summedPointCount` is incremented in the same loop iteration that adds to `totalLat`/`totalLon`, so an attacker cannot craft a point distribution where numerator and divisor disagree. Existing `?? 0` / `?? Infinity` coalescing on `point.$.lat`/`$.lon` is preserved verbatim — no new coercion path is introduced. |
| T-33-02 | Tampering (data integrity) | Zero-point / empty-segment GPX reaching `centroid` | accept | A GPX with no track points still yields `NaN` centroid and `Infinity` bounding-box sentinels — pre-existing behavior, unchanged by this plan and explicitly asserted by the Task 2 regression guard. Downstream `gpx_util.ts:82` already gates the bbox write behind `Number.isFinite(bbox.minLat)`, so non-finite bounds never reach PocketBase. `centroid` is not persisted by `gpx2trail()`. |
| T-33-03 | Denial of Service | `allPoints.push(...points)` spread over a very large track | accept | Unchanged by this plan — the same array with the same element count is built before and after the loop-bound fix. Body-size limits on the upload/convert routes remain the controlling bound. No new allocation proportional to input is added here. |
| T-33-04 | Tampering | XML entity expansion / XXE via `isomorphic-xml2js` in `GPX.parse()` | accept | Out of scope and untouched: this plan does not modify `GPX.parse()`, its `xml2js.parseString` options, or the sanitizing regexes at `gpx.ts:221`. Parser configuration is identical before and after. |
| T-33-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | No package-manager install task exists in this plan or anywhere in phase 33. 33-RESEARCH.md § "Package Legitimacy Audit" records "Not applicable — no new npm packages are introduced." Vitest 4.1.9 is already installed. |
</threat_model>

<verification>
- `cd web && npx vitest run` exits 0 with the new `gpx.test.ts` included.
- `cd web && npx svelte-check --tsconfig ./tsconfig.json` shows no new errors in `src/lib/models/gpx/`.
- `grep -n "for (let i" web/src/lib/models/gpx/gpx.ts` shows the accumulation loop starting at 0.
- `web/src/lib/models/gpx/gpx-metrics-computation.ts` is unchanged by this plan (`git diff --name-only` does not list it).
</verification>

<success_criteria>
1. A 2-point track segment ~134.6 m long reports ~134.6 m instead of 0 (CONV-01).
2. `boundingBox` and `centroid` include every segment's first point; the centroid divisor is
   `summedPointCount` and `allPoints.length` no longer appears in `gpx.ts` (CONV-02).
3. A two-leg planner route reports 444.780 m — the full polyline through its shared anchor —
   instead of 333.585 m.
4. `web/src/lib/models/gpx/gpx.test.ts` exists, all its tests pass, and the whole `web` unit
   suite is green.
5. No metrics-anchor reset was introduced at segment boundaries (33-RESEARCH.md Pitfall 2).
</success_criteria>

<output>
Create `.planning/phases/33-conversion-correctness/33-01-SUMMARY.md` when done.
</output>
