---
phase: 18-retire-flutter-map-and-the-flomp-forks
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified: []
autonomous: false
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]
must_haves:
  truths:
    - "On a physical device, all six map surfaces (trail detail nav flow, trail map, list, list map, map screen, navigation) render online exactly as they did before the migration began."
    - "All six surfaces render in airplane mode for a downloaded trail — basemap from local .pmtiles with place-name labels, no network."
    - "The location puck / user position still renders on map screen and navigation after the LocationMarkerPosition local-class swap and the flutter_map_location_marker native-plugin removal (RESEARCH Open Question 2)."
    - "A fresh clean build (flutter clean + pub get) runs without the removed native plugin registrations causing any runtime crash or permission regression."
  artifacts: []
  key_links:
    - from: "physical device (GPS + native GL render)"
      to: "six map surfaces built from published-packages-only dependency tree"
      via: "manual on-device verification, online and airplane mode"
      pattern: "human-check"
---

<objective>
On-device regression verification (success criterion 4) that removing the six map
packages and the two flomp forks — and pinning maplibre — did not regress any map
surface. Static analysis and the widget-test harness cannot prove native GL
rendering, GPS behavior, offline `.pmtiles` tile loading, or that removing the
`flutter_map_location_marker` / `flutter_map_marker_cluster` native platform-channel
plugins left no runtime artifact. This continues the project's established pattern of
gating map/GPS behavior on a physical device (Phase 15/16/17 checkpoints), and follows
the exact checkpoint shape of 17-03-PLAN.md.

Purpose: Confirm CLEAN-01/02/03 landed without regression before the phase — and the
v1.4 MapLibre Migration milestone — closes, while there is still an open plan to fix
any defect via `/gsd-plan-phase 18 --gaps`.

Output: A signed-off on-device checkpoint (or a list of concrete defects to feed
`/gsd-plan-phase 18 --gaps`).
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-retire-flutter-map-and-the-flomp-forks/18-RESEARCH.md
@.planning/phases/17-navigation-on-maplibre/17-03-PLAN.md
</context>

<artifacts_this_phase_produces>
None — this plan is a verification checkpoint. It modifies no files. Any defect it
finds is fed back into a gap-closure plan (`/gsd-plan-phase 18 --gaps`), not fixed
inline.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Physical-device regression walk of all six map surfaces, online and in airplane mode</name>
  <files>none (verification-only)</files>
  <read_first>
    - .planning/phases/18-retire-flutter-map-and-the-flomp-forks/18-RESEARCH.md — the "Six Map Surfaces" table (surface → screen file → host widget), Open Question 1 (trail_detail_screen has no inline map; verify the navigation flow into trail_detail_map_screen, not a map on that screen), and Open Question 2 (a full `flutter clean && flutter pub get` plus a non-hot-reloaded fresh launch is sufficient to clear the removed native plugin registrations; the puck's GPS is via Geolocator, unaffected by the LocationMarkerPosition swap).
    - .planning/phases/17-navigation-on-maplibre/17-03-PLAN.md — the established on-device checkpoint pattern this plan mirrors (automated precondition, per-item PASS/FAIL, resume-signal feeding a --gaps plan).
  </read_first>
  <what-built>
Plan 18-01 severed every source dependency on the six packages: `effectiveBrightness`
was relocated into `map_style_json_provider.dart`, `LocationMarkerPosition` /
`ServiceDisabledException` became file-local classes in
`foreground_position_stream_provider.dart`, and the four dead files
(`map_style_provider.dart` + `.g.dart`, `tool/extract_map_styles.dart`,
`map_coordinate_adapter.dart`) were deleted. Plan 18-02 removed all six packages and
both `flomp/*` git overrides from `pubspec.yaml`, kept the `meta` override, pinned
`maplibre: 0.3.5` exact, and passed a whole-package `flutter analyze` + `flutter test`
with `flutter pub deps` showing none of the six in the tree. The app now builds from
published pub.dev packages only. The remaining unknowns are runtime: does every map
surface still render online and offline on real hardware, and did removing the two
native-plugin packages leave any GPS / permission / render artifact?
  </what-built>
  <action>
Automated precondition first: the executor confirms the build is device-ready with a
fresh clean resolve — `cd app && flutter clean && flutter pub get`, then
`cd app && flutter analyze` clean and `cd app && flutter test` green — before handing
off. Then the HUMAN performs the six-surface walk in `<how-to-verify>` on a physical
Android device, each surface online then in airplane mode, and records PASS/FAIL per
item. This task pauses for that human verification — no code is written here. FAILs are
captured verbatim for a follow-up gap-closure plan (`/gsd-plan-phase 18 --gaps`), not
fixed inline.
  </action>
  <how-to-verify>
Build/install with a FRESH clean build on a PHYSICAL Android device (not the simulator —
GPS + native GL differ): `cd app && flutter clean && flutter pub get && flutter run
--release`. A clean build is required so the removed native plugin registrations
(flutter_map_location_marker, flutter_map_marker_cluster ship platform-channel code) do
not linger from a prior install (RESEARCH Open Question 2). Then verify each of the six
surfaces, recording PASS/FAIL. For each surface, check it ONLINE first, then enable
airplane mode (leave device GPS/location ON) for a downloaded trail and confirm it still
renders from local `.pmtiles`:

1. Trail detail nav flow (trail_detail_screen.dart — a route shell with no inline map,
   per RESEARCH Open Question 1): open a trail's detail, tap through to its map tab/route
   → the navigation transition into the trail map works, no crash.

2. Trail map (trail_detail_map_screen.dart → WandererMap): basemap, GPX track + casing,
   directional arrows, waypoints (tappable), and start/finish pins render; the compass /
   rotation-reset control works. Airplane mode: a downloaded trail renders its basemap
   and place-name labels from `.pmtiles` with no network.

3. List (list_detail_screen.dart → SearchMap): open a list → every trail's polyline
   renders on one map and the camera fits all of them. Airplane mode: same for a list of
   downloaded trails.

4. List map (list_detail_map_screen.dart → SearchMap): the full-screen list map renders
   and fits every trail. Airplane mode: same.

5. Map screen (map_screen.dart → SearchMap): pan/zoom re-queries the cluster endpoint;
   clusters render as native circles sized by count; tapping a cluster zooms; tapping an
   unclustered point selects the trail and fits its polyline; category-icon markers show;
   AND the user-location puck renders (this is the LocationMarkerPosition local-class swap
   surface — confirm the puck still appears and updates after the refactor + native-plugin
   removal). Airplane mode: the map still renders; the puck still tracks via GPS.

6. Navigation (navigation_screen.dart → ml.MapLibreMap): tap Navigate → route line, the
   native location puck, and heading-up follow render; maneuvers advance; drag breaks
   follow and recenter restores it; the compass resets to north. Airplane mode: maneuvers
   still advance from the ObjectBox cache, the offline indicator shows, the `.pmtiles`
   basemap renders with labels, and the puck still renders and follows (RESEARCH Open
   Question 2 — confirm removing flutter_map_location_marker did not degrade Geolocator-based
   positioning).

7. No-crash sanity after the walk: background and re-foreground the app, and confirm no
   crash, no leaked GPS subscription, and no missing-plugin runtime error surfaced by the
   removed native packages.
  </how-to-verify>
  <verify>
    <automated>cd app && flutter clean && flutter pub get && flutter analyze && flutter test</automated>
    <human-check>All six map surfaces render correctly online AND in airplane mode on a physical Android device, the location puck renders on map screen and navigation after the LocationMarkerPosition swap, and no crash or missing-plugin error appears.</human-check>
  </verify>
  <resume-signal>
Type "approved" if items 1–7 all pass. Otherwise list each FAIL with the surface/item
number and the observed behavior — these become gap items for `/gsd-plan-phase 18 --gaps`.
  </resume-signal>
  <acceptance_criteria>
    - Automated precondition: a fresh `flutter clean && flutter pub get` succeeds, `flutter analyze` reports no errors, and `flutter test` passes before the device hand-off.
    - Item 1 (trail detail nav flow): navigating into the trail map from trail detail works with no crash.
    - Item 2 (trail map): WandererMap renders basemap, track, arrows, waypoints, pins, and compass online; airplane-mode downloaded trail renders `.pmtiles` basemap + labels.
    - Item 3 (list): SearchMap renders every list trail and fits the camera; airplane-mode equivalent renders.
    - Item 4 (list map): full-screen list map renders and fits every trail online and offline.
    - Item 5 (map screen): cluster search, cluster/point tap, category-icon markers, AND the location puck all work online; puck still tracks in airplane mode.
    - Item 6 (navigation): route, native puck, heading-up follow, drag-break, compass, and online maneuvers work; airplane-mode maneuvers advance from cache, offline indicator shows, `.pmtiles` basemap renders, puck follows.
    - Item 7: background/re-foreground produces no crash, leaked GPS subscription, or missing-plugin error from the removed native packages.
    - All seven items marked PASS, or each FAIL recorded with the item number and observed behavior for gap closure.
  </acceptance_criteria>
  <done>
All six map surfaces render correctly online and in airplane mode on a physical device
(or FAILs are documented for `/gsd-plan-phase 18 --gaps`), confirming CLEAN-01/02/03
landed with no regression — closing the v1.4 MapLibre Migration milestone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — this is a manual verification plan that ships no code. It
exercises, on real hardware, the two supply-chain mitigations Plan 18-02 delivered:
removal of the flomp `git: ref: main` overrides and removal of the four unused
native-code plugins.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 (verify) | Tampering | published-packages-only dependency tree | mitigate | The app builds and runs from the post-removal `pubspec.lock` (hosted / SDK sources only); a successful on-device run confirms no flomp-fork code path is reachable at runtime. |
| T-18-02 (verify) | Elevation of Privilege | removed native plugin registrations | mitigate | Item 7 (background/re-foreground + no missing-plugin error) confirms the removed `flutter_map_location_marker` / `flutter_map_marker_cluster` native code left no runtime artifact or permission regression. |
| T-18-SC | Tampering | pub package installs | n/a | No package installs — verification-only plan. |
</threat_model>

<verification>
- All six map surfaces render correctly online and in airplane mode on a physical Android device.
- The location puck renders on map screen and navigation after the LocationMarkerPosition local-class swap.
- No crash, leaked GPS subscription, or missing-plugin runtime error from the removed native packages.
- Any FAIL is documented with enough detail to drive a gap-closure plan.
</verification>

<success_criteria>
- Success criterion 4: a hiker walks every map surface — trail detail, trail map, list, list map, map screen, navigation — online and in airplane mode, and each renders as it did before the migration began.
- CLEAN-01/02/03 confirmed on real hardware with no regression.
</success_criteria>

<output>
Create `.planning/phases/18-retire-flutter-map-and-the-flomp-forks/18-03-SUMMARY.md` when done,
recording the PASS/FAIL result of each of the seven checks.
</output>
