---
phase: 06-admin-browser-ui
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - db/routes/federation_ui.go
  - db/main.go
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "GET /federation/ returns the embedded federation_ui.html with Content-Type text/html; charset=utf-8 and HTTP 200"
    - "The page-serving handler has NO HasSuperuserAuth guard — the page itself is public; auth is enforced in JS and by the six API handlers"
    - "GET /federation/ does not shadow or conflict with /federation/peers, /federation/discover, or the other API routes"
    - "The db package compiles with the new go:embed directive (go build and go vet succeed)"
  artifacts:
    - path: "db/routes/federation_ui.go"
      provides: "FederationDashboard handler + //go:embed of federation_ui.html"
      min_lines: 15
      contains: "FederationDashboard"
    - path: "db/main.go"
      provides: "Route registration for GET /federation/"
      contains: "routes.FederationDashboard"
  key_links:
    - from: "db/routes/federation_ui.go"
      to: "db/routes/federation_ui.html"
      via: "//go:embed federation_ui.html into federationUIHTML"
      pattern: "go:embed federation_ui.html"
    - from: "db/main.go"
      to: "routes.FederationDashboard"
      via: "se.Router.GET(\"/federation/\", ...)"
      pattern: "se.Router.GET\\(\"/federation/\""
---

<objective>
Create `db/routes/federation_ui.go` with the `FederationDashboard` handler that embeds and serves `federation_ui.html`, and register `GET /federation/` in `db/main.go`. This wires the Plan 01 page into the running Go/PocketBase binary so an admin can reach the dashboard at `/federation/`.

Purpose: Completes Phase 6 (DASH-01, DASH-02) by exposing the embedded SPA over HTTP. The handler is deliberately auth-free at the page level (D-01) — auth is enforced in JS and by the six already-guarded Phase 5 API handlers (DASH-02).

Output: One new file `db/routes/federation_ui.go`; one modified file `db/main.go`. Zero new packages.
</objective>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — do not flag as drift):
- File: `db/routes/federation_ui.go`
- Go function: `routes.FederationDashboard(e *core.RequestEvent) error`
- Go package-level var: `federationUIHTML []byte` (target of `//go:embed federation_ui.html`)
- New route registration line in `db/main.go`: `se.Router.GET("/federation/", routes.FederationDashboard)`
</artifacts_this_phase_produces>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-browser-ui/06-CONTEXT.md
@.planning/phases/06-admin-browser-ui/06-RESEARCH.md
@.planning/phases/06-admin-browser-ui/06-PATTERNS.md
@db/routes/nodeinfo.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FederationDashboard handler with go:embed and register the route</name>
  <files>db/routes/federation_ui.go, db/main.go</files>
  <read_first>
    - db/routes/nodeinfo.go (NodeInfo21 lines 22-36 — the exact raw-bytes write pattern: Header().Set Content-Type, WriteHeader(http.StatusOK), Response.Write(...); also confirms `package routes` and `net/http` import)
    - db/routes/federation_ui.html (must already exist from Plan 01 — //go:embed reads it at compile time; if absent the build fails)
    - db/main.go (registerRoutes function around lines 172-218 — the federation block ending at `se.Router.GET("/federation/peers", routes.FederationPeers)` on line 216 is the insertion point)
    - .planning/phases/06-admin-browser-ui/06-PATTERNS.md (federation_ui.go pattern assignment, embed directive, no-auth-guard note, main.go insertion point)
    - .planning/phases/06-admin-browser-ui/06-CONTEXT.md (D-01, D-09 — public page, embed serving)
  </read_first>
  <action>
    Create `db/routes/federation_ui.go` in `package routes`. Import block must include the blank import `_ "embed"` (REQUIRED to activate `//go:embed` even when embedding into a `[]byte`, per RESEARCH.md Pitfall 2 — omitting it is a compile error), `"net/http"`, and `"github.com/pocketbase/pocketbase/core"`. Declare the embed directive `//go:embed federation_ui.html` immediately above a package-level `var federationUIHTML []byte`.

    Implement `func FederationDashboard(e *core.RequestEvent) error` following the `NodeInfo21` raw-response pattern exactly (nodeinfo.go lines 32-35): set header `Content-Type` to `text/html; charset=utf-8`, optionally set `X-Frame-Options` to `DENY` (clickjacking mitigation T-06-05 from RESEARCH.md security domain), call `e.Response.WriteHeader(http.StatusOK)`, then `_, err := e.Response.Write(federationUIHTML)` and `return err`. Do NOT add an `e.HasSuperuserAuth()` guard — this page is intentionally public (D-01); auth lives in the page JS and in the six Phase 5 API handlers.

    In `db/main.go`, inside `registerRoutes`, add exactly one line immediately after the existing `se.Router.GET("/federation/peers", routes.FederationPeers)` line (currently line 216): `se.Router.GET("/federation/", routes.FederationDashboard)`. The trailing slash is load-bearing — the PocketBase Echo router does exact path matching, so `/federation/` will not conflict with `/federation/peers`, `/federation/discover`, etc. (RESEARCH.md Pitfall 4). Do not modify any other route line.
  </action>
  <verify>
    <automated>cd db && go build ./... && go vet ./routes/ && grep -q "go:embed federation_ui.html" routes/federation_ui.go && grep -q "se.Router.GET(\"/federation/\", routes.FederationDashboard)" main.go && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `db/routes/federation_ui.go` exists in `package routes`, imports `_ "embed"`, and declares `//go:embed federation_ui.html` directly above `var federationUIHTML []byte`.
    - `FederationDashboard` sets `Content-Type: text/html; charset=utf-8`, writes `http.StatusOK`, writes `federationUIHTML`, and returns the write error; it contains NO call to `HasSuperuserAuth`.
    - `grep -c "HasSuperuserAuth" db/routes/federation_ui.go` returns 0.
    - `db/main.go` contains exactly one new line `se.Router.GET("/federation/", routes.FederationDashboard)` placed after the `/federation/peers` registration; the six existing federation route lines are unchanged.
    - `cd db && go build ./...` exits 0 and `go vet ./routes/` exits 0 (embed resolves because federation_ui.html exists from Plan 01).
  </acceptance_criteria>
  <done>The db package compiles with the embedded HTML; GET /federation/ is registered and serves the dashboard as text/html without an auth guard, while the six API routes remain intact and superuser-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated browser → GET /federation/ | The page document itself is served with no auth (D-01); the response is static HTML containing no secrets |
| Browser JS → Phase 5 API routes | Every data-bearing request is auth-gated server-side by HasSuperuserAuth in the existing handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | FederationDashboard serving HTML publicly | accept | The embedded HTML contains no secrets or tokens; it only contains static markup and CDN URLs. The superuser token is never embedded — it is read at runtime from the admin's own localStorage. Serving the page publicly is the locked decision D-01 |
| T-06-03 | Elevation of Privilege | GET /federation/ has no HasSuperuserAuth guard | accept | Intentional (D-01); the page cannot perform any privileged action — every action it triggers calls a Phase 5 endpoint that enforces HasSuperuserAuth server-side (DASH-02) |
| T-06-05 | Tampering (Clickjacking) | /federation/ framed by a malicious site | mitigate | Set `X-Frame-Options: DENY` in FederationDashboard so the admin page cannot be embedded in a hostile iframe (RESEARCH.md security domain) |
| T-06-06 | Spoofing | route path collision (/federation/ shadowing /federation/peers) | mitigate | Register with trailing slash `/federation/`; PocketBase Echo router does exact-path matching, verified no overlap with the six existing /federation/* routes (RESEARCH.md Pitfall 4) — go build/vet and the peers route returning JSON confirm no shadowing |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase; only stdlib `embed`/`net/http` and the already-present PocketBase core are imported (RESEARCH.md Package Legitimacy Audit — no new dependencies); no legitimacy checkpoint required |
</threat_model>

<verification>
- `cd db && go build ./...` exits 0 (embed compiles; HTML present from Plan 01).
- `cd db && go vet ./routes/` exits 0.
- `db/routes/federation_ui.go` has the embed directive and no auth guard: `grep -c "go:embed federation_ui.html" db/routes/federation_ui.go` returns 1, `grep -c "HasSuperuserAuth" db/routes/federation_ui.go` returns 0.
- `db/main.go` registers the route once: `grep -c 'se.Router.GET("/federation/", routes.FederationDashboard)' db/main.go` returns 1.
- The six pre-existing federation API route registrations are still present and unmodified.
</verification>

<success_criteria>
- DASH-01: Navigating to `/federation/` returns HTTP 200 with `Content-Type: text/html; charset=utf-8` and serves the dashboard built in Plan 01.
- DASH-02 (server side): The page-serving route is public by design; every privileged operation it invokes hits a Phase 5 handler that returns 401 for unauthenticated or non-superuser tokens — no second auth layer is added and none is bypassed.
- The implementation adds zero packages and the db module builds cleanly.
</success_criteria>

<output>
Create `.planning/phases/06-admin-browser-ui/06-02-SUMMARY.md` when done.
</output>
