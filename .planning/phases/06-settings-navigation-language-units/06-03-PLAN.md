---
phase: 06-settings-navigation-language-units
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/lib/components/trail/trail_card.dart
  - app/lib/components/trail/trail_list_item.dart
  - app/lib/components/trail/trail_panel.dart
  - app/lib/components/trail/trail_quick_filter_bar.dart
  - app/lib/components/trail/summit_log_card.dart
  - app/lib/components/list/list_card.dart
  - app/lib/components/list/list_list_item.dart
  - app/lib/components/trail/elevation_profile.dart
  - app/lib/components/trail/waypoint_sheet.dart
  - app/lib/components/trail/trail_timeline.dart
  - app/lib/routes/global_search_screen.dart
  - app/lib/routes/list_detail_screen.dart
  - app/lib/routes/navigation_screen.dart
  - app/lib/routes/trail_filter_screen.dart
  - app/test/util/format_util_test.dart
autonomous: true
requirements: [LANG-02]
must_haves:
  truths:
    - "Every format_util call site reads the current unit from unitProvider instead of defaulting to metric"
    - "Switching units in settings re-renders distances, elevations, and speeds across trail cards, lists, navigation, and elevation profiles"
    - "No implicit metric default remains at any call site after this phase"
  artifacts:
    - path: "app/lib/components/trail/elevation_profile.dart"
      provides: "unit-aware elevation profile (ConsumerStatefulWidget conversion)"
      contains: "unitProvider"
    - path: "app/lib/components/trail/trail_timeline.dart"
      provides: "unit passed via constructor to _TimelineRow"
    - path: "app/lib/components/trail/waypoint_sheet.dart"
      provides: "unit-aware waypoint sheet (ConsumerWidget conversion)"
    - path: "app/test/util/format_util_test.dart"
      provides: "imperial conversion tests for formatDistance + formatElevation"
      contains: "unit: 'imperial'"
  key_links:
    - from: "app/lib/routes/navigation_screen.dart"
      to: "unitProvider"
      via: "ref.watch passed to formatDistance/Elevation/Speed"
      pattern: "ref\\.watch\\(unitProvider\\)"
    - from: "app/lib/components/trail/trail_card.dart"
      to: "unitProvider"
      via: "ref.watch in build then unit param"
      pattern: "unit: unit"
---

<objective>
Wire `unitProvider` (from Plan 01) into every `format_util` call site so distances, elevations, and speeds across the app honor the user's metric/imperial preference, and add imperial-conversion tests for `formatDistance`/`formatElevation`.

Purpose: Completes the display half of LANG-02 (D-04, D-05). Without this, toggling units in Plan 02's screen saves the preference but nothing re-renders. Of the 14 call-site files, 11 already have a Riverpod `ref` and take a one-line change; 3 (`elevation_profile.dart`, `waypoint_sheet.dart`, `trail_timeline.dart`) have no `ref` and need a structural change (Pitfall 4) — those carry the real effort.
Output: 14 modified call-site files with no implicit metric default, plus extended `format_util_test.dart`.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-navigation-language-units/06-CONTEXT.md
@.planning/phases/06-settings-navigation-language-units/06-PATTERNS.md
@.planning/phases/06-settings-navigation-language-units/06-01-SUMMARY.md
@app/lib/util/format_util.dart
</context>

<artifacts_this_phase_produces>
See 06-01-PLAN.md `<artifacts_this_phase_produces>` for the full Phase 6 symbol inventory. This plan consumes `unitProvider` from Plan 01 and converts `elevation_profile.dart` / `waypoint_sheet.dart` to Consumer variants and threads a `unit` constructor param through `trail_timeline.dart`'s private `_TimelineRow`. No new public symbols beyond those widget-signature changes.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire unitProvider into the 11 Consumer call sites</name>
  <files>app/lib/components/trail/trail_card.dart, app/lib/components/trail/trail_list_item.dart, app/lib/components/trail/trail_panel.dart, app/lib/components/trail/trail_quick_filter_bar.dart, app/lib/components/trail/summit_log_card.dart, app/lib/components/list/list_card.dart, app/lib/components/list/list_list_item.dart, app/lib/routes/global_search_screen.dart, app/lib/routes/list_detail_screen.dart, app/lib/routes/navigation_screen.dart, app/lib/routes/trail_filter_screen.dart</files>
  <read_first>
    - app/lib/util/format_util.dart (the three functions and their `{String unit = 'metric'}` default that this task overrides at call sites)
    - app/lib/provider/local_settings_provider.dart (source of `unitProvider` from Plan 01)
    - app/lib/routes/navigation_screen.dart (6 call sites at lines 470, 580, 585, 650, 655, 660 — the heaviest file; already a Consumer)
    - app/lib/components/trail/trail_card.dart (3 call sites at lines 368, 378, 382 — representative simple Consumer case)
    - app/lib/routes/trail_filter_screen.dart (6 call sites — already a Consumer)
  </read_first>
  <action>
    For each of these 11 files (all confirmed `ConsumerWidget` / `ConsumerStatefulWidget` / `ConsumerState`, so `ref` is already in scope), add `final unit = ref.watch(unitProvider);` near the top of the relevant `build` method (or in the `ConsumerState.build`), import `package:wanderer/provider/local_settings_provider.dart` if not already imported, and pass `unit: unit` to every `formatDistance(...)`, `formatElevation(...)`, and `formatSpeed(...)` call in that file. Replace the implicit metric default — every call must now explicitly pass `unit: unit`.

    Call-site counts to update (grep-verified): trail_card.dart 3, trail_list_item.dart 3, trail_panel.dart 3, trail_quick_filter_bar.dart 4, summit_log_card.dart 3, list_card.dart 3, list_list_item.dart 3, global_search_screen.dart 1, list_detail_screen.dart 3, navigation_screen.dart 6, trail_filter_screen.dart 6.

    If a `format_util` call lives inside a private helper method or a nested builder that does not have `ref`/`unit` in scope, pass `unit` down as a parameter to that helper rather than re-reading the provider (keep a single `ref.watch(unitProvider)` per build). Do NOT introduce any hardcoded `unit: 'metric'` or `unit: 'imperial'` literal — always pass the watched `unit` variable.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/components lib/routes/navigation_screen.dart lib/routes/global_search_screen.dart lib/routes/list_detail_screen.dart lib/routes/trail_filter_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 11 files contains `ref.watch(unitProvider)` exactly once in its build path
    - In `trail_card.dart`, every `formatDistance`/`formatElevation` call passes `unit:` (verify: `grep -nE 'format(Distance|Elevation|Speed)\(' lib/components/trail/trail_card.dart | grep -v 'unit:'` returns no lines)
    - In `navigation_screen.dart`, all 6 format calls pass `unit:` (verify: `grep -nE 'format(Distance|Elevation|Speed)\(' lib/routes/navigation_screen.dart | grep -v 'unit:'` returns no lines)
    - No file in this task contains the literal `unit: 'metric'` or `unit: 'imperial'`
    - `flutter analyze` on the listed paths reports no errors
  </acceptance_criteria>
  <done>All 11 Consumer call-site files read the live unit from unitProvider and pass it to every format_util call, with zero analyzer errors and no hardcoded unit literals.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the 3 non-Consumer call sites and add imperial format tests</name>
  <files>app/lib/components/trail/elevation_profile.dart, app/lib/components/trail/waypoint_sheet.dart, app/lib/components/trail/trail_timeline.dart, app/test/util/format_util_test.dart</files>
  <read_first>
    - app/lib/components/trail/elevation_profile.dart (currently `StatefulWidget` line 13; 7 format calls at lines 140, 142, 161, 163, 167, 284, 304 — some inside private build helpers and the fl_chart axis builders)
    - app/lib/components/trail/waypoint_sheet.dart (currently `StatelessWidget` line 9; 1 format call)
    - app/lib/components/trail/trail_timeline.dart (currently `StatelessWidget` line 11 with nested private `_TimelineRow` line 77; 2 format calls)
    - app/lib/provider/local_settings_provider.dart (source of `unitProvider`)
    - app/test/util/format_util_test.dart (existing tests — `formatSpeed` already has an imperial case at the `unit: 'imperial'` test; mirror that structure for distance/elevation)
    - app/lib/util/format_util.dart (imperial math: distance × 0.000621371 → "mi"; elevation × 3.28084 → "ft")
  </read_first>
  <action>
    elevation_profile.dart (Pitfall 4 — convert to Consumer): change the widget from `StatefulWidget`/`State` to `ConsumerStatefulWidget`/`ConsumerState` (so `ref` is available in `build` and in `State` methods). Import `flutter_riverpod` and `local_settings_provider`. In `build`, read `final unit = ref.watch(unitProvider);`. The 7 format calls live partly inside private build-helper methods (`_buildStatText` usages) and fl_chart axis `getTitlesWidget` callbacks — thread `unit` into those helpers/closures as a parameter or via a `State` field set in `build` (an instance field `String _unit` assigned at the top of `build` is acceptable for the fl_chart closures since they execute during the same build). Pass `unit:` to all 7 calls.

    waypoint_sheet.dart (convert to Consumer): change `StatelessWidget` → `ConsumerWidget`, `build(BuildContext context)` → `build(BuildContext context, WidgetRef ref)`. Import `flutter_riverpod` and `local_settings_provider`. Read `final unit = ref.watch(unitProvider);` and pass `unit: unit` to the single format call.

    trail_timeline.dart (constructor-param strategy — avoid touching the private widget's ref): in the public `TrailTimeline` widget, convert to `ConsumerWidget` (or read unit in its build if already a Consumer-capable context), read `final unit = ref.watch(unitProvider);`, and pass `unit` as a new `required String unit` (or `String unit`) constructor parameter into each `_TimelineRow(...)` instantiation. Add the `unit` field + constructor param to the private `_TimelineRow` widget and use `widget.unit`/`unit` at its 2 format calls. This keeps the nested private widget ref-free per PATTERNS guidance.

    format_util_test.dart: add an imperial test to the `formatDistance` group and an imperial test to the `formatElevation` group (create the groups if absent). Use exact expected values: `formatDistance(1000, unit: 'imperial')` → distance in miles (1000 × 0.000621371 = 0.621371 → `'0.62 mi'`); `formatElevation(100, unit: 'imperial')` → 100 × 3.28084 = 328.084 → `'328 ft'`. Also assert metric defaults are unchanged (`formatDistance(1000)` → `'1.00 km'`, `formatElevation(100)` → `'100 m'`).
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/components/trail/elevation_profile.dart lib/components/trail/waypoint_sheet.dart lib/components/trail/trail_timeline.dart && flutter test test/util/format_util_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `elevation_profile.dart` declares `ConsumerStatefulWidget` (not plain `StatefulWidget`) and contains `ref.watch(unitProvider)`
    - `waypoint_sheet.dart` declares `ConsumerWidget` with `build(BuildContext context, WidgetRef ref)` and contains `ref.watch(unitProvider)`
    - `trail_timeline.dart` `_TimelineRow` accepts a `unit` constructor parameter and uses it at both format calls
    - None of the 3 files leave a format call without an explicit `unit:` argument (verify per-file with `grep -nE 'format(Distance|Elevation|Speed)\(' <file> | grep -v 'unit:'` returning no lines)
    - `format_util_test.dart` contains `unit: 'imperial'` for both a distance case and an elevation case and asserts `'0.62 mi'` and `'328 ft'`
    - `flutter test test/util/format_util_test.dart` exits 0
  </acceptance_criteria>
  <done>The 3 non-Consumer widgets read the live unit (via Consumer conversion or constructor threading), all their format calls honor it, and format_util has imperial coverage for distance and elevation.</done>
</task>

</tasks>

<verification>
- `cd app && flutter analyze` reports no new errors across all 14 call-site files
- `cd app && flutter test test/util/format_util_test.dart` passes (imperial distance/elevation cases green)
- Repo-wide check: `grep -rn "format\(Distance\|Elevation\|Speed\)" app/lib --include=*.dart | grep -v "format_util.dart" | grep -v "unit:"` returns no lines (every call site passes a unit)
- Manual smoke: toggle imperial in the Language & Units screen; trail cards, list cards, navigation stats, and the elevation profile re-render in mi/ft on the next frame
</verification>

<success_criteria>
- LANG-02 (display half, D-04/D-05): all ~50 format_util calls across 14 files honor unitProvider; no implicit metric default remains
- The 3 non-Consumer widgets are correctly converted/threaded without breaking their existing rendering
- format_util has imperial test coverage for distance and elevation
</success_criteria>

<output>
Create `.planning/phases/06-settings-navigation-language-units/06-03-SUMMARY.md` when done
</output>
