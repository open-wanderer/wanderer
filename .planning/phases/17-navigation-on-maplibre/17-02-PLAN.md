---
phase: 17-navigation-on-maplibre
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - app/lib/components/map/map_compass.dart
  - app/lib/vendor/vector_map_tiles/pm_tile_provider.dart
  - app/lib/components/map/trail_layer.dart
  - app/lib/routes/trail_detail_map_screen.dart
autonomous: true
requirements: [CORE-05, CORE-06, CORE-07, OFFL-06]
must_haves:
  truths:
    - "app/lib/components/map/map_compass.dart no longer exists (CORE-05)."
    - "app/lib/vendor/vector_map_tiles/pm_tile_provider.dart no longer exists (OFFL-06, unblocked now that navigation was its last caller)."
    - "No AnimatedMapController or CurrentLocationLayer reference remains anywhere in app/lib/ (ROADMAP criterion 4)."
    - "trail_detail_map_screen shows a native ml.MapCompass control again, closing the Phase-15 CORE-05 gap."
    - "trail_layer.dart no longer defines the legacy flutter_map TrailLayer widget and no longer imports flutter_map or map_coordinate_adapter."
  artifacts:
    - path: app/lib/components/map/trail_layer.dart
      provides: "native addTrailTrackLayers + TrailMarkerLayer only; legacy flutter_map TrailLayer/_TrailLayerState widget removed"
      contains: "addTrailTrackLayers"
    - path: app/lib/routes/trail_detail_map_screen.dart
      provides: "native ml.MapCompass restored to the WandererMap controls column"
      contains: "ml.MapCompass"
  key_links:
    - from: "app/lib/routes/trail_detail_map_screen.dart"
      to: "ml.MapCompass"
      via: "WandererMap controls list"
      pattern: "ml.MapCompass"
---

<objective>
Retire the `flutter_map` holdout files that plan 17-01 stopped importing: delete
the app-local `map_compass.dart` (CORE-05) and the deferred
`pm_tile_provider.dart` (OFFL-06), strip the legacy `TrailLayer` widget from
`trail_layer.dart`, and restore a native `ml.MapCompass` to
`trail_detail_map_screen.dart` — closing the Phase-15 CORE-05 gap that STATE.md
explicitly deferred to "Phase 17 (CORE-05)". Then grep-verify that no
`AnimatedMapController` or `CurrentLocationLayer` reference survives anywhere in
`lib/` (ROADMAP Phase 17 success criterion 4).

Purpose: Formally close CORE-05/06/07 by removing the last call sites and the
now-orphaned widget, and discharge the documented STATE.md blocker restoring the
trail-detail compass. The `flutter_map`/plugin *pubspec removal* itself is
Phase 18 (CLEAN-01/02/03) and out of scope here.

Output: `map_compass.dart` and `pm_tile_provider.dart` deleted; `trail_layer.dart`
free of `flutter_map`; `trail_detail_map_screen.dart` with a native compass;
`flutter analyze` clean across `lib/`.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-navigation-on-maplibre/17-CONTEXT.md
@.planning/phases/17-navigation-on-maplibre/17-RESEARCH.md
@.planning/phases/17-navigation-on-maplibre/17-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by this plan:

- `app/lib/routes/trail_detail_map_screen.dart` — adds `const ml.MapCompass(hideIfRotatedNorth: true)`
  to the `WandererMap.controls` column (existing widget from `maplibre`, new call site here).

Symbols removed (not artifacts, but named so drift verification knows they are intentional deletions):
- `app/lib/components/map/map_compass.dart` — whole file deleted (`MapCompass` / `_MapCompassState`).
- `app/lib/vendor/vector_map_tiles/pm_tile_provider.dart` — whole file deleted (`MultiPmTilesVectorTileProvider`).
- `app/lib/components/map/trail_layer.dart` — `TrailLayer` + `_TrailLayerState` classes removed, plus its
  `package:flutter_map/flutter_map.dart` and `util/map_coordinate_adapter.dart` imports.

Deliberately NOT deleted this phase: `app/lib/util/map_coordinate_adapter.dart` — left in place for
Phase 18/CLEAN even if it becomes zero-caller (RESEARCH Pitfall 6). Verify-only, no edit.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete map_compass.dart + pm_tile_provider.dart and strip the legacy TrailLayer widget from trail_layer.dart</name>
  <files>app/lib/components/map/map_compass.dart, app/lib/vendor/vector_map_tiles/pm_tile_provider.dart, app/lib/components/map/trail_layer.dart</files>
  <read_first>
    - app/lib/components/map/trail_layer.dart — the legacy `TrailLayer` (lines 275–398) + `_TrailLayerState` to remove; its `flutter_map` import (line 5) and `map_coordinate_adapter` import (line 11); the doc comment on lines 270–274 marking it for Phase-17 deletion. KEEP `addTrailTrackLayers` (lines 53–129), `_colorToHex` (131–136), `TrailMarkerLayer` (145–237), and `_buildCircularMarker` (242–268) — `_buildCircularMarker` is shared by `TrailMarkerLayer`.
    - app/lib/components/map/map_compass.dart — the whole flutter_map-only widget being deleted (CORE-05).
    - app/lib/vendor/vector_map_tiles/pm_tile_provider.dart — `MultiPmTilesVectorTileProvider`, being deleted (OFFL-06). Grep confirmed navigation_screen.dart (after 17-01) is its last caller.
    - .planning/phases/17-navigation-on-maplibre/17-CONTEXT.md — Claude's Discretion (legacy TrailLayer deletion timing) + Deferred Ideas; RESEARCH Pitfall 6 (leave map_coordinate_adapter.dart alone).
  </read_first>
  <action>
This task runs AFTER 17-01, so navigation_screen.dart no longer imports any of these
files — confirm that precondition with a grep before deleting (see verify).

Delete the file `app/lib/components/map/map_compass.dart` entirely (CORE-05 —
`lib/components/map/map_compass.dart` is deleted). Its only real importer was
navigation_screen.dart; the only other repo mention is a code comment in
trail_detail_map_screen.dart (handled in Task 2).

Delete the file `app/lib/vendor/vector_map_tiles/pm_tile_provider.dart` entirely
(OFFL-06 — `MultiPmTilesVectorTileProvider` is retired now that navigation, its last
caller, migrated in 17-01).

In `app/lib/components/map/trail_layer.dart`: remove the legacy `TrailLayer` StatefulWidget
class and its `_TrailLayerState` (the block starting at the `/// Legacy flutter_map-based...`
doc comment through the class end — current lines ~270–398). Then remove the now-unused
`import 'package:flutter_map/flutter_map.dart';` and `import 'package:wanderer/util/map_coordinate_adapter.dart';`
lines. Do NOT touch `addTrailTrackLayers`, `_colorToHex`, `TrailMarkerLayer`, `_buildCircularMarker`,
`kTrailRouteColor`, `_kArrowSpacing`, `_kTrailArrowImageId`, or the `gpx_util` import — those remain
in use by `WandererMap` and (now) `navigation_screen.dart`.

Do NOT delete `app/lib/util/map_coordinate_adapter.dart` — even if it becomes zero-caller after
this task, leave it for Phase 18/CLEAN per RESEARCH Pitfall 6 (it still imports `flutter_map`,
which stays in pubspec until Phase 18; a lone unused-file is harmless).
  </action>
  <verify>
    <automated>cd app && ! grep -qE "map_compass\.dart|pm_tile_provider\.dart" lib/routes/navigation_screen.dart && test ! -e lib/components/map/map_compass.dart && test ! -e lib/vendor/vector_map_tiles/pm_tile_provider.dart && ! grep -nE "class TrailLayer |package:flutter_map/flutter_map.dart|map_coordinate_adapter" lib/components/map/trail_layer.dart && grep -q "class TrailMarkerLayer" lib/components/map/trail_layer.dart && flutter analyze lib/components/map/trail_layer.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/components/map/map_compass.dart` does not exist (`test ! -e` succeeds).
    - `lib/vendor/vector_map_tiles/pm_tile_provider.dart` does not exist.
    - `trail_layer.dart` contains no `class TrailLayer `, no `package:flutter_map/flutter_map.dart` import, and no `map_coordinate_adapter` import (grep returns nothing).
    - `trail_layer.dart` still contains `class TrailMarkerLayer`, `addTrailTrackLayers`, and `_buildCircularMarker`; `flutter analyze lib/components/map/trail_layer.dart` is clean.
    - `navigation_screen.dart` references neither `map_compass.dart` nor `pm_tile_provider.dart` (precondition holds — 17-01 already removed them).
    - `lib/util/map_coordinate_adapter.dart` is NOT deleted (still present on disk).
  </acceptance_criteria>
  <done>
The app-local compass, the deferred pmtiles provider, and the legacy flutter_map
TrailLayer widget are gone; trail_layer.dart is flutter_map-free and still
exports the native track/marker APIs.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restore native ml.MapCompass to trail_detail_map_screen and grep-verify no flutter_map controller/location references remain in lib/</name>
  <files>app/lib/routes/trail_detail_map_screen.dart</files>
  <read_first>
    - app/lib/routes/trail_detail_map_screen.dart — the `WandererMap` `controls:` list (lines 93–102) with the stale comment (lines 95–101) explaining the Phase-15 compass drop; `_mapController` captured at lines 103–104. It already imports `package:maplibre/maplibre.dart as ml`.
    - app/lib/routes/map_screen.dart — line 398: the shipped `const ml.MapCompass(hideIfRotatedNorth: true)` precedent (north-reset-only, one consistent treatment across map screens).
    - .planning/STATE.md — Blockers/Concerns: "[Phase 15 — CORE-05 gap, expected] trail_detail_map_screen's MapCompass ... has no compass/rotation-reset control until Phase 17 (CORE-05) wires maplibre's native equivalent."
  </read_first>
  <action>
In `trail_detail_map_screen.dart`, restore the compass control that Phase 15 dropped
(discharging the STATE.md CORE-05 gap). In the `WandererMap` `controls:` list, replace the
stale explanatory comment (lines ~95–101) with a `const ml.MapCompass(hideIfRotatedNorth: true)`
entry, mirroring `map_screen.dart` line 398 exactly — the standard north-reset-only treatment
(this screen is a browse/detail surface, not turn-by-turn, so it uses `hideIfRotatedNorth: true`
like the map screen, NOT navigation's always-visible toggle). Keep `_buildMapControls(context, trail)`
as the first control and add the compass after it so the layout order is [eye/expand/mountain
control block, compass]. Do not restyle the compass — accept the native default glyph (UI-SPEC
precedent: one consistent native compass treatment across screens).

This uses `WandererMap`'s existing `controls` slot, which renders inside the `ml.MapLibreMap`
children tree (an `Align`→`Column`), so `ml.MapCompass`'s `MapController.maybeOf(context)` resolves
correctly — the same wiring `map_screen.dart` relies on.

Then run the repo-wide grep gate: assert that `AnimatedMapController` and `CurrentLocationLayer`
appear nowhere in `app/lib/` (ROADMAP Phase 17 criterion 4), and that `map_compass.dart` /
`pm_tile_provider.dart` / `MultiPmTilesVectorTileProvider` are referenced by no file. Fix any stray
reference surfaced (there should be none after 17-01 + Task 1). Finish with a full `flutter analyze`
over `lib/`.
  </action>
  <verify>
    <automated>cd app && grep -q "ml.MapCompass" lib/routes/trail_detail_map_screen.dart && ! grep -rn "AnimatedMapController\|CurrentLocationLayer" lib/ && ! grep -rn "map_compass.dart\|pm_tile_provider.dart\|MultiPmTilesVectorTileProvider" lib/ && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `trail_detail_map_screen.dart` contains `const ml.MapCompass(hideIfRotatedNorth: true)` inside the `WandererMap` `controls` list, and the stale "MapCompass (map_compass.dart) is flutter_map-only" comment is gone.
    - `grep -rn "AnimatedMapController\|CurrentLocationLayer" lib/` returns no matches (ROADMAP Phase 17 criterion 4).
    - `grep -rn "map_compass.dart\|pm_tile_provider.dart\|MultiPmTilesVectorTileProvider" lib/` returns no matches.
    - `flutter analyze` over the whole `lib/` tree reports no errors.
  </acceptance_criteria>
  <done>
trail_detail_map_screen shows a native compass again, and a repo-wide grep proves
no AnimatedMapController / CurrentLocationLayer / deleted-file references remain
anywhere in lib/. The whole app analyzes clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan only deletes dead `flutter_map`
code paths and adds a native compass widget (a display control with no input
surface), which strictly *reduces* the app's attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | file deletions (`map_compass.dart`, `pm_tile_provider.dart`) + `trail_layer.dart` edit | accept | Deletions remove code, not add surface; the grep gate (Task 2) proves no dangling reference is left that could resolve to a stale/incorrect path or a build break. |
| T-17-05 | Elevation of Privilege | restored `ml.MapCompass` in `trail_detail_map_screen` | accept | The native compass reads `MapController.getCamera().bearing` and issues a camera animation only; it takes no external input and crosses no privilege boundary. |
| T-17-SC | Tampering | package installs | n/a | No package-manager installs this phase — only deletions and one native-widget call site reusing the already-installed `maplibre` 0.3.5. No slopcheck/legitimacy checkpoint required. |
</threat_model>

<verification>
- `map_compass.dart` and `pm_tile_provider.dart` are absent from disk.
- `trail_layer.dart` no longer imports `flutter_map` or `map_coordinate_adapter` and no longer defines `TrailLayer`, while still exporting `addTrailTrackLayers` / `TrailMarkerLayer`.
- `trail_detail_map_screen.dart` renders a native `ml.MapCompass`.
- Repo-wide grep: zero `AnimatedMapController`, `CurrentLocationLayer`, `map_compass.dart`, `pm_tile_provider.dart`, or `MultiPmTilesVectorTileProvider` references in `lib/`.
- `flutter analyze` over `lib/` is clean.
</verification>

<success_criteria>
- CORE-05: the app uses maplibre's built-in `MapCompass`; `lib/components/map/map_compass.dart` is deleted.
- OFFL-06: `lib/vendor/vector_map_tiles/pm_tile_provider.dart` is deleted.
- CORE-06/CORE-07 closure verified: no `AnimatedMapController` or `CurrentLocationLayer` reference remains anywhere in `lib/`.
- trail_detail_map_screen's Phase-15 compass gap (STATE.md blocker) is closed with a native `ml.MapCompass`.
- `flutter analyze` clean.
</success_criteria>

<output>
Create `.planning/phases/17-navigation-on-maplibre/17-02-SUMMARY.md` when done.
</output>
