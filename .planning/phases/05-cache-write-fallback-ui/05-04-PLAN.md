---
phase: 05-cache-write-fallback-ui
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/lib/services/trail_download_service.dart
autonomous: true
requirements: [OFFLINE-01]
must_haves:
  truths:
    - "Downloading a trail also fetches Valhalla maneuvers and stores them as navCacheJson on the TrailEntity so navigation works offline afterward"
    - "A Valhalla outage (or any error) during the cache step does not block, error, or roll back the trail download — the tiles and entity still persist"
    - "When the trail has no parsed GPX, the cache write is skipped silently with no error"
  artifacts:
    - path: "app/lib/services/trail_download_service.dart"
      provides: "best-effort Valhalla cache write before box.put inside downloadTrail"
      contains: "navCacheJson"
  key_links:
    - from: "app/lib/services/trail_download_service.dart"
      to: "buildNavShape"
      via: "shared helper builds the same shape the online path sends"
      pattern: "buildNavShape\\("
    - from: "app/lib/services/trail_download_service.dart"
      to: "POST /valhalla/navigate"
      via: "_api.post inside best-effort try/catch"
      pattern: "/valhalla/navigate"
    - from: "app/lib/services/trail_download_service.dart"
      to: "entity.navCacheJson"
      via: "jsonEncode(response.toJson()) assigned before box.put"
      pattern: "entity.navCacheJson ="
---

<objective>
Deliver OFFLINE-01: inside `TrailDownloadService.downloadTrail`, after the map-tile download succeeds and before persisting the entity, add a BEST-EFFORT, silent Valhalla cache write — build the shape from `trail.expand?.gpx.allPoints` with the shared `buildNavShape` helper (05-01), POST to `/valhalla/navigate`, and set `entity.navCacheJson = jsonEncode(response.toJson())`. Any failure (Valhalla outage, parse error, null GPX) is swallowed and the download proceeds unaffected (D-06, D-07, D-08).

Purpose: At download time the app captures the maneuver list so a hiker can navigate the trail later with no network.
Output: downloadTrail writes navCacheJson best-effort; tile download and entity persistence are never blocked by the cache step.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-cache-write-fallback-ui/05-CONTEXT.md
@app/lib/util/gpx_util.dart
@app/lib/models/navigate_response.dart
@app/lib/entities/trail_entity.dart
</context>

<artifacts_this_phase_produces>
Symbols CREATED/CHANGED in this plan (exclude from drift verification):
- New imports in `trail_download_service.dart`: `dart:convert` (`jsonEncode`), `package:wanderer/util/gpx_util.dart` (`buildNavShape`), `package:wanderer/models/navigate_response.dart` (`NavigateResponse`)
- New assignment `entity.navCacheJson = ...` inside `downloadTrail` before `box.put(entity)`
- Pre-existing symbols REUSED (do not flag): `buildNavShape` (05-01), `NavigateResponse`/`.fromJson`/`.toJson`, `TrailEntity.navCacheJson`, the existing `_api` Dio client, `_store.runInTransaction`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Best-effort Valhalla cache write in downloadTrail</name>
  <files>app/lib/services/trail_download_service.dart</files>
  <read_first>
    - app/lib/services/trail_download_service.dart (full file — lines 53-76: the tile download try/catch with `rethrow`, then entity construction `TrailEntity.fromModel(trail)`, then `_store.runInTransaction(TxMode.write, () { box.put(entity); })`; and lines 180-217 `_downloadPhotos` showing the canonical best-effort `on DioException` swallow-and-continue pattern with `CancelToken.isCancel(e)` rethrow)
    - app/lib/util/gpx_util.dart (`buildNavShape(List<LatLng>)` from 05-01; the `GpxMappingUtils.allPoints` getter returns `List<LatLng>`)
    - app/lib/util/navigation_launch_util.dart lines 18-26 and 109-110 (`_costingFor` derivation: lowercased category name containing bike/cycling/bicycle → 'bicycle' else 'pedestrian'; and the POST shape `data: {'shape': shape, 'costing': costing}`) — replicate the SAME request contract so the cached maneuvers match the online path exactly
    - app/lib/models/navigate_response.dart (`NavigateResponse.fromJson`, `.toJson`)
  </read_first>
  <action>
    Add imports: `import 'dart:convert';` (jsonEncode), `import 'package:wanderer/util/gpx_util.dart';` (buildNavShape), `import 'package:wanderer/models/navigate_response.dart';` (NavigateResponse).

    Insert a best-effort cache block in `downloadTrail` AFTER `final entity = TrailEntity.fromModel(trail);` and the photo/tile assignments (lines 66-72), and BEFORE the `_store.runInTransaction(TxMode.write, () { box.put(entity); })` call (D-06 — so navCacheJson is persisted in the same put). Wrap the entire block in `try { ... } catch (_) { /* best-effort: Valhalla outage must not block download (D-06) */ }` — a bare swallowing catch, NOT a rethrow (this is the opposite of the tile-download block which rethrows).

    Inside the try:
    - Read `final gpx = trail.expand?.gpx;` — if `gpx == null`, do nothing (skip silently per D-07; no fallback to waypointsViaTrail).
    - `final points = gpx.allPoints;` — if `points.length < 2`, skip silently.
    - `final shape = buildNavShape(points);` (D-08 — same helper as the online path).
    - Derive costing from `trail.expand?.category?.name` using the SAME logic as `_costingFor` in navigation_launch_util.dart (lowercase contains bike/cycling/bicycle → 'bicycle' else 'pedestrian'); inline it as a local since `_costingFor` is private to the other file — do NOT export it.
    - `final res = await _api.post('/valhalla/navigate', data: {'shape': shape, 'costing': costing}, cancelToken: cancelToken);`
    - `final response = NavigateResponse.fromJson(res.data as Map<String, dynamic>);`
    - If `response.maneuvers.isNotEmpty && response.shape.isNotEmpty`, set `entity.navCacheJson = jsonEncode(response.toJson());`. (If empty, leave navCacheJson null — no broken cache.)

    Do NOT change the tile-download try/catch, the `rethrow` on tile failure, or the `runInTransaction` put. The cache write only mutates `entity.navCacheJson` in memory before the existing put persists it.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/services/trail_download_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - `trail_download_service.dart` contains `buildNavShape(` and a POST to `/valhalla/navigate` with `data: {'shape': shape, 'costing': costing}`
    - The cache block is wrapped in a swallowing `catch` (no `rethrow`) and sits before `box.put(entity)`
    - `entity.navCacheJson = jsonEncode(response.toJson())` is only assigned when `response.maneuvers.isNotEmpty && response.shape.isNotEmpty`
    - The null-GPX path skips silently (a `trail.expand?.gpx == null` guard returns/does-nothing without throwing)
    - Imports for `dart:convert`, `gpx_util.dart`, and `navigate_response.dart` are present
    - `flutter analyze lib/services/trail_download_service.dart` reports no errors
  </acceptance_criteria>
  <done>downloadTrail performs a silent best-effort Valhalla fetch and stores navCacheJson alongside tiles; any cache failure leaves the download intact.</done>
</task>

</tasks>

<verification>
- `cd app && flutter analyze lib/services/trail_download_service.dart` → no errors
- `cd app && flutter analyze` (whole app, to confirm the shared helper + entity field integrate cleanly across plans) → no errors
- The cached shape is produced by the SAME `buildNavShape` helper as the online path (no divergence)
</verification>

<success_criteria>
After downloading a trail, its TrailEntity carries a navCacheJson maneuver list (when GPX + Valhalla are available), enabling offline navigation. A Valhalla outage during download is silent and non-blocking — tiles and the entity still persist.
</success_criteria>

<output>
Create `.planning/phases/05-cache-write-fallback-ui/05-04-SUMMARY.md` when done.
</output>
