---
phase: 05-cache-write-fallback-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/util/gpx_util.dart
  - app/test/util/gpx_util_test.dart
autonomous: true
requirements: [OFFLINE-01, OFFLINE-02]
must_haves:
  truths:
    - "A single shared helper produces the Valhalla shape list for both the online launch path and the download cache path, so the two cannot diverge"
    - "The helper downsamples to at most 500 points while always preserving the first and last point"
  artifacts:
    - path: "app/lib/util/gpx_util.dart"
      provides: "buildNavShape(List<LatLng> points) shared downsampling helper"
      contains: "List<Map<String, double>> buildNavShape"
    - path: "app/test/util/gpx_util_test.dart"
      provides: "Unit tests proving downsampling cap, first/last preservation, and small-list passthrough"
      contains: "buildNavShape"
  key_links:
    - from: "app/lib/util/navigation_launch_util.dart"
      to: "buildNavShape"
      via: "shared helper call (wired in plan 05-03)"
      pattern: "buildNavShape\\("
    - from: "app/lib/services/trail_download_service.dart"
      to: "buildNavShape"
      via: "shared helper call (wired in plan 05-04)"
      pattern: "buildNavShape\\("
---

<objective>
Extract the Valhalla shape-downsampling logic that currently lives inline in `launchNavigation` into a shared, public helper `buildNavShape(List<LatLng> points)` in `app/lib/util/gpx_util.dart`, and cover it with unit tests. This is the foundational contract for OFFLINE-01: both the download-time cache write (plan 05-04) and the online launch path (plan 05-03) must call the SAME helper so the cached shape and the online-requested shape are byte-identical and can never drift (D-08).

Purpose: Prevent the cache path and the online path from diverging on shape sampling, which would produce different maneuver alignment offline vs online.
Output: A tested `buildNavShape` helper with the exact downsampling behavior currently in `navigation_launch_util.dart` lines 116-138.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-cache-write-fallback-ui/05-CONTEXT.md
</context>

<artifacts_this_phase_produces>
Symbols CREATED in this plan (not pre-existing — exclude from drift verification):
- `buildNavShape` — top-level public function in `app/lib/util/gpx_util.dart`, signature `List<Map<String, double>> buildNavShape(List<LatLng> points)`
- `app/test/util/gpx_util_test.dart` — new test file
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract buildNavShape helper into gpx_util.dart</name>
  <files>app/lib/util/gpx_util.dart</files>
  <read_first>
    - app/lib/util/gpx_util.dart (current file — note it imports latlong2 and flutter_map already; you are ADDING a top-level function, not modifying the GpxMappingUtils extension)
    - app/lib/util/navigation_launch_util.dart lines 112-138 (the exact inline downsampling logic to extract — `if (points.length > 500)` block with `step = (points.length / 499).ceil()`, modulo sampling, and last-point append/dedup)
  </read_first>
  <behavior>
    - Given a list of 2 points → returns 2 shape maps `[{'lat': p.latitude, 'lon': p.longitude}, ...]` (passthrough, no downsampling)
    - Given a list of exactly 500 points → returns all 500 unchanged (≤500 branch)
    - Given a list of 1000 points → returns at most 500 entries; first entry equals first input point; last entry equals last input point
    - Given a list of 501 points → step = ceil(501/499) = 2; result length ≤ 500 and the last input point is present exactly once (no duplicate when modulo sampling already included it)
  </behavior>
  <action>
    Add a top-level public function `List<Map<String, double>> buildNavShape(List<LatLng> points)` to `app/lib/util/gpx_util.dart` (outside the `GpxMappingUtils` extension, since it takes a `List<LatLng>` not a `Gpx`). Move the downsampling algorithm VERBATIM from `navigation_launch_util.dart` lines 116-138: when `points.length > 500`, compute `step = (points.length / 499).ceil()`, iterate and collect `{'lat': points[i].latitude, 'lon': points[i].longitude}` for every `i % step == 0`, then append the last point as `{'lat': points.last.latitude, 'lon': points.last.longitude}` only if `sampled.isEmpty || sampled.last != lastPoint` (dedup). When `points.length <= 500`, map every point to `{'lat': p.latitude, 'lon': p.longitude}`. The function takes `List<LatLng>` because `gpx.allPoints` returns `List<LatLng>` (confirmed in gpx_util.dart line 28) — NOTE: CONTEXT D-08 says `WptType`, but the actual `allPoints` getter returns `List<LatLng>`; use `List<LatLng>`. Keep the existing `import 'package:latlong2/latlong.dart';` (already present). Add a `///` doc comment naming OFFLINE-01 / D-08 and stating "≤500 points, first and last preserved; shared by launchNavigation and downloadTrail".
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/util/gpx_util.dart</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/util/gpx_util.dart` contains `List<Map<String, double>> buildNavShape(List<LatLng> points)` as a top-level function (not inside an extension)
    - The function body contains the literal expression `(points.length / 499).ceil()` and the `points.length > 500` branch
    - `flutter analyze lib/util/gpx_util.dart` reports no errors
    - The function returns `List<Map<String, double>>` with keys exactly `'lat'` and `'lon'`
  </acceptance_criteria>
  <done>buildNavShape exists in gpx_util.dart, compiles, and implements the exact ≤500 downsampling with first/last preservation taken from the current launchNavigation inline code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test buildNavShape downsampling guarantees</name>
  <files>app/test/util/gpx_util_test.dart</files>
  <read_first>
    - app/test/util/format_util_test.dart (existing test in the same directory — follow its `flutter_test` import + `group`/`test`/`expect` structure and file layout)
    - app/lib/util/gpx_util.dart (the helper under test, after Task 1)
  </read_first>
  <behavior>
    - 2-point input → output length 2, output[0] == {'lat': <p0.lat>, 'lon': <p0.lon>}
    - 500-point input → output length 500 (no downsampling)
    - 1000-point input → output.length <= 500, output.first == first point's lat/lon map, output.last == last point's lat/lon map
    - 501-point input → output.length <= 500 AND the last point appears exactly once (count entries equal to the last point's map == 1)
  </behavior>
  <action>
    Create `app/test/util/gpx_util_test.dart` importing `package:flutter_test/flutter_test.dart`, `package:latlong2/latlong.dart`, and `package:wanderer/util/gpx_util.dart`. Write a `group('buildNavShape', ...)` with four tests covering the four behavior cases above. Build inputs as `List.generate(n, (i) => LatLng(i.toDouble(), i.toDouble()))` so first/last points are uniquely identifiable. Assert exact map equality for first/last entries (e.g., `expect(result.first, {'lat': 0.0, 'lon': 0.0})`). For the 501-point dedup test, assert `result.where((m) => m['lat'] == 500.0).length == 1`. Do not use a `MISSING` placeholder — this IS the test scaffold for the helper.
  </action>
  <verify>
    <automated>cd app && flutter test test/util/gpx_util_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `app/test/util/gpx_util_test.dart` exists and imports `package:wanderer/util/gpx_util.dart`
    - File contains `group('buildNavShape'` and at least 4 `test(` calls
    - `flutter test test/util/gpx_util_test.dart` exits 0 with all tests passing
    - Tests assert output length ≤ 500 for >500 inputs and first/last preservation
  </acceptance_criteria>
  <done>gpx_util_test.dart passes, proving the cap, first/last preservation, and last-point dedup.</done>
</task>

</tasks>

<verification>
- `cd app && flutter analyze lib/util/gpx_util.dart` → no errors
- `cd app && flutter test test/util/gpx_util_test.dart` → all pass
- `buildNavShape` is public and reusable by both navigation_launch_util.dart and trail_download_service.dart
</verification>

<success_criteria>
A shared `buildNavShape(List<LatLng>)` helper exists, is unit-tested, and is ready to be called from both the launch path (05-03) and download path (05-04) so cached and online shapes are identical.
</success_criteria>

<output>
Create `.planning/phases/05-cache-write-fallback-ui/05-01-SUMMARY.md` when done.
</output>
