---
phase: 05-cache-write-fallback-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/routes/navigation_screen.dart
  - app/lib/provider/router_provider.dart
autonomous: true
requirements: [OFFLINE-04]
must_haves:
  truths:
    - "NavigationScreen accepts an isOffline flag and renders a wifi-off icon in the maneuver banner only when it is true"
    - "The navigate route unpacks a (NavigateResponse, bool) record and passes both values to NavigationScreen"
    - "When isOffline is false, no offline icon and no placeholder widget is rendered"
  artifacts:
    - path: "app/lib/routes/navigation_screen.dart"
      provides: "isOffline constructor param + conditional FaIcon(FontAwesomeIcons.wifiSlash) in the active banner row"
      contains: "final bool isOffline"
    - path: "app/lib/provider/router_provider.dart"
      provides: "navigate route handler that unpacks (NavigateResponse, bool) record"
      contains: "(NavigateResponse"
  key_links:
    - from: "app/lib/provider/router_provider.dart"
      to: "NavigationScreen"
      via: "constructor call passing id, response, and isOffline"
      pattern: "NavigationScreen\\(.*isOffline"
    - from: "app/lib/routes/navigation_screen.dart"
      to: "FontAwesomeIcons.wifiSlash"
      via: "conditional FaIcon in _buildActiveBannerContent"
      pattern: "FontAwesomeIcons.wifiSlash"
---

<objective>
Deliver the OFFLINE-04 UI contract: add an `isOffline: bool` constructor parameter to `NavigationScreen`, render a trailing `FaIcon(FontAwesomeIcons.wifiSlash)` inside the existing maneuver banner row only when `isOffline == true`, and update the `/trail/:id/navigate` route to unpack the new `(NavigateResponse, bool)` record `extra` (D-01, D-02, D-04, D-05). This makes the offline indicator wireable; the launch path that actually sets the flag is plan 05-03.

Purpose: Give hikers a clear, icon-only signal (per UI-SPEC) that navigation is running from cache, without new screens, overlays, colors, or strings.
Output: NavigationScreen with isOffline param + conditional wifi-off banner icon, and a router that passes the flag through.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-cache-write-fallback-ui/05-CONTEXT.md
@.planning/phases/05-cache-write-fallback-ui/05-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols CREATED/CHANGED in this plan (exclude from drift verification):
- `NavigationScreen.isOffline` — new `final bool isOffline` field + constructor parameter (defaulted to `false`) in `app/lib/routes/navigation_screen.dart`
- Conditional `FaIcon(FontAwesomeIcons.wifiSlash)` rendered inside `_buildActiveBannerContent`
- The `/trail/:id/navigate` route `extra` type changes from `NavigateResponse` to `(NavigateResponse, bool)` record
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add isOffline param + wifi-off banner icon to NavigationScreen</name>
  <files>app/lib/routes/navigation_screen.dart</files>
  <read_first>
    - app/lib/routes/navigation_screen.dart (full file — note the constructor at lines 25-33, `_buildBanner` at 284-307, and `_buildActiveBannerContent` at 309-355 where the Row holds a leading maneuver Icon + Expanded instruction column)
    - .planning/phases/05-cache-write-fallback-ui/05-UI-SPEC.md (the Interaction & Placement Contract table and Color section — the icon MUST be tinted `Theme.of(context).colorScheme.onSurface`, size 20–24, with an 8px leading gap, placed at the trailing edge AFTER the Expanded instruction column)
  </read_first>
  <action>
    In `NavigationScreen`: add `final bool isOffline;` field and `this.isOffline = false` to the const constructor (D-01 — default false so existing call sites and the deep-link fallback compile unchanged). In `_buildActiveBannerContent`, append to the `Row` children, AFTER the `Expanded(child: Column(...))`, a conditional `if (widget.isOffline) ...[const SizedBox(width: 8), FaIcon(FontAwesomeIcons.wifiSlash, color: Theme.of(context).colorScheme.onSurface, size: 20)]`. Per UI-SPEC: NO new color (use `colorScheme.onSurface`, NOT red/error), size `20` (existing maneuver icon is 24; 20 is the approved secondary size), 8px leading gap to mirror the existing `EdgeInsets.only(right: 8.0)` maneuver-icon gap, and NO placeholder when false (use the collection-`if` spread so nothing renders). Do not add a Stack, Positioned, AppBar action, or any string/tooltip (OFFLINE-04 is icon-only per UI-SPEC Copywriting Contract). `FontAwesomeIcons.wifiSlash` is already importable — `font_awesome_flutter` is imported at line 9. Leave `_buildCompletionBannerContent` unchanged (offline icon on the completion banner is out of scope; D-04 targets the active banner).
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/navigation_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `navigation_screen.dart` contains `final bool isOffline;` and the constructor accepts `this.isOffline = false`
    - `_buildActiveBannerContent` contains `if (widget.isOffline)` guarding a `FaIcon(FontAwesomeIcons.wifiSlash`
    - The wifiSlash FaIcon is tinted with `Theme.of(context).colorScheme.onSurface` and sized `20` (NOT a red/error color)
    - A `SizedBox(width: 8)` precedes the wifiSlash icon
    - `flutter analyze lib/routes/navigation_screen.dart` reports no errors
  </acceptance_criteria>
  <done>NavigationScreen exposes isOffline and conditionally shows the wifi-off icon at the trailing edge of the maneuver banner, matching the UI-SPEC tint/size/gap.</done>
</task>

<task type="auto">
  <name>Task 2: Unpack (NavigateResponse, bool) record in the navigate route</name>
  <files>app/lib/provider/router_provider.dart</files>
  <read_first>
    - app/lib/provider/router_provider.dart lines 197-210 (current navigate GoRoute: reads `state.extra`, type-checks `if (response is! NavigateResponse)`, falls back to TrailDetailScreen, else builds `NavigationScreen(id: trailId, response: response)`)
    - app/lib/routes/navigation_screen.dart (the NavigationScreen constructor after Task 1 — now needs id, response, isOffline)
  </read_first>
  <action>
    Update the `/trail/:id/navigate` GoRoute builder to unpack the new record `extra` (D-02). Replace the current `final response = state.extra;` + `if (response is! NavigateResponse)` logic with a record type-check: read `final extra = state.extra;` then `if (extra is! (NavigateResponse, bool))` → return `TrailDetailScreen(id: trailId)` (preserve the existing deep-link/process-restart fallback rationale and comment). On the happy path, destructure `final (response, isOffline) = extra;` and build `NavigationScreen(id: trailId, response: response, isOffline: isOffline)`. The `NavigateResponse` import at line 23 stays. Do not change any other route. This makes the route consume what plan 05-03's `context.push(..., extra: (response, isOffline))` will produce.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/provider/router_provider.dart</automated>
  </verify>
  <acceptance_criteria>
    - `router_provider.dart` navigate route contains `(NavigateResponse, bool)` in the type check
    - The route destructures the record (e.g., `final (response, isOffline) = extra;`) and calls `NavigationScreen(id: trailId, response: response, isOffline: isOffline)`
    - The non-matching-extra fallback still returns `TrailDetailScreen(id: trailId)`
    - `flutter analyze lib/provider/router_provider.dart` reports no errors
  </acceptance_criteria>
  <done>The navigate route unpacks a (NavigateResponse, bool) record, passes isOffline to NavigationScreen, and keeps the deep-link fallback.</done>
</task>

</tasks>

<verification>
- `cd app && flutter analyze lib/routes/navigation_screen.dart lib/provider/router_provider.dart` → no errors
- The route compiles against the new NavigationScreen constructor signature
- No new colors/fonts/spacing tokens introduced beyond the 8px gap (UI-SPEC compliance)
</verification>

<success_criteria>
NavigationScreen renders a wifi-off icon in the maneuver banner when isOffline is true and nothing when false; the navigate route threads a (NavigateResponse, bool) record into the screen. The flag is now wireable by the launch path (05-03).
</success_criteria>

<output>
Create `.planning/phases/05-cache-write-fallback-ui/05-02-SUMMARY.md` when done.
</output>
