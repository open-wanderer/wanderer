---
phase: 11-trail-filter-subcategory-support
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - app/lib/components/trail/trail_quick_filter_bar.dart
autonomous: false
requirements: [FILTER-04, FILTER-05, FILTER-06, FILTER-07]
must_haves:
  truths:
    - "The quick filter bar Category chip stays a single chip opening one bottom sheet"
    - "That bottom sheet contains both a Categories section and a Subcategories section"
    - "The Category chip highlights active when either a category OR a subcategory is selected"
    - "Sheet category/subcategory chips show locale names, icon avatars, and respect visibility prefs"
  artifacts:
    - path: "app/lib/components/trail/trail_quick_filter_bar.dart"
      provides: "Extended category bottom sheet with Subcategories section + _isCategoryActive OR-check"
      contains: "l10n.subcategories"
  key_links:
    - from: "app/lib/components/trail/trail_quick_filter_bar.dart"
      to: "TrailFilter.subcategory via trailFilterProvider.updateFilter"
      via: "copyWith(subcategory: sel)"
      pattern: "copyWith\\(subcategory:"
    - from: "app/lib/components/trail/trail_quick_filter_bar.dart"
      to: "filter.subcategory active-state check"
      via: "_isCategoryActive OR subcategory"
      pattern: "filter\\.subcategory\\.isNotEmpty"
---

<objective>
Extend the quick filter bar's existing Category bottom sheet to include a "Subcategories" section (same `AnimatedSize` pattern as TrailFilterScreen), switch its category chip labels to locale-resolved names with icon avatars, apply preference-based visibility filtering, and update `_isCategoryActive()` so the Category chip highlights when either a category or a subcategory is selected. No second chip is added to the bar.

Purpose: FILTER-05 (quick-bar subcategory selection), FILTER-04 (locale category labels), FILTER-06/07 (hide visible:false), D-11 (single chip, one sheet), D-13 (active-state OR), D-14 (keep `initialChildSize: 0.5`). Mirrors Plan 03 exactly so both surfaces behave identically (UI-SPEC Consistency). Consumes Plan 01 (`TrailFilter.subcategory`), Plan 02 (`avatarBuilder`, `l10n.subcategories`), and Phase 10 providers.

Output: a quick filter bar whose Category chip opens one sheet for both categories and subcategories, with consistent locale labels, icons, badges, visibility filtering, and active-state highlighting.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-trail-filter-subcategory-support/11-CONTEXT.md
@.planning/phases/11-trail-filter-subcategory-support/11-RESEARCH.md
@.planning/phases/11-trail-filter-subcategory-support/11-PATTERNS.md
@.planning/phases/11-trail-filter-subcategory-support/11-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (exclude from drift verification):

- `_categoryAvatar` / `_subcategoryAvatar` helpers in `trail_quick_filter_bar.dart` (same logic as Plan 03; duplicated here per PATTERNS guidance — both surfaces are separate widget classes)
- The Subcategories `AnimatedSize` block inside `_showCategorySheet`
- Updated `_isCategoryActive` body (OR subcategory check)

Symbols consumed (created by sibling plans / Phase 10 — NOT drift):
- `TrailFilter.subcategory` (Plan 01), `WandererFilterChip.avatarBuilder` + `l10n.subcategories` (Plan 02)
- `subcategoryProvider`, `categoryPreferenceProvider`, `subcategoryPreferenceProvider` (Phase 10)
- `CategoryDisplay.displayName`, `SubcategoryDisplay.displayName` (Phase 10)
- `fontAwesomeIconsMap` (existing, `icon_util.dart`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Locale labels, icon avatars, visibility filter, _isCategoryActive OR</name>
  <files>app/lib/components/trail/trail_quick_filter_bar.dart</files>
  <read_first>
    - app/lib/components/trail/trail_quick_filter_bar.dart (lines 1-16 imports; lines 97-99 `_isCategoryActive`; lines 230-298 `_showCategorySheet` with the Category chip `labelBuilder: (c) => c.name` at line 279)
    - app/lib/models/category.dart (CategoryDisplay.displayName; `icon`)
    - app/lib/models/subcategory.dart (SubcategoryDisplay.displayName; `icon`, `badgeIcon`, `category`)
    - app/lib/models/category_preference.dart / subcategory_preference.dart (fields `category`/`subcategory`, `visible`)
    - app/lib/util/icon_util.dart (line 1018 — `fontAwesomeIconsMap`, keys have no `fa-` prefix)
    - .planning/phases/11-trail-filter-subcategory-support/11-PATTERNS.md (trail_quick_filter_bar.dart section — _isCategoryActive update, imports, labelBuilder fix, avatar helpers)
    - .planning/phases/11-trail-filter-subcategory-support/11-RESEARCH.md (Pitfall 2 — subcategoryProvider synchronous; MEMORY — use .value not .valueOrNull)
  </read_first>
  <action>
    Add imports to `trail_quick_filter_bar.dart`: `package:wanderer/models/subcategory.dart`, `package:wanderer/provider/trail/subcategory_provider.dart`, `package:wanderer/provider/category_preference_provider.dart`, `package:wanderer/provider/subcategory_preference_provider.dart`, `package:wanderer/util/icon_util.dart`, and `package:collection/collection.dart`.

    D-13: change `_isCategoryActive` (line 97-99) from `return filter.category.isNotEmpty;` to `return filter.category.isNotEmpty || filter.subcategory.isNotEmpty;`.

    FILTER-04 (D-10): in `_showCategorySheet`, change the Category `WandererFilterChip` `labelBuilder` from `(c) => c.name` to `(c) => c.displayName(Localizations.localeOf(context))`.

    FILTER-06: in the sheet's `Consumer` builder (where `categories` is watched), compute `catPrefs = ref.watch(categoryPreferenceProvider).value ?? []` and `visibleCategories = (categories.value ?? []).where((c) => catPrefs.firstWhereOrNull((p) => p.category == c.id)?.visible != false).toList()`; pass `visibleCategories` as the Category chip `options`.

    Add `avatarBuilder: (c) => _categoryAvatar(c)` to the Category chip. Add the two private helpers `_categoryAvatar(Category)` and `_subcategoryAvatar(Subcategory, Category?)` to the widget class — identical logic to Plan 03 (strip `fa-`, `fontAwesomeIconsMap` lookup, `Icons.category` fallback; subcategory uses a `Stack(clipBehavior: Clip.none)` with primary 16px + optional badge 10px `Positioned(right: -2, bottom: -2)`; no hardcoded icon color). Per PATTERNS, duplicating these helpers here (rather than sharing) is acceptable since this is a separate widget class.
  </action>
  <verify>
    <automated>cd app && grep -q "filter.subcategory.isNotEmpty" lib/components/trail/trail_quick_filter_bar.dart && grep -q "displayName(Localizations.localeOf(context))" lib/components/trail/trail_quick_filter_bar.dart && grep -q "_subcategoryAvatar" lib/components/trail/trail_quick_filter_bar.dart && grep -q "categoryPreferenceProvider" lib/components/trail/trail_quick_filter_bar.dart && flutter analyze lib/components/trail/trail_quick_filter_bar.dart</automated>
  </verify>
  <acceptance_criteria>
    - `_isCategoryActive` returns `filter.category.isNotEmpty || filter.subcategory.isNotEmpty`
    - The sheet's Category chip `labelBuilder` uses `displayName(Localizations.localeOf(context))` (no remaining `(c) => c.name`)
    - Category chip `options` is the preference-filtered `visibleCategories` list (uses `categoryPreferenceProvider`)
    - `trail_quick_filter_bar.dart` defines `_categoryAvatar` and `_subcategoryAvatar` with the badge `Stack(clipBehavior: Clip.none)` + `Positioned(right: -2, bottom: -2)`
    - `flutter analyze lib/components/trail/trail_quick_filter_bar.dart` exits 0
  </acceptance_criteria>
  <done>The Category chip highlights on category-or-subcategory selection, sheet category chips show locale names + icons, hidden categories are dropped, and both avatar helpers exist; analyzer passes.</done>
</task>

<task type="auto">
  <name>Task 2: AnimatedSize Subcategories section inside the category sheet</name>
  <files>app/lib/components/trail/trail_quick_filter_bar.dart</files>
  <read_first>
    - app/lib/components/trail/trail_quick_filter_bar.dart (lines 247-292 — the sheet `Consumer` builder Column where the category `WandererFilterChip` lives at lines 276-287; the new section goes after it inside the same Column)
    - app/lib/provider/trail/subcategory_provider.dart (build() returns `List<Subcategory>` synchronously — no `.value`)
    - app/lib/provider/trail/trail_filter_provider.dart (updateFilter lines 60-65)
    - .planning/phases/11-trail-filter-subcategory-support/11-PATTERNS.md (trail_quick_filter_bar.dart section — the full AnimatedSize Subcategory block, lines 296-334)
    - .planning/phases/11-trail-filter-subcategory-support/11-UI-SPEC.md (Spacing — bottom-sheet 12px label→chips rhythm; Interaction Contract — AnimatedSize 200ms topCenter; sheet stays initialChildSize 0.5 / maxChildSize 0.9)
  </read_first>
  <action>
    Inside `_showCategorySheet`, after the existing category `WandererFilterChip` in the sheet's Column children, add a `Builder` (or inline block) that computes: `final subcategories = ref.watch(subcategoryProvider);` (synchronous `List` — Pitfall 2, no `.value`), `final subPrefs = ref.watch(subcategoryPreferenceProvider).value ?? [];`, `final selectedCategoryIds = currentFilter.category.map((c) => c.id).toSet();`, `final visibleSubs = subcategories.where((s) => selectedCategoryIds.contains(s.category)).where((s) => subPrefs.firstWhereOrNull((p) => p.subcategory == s.id)?.visible != false).toList();` (D-05 + FILTER-07).

    Render `AnimatedSize(duration: const Duration(milliseconds: 200), alignment: Alignment.topCenter, child: ...)`. Child is `const SizedBox.shrink()` when `currentFilter.category.isEmpty || visibleSubs.isEmpty`; otherwise a `Column(crossAxisAlignment: CrossAxisAlignment.start)` with: `const SizedBox(height: 12)`, `Text(l10n.subcategories, style: Theme.of(context).textTheme.titleMedium)` (sheet uses titleMedium per UI-SPEC Typography — match the existing sheet "Categories" header style), `const SizedBox(height: 8)`, and a `WandererFilterChip<Subcategory>` (`options: visibleSubs`, `selectedValues: currentFilter.subcategory`, `multiple: true`, `labelBuilder: (s) => s.displayName(Localizations.localeOf(context))`, `avatarBuilder: (s) => _subcategoryAvatar(s, currentFilter.category.firstWhereOrNull((c) => c.id == s.category))`, `onChanged: (sel) => ref.read(trailFilterProvider(filterId).notifier).updateFilter((f) => f.copyWith(subcategory: sel))`).

    Keep the sheet's `DraggableScrollableSheet` `initialChildSize: 0.5` and `maxChildSize: 0.9` unchanged (D-14); the existing scroll controller handles overflow.
  </action>
  <verify>
    <automated>cd app && grep -q "l10n.subcategories" lib/components/trail/trail_quick_filter_bar.dart && grep -q "AnimatedSize" lib/components/trail/trail_quick_filter_bar.dart && grep -q "copyWith(subcategory:" lib/components/trail/trail_quick_filter_bar.dart && grep -q "ref.watch(subcategoryProvider)" lib/components/trail/trail_quick_filter_bar.dart && grep -q "initialChildSize: 0.5" lib/components/trail/trail_quick_filter_bar.dart && flutter analyze lib/components/trail/trail_quick_filter_bar.dart</automated>
  </verify>
  <acceptance_criteria>
    - The sheet contains an `AnimatedSize` whose child collapses to `SizedBox.shrink()` when `currentFilter.category.isEmpty || visibleSubs.isEmpty`
    - Subcategory chip `options` scoped by `selectedCategoryIds.contains(s.category)` AND filtered by `subcategoryPreferenceProvider`
    - Subcategory `onChanged` calls `updateFilter((f) => f.copyWith(subcategory: sel))`
    - `ref.watch(subcategoryProvider)` used as a `List` (no `.value` — Pitfall 2)
    - `initialChildSize: 0.5` unchanged (D-14)
    - `flutter analyze lib/components/trail/trail_quick_filter_bar.dart` exits 0
  </acceptance_criteria>
  <done>The category sheet shows a scoped, preference-filtered Subcategories section matching TrailFilterScreen, its toggles update TrailFilter.subcategory, and the sheet size is unchanged; analyzer passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify quick filter bar category+subcategory sheet</name>
  <files>app/lib/components/trail/trail_quick_filter_bar.dart</files>
  <action>This is a human-verification checkpoint, not a code task. Claude must have completed Tasks 1-2 (locale labels, avatars, active-state OR, animated subcategory section in the sheet) before reaching here; the executor pauses and presents the verification steps below to the user. See `<what-built>`, `<how-to-verify>`, and `<resume-signal>`.</action>
  <what-built>The quick filter bar's single Category chip now opens one bottom sheet containing both a Categories section (locale names + icons) and a Subcategories section (scoped to selected categories, with primary+badge icons, hidden ones omitted). The Category chip highlights when a category OR subcategory is selected.</what-built>
  <how-to-verify>
    1. Run the app: `cd app && flutter run` (or hot-reload).
    2. On a screen with the quick filter bar (e.g. map), tap the Category chip — confirm ONE bottom sheet opens (no second chip was added).
    3. Confirm the sheet's category chips show icons and locale-resolved names.
    4. Select a category in the sheet. Confirm a "Subcategories" section animates in below, scoped to that category, with primary + badge icons and no clipping (Pitfall 5).
    5. Pick a subcategory, close the sheet. Confirm the Category chip stays highlighted (active) because a subcategory is selected (D-13). Re-open: selection persisted.
    6. Deselect everything. Confirm the Subcategories section collapses and the Category chip de-highlights.
    7. Confirm the sheet still opens at roughly half height (initialChildSize 0.5) and scrolls if content overflows (D-14).
  </how-to-verify>
  <verify><human-check>Reviewer confirms single-sheet layout, locale labels, icon/badge rendering, scoping, active-state OR highlight, and sheet sizing all behave correctly.</human-check></verify>
  <resume-signal>Type "approved" if the single-sheet layout, locale labels, icon/badge rendering, scoping, active-state OR highlight, and sheet sizing all behave correctly, or describe what is off.</resume-signal>
  <done>User has visually confirmed the quick filter bar category+subcategory sheet behaves per spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider data → quick-filter sheet | Category/Subcategory/preference data from Phase 10 providers (server-issued, cached). User taps drive in-memory `TrailFilter` selection only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information disclosure | hidden categories/subcategories appearing in the sheet | mitigate | Visibility filter (`visible != false` against category/subcategory preference providers) removes hidden taxonomy from sheet chip options (FILTER-06/07). Trail RESULTS independently filtered server-side by `withTrailPreferenceFilter` — UI omission is defense-in-depth, not the sole control. |
| T-11-08 | Denial of service | null/empty provider data crashing the sheet | mitigate | AsyncValue providers consumed via `.value ?? []`; `subcategoryProvider` synchronous cache-first List; empty/loading renders `SizedBox.shrink()`. Sheet keeps its existing scroll controller so overflow cannot break layout. |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed; `package:collection` is an existing transitive dependency. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `flutter analyze lib/components/trail/trail_quick_filter_bar.dart` exits 0
- `grep "filter.subcategory.isNotEmpty" app/lib/components/trail/trail_quick_filter_bar.dart` shows the active-state OR (D-13)
- `grep -c "(c) => c.name" app/lib/components/trail/trail_quick_filter_bar.dart` returns 0 in the category chip context (raw-name label removed)
- `grep "copyWith(subcategory:" app/lib/components/trail/trail_quick_filter_bar.dart` shows the subcategory update wiring
- Human-verify checkpoint approved (single sheet, scoping, icons/badges, active-state OR, sizing)
</verification>

<success_criteria>
- The single Category chip opens one bottom sheet with both Categories and Subcategories sections (FILTER-05, D-11)
- Sheet category chips show locale names + FA icons (FILTER-04, D-08); subcategory chips show primary + badge icons (D-09)
- Hidden categories/subcategories omitted (FILTER-06, FILTER-07, D-06)
- `_isCategoryActive` highlights on category OR subcategory selection (D-13)
- Subcategory selections flow into `TrailFilter.subcategory`; sheet sizing unchanged (D-14)
- Behavior is identical to TrailFilterScreen (UI-SPEC Consistency)
</success_criteria>

<output>
Create `.planning/phases/11-trail-filter-subcategory-support/11-04-SUMMARY.md` when done
</output>
