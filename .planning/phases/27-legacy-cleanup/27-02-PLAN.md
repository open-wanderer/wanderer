---
phase: 27-legacy-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - app/lib/entities/trail_entity.dart
  - app/lib/models/trail.dart
  - app/lib/models/trail.freezed.dart
  - app/lib/models/trail.g.dart
  - app/lib/objectbox-model.json
  - app/lib/objectbox.g.dart
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "TrailEntity has no pmTiles or demPmTiles field declarations and no pmTiles/demPmTiles references in the TrailEntityMapping bridge methods (D-06)"
    - "Trail freezed model has no pmTiles or demPmTiles fields (D-06)"
    - "grep for pmTiles / demPmTiles returns zero hits across app/lib and app/test — declarations, mappings, and regenerated generated-code properties all gone (D-06)"
    - "build_runner ran exactly once, at the end, after all source edits (trail_entity.dart, trail.dart, and 27-01's map_cell.dart deletion) were complete (constraint 4, Pitfall 2)"
    - "cd app && flutter analyze reports no issues for the whole project after regeneration"
    - "cd app && flutter test passes — the Phase 26 26-04/26-05 regression coverage stays green (no functional regression)"
    - "A hiker can still download a trail and use it fully offline (basemap + navigation) purely through the region system (ROADMAP success criterion #3)"
  artifacts:
    - path: "app/lib/entities/trail_entity.dart"
      provides: "TrailEntity without tile fields or mapping refs"
      contains: "TrailEntity"
    - path: "app/lib/models/trail.dart"
      provides: "Trail freezed model without pmTiles/demPmTiles"
      contains: "class Trail"
    - path: "app/lib/objectbox-model.json"
      provides: "regenerated ObjectBox schema without pmTiles/demPmTiles properties"
      contains: "TrailEntity"
  key_links:
    - from: "app/lib/entities/trail_entity.dart"
      to: "app/lib/models/trail.dart"
      via: "TrailEntityMapping fromModel/toModel bridge (edited together before regeneration, constraint 1)"
      pattern: "toModel|fromModel"
---

<objective>
Complete CLEAN-01: remove the persisted `pmTiles`/`demPmTiles` fields from the `TrailEntity` ObjectBox entity (declarations + `TrailEntityMapping` bridge references) and the `Trail` freezed model together (constraint 1), then regenerate all generated code in a single `build_runner build --delete-conflicting-outputs` pass (D-06, constraint 4). After this plan, the app builds and runs with zero remaining references to the legacy trail-scoped tile system.

Purpose: Land the persistence half of ROADMAP Phase 27 success criterion #1 (zero remaining references, app builds/runs) and prove criterion #3 (no functional regression — the region-based offline download path still works) by re-running the 26-04/26-05 regression suite against the fully-regenerated tree.
Output: Field-free `TrailEntity`/`Trail`, and regenerated `trail.freezed.dart`/`trail.g.dart`/`objectbox-model.json`/`objectbox.g.dart` with no `pmTiles`/`demPmTiles` properties.

Ordering (constraints 1-4): runs in Wave 2 after plan 27-01 because `build_runner` must run ONCE, at the very end, after ALL source edits — including 27-01's service surgery and `map_cell.dart` deletion. The entity field, the freezed field, and the mapping-extension references are edited together in Task 1 before regeneration (constraint 1 / Pitfall 2), then the single regeneration + final regression gate runs in Task 2. No ObjectBox migration step is needed for a field removal in a pre-production app (D-06, CLAUDE.md "no migration path").

Scope note (D-05): CLEAN-02 (the one-time on-device cleanup sweep) and ROADMAP Phase 27 success criterion #2 remain intentionally descoped — no task addresses them; their absence is intentional, not a gap.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-legacy-cleanup/27-CONTEXT.md
@.planning/phases/27-legacy-cleanup/27-RESEARCH.md
@.planning/phases/27-legacy-cleanup/27-PATTERNS.md
@app/lib/entities/trail_entity.dart
@app/lib/models/trail.dart
</context>

## Artifacts this phase removes

Master phase list (present in both plans). This plan (27-02) removes:
- `TrailEntity.pmTiles` and `TrailEntity.demPmTiles` field declarations (~lines 41-42) and their references in the `TrailEntityMapping` bridge methods (~lines 158-159)
- `Trail.pmTiles` and `Trail.demPmTiles` freezed fields (~lines 102-103 of `trail.dart`)
- The `pmTiles`/`demPmTiles` properties from regenerated `trail.freezed.dart`, `trail.g.dart`, `objectbox-model.json`, and `objectbox.g.dart`

Removed in plan 27-01: the three `TrailDownloadService` tile methods + wiring, `map_cell.dart` (+ siblings), and `DownloadNotificationService.showGenerating()`.

<tasks>

<task type="auto">
  <name>Task 1: Remove pmTiles/demPmTiles from TrailEntity and Trail together (no regeneration yet)</name>
  <files>app/lib/entities/trail_entity.dart, app/lib/models/trail.dart</files>
  <read_first>
    - `app/lib/entities/trail_entity.dart` — the field declarations `List<String> pmTiles = [];` / `List<String> demPmTiles = [];` (~lines 41-42) and the `TrailEntityMapping` bridge references `pmTiles: pmTiles,` / `demPmTiles: demPmTiles,` (~lines 158-159). Confirm via `grep -n "pmTiles\|demPmTiles" app/lib/entities/trail_entity.dart` — expect exactly four hits (41, 42, 158, 159), all in this file.
    - `app/lib/models/trail.dart` — the freezed fields `@Default([]) List<String> pmTiles,` / `@Default([]) List<String> demPmTiles,` (~lines 102-103).
    - 27-PATTERNS.md → "Deletion Boundaries → trail_entity.dart" and "→ trail.dart" (exact lines), plus the Pitfall-2 note (edit entity, model, and mapping extension together before regeneration).
    - 27-RESEARCH.md → Pitfall 2 (regenerating mid-edit produces confusing freezed constructor-mismatch errors) and Anti-Patterns (ObjectBox migration is NOT needed — plain field removal + regeneration is sufficient for a pre-production app).
  </read_first>
  <action>
    Edit `trail_entity.dart` and `trail.dart` together in this one task (constraint 1 / Pitfall 2 — the hand-written `TrailEntityMapping` bridge references both sides, so both must be consistent before `build_runner` runs). In `trail_entity.dart`, delete the two field declarations `List<String> pmTiles = [];` and `List<String> demPmTiles = [];` AND both mapping references (`pmTiles: pmTiles,` / `demPmTiles: demPmTiles,`) inside the `TrailEntityMapping` extension's `toModel()`/`fromModel` bridge — every `pmTiles`/`demPmTiles` occurrence in the file. In `trail.dart`, delete the two freezed fields `@Default([]) List<String> pmTiles,` and `@Default([]) List<String> demPmTiles,` from the `Trail` constructor (D-06). Do NOT run `build_runner` in this task — the generated files (`trail.freezed.dart`, `trail.g.dart`, `objectbox.g.dart`) still reference the removed fields and the tree will not compile until Task 2 regenerates; that broken-generated state is expected and intentional between Task 1 and Task 2 (Pitfall 2). Do not hand-edit any generated file.
  </action>
  <verify>
    <automated>! grep -q "pmTiles\|demPmTiles" app/lib/entities/trail_entity.dart app/lib/models/trail.dart && echo SOURCE_FIELDS_REMOVED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pmTiles\|demPmTiles" app/lib/entities/trail_entity.dart` returns zero lines (all four occurrences — 2 declarations + 2 mapping refs — removed).
    - `grep -n "pmTiles\|demPmTiles" app/lib/models/trail.dart` returns zero lines (both freezed fields removed).
    - `SOURCE_FIELDS_REMOVED` is printed.
    - `build_runner` was NOT run in this task (generated files still reference the fields; project intentionally does not compile yet — resolved in Task 2).
  </acceptance_criteria>
  <done>`pmTiles`/`demPmTiles` are gone from both `TrailEntity` (declarations + mapping) and `Trail` (freezed fields); no generated file was hand-edited; regeneration is deferred to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate generated code once and run the final regression gate</name>
  <files>app/lib/models/trail.freezed.dart, app/lib/models/trail.g.dart, app/lib/objectbox-model.json, app/lib/objectbox.g.dart</files>
  <read_first>
    - 27-PATTERNS.md → "Shared Patterns → Generated-code regeneration (single pass, end of phase)" and "→ Import-cleanliness check after file deletion".
    - 27-RESEARCH.md → "Standard Stack" (regeneration command + versions) and Pitfall 2 (run only once, after all source edits complete).
    - `.planning/phases/26-trail-download-guard/26-05-SUMMARY.md` — the invariants the final `flutter test` must confirm are intact.
  </read_first>
  <action>
    Now that ALL source edits across the phase are complete (27-01's service surgery + `map_cell.dart` deletion, and Task 1's entity/model field removal), run the single regeneration pass per D-06 and constraint 4: `cd app && dart run build_runner build --delete-conflicting-outputs`. This regenerates `trail.freezed.dart`, `trail.g.dart`, `objectbox-model.json`, and `objectbox.g.dart` without the `pmTiles`/`demPmTiles` properties, and retires their ObjectBox property UIDs cleanly (no migration needed for a pre-production app). Then run the final regression gate: `flutter analyze` (whole project — expect "No issues found!") and `flutter test` (expect the 26-04/26-05 coverage green — no functional regression, proving ROADMAP success criterion #3). Do not hand-edit any generated file; if `build_runner` reports conflicts, the `--delete-conflicting-outputs` flag resolves them. If the ObjectBox model reports property-UID issues, do not fabricate a migration — a field removal is a supported regeneration (D-06).
  </action>
  <verify>
    <automated>cd app && dart run build_runner build --delete-conflicting-outputs && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && dart run build_runner build --delete-conflicting-outputs` completes with "Succeeded" (no errors).
    - `grep -rn "pmTiles\|demPmTiles" app/lib app/test --include="*.dart"` returns zero lines (source AND regenerated generated code both clean).
    - `grep -rn "pmTiles\|demPmTiles\|showGenerating\|onGeneratingChanged\|MapCellInfo\|map_cell\|_downloadMapTiles\|_fetchCellList\|_pollUntilReady" app/lib app/test --include="*.dart"` returns zero lines — the whole phase's deletion is complete with zero remaining references (ROADMAP success criterion #1).
    - `cd app && flutter analyze` prints "No issues found!".
    - `cd app && flutter test` passes — 26-04/26-05 invariants (single remove(trail.id), single invalidate(regionListNotifierProvider), vectorLatched/demLatched latch, trailSucceeded-gated showSuccess) remain green.
  </acceptance_criteria>
  <done>Generated code is regenerated in one pass with no `pmTiles`/`demPmTiles` properties; the whole project analyzes clean and the full test suite passes; the phase has zero remaining references to the legacy trail-scoped tile system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This plan performs a local ObjectBox schema change and code regeneration only — no network, no untrusted input, no filesystem paths derived from external data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering | ObjectBox schema regeneration (`objectbox-model.json` UID tracking) after field removal | accept | Field removal is a supported ObjectBox regeneration; property UIDs retire cleanly. No data-migration or downgrade path exists or is needed in a pre-production app (D-06). No untrusted input crosses any boundary; this is a net reduction in persisted-schema surface. |
| T-27-SC | Tampering | pub / `build_runner` regeneration tooling | accept | No new packages are installed. Regeneration uses already-pinned, previously-used dev tooling (`build_runner ^2.13.1`, `objectbox_generator`/`freezed` per RESEARCH Standard Stack). No `[ASSUMED]`/`[SUS]` package; no slopcheck checkpoint required. |

Net effect: this plan removes persisted-schema surface and adds none. No new input validation, auth, session, or crypto is introduced.
</threat_model>

<verification>
- `cd app && dart run build_runner build --delete-conflicting-outputs` succeeds (single pass, end of phase).
- `cd app && flutter analyze` prints "No issues found!".
- `cd app && flutter test` passes — 26-04/26-05 regression coverage green.
- `grep -rn "pmTiles\|demPmTiles\|showGenerating\|onGeneratingChanged\|MapCellInfo\|map_cell\|_downloadMapTiles\|_fetchCellList\|_pollUntilReady" app/lib app/test --include="*.dart"` returns zero lines across the whole phase.
</verification>

<success_criteria>
- CLEAN-01 (persistence portion): `TrailEntity.pmTiles`/`demPmTiles` and `Trail.pmTiles`/`demPmTiles` are deleted and generated code regenerated; the app builds and runs with zero remaining references to the legacy tile system. (ROADMAP success criterion #1.)
- No functional regression: a hiker can still download a trail and use it fully offline (basemap + navigation) purely through the region system; the 26-04/26-05 invariants remain green. (ROADMAP success criterion #3.)
- ROADMAP success criterion #2 (legacy-file sweep, CLEAN-02) is intentionally NOT addressed — descoped per D-05.
</success_criteria>

<output>
Create `.planning/phases/27-legacy-cleanup/27-02-SUMMARY.md` when done.
</output>
