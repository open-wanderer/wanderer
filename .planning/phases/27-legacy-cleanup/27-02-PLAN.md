---
phase: 27-legacy-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - app/lib/provider/trail/trail_download_state_provider.dart
  - app/lib/services/download_notification_service.dart
  - app/lib/entities/trail_entity.dart
  - app/lib/models/trail.dart
  - app/lib/models/trail.freezed.dart
  - app/lib/models/trail.g.dart
  - app/lib/objectbox-model.json
  - app/lib/objectbox.g.dart
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "grep for onGeneratingChanged / isGenerating / showGenerating returns zero hits across app/lib (D-03, D-07)"
    - "grep for pmTiles / demPmTiles returns zero hits across app/lib and app/test — definitions, mappings, and generated properties all gone (D-06)"
    - "cd app && flutter analyze reports no errors for the whole project after regeneration"
    - "cd app && flutter test passes — Phase 26 26-04/26-05 regression coverage stays green"
    - "The Phase 26 guard invariants are untouched: single state..remove(trail.id), single ref.invalidate(regionListNotifierProvider), vectorLatched/demLatched monotonic latch, trailSucceeded-gated showSuccess"
    - "downloadTrail() is now called with only onProgress (no onGeneratingChanged), and the START-time hasSelectedPackages branch is preserved (D-03)"
    - "build_runner regenerated trail.freezed.dart / trail.g.dart / objectbox-model.json / objectbox.g.dart with no pmTiles/demPmTiles properties"
  artifacts:
    - path: "app/lib/provider/trail/trail_download_state_provider.dart"
      provides: "Simplified downloadTrail call site: onProgress only; aggregate-vs-showProgress branch intact"
      contains: "onProgress:"
    - path: "app/lib/services/download_notification_service.dart"
      provides: "Notification service without the trail-only generating spinner"
      contains: "showAggregateProgress"
    - path: "app/lib/entities/trail_entity.dart"
      provides: "TrailEntity without pmTiles/demPmTiles fields or mapping refs"
      contains: "TrailEntity"
    - path: "app/lib/models/trail.dart"
      provides: "Trail freezed model without pmTiles/demPmTiles"
      contains: "class Trail"
  key_links:
    - from: "app/lib/provider/trail/trail_download_state_provider.dart"
      to: "trailDownloadService.downloadTrail"
      via: "single onProgress callback (onGeneratingChanged removed)"
      pattern: "onProgress:"
    - from: "app/lib/provider/trail/trail_download_state_provider.dart"
      to: "regionListNotifierProvider"
      via: "single ref.invalidate (26-05 invariant, must survive)"
      pattern: "invalidate\\(regionListNotifierProvider\\)"
---

<objective>
Complete CLEAN-01: rewire the Phase 26 download guard to the simplified `downloadTrail()` signature (D-03), conditionally delete the now-dead `DownloadNotificationService.showGenerating()` (D-07), then remove `pmTiles`/`demPmTiles` from `TrailEntity` and `Trail` and regenerate all generated code in a single `build_runner` pass (D-06). After this plan, the app builds and runs with zero remaining references to the legacy tile system.

Purpose: Land the second half of ROADMAP Phase 27 success criterion #1 (zero remaining references, app builds/runs) while proving criterion #3 (no functional regression — the region-based offline download path still works) by keeping every Phase 26 guard invariant untouched and re-running the 26-04/26-05 regression suite.
Output: A rewired `trail_download_state_provider.dart`, a `download_notification_service.dart` without `showGenerating()`, field-free `TrailEntity`/`Trail`, and regenerated `.freezed.dart`/`.g.dart`/`objectbox-model.json`/`objectbox.g.dart`.

Ordering (per constraints 1-4): this plan runs in Wave 2 after Plan 27-01 because `build_runner` must run ONCE, at the very end, after ALL source edits (including the service surgery in 27-01). The provider edit (Task 1) happens before the entity/model removal + regeneration (Task 2) so the `showGenerating` re-grep runs against the post-edit provider, and the 26-04/26-05 regression test runs on a fully consistent tree before regeneration.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-legacy-cleanup/27-PATTERNS.md
@.planning/phases/27-legacy-cleanup/27-RESEARCH.md
@app/lib/provider/trail/trail_download_state_provider.dart
@app/lib/services/download_notification_service.dart
@app/lib/entities/trail_entity.dart
@app/lib/models/trail.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the Phase 26 guard and conditionally delete showGenerating()</name>
  <files>app/lib/provider/trail/trail_download_state_provider.dart, app/lib/services/download_notification_service.dart</files>
  <read_first>
    - app/lib/provider/trail/trail_download_state_provider.dart (lines 100-324 — the guard body)
    - 27-PATTERNS.md "Deletion Boundaries -> app/lib/provider/trail/trail_download_state_provider.dart" (delete the onGeneratingChanged param + its whole callback body at 206-231, keep onProgress; the explicit KEEP list at lines 234-239: hasSelectedPackages START branch at 200-201, vectorLatched/demLatched + updateAggregate at 145-174, single remove(trail.id) at 274, single invalidate(regionListNotifierProvider) at 293, trailSucceeded-gated showSuccess at 298-312)
    - 27-PATTERNS.md "Deletion Boundaries -> app/lib/services/download_notification_service.dart" (showGenerating() at 37-67, and the KEEP list: showProgress/showAggregateProgress/showSuccess/showError/dismiss/_ensureInitialized)
    - 27-RESEARCH.md Pitfall 3 (0-region path loses its "Generating map tiles..." interstitial — intentional, not a gap; showProgress's indeterminate:total==0 already covers the pre-first-callback window) and Pitfall 4 (re-run greps fresh, do not trust cached claims)
    - .planning/phases/26-trail-download-guard/26-05-SUMMARY.md (the vectorLatched/demLatched/trailSucceeded mechanism this edit must NOT regress)
  </read_first>
  <action>
    Per D-03, in `trail_download_state_provider.dart` delete the entire `onGeneratingChanged:` parameter and its callback body from the `downloadTrail(...)` call site — the whole `if (!isGenerating) return; if (hasSelectedPackages) { updateAggregate(); } else { notificationService.showGenerating(...) }` block — leaving `downloadTrail(trail, onProgress: ...)` with only the `onProgress` callback. Do NOT touch the START-time `hasSelectedPackages` branch (the `updateAggregate()`-vs-`showProgress` decision when the download begins), the `vectorLatched`/`demLatched` latch maps and `updateAggregate()`, the single `state = {...state}..remove(trail.id)` in `finally`, the single `ref.invalidate(regionListNotifierProvider)`, or the `trailSucceeded`-gated deferred `showSuccess` — these are the 26-04/26-05 invariants and must survive byte-for-byte. Then, per D-07, re-run the `showGenerating` grep FRESH (constraint 2 — only after the provider edit lands): if it shows zero hits outside `download_notification_service.dart`'s own method definition, delete `showGenerating()` (its dartdoc + the whole method, the `'Generating map tiles...'` string included) from `download_notification_service.dart`; keep `showProgress`/`showAggregateProgress`/`showSuccess`/`showError`/`dismiss`/`_ensureInitialized` untouched. If any real caller unexpectedly remains, keep the method and remove only the trail-path call site — the intent is "delete if truly dead," not unconditional. Finally (constraint 3) run `flutter analyze` and `flutter test` to confirm no regression to the 26-04/26-05 invariants. Do NOT run `build_runner` yet — entity/model fields are still present and generated files are still consistent, so the test tree compiles and the regression suite is meaningful here.
  </action>
  <verify>
    <automated>cd app && grep -rn "showGenerating" lib --include="*.dart"; grep -n "onGeneratingChanged\|isGenerating\|showGenerating" lib/provider/trail/trail_download_state_provider.dart; grep -n "invalidate(regionListNotifierProvider)\|..remove(trail.id)\|vectorLatched\|demLatched\|trailSucceeded" lib/provider/trail/trail_download_state_provider.dart; flutter analyze lib/provider/trail/trail_download_state_provider.dart lib/services/download_notification_service.dart; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - The `grep -rn "showGenerating" lib` prints NO matches (zero hits anywhere in app/lib) — confirming the method and its only caller are both gone (D-07).
    - The `onGeneratingChanged\|isGenerating\|showGenerating` grep against trail_download_state_provider.dart prints NO matches.
    - The invariant grep still prints the 26-04/26-05 lines: exactly one `invalidate(regionListNotifierProvider)`, exactly one `..remove(trail.id)`, and the `vectorLatched`/`demLatched`/`trailSucceeded` references — all present and unchanged.
    - `flutter analyze` on both edited files reports "No issues found".
    - `flutter test` passes with the same suite green as Phase 26 left it (26-04/26-05 regression coverage) — zero new failures.
  </acceptance_criteria>
  <done>The guard calls downloadTrail with onProgress only; showGenerating() is deleted (grep-verified dead, D-07); every 26-04/26-05 invariant is intact; flutter analyze and flutter test are green. build_runner is still NOT run — entity/model edits happen next in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Remove pmTiles/demPmTiles from TrailEntity + Trail and regenerate once</name>
  <files>app/lib/entities/trail_entity.dart, app/lib/models/trail.dart, app/lib/models/trail.freezed.dart, app/lib/models/trail.g.dart, app/lib/objectbox-model.json, app/lib/objectbox.g.dart</files>
  <read_first>
    - app/lib/entities/trail_entity.dart (fields at 41-42; TrailEntity.fromModel mapping refs at 158-159 — plus re-grep the file for any toModel()/reverse TrailEntityMapping reference to these two fields)
    - app/lib/models/trail.dart (freezed fields at 102-103)
    - 27-PATTERNS.md "Deletion Boundaries -> trail_entity.dart" and "-> trail.dart", plus "Shared Patterns -> Generated-code regeneration (single pass, end of phase)"
    - 27-RESEARCH.md Pitfall 2 (edit TrailEntity + Trail + the TrailEntityMapping extension together BEFORE regenerating) and "Anti-Patterns -> Assuming ObjectBox migration is needed" (plain field removal + build_runner is sufficient for a pre-production app, no migration, D-06)
  </read_first>
  <action>
    Per D-06 and ordering constraint 1 (Pitfall 2): in a single coherent edit before any regeneration, delete `List<String> pmTiles = [];` and `List<String> demPmTiles = [];` from `TrailEntity`; delete the `pmTiles: pmTiles,` and `demPmTiles: demPmTiles,` lines from `TrailEntity.fromModel` (and any surviving `toModel()`/reverse `TrailEntityMapping` reference to these two fields — re-grep the file first to catch all sites); delete `@Default([]) List<String> pmTiles,` and `@Default([]) List<String> demPmTiles,` from the `Trail` freezed constructor in `trail.dart`. Do NOT add an ObjectBox migration step — a plain property removal is a supported non-breaking regeneration (ObjectBox tracks property UIDs in `objectbox-model.json`), and the app is pre-production with no installed-base data to migrate (D-06). Then, as the LAST source action of the entire phase (ordering constraint 4 — all edits in 27-01 and this plan's Task 1 are already complete), run the single regeneration pass `dart run build_runner build --delete-conflicting-outputs` from the `app/` directory to regenerate `trail.freezed.dart`, `trail.g.dart`, `objectbox-model.json`, and `objectbox.g.dart` without the two properties. Finally run `flutter analyze` (whole project) and `flutter test` to confirm the app builds clean with zero remaining references and the region-based offline download path is functionally unregressed (ROADMAP criterion #3).
  </action>
  <verify>
    <automated>cd app && dart run build_runner build --delete-conflicting-outputs && grep -rn "pmTiles\|demPmTiles" lib test --include="*.dart"; grep -n "pmTiles\|demPmTiles" objectbox-model.json; flutter analyze; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `dart run build_runner build --delete-conflicting-outputs` prints "Succeeded" with NO generator errors (no freezed "missing implementations", no ObjectBox property errors).
    - `grep -rn "pmTiles\|demPmTiles" lib test` prints NO matches across app/lib and app/test.
    - `grep -n "pmTiles\|demPmTiles" objectbox-model.json` prints NO matches — the properties are removed from the regenerated model.
    - `flutter analyze` (whole project) reports "No issues found".
    - `flutter test` passes — full suite green, confirming no functional regression to the region-based offline download path (ROADMAP criterion #3).
  </acceptance_criteria>
  <done>TrailEntity and Trail no longer declare pmTiles/demPmTiles, the mapping extension no longer references them, all four generated files are regenerated without the properties, and flutter analyze + flutter test are green across the project. The legacy trail-scoped tile system has zero remaining references anywhere in app/lib or app/test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> local notification surface | `DownloadNotificationService` shows local-only notifications; no network, no untrusted input. `showGenerating` (trail-only) is removed; shared methods unchanged. |
| Trail persistence -> ObjectBox (local) | Field removal is a local schema change; no server involvement, no new data crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering | ObjectBox schema regeneration (property removal in `trail_entity.dart` -> `objectbox-model.json`/`objectbox.g.dart`) | mitigate | Regenerate via `build_runner` (never hand-edit generated files); no data migration needed (pre-production, D-06); verify integrity via `flutter analyze` + `flutter test`. |
| T-27-04 | Denial of Service (functional regression) | Phase 26 guard rewiring in `trail_download_state_provider.dart` | mitigate | Keep every 26-04/26-05 invariant byte-for-byte (single remove/invalidate, vectorLatched/demLatched latch, trailSucceeded-gated showSuccess); re-run the `flutter test` regression suite (constraint 3) before and after regeneration. |
| T-27-SC | Tampering | npm/pip/cargo/pub installs | not applicable | No packages installed. `build_runner`/`freezed`/`objectbox_generator` are already-pinned existing dev-deps used only for regeneration. No slopcheck target; no `[ASSUMED]`/`[SUS]` package. |

This plan is pure code deletion plus code regeneration — it removes attack surface (one fewer notification path, one fewer externally-parsed field family) and adds none. No new input validation, auth, session, or crypto is introduced.
</threat_model>

<artifacts_removed>
## Artifacts this phase removes (master list)

Physically removed in Plan 27-01: `TrailDownloadService._downloadMapTiles`/`_fetchCellList`/`_pollUntilReady`/`handleGeneratingChanged`, `downloadTrail()`'s `onGeneratingChanged` param, `tileResult` + `entity.pmTiles`/`entity.demPmTiles` writes, `_pollInterval`/`_pollTimeout` statics, tile-only `onCellPointsDelta`, and `map_cell.dart`'s `MapCellInfoList`/`MapCellInfo`/`MapCellStatusResponse`/`MapCellStatus` (whole file + `.freezed.dart` + `.g.dart`).

Physically removed in Plan 27-02:
- `TrailEntity.pmTiles` / `TrailEntity.demPmTiles` (fields + `fromModel` mapping refs) (D-06)
- `Trail.pmTiles` / `Trail.demPmTiles` (freezed fields) (D-06)
- `DownloadNotificationService.showGenerating()` + its `'Generating map tiles...'` string (conditional on the fresh post-edit grep showing zero remaining callers, D-07)
- The `pmTiles`/`demPmTiles` properties in regenerated `trail.freezed.dart` / `trail.g.dart` / `objectbox-model.json` / `objectbox.g.dart` (via `build_runner`)
</artifacts_removed>

<verification>
- `showGenerating` / `onGeneratingChanged` / `isGenerating` grep to zero hits across app/lib (D-03, D-07).
- `pmTiles` / `demPmTiles` grep to zero hits across app/lib, app/test, and `objectbox-model.json` (D-06).
- `flutter analyze` (whole project) is clean; `build_runner` regeneration Succeeded with no generator errors.
- `flutter test` is green — the 26-04/26-05 invariants hold and the region-based offline download path is unregressed (ROADMAP criterion #3).
</verification>

<success_criteria>
- CLEAN-01 (final): the app builds and runs with zero remaining references to the legacy trail-scoped tile system (ROADMAP criterion #1).
- No functional regression: a hiker can still download and use a trail fully offline via the region system; the Phase 26 guard behaves identically (ROADMAP criterion #3).
- CLEAN-02 is intentionally NOT addressed by this phase (descoped per D-05 — pre-production app, no install base to sweep, and `region_disk_usage_util.dart` never summed legacy tile bytes to begin with). This is a deliberate scope cut, not a gap; `.planning/REQUIREMENTS.md` and ROADMAP success criterion #2 should be updated to reflect the cut in a future editing pass.
</success_criteria>

<output>
Create `.planning/phases/27-legacy-cleanup/27-02-SUMMARY.md` when done.
</output>
