---
phase: 19-route-planner-core-waypoint-editing-routing-engine
plan: 04
type: execute
wave: 4
depends_on: ["19-02", "19-03"]
files_modified:
  - app/lib/routes/route_planner_screen.dart
autonomous: true
requirements: [WAYP-01, WAYP-02, WAYP-03, ROUTE-01, ROUTE-02, ROUTE-04, ROUTE-05]
must_haves:
  truths:
    - "A user can tap anywhere on the Route Planner map to add a route anchor (WAYP-01), connected to the previous anchor by a segment"
    - "A user can drag an existing anchor to reposition it; its connected segments re-resolve automatically to the current routing mode (WAYP-02)"
    - "A user can tap an existing route segment to insert a new anchor between its endpoints (WAYP-03); tapping a blocked segment instead retries auto-routing for it (D-09)"
    - "A user can toggle auto-routing on/off via the top-right map control button; toggling on re-resolves every existing segment via Valhalla, toggling off leaves existing segments untouched and only affects segments created afterward (ROUTE-01/02)"
    - "A user can undo and redo waypoint edits via app-bar icon buttons, disabled when their history direction is empty (ROUTE-04/D-11)"
    - "A blocked segment shows a persistent error toast the first time it fails, distinct copy on a second consecutive failed retry, and a transient 'retrying' toast while a retry is in flight (ROUTE-05, UI-SPEC Copywriting Contract)"
  artifacts:
    - path: "app/lib/routes/route_planner_screen.dart"
      provides: "RoutePlannerScreen — hosts the native map, wires all Phase 19 gestures/state to the UI, app bar undo/redo"
      contains: "class RoutePlannerScreen"
  key_links:
    - from: "app/lib/routes/route_planner_screen.dart"
      to: "app/lib/provider/route_anchor_provider.dart"
      via: "onEvent tap routing calling appendAnchor/insertAnchorOnSegment/retrySegment; toggle button calling toggleAutoRouting; app bar calling undo/redo"
      pattern: "routeAnchorsProvider"
    - from: "app/lib/routes/route_planner_screen.dart"
      to: "app/lib/components/map/route_segment_layer.dart"
      via: "RouteSegmentLayer().update(style, segments) called from onStyleLoaded and on every state change via ref.listen"
      pattern: "_segmentLayer.update"
    - from: "app/lib/routes/route_planner_screen.dart"
      to: "app/lib/provider/toast_provider.dart"
      via: "toastProvider.notifier.add(ToastMessage(...)) for blocked/retry copy"
      pattern: "toastProvider"
---

<objective>
Build `RoutePlannerScreen` — the screen that makes every Phase 19 requirement actually reachable by a user: it hosts the native map (style-loaded race buffer, full gestures), embeds 19-03's `RouteAnchorLayer`/`RouteSegmentLayer`, disambiguates marker/segment/empty-map taps into the correct 19-02 mutation, exposes the auto-routing toggle in the top-right controls slot, and puts undo/redo in the app bar with the blocked-segment/retry toast copy from UI-SPEC.

Purpose: This is the phase's final integration point — 19-01/19-02/19-03 built correct, tested components in isolation; this plan wires them into one screen so WAYP-01/02/03 and ROUTE-01/02/04/05 become literally exercisable, closing the goal-backward reachability chain for the whole phase.
Output: `route_planner_screen.dart`. (Router registration/entry-point wiring is HANDOFF-02/03, explicitly Phase 21's scope — not touched here.)
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-CONTEXT.md
@.planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-RESEARCH.md
@.planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-PATTERNS.md
@.planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-UI-SPEC.md
@app/lib/components/base/trail_map.dart
@app/lib/routes/trail_create_screen.dart
@app/lib/provider/toast_provider.dart
</context>

## Artifacts this phase produces (Plan 19-04)

New symbols introduced by this plan (exclude from source-grounding drift checks):

- `class RoutePlannerScreen extends ConsumerStatefulWidget` — constructor `{required String travelProfile, required ml.Geographic initialCenter, String title = 'Plan a Route'}`
- `_RoutePlannerScreenState._buildAutoRoutingToggle(bool enabled)` — top-right pill control button (D-06)
- `_RoutePlannerScreenState._onStyleLoaded`, `_pendingStyle`, `_resolvedStyle` fields — style-loaded race buffer for this screen
- `_RoutePlannerScreenState._blockedNotified`, `_retryAttempted` — local toast-dedup bookkeeping sets

<tasks>

<task type="auto">
  <name>Task 1: Map host, tap routing, auto-routing toggle, empty-state hint</name>
  <files>app/lib/routes/route_planner_screen.dart</files>
  <read_first>
    - app/lib/components/base/trail_map.dart (`TrailMap`: the exact `_pendingStyle` race-buffer pattern (lines ~78-89, 199-214), `onEvent`/`MapEventClick` tap routing (lines ~215-220), top-right `controls` slot `Align`/`Column` (lines ~245-251), `MapGestures.all()` gating (lines ~194-196) — this screen's map host copies this shape, minus the offline/pmtiles rewrite branch which does not apply here)
    - app/lib/components/map/route_anchor_layer.dart (19-03's `RouteAnchorLayer` — embedded as a `children` entry, constructed with `travelProfile: widget.travelProfile`)
    - app/lib/components/map/route_segment_layer.dart (19-03's `RouteSegmentLayer` — `update(style, segments)`/`remove(style)`, called from this screen's style-loaded handler and state listener)
    - app/lib/provider/route_anchor_provider.dart (19-02's `routeAnchorsProvider(travelProfile)` family; `.notifier.appendAnchor`/`.insertAnchorOnSegment`/`.retrySegment`/`.toggleAutoRouting`)
    - .planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-RESEARCH.md — "Map Tap Routing" (the exact `featuresAtPoint(event.screenPoint, layerIds: ['route-segments-hit'])` disambiguation sequence) and Pitfall 5 (style-loaded/map-created ordering race)
    - .planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-UI-SPEC.md — "Map Control & App Bar Contract" (auto-routing toggle pill styling/icon/tooltip) and "Copywriting Contract" (empty-state hint exact text)
    - .planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-CONTEXT.md — D-03 (empty-map tap always appends, no add-mode toggle), D-04 (marker tap wins over segment tap — satisfied structurally since `RouteAnchorLayer`'s own `GestureDetector` consumes marker taps first), D-06 (toggle lives in top-right controls slot)
  </read_first>
  <action>
    Create `app/lib/routes/route_planner_screen.dart` as `class RoutePlannerScreen extends ConsumerStatefulWidget` with constructor `{super.key, required this.travelProfile, required this.initialCenter, this.title = 'Plan a Route'}` (`travelProfile` is `'pedestrian'`/`'bicycle'`, fixed per D-07; `initialCenter` is `required` with no fallback default — Phase 21's entry point supplies the real value, e.g. the user's current location or the trail-source-select flow's last map camera; inventing a hardcoded fallback here would silently mask a caller bug). In `_RoutePlannerScreenState`, declare `ml.MapController? _mapController;`, `ml.StyleController? _pendingStyle;` (buffer, copied verbatim from `TrailMap`'s pattern per Pitfall 5), `ml.StyleController? _resolvedStyle;`, `static const _segmentLayer = RouteSegmentLayer();`, `bool _hintDismissed = false;`. In `build()`: `final state = ref.watch(routeAnchorsProvider(widget.travelProfile));` then `ref.listen(routeAnchorsProvider(widget.travelProfile), (prev, next) { final style = _resolvedStyle; if (style != null && !identical(prev?.segments, next.segments)) { _segmentLayer.update(style, next.segments).ignore(); } if (next.anchors.isNotEmpty) _hintDismissed = true; });` — keeps the segment layer's GeoJSON in sync with every mutation via `updateGeoJsonSource` (no remove/re-add flicker). Resolve the base style the same way every other map screen does: `final styleJson = ref.watch(mapStyleJsonProvider).value;` (no offline/pmtiles rewrite branch — out of this phase's scope). Build `ml.MapLibreMap(options: ml.MapOptions(initStyle: styleJson, initCenter: widget.initialCenter, initZoom: 15, gestures: const ml.MapGestures.all(), androidForegroundLoadColor: Theme.of(context).colorScheme.surface), onMapCreated: ..., onStyleLoaded: ..., onEvent: ..., children: [...])`. Wire `onMapCreated`/`onStyleLoaded` with `TrailMap`'s exact buffer-and-replay structure, renaming fields to this class's (`_mapController`, `_pendingStyle`); inside `_onStyleLoaded(style)`, set `_resolvedStyle = style;` then call `_segmentLayer.update(style, ref.read(routeAnchorsProvider(widget.travelProfile)).segments).ignore();` (first-time add of all 5 native layers). Wire `onEvent: (event) { if (event is! ml.MapEventClick) return; final hits = _mapController?.featuresAtPoint(event.screenPoint, layerIds: const ['route-segments-hit']) ?? const []; final notifier = ref.read(routeAnchorsProvider(widget.travelProfile).notifier); if (hits.isNotEmpty) { final props = hits.first.properties; final before = props['beforeAnchorId'] as String; final after = props['afterAnchorId'] as String; final segState = props['state'] as String; if (segState == 'blocked') { _retryAttempted.add('${before}_$after'); ref.read(toastProvider.notifier).add(ToastMessage(type: ToastType.info, icon: FontAwesomeIcons.route, text: 'Retrying route…')); notifier.retrySegment(before, after); } else { notifier.insertAnchorOnSegment(before, after, event.point); } return; } notifier.appendAnchor(event.point); }` — a marker tap never reaches this handler because `RouteAnchorLayer`'s own `GestureDetector.onTap` consumes it first (D-04); an empty-map tap always appends (D-03, no add-mode toggle exists). In `children:`, include `RouteAnchorLayer(travelProfile: widget.travelProfile)`, the top-right controls `Align(alignment: Alignment.topRight, child: Column(crossAxisAlignment: CrossAxisAlignment.end, children: [_buildAutoRoutingToggle(state.autoRoutingEnabled)]))` (D-06, matching `TrailMap`'s exact controls-slot shape — this slot is shared with Phase 20's future list/elevation toggle buttons, so do not hardcode assumptions about being the only child), and, when `state.anchors.isEmpty && !_hintDismissed`, a centered `Text('Tap the map to start your route.')` banner (UI-SPEC's exact empty-state copy) that is never re-shown once `_hintDismissed` flips true. Implement `Widget _buildAutoRoutingToggle(bool enabled)`: a pill `Padding(padding: const EdgeInsets.all(8.0), child: Container(decoration: BoxDecoration(color: Theme.of(context).canvasColor, borderRadius: BorderRadius.circular(24)), child: IconButton(visualDensity: VisualDensity.compact, tooltip: enabled ? 'Auto-routing on' : 'Auto-routing off', icon: FaIcon(FontAwesomeIcons.route, size: 18, color: enabled ? (Theme.of(context).brightness == Brightness.dark ? const Color(0xff3E435B) : const Color(0xff242734)) : Theme.of(context).colorScheme.onSurface.withValues(alpha: .4)), onPressed: () => ref.read(routeAnchorsProvider(widget.travelProfile).notifier).toggleAutoRouting())))` — matches `_buildMapControls`' existing pill container styling exactly (UI-SPEC's Map Control & App Bar Contract).
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/route_planner_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && flutter analyze lib/routes/route_planner_screen.dart` reports no new issues.
    - `grep -n "featuresAtPoint" app/lib/routes/route_planner_screen.dart` shows the call with `layerIds: const ['route-segments-hit']`.
    - `grep -n "appendAnchor\|insertAnchorOnSegment\|retrySegment\|toggleAutoRouting" app/lib/routes/route_planner_screen.dart` shows all four notifier calls present.
    - `grep -n "'blocked'" app/lib/routes/route_planner_screen.dart` appears in the branch that calls `retrySegment`, never `insertAnchorOnSegment` — source assertion for D-09's mutual exclusivity.
    - `grep -c "'Tap the map to start your route.'" app/lib/routes/route_planner_screen.dart` returns 1 (exact UI-SPEC copy, not a paraphrase).
    - `grep -n "_pendingStyle" app/lib/routes/route_planner_screen.dart` shows the buffer-and-replay pattern present (Pitfall 5).
    - No widget-test precedent exists for a native-map-hosting screen in this codebase (`trail_map.dart`/`trail_create_screen.dart` have none, confirmed via repo search) — `ml.MapLibreMap`'s native platform channel cannot be constructed in the widget-test harness. Automated verification is static (`flutter analyze`); interactive correctness (tap-to-add, drag, insert, toggle) is confirmed via end-of-phase human verification per `human_verify_mode=end-of-phase`.
  </acceptance_criteria>
  <done>The screen hosts the native map with the standard style-loaded race buffer, correctly disambiguates marker/segment/empty-map taps into the right 19-02 mutation, exposes a working auto-routing toggle in the shared top-right controls slot, and shows/dismisses the empty-state hint exactly once per session.</done>
</task>

<task type="auto">
  <name>Task 2: App bar undo/redo + blocked-segment/retry toast copy</name>
  <files>app/lib/routes/route_planner_screen.dart</files>
  <read_first>
    - app/lib/routes/route_planner_screen.dart (as built by Task 1 in this same plan — the map host/body this task wraps in a `Scaffold`/`AppBar`)
    - app/lib/routes/trail_create_screen.dart (`AppBar` shape, lines ~378-410: `leading` back button via `context.canPop() ? context.pop() : context.pushReplacement(...)`, transparent background/elevation 0/`scrolledUnderElevation` 0, the `_saving ? null : ...` disabled-icon-button precedent for D-11)
    - app/lib/provider/toast_provider.dart (`ToastMessage(type:, icon:, text:)`, `ToastType` enum, `toastProvider.notifier.add(...)`)
    - .planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-UI-SPEC.md — "Map Control & App Bar Contract" (undo before redo, `FontAwesomeIcons.arrowRotateLeft`/`arrowRotateRight`, 18px, enabled=accent tint/disabled=standard ~38% opacity) and "Copywriting Contract" (exact Error-state / Retry-in-flight / Retry-failed-again strings)
    - .planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-CONTEXT.md — D-10 (undo/redo in app bar, not map controls), D-11 (disabled when history direction empty, never a no-op tap)
  </read_first>
  <action>
    Wrap Task 1's map-host body in `Scaffold(extendBodyBehindAppBar: true, appBar: AppBar(leading: IconButton(icon: const FaIcon(FontAwesomeIcons.arrowLeft, size: 18), onPressed: () => context.canPop() ? context.pop() : context.pushReplacement('/map'), style: IconButton.styleFrom(backgroundColor: Theme.of(context).colorScheme.surface)), title: Text(widget.title, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.w600)), actions: [IconButton(icon: const FaIcon(FontAwesomeIcons.arrowRotateLeft, size: 18), tooltip: 'Undo', onPressed: state.undoStack.isNotEmpty ? () => ref.read(routeAnchorsProvider(widget.travelProfile).notifier).undo() : null, style: IconButton.styleFrom(backgroundColor: Theme.of(context).colorScheme.surface)), IconButton(icon: const FaIcon(FontAwesomeIcons.arrowRotateRight, size: 18), tooltip: 'Redo', onPressed: state.redoStack.isNotEmpty ? () => ref.read(routeAnchorsProvider(widget.travelProfile).notifier).redo() : null, style: IconButton.styleFrom(backgroundColor: Theme.of(context).colorScheme.surface))], backgroundColor: Colors.transparent, shadowColor: Colors.transparent, elevation: 0, scrolledUnderElevation: 0), body: <Task 1's map>)` — undo listed BEFORE redo (D-10 order); a `null` `onPressed` renders Flutter's built-in disabled/~38%-opacity `IconButton` style automatically (D-11 — no extra styling needed, exactly the `_saving ? null : ...` precedent at `trail_create_screen.dart` line 399). Add two instance fields to `_RoutePlannerScreenState`: `final Set&lt;String&gt; _blockedNotified = {};` and `final Set&lt;String&gt; _retryAttempted = {};` (the latter is populated by Task 1's `onEvent` handler when a blocked-segment tap fires `retrySegment`). Extend the `ref.listen` block from Task 1: for every segment in `next.segments` whose `state == SegmentState.blocked`, compute `key = segmentKey(s.beforeAnchorId, s.afterAnchorId)` (import `route_segment_util.dart`); if `key` is not already in `_blockedNotified`, add it and show a toast — `ToastType.error` with text `"Still couldn't find a route here. Check your connection and try again."` if `key` was in `_retryAttempted` (a retry was in flight for this exact segment; remove it from `_retryAttempted` after use — this is UI-SPEC's Retry-failed-again copy), otherwise `ToastType.error` with text `"Couldn't find a route for this segment. Tap the dashed line to retry."` (UI-SPEC's first-time Error-state copy). For every segment in `next.segments` whose `state != SegmentState.blocked`, remove its `key` from `_blockedNotified` (so a FUTURE failure on that same segment pair shows the first-time copy again, not a stale "still couldn't find" from a prior, already-resolved failure). Import `FontAwesomeIcons.triangleExclamation` for the error toasts' icon field (matching `ToastMessage`'s required `icon` field) and `FontAwesomeIcons.route` for the info-level "Retrying route…" toast Task 1 already dispatches at the tap-to-retry call site.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/route_planner_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && flutter analyze lib/routes/route_planner_screen.dart` reports no new issues.
    - `grep -n "arrowRotateLeft" app/lib/routes/route_planner_screen.dart` appears before `grep -n "arrowRotateRight"` by line number (D-10 ordering — verify via `grep -n` line numbers).
    - `grep -n "undoStack.isNotEmpty ? \|redoStack.isNotEmpty ? " app/lib/routes/route_planner_screen.dart` shows both conditional-disable expressions present (D-11).
    - `grep -c "Couldn't find a route for this segment. Tap the dashed line to retry.\|Still couldn't find a route here. Check your connection and try again.\|Retrying route…" app/lib/routes/route_planner_screen.dart` returns 3 (all three UI-SPEC copy strings present verbatim).
    - `grep -n "_blockedNotified\|_retryAttempted" app/lib/routes/route_planner_screen.dart` shows both bookkeeping sets used in the listener (first-time vs. second-consecutive-failure distinction is source-verifiable, not just visual).
  </acceptance_criteria>
  <done>Undo/redo app-bar buttons are correctly ordered and disabled exactly when their stack is empty; a blocked segment shows the first-time error toast once, a second consecutive failed retry shows the distinct "still couldn't find" copy, and every retry attempt shows the transient "Retrying route…" toast — matching UI-SPEC's Copywriting Contract exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User gesture (tap/drag) → native GL hit-test → state mutation | The only externally-influenced input at this layer is where the user taps; the properties read back from `featuresAtPoint` originate from this app's own locally-built GeoJSON (19-03), never from network/remote data |
| Screen navigation (`context.pop()`/`pushReplacement`) | Standard in-app navigation, no new route/deep-link surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | `RenderedFeature.properties` read in `onEvent` assumed to be `String`/well-formed | mitigate | Properties originate from this app's own `buildSegmentsGeoJson` (19-03), not remote/attacker data — but the cast (`as String`) still fails loudly on a null/malformed value rather than silently proceeding; acceptable because the only producer of this GeoJSON is this app's own trusted local state, not an external input crossing a trust boundary |
| T-19-04-02 | Information Disclosure | Toast copy accidentally surfacing a raw exception message/stack trace to the user | mitigate | All toast text in this plan is fixed UI-SPEC copy, never string-interpolated from a caught exception's `toString()` |
| T-19-04-03 | Denial of Service | Rapid repeated blocked-segment taps spamming retry requests | accept | Each tap calls `retrySegment`, which reuses 19-02's per-segment `CancelToken` cancel-before-replace guard — at most one in-flight request per segment regardless of tap frequency; already mitigated at the provider layer (T-19-02-02), not duplicated here |
| T-19-04-SC | Tampering | npm/pip/cargo/pub package installs | accept | No new packages introduced by this plan |
</threat_model>

<verification>
- `cd app && flutter analyze lib/routes/route_planner_screen.dart` — no new analyzer issues across both tasks.
- End-of-phase human verification (per `human_verify_mode=end-of-phase`): tap-to-add, drag-to-reposition, tap-to-insert, blocked-segment tap-to-retry, auto-routing toggle re-resolving all segments, undo/redo round-tripping, and toast copy — all exercised on a physical/emulated device, since the native `MapLibreMap` platform channel cannot be constructed in the widget-test harness (no precedent for this in the codebase; confirmed via repo search of `trail_map.dart`/`trail_create_screen.dart`).
</verification>

<success_criteria>
- Tap-on-empty-map adds an anchor; drag repositions and re-resolves ≤2 adjacent segments; tap-on-segment inserts (or retries, if blocked) — all reachable end-to-end through this screen.
- Auto-routing toggle re-resolves every segment to the new mode; undo/redo round-trip through the app bar, correctly disabled when their stack is empty.
- Blocked-segment/retry toast copy matches UI-SPEC's Copywriting Contract exactly, with correct first-failure vs. second-consecutive-failure distinction.
</success_criteria>

<output>
Create `.planning/phases/19-route-planner-core-waypoint-editing-routing-engine/19-04-SUMMARY.md` when done.
</output>
