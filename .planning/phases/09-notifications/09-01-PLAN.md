---
phase: 09-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/i18n/app_en.arb
  - app/lib/i18n/app_localizations.dart
  - app/lib/i18n/app_localizations_en.dart
  - app/lib/routes/settings_notifications_screen.dart
  - app/test/routes/settings_notifications_screen_test.dart
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - NOTIF-06
  - NOTIF-07
  - NOTIF-08
  - NOTIF-09

must_haves:
  truths:
    - "Tapping Notifications from Settings opens a screen listing all nine notification types in web-client order (NOTIF-01..09)"
    - "Each notification type shows an independent Web toggle row and Email toggle row (D-01)"
    - "Toggling any switch auto-saves to the server via settingsProvider.saveToServer; a failed save surfaces an error toast and leaves the persisted value intact (D-08)"
    - "With no saved preference, every toggle renders ON (default true) per web parity (D-07)"
    - "The Web toggle label resolves from the new l10n.web ARB key; the Email label from existing l10n.email (D-03)"
    - "Section headers use Padding+Text(titleSmall, color: onSurfaceVariant) matching the Language & Units and Privacy screen pattern (D-02)"
    - "Section header text uses existing settings_notification_* ARB keys (e.g. l10n.settings_notification_trail_comment) as human-readable descriptions (D-04)"
    - "Map keys are snake_case @JsonValue strings (e.g. 'trail_comment'); .name is never used as a key (D-06)"
  artifacts:
    - path: "app/lib/routes/settings_notifications_screen.dart"
      provides: "ConsumerWidget rendering 9 section headers + 18 SwitchListTiles with auto-save"
      contains: "SwitchListTile"
      min_lines: 90
    - path: "app/lib/i18n/app_en.arb"
      provides: "New 'web' ARB key"
      contains: "\"web\": \"Web\""
    - path: "app/lib/i18n/app_localizations.dart"
      provides: "Generated l10n.web getter"
      contains: "web"
    - path: "app/test/routes/settings_notifications_screen_test.dart"
      provides: "Widget test asserting 18 SwitchListTiles, 9 headers, defaults-true, Web/Email labels"
      contains: "findsNWidgets(18)"
  key_links:
    - from: "app/lib/routes/settings_notifications_screen.dart"
      to: "settingsProvider"
      via: "ref.watch(settingsProvider) read + ref.read(settingsProvider.notifier).saveToServer(...) write"
      pattern: "saveToServer"
    - from: "app/lib/routes/settings_notifications_screen.dart"
      to: "Settings.notifications map"
      via: "settings?.notifications?['<snake_case_key>']?.web ?? true read; Map.from(...)..['key']=pref write"
      pattern: "notifications\\?\\["
    - from: "app/lib/routes/settings_notifications_screen.dart"
      to: "l10n.web"
      via: "SwitchListTile title for the Web row"
      pattern: "l10n\\.web"
---

<objective>
Fill the stub `SettingsNotificationsScreen` so users can independently toggle web and email delivery for all nine notification types, with each toggle auto-saving to the server (NOTIF-01..09).

Purpose: Completes the v1.2 Settings milestone — Notifications is the final settings sub-screen. The data model (`NotificationType`, `NotificationPreference`, `Settings.notifications`), persistence path (`settingsProvider.saveToServer`), and route registration (`/settings/notifications`) already exist from Phases 6–8. This phase is a pure UI fill-in plus one ARB key.

Output: A `ConsumerWidget` Notifications screen (9 section headers + 18 `SwitchListTile`s), one new `"web"` ARB key with regenerated localizations, and a widget test asserting render + defaults.
</objective>

<artifacts_this_phase_produces>
New symbols created by this phase (exclude these from drift/source-grounding verification — they do not exist before this phase runs):

- `l10n.web` — new generated getter from the `"web": "Web"` ARB key added to `app_en.arb`
- `SettingsNotificationsScreen` body — replaces the existing stub `SizedBox.shrink()` body (the class name already exists; its implementation is new)
- `_save(WidgetRef, AppLocalizations, Settings)` — private helper copied verbatim from `settings_privacy_screen.dart`
- `_sectionHeader(BuildContext, String)` — private helper copied verbatim from `settings_privacy_screen.dart`
- `_onToggle(...)` (or inline equivalent) — private helper that builds the updated notifications map per D-09
- `app/test/routes/settings_notifications_screen_test.dart` — new widget test file

All other symbols referenced (`NotificationType`, `NotificationPreference`, `Settings`, `settingsProvider`, `saveToServer`, `toastProvider`, `ToastMessage`, `ToastType`, `l10n.email`, `l10n.notifications`, `l10n.error_saving_settings`, the nine `l10n.settings_notification_*` getters) already exist and are unchanged.
</artifacts_this_phase_produces>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-notifications/09-CONTEXT.md
@.planning/phases/09-notifications/09-RESEARCH.md
@.planning/phases/09-notifications/09-UI-SPEC.md

# Canonical screen pattern to mirror (copy _save + _sectionHeader verbatim):
@app/lib/routes/settings_privacy_screen.dart

# SwitchListTile + activeColor precedent (units toggle screen):
@app/lib/routes/settings_language_screen.dart

# Data model (NotificationType @JsonValue snake_case strings, NotificationPreference, Settings.notifications):
@app/lib/models/settings.dart

# Widget-test pattern to mirror (ProviderScope override, tall viewport, localization delegates):
@app/test/routes/settings_privacy_screen_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "web" ARB key and regenerate localizations</name>
  <files>app/lib/i18n/app_en.arb, app/lib/i18n/app_localizations.dart, app/lib/i18n/app_localizations_en.dart</files>
  <read_first>
    - app/lib/i18n/app_en.arb (confirm `"web"` is absent; `"email"`, `"notifications"`, `"error_saving_settings"`, and the nine `"settings_notification_*"` keys are present)
    - app/l10n.yaml (arb-dir: lib/i18n; template-arb-file: app_en.arb; output-localization-file: app_localizations.dart)
  </read_first>
  <action>
    Add a single ARB entry `"web": "Web"` to `app/lib/i18n/app_en.arb`. Place it near the existing `"email": "Email"` entry (insert in alphabetical proximity; JSON key order is cosmetic). Do NOT add a metadata `"@web"` block — sibling keys like `"email"` and `"notifications"` have no metadata blocks, so omit one for consistency. Do NOT add `"web"` to any other locale ARB file by hand; gen-l10n synthesizes missing keys from the template. Then run `cd app && flutter gen-l10n` to regenerate `app_localizations.dart` and `app_localizations_en.dart` so the `String get web` getter exists. Do NOT hand-edit any generated `app_localizations*.dart` file. Per D-03 this is the only new label key — "Email" reuses the existing `l10n.email`.
  </action>
  <verify>
    <automated>cd app && grep -q '"web": "Web"' lib/i18n/app_en.arb && grep -q "get web" lib/i18n/app_localizations.dart && flutter analyze lib/i18n/app_localizations.dart</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/i18n/app_en.arb` contains the exact line `"web": "Web"`.
    - `app/lib/i18n/app_localizations.dart` contains a `web` getter (`grep "get web"` matches).
    - `cd app && flutter analyze lib/i18n/app_localizations.dart` reports no errors for the generated file.
    - No generated `app_localizations*.dart` file was hand-edited (only gen-l10n produced changes).
  </acceptance_criteria>
  <done>`l10n.web` resolves to "Web" in English and all 14 locale files were regenerated by gen-l10n without manual edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill SettingsNotificationsScreen with 9 sections of Web/Email toggles + auto-save</name>
  <files>app/lib/routes/settings_notifications_screen.dart</files>
  <read_first>
    - app/lib/routes/settings_notifications_screen.dart (the stub being replaced — currently a StatelessWidget with `SizedBox.shrink()` body)
    - app/lib/routes/settings_privacy_screen.dart (copy `_save` and `_sectionHeader` verbatim; copy the `activeColor` derivation and the `settings == null` error-toast guard)
    - app/lib/routes/settings_language_screen.dart (SwitchListTile-equivalent toggle precedent + `activeColor` usage)
    - app/lib/models/settings.dart (NotificationType `@JsonValue` snake_case strings; NotificationPreference fields `web`, `email`; Settings.notifications type `Map<String, NotificationPreference>?`)
    - app/lib/provider/settings_provider.dart (confirm `build()` returns `Settings?` SYNCHRONOUSLY — NOT AsyncValue; `ref.watch(settingsProvider)` yields a plain `Settings?`)
    - app/lib/provider/toast_provider.dart (ToastType.error, ToastMessage signature)
  </read_first>
  <behavior>
    - Renders 9 section headers in D-05 order: trail_comment, new_follower, trail_share, trail_like, list_share, summit_log_create, trail_mention, comment_mention, summit_log_mention — each header text is the matching `l10n.settings_notification_*` description.
    - Renders exactly 18 SwitchListTiles (one "Web" + one "Email" per type), Web row first within each section.
    - Each toggle's value reads `settings?.notifications?['<snake_case_key>']?.web ?? true` (or `.email ?? true`); when settings has no saved notifications, all 18 toggles render ON (true).
    - Web row title is `l10n.web`; Email row title is `l10n.email`.
    - Toggling a switch builds an updated map touching only the changed key + changed field (D-09) and calls `_save`; when `settings == null`, it shows the error toast instead of saving (mirror the Privacy guard).
  </behavior>
  <action>
    Convert `SettingsNotificationsScreen` from `StatelessWidget` to `ConsumerWidget` (signature `Widget build(BuildContext context, WidgetRef ref)`). Keep the existing `Scaffold` + `AppBar(leading IconButton(Icons.arrow_back) → context.pop(), title: Text(l10n.notifications))` (D-10). Read `final settings = ref.watch(settingsProvider);` as a plain `Settings?` — DO NOT use `.value`, `.valueOrNull`, `.when`, or treat it as `AsyncValue` (the provider is synchronous; this corrects CONTEXT D-11, per RESEARCH Pitfall 1). Copy `_save(WidgetRef, AppLocalizations, Settings)` and `_sectionHeader(BuildContext, String)` verbatim from `settings_privacy_screen.dart`. Derive `activeColor` exactly as the sibling screens: `Theme.of(context).brightness == Brightness.dark ? colorScheme.onSurface : colorScheme.primary`, and pass it to every `SwitchListTile` (use whichever of `activeColor`/`activeThumbColor` the sibling screens currently use to avoid a divergent API). Build an ordered list of `(String key, String label)` records in D-05 order where each `key` is the literal snake_case `@JsonValue` string ('trail_comment', 'new_follower', 'trail_share', 'trail_like', 'list_share', 'summit_log_create', 'trail_mention', 'comment_mention', 'summit_log_mention') and each `label` is the matching `l10n.settings_notification_*` getter. NEVER use `NotificationType.x.name` as a map key — `.name` returns camelCase ('trailComment') and would never match the server's snake_case keys (RESEARCH Pitfall 2). Render the body as a single `ListView` that, for each item, emits `_sectionHeader(context, item.label)` followed by a "Web" `SwitchListTile` (`value: settings?.notifications?[item.key]?.web ?? true`, `title: Text(l10n.web)`) and an "Email" `SwitchListTile` (`value: settings?.notifications?[item.key]?.email ?? true`, `title: Text(l10n.email)`). For `onChanged`, implement the D-09 update: read `final existing = settings.notifications?[item.key] ?? const NotificationPreference(web: true, email: true);`, build `final pref = isWeb ? existing.copyWith(web: value) : existing.copyWith(email: value);`, then `final updated = Map<String, NotificationPreference>.from(settings.notifications ?? {})..[item.key] = pref;` and call `_save(ref, l10n, settings.copyWith(notifications: updated));`. Guard `if (settings == null)` before building the map and surface the same `l10n.error_saving_settings` toast the Privacy screen uses (D-08). Add no Save button. Update the file's imports to include flutter_riverpod, font_awesome_flutter, settings model, settings_provider, and toast_provider (mirror the Privacy screen import block).
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/settings_notifications_screen.dart && ! grep -q '\.name' lib/routes/settings_notifications_screen.dart && ! grep -qE '\.value(OrNull)?\b|\.when\(|AsyncValue' lib/routes/settings_notifications_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && flutter analyze lib/routes/settings_notifications_screen.dart` reports no errors.
    - The file declares `class SettingsNotificationsScreen extends ConsumerWidget` (grep matches `extends ConsumerWidget`).
    - The file contains all nine snake_case literal keys: 'trail_comment', 'new_follower', 'trail_share', 'trail_like', 'list_share', 'summit_log_create', 'trail_mention', 'comment_mention', 'summit_log_mention'.
    - The file contains `l10n.web` and `l10n.email` as SwitchListTile titles, and references `saveToServer`.
    - The file does NOT contain `.name` (no camelCase map keys) and does NOT contain `.value`, `.valueOrNull`, `.when(`, or `AsyncValue` (provider is synchronous).
    - The file contains no Save button (no `ElevatedButton`/`FilledButton`/`TextButton` wired to a save action) — auto-save only.
  </acceptance_criteria>
  <done>The Notifications screen renders 9 section headers and 18 SwitchListTiles in D-05 order, each toggle defaults to true with no saved preference, and toggling auto-saves via the D-09 map-copy pattern with an error-toast fallback. `flutter analyze` is clean for the file.</done>
</task>

<task type="auto">
  <name>Task 3: Add widget test asserting render, ordering, defaults, and labels</name>
  <files>app/test/routes/settings_notifications_screen_test.dart</files>
  <read_first>
    - app/test/routes/settings_privacy_screen_test.dart (mirror: ProviderScope with settingsProvider.overrideWithValue, tall viewport, full localization delegates, locale Locale('en'))
    - app/lib/routes/settings_notifications_screen.dart (the screen under test, from Task 2)
    - app/lib/models/settings.dart (Settings + NotificationPreference constructors for the override fixture)
  </read_first>
  <action>
    Create `app/test/routes/settings_notifications_screen_test.dart` mirroring `settings_privacy_screen_test.dart`. Use a tall viewport so the lazy `ListView` mounts all rows: `tester.view.physicalSize = const Size(1080, 8000); tester.view.devicePixelRatio = 1.0;` with `addTearDown` resets (18 tiles + 9 headers need more height than the Privacy test's 4000). Wrap the screen in `ProviderScope(overrides: [settingsProvider.overrideWithValue(...)])` over a `MaterialApp` with `localizationsDelegates` = [AppLocalizations.delegate, GlobalMaterialLocalizations.delegate, GlobalWidgetsLocalizations.delegate, GlobalCupertinoLocalizations.delegate], `supportedLocales: AppLocalizations.supportedLocales`, `locale: const Locale('en')`, `home: const SettingsNotificationsScreen()`. Write two tests: (1) "notifications screen renders nine sections with web+email toggles (NOTIF-01..09)" — override with `const Settings(id: '1')` (no notifications map), `pumpAndSettle`, then assert `find.byType(SwitchListTile)` is `findsNWidgets(18)`, assert `find.text('Web')` is `findsNWidgets(9)` and `find.text('Email')` is `findsNWidgets(9)`, and assert at least one section header description renders, e.g. `find.text('Someone left a comment on your trail')` is `findsOneWidget`. (2) "null notifications defaults every toggle to ON (D-07)" — same `const Settings(id: '1')` override, after settle collect `tester.widgetList<SwitchListTile>(find.byType(SwitchListTile))` and assert every `.value` is `true`. Do NOT assert tap-to-save behavior — mirror the Privacy test's documented exclusion (the save path needs an apiProvider/HTTP override the harness does not provide); leave save to acceptance criteria / manual verification.
  </action>
  <verify>
    <automated>cd app && flutter test test/routes/settings_notifications_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && flutter test test/routes/settings_notifications_screen_test.dart` passes (all tests green).
    - The test file asserts `findsNWidgets(18)` for `SwitchListTile`.
    - The test file asserts `find.text('Web')` and `find.text('Email')` each resolve to 9 widgets.
    - The test file asserts every `SwitchListTile.value` is `true` when the override Settings has no notifications map.
    - The test contains NO tap/save assertion (no `tester.tap` on a switch followed by a save expectation).
  </acceptance_criteria>
  <done>The widget test passes and proves 18 toggles render in the lazy ListView, Web/Email labels appear 9× each, and absent preferences default every toggle to ON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → SvelteKit `/settings/{id}` API | Toggle changes cross to the server via the existing authenticated dio client (unchanged path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | POST `/settings/{id}` (another user's settings) | accept | Server-side ownership check on `/settings/{id}` is existing and unchanged; this phase adds no new endpoint or auth code. Boolean-only input from `SwitchListTile`. |
| T-09-02 | Information Disclosure | `_save` error path / logging | mitigate | `_save` surfaces only the generic `l10n.error_saving_settings` toast (no server internals); do not `print()` settings payloads. Copied verbatim from the audited Privacy screen. |
| T-09-SC | Tampering | npm/pub/cargo installs | accept | No package installs in this phase — no `flutter pub add`, no `pubspec.yaml` change, no new transitive deps (RESEARCH "Package Legitimacy Audit: Not applicable"). No supply-chain surface introduced. |

**Net assessment:** No new security-relevant code. Input is boolean-only, error messaging is generic, and persistence reuses the already-audited `saveToServer` path. No high-severity threats — nothing to block on.
</threat_model>

<verification>
Phase-level checks (run before `/gsd-verify-work`):

- `cd app && flutter analyze` is clean (no errors).
- `cd app && flutter test test/routes/` passes (includes the new notifications test and the existing privacy/language/account/settings tests).
- `grep -q '"web": "Web"' app/lib/i18n/app_en.arb` and `grep -q "get web" app/lib/i18n/app_localizations.dart`.
- `settings_notifications_screen.dart` contains no `.name`, `.value`, `.valueOrNull`, `.when(`, or `AsyncValue` references.
- The screen file is unchanged from the stub in all respects except the body fill (AppBar title/back behavior preserved per D-10).
- No changes to `settings_provider.dart`, `settings_entity.dart`, `settings.dart`, or the go_router config (all complete per RESEARCH — the planner's key check).
</verification>

<success_criteria>
Measurable completion (maps to ROADMAP Phase 9 success criteria):

1. Tapping Notifications from Settings opens a screen listing all nine notification types in web-client order (9 section headers, D-05) — NOTIF-01..09.
2. Each notification type exposes an independent Web toggle and Email toggle (18 SwitchListTiles total, D-01).
3. Toggling any switch saves to the server via `settingsProvider.saveToServer` and the new state persists after reopening; a failed save surfaces `l10n.error_saving_settings` and leaves the persisted value intact (D-08).
4. With no saved preference, every toggle renders ON (default true, D-07).
5. `flutter analyze` clean and `flutter test test/routes/` green.
</success_criteria>

<output>
Create `.planning/phases/09-notifications/09-01-SUMMARY.md` when done.
</output>
