---
phase: 29-polygon-based-extraction-region-api
plan: 04
type: execute
wave: 3
depends_on: [29-01, 29-02, 29-03]
files_modified: []
autonomous: false
requirements: [EXTRACT-01, EXTRACT-02, EXTRACT-03]

must_haves:
  truths:
    - "A real enabled leaf builds an archive whose tile coverage follows its polygon boundary, not its bbox"
    - "The cron builds only enabled leaves and reads no region_config.json"
    - "GET /api/v1/regions returns parent/path/depth/kind for group and leaf rows, and a '.'-path leaf downloads successfully"
  artifacts: []
  key_links:
    - from: "enabled leaf in regions table"
      to: "polygon-clipped .pmtiles archive on disk"
      via: "region-archive-build cron -> pmtiles extract --region"
      pattern: "pmtiles"
---

<objective>
End-to-end confirmation that Phase 29's three deliverables actually hold against real data — the one thing the per-plan automated unit/build/grep gates cannot prove: that a real cron run produces a polygon-clipped archive, that target selection is table-driven, and that the hierarchy-aware API + relaxed download route work for a real `.`-containing path.

Purpose: the polygon-clipping mechanism was already verified live in 29-RESEARCH.md (209/340 tiles against the `munich` archive), and the wiring is covered by unit tests + `go build`/`go vet` + source-assertion greps in 29-01..03. This plan closes the loop with a human running the cron once and inspecting the result, because building a real region archive cannot be cheaply asserted in CI.

Output: a human-verified checkpoint (no source files changed).
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-polygon-based-extraction-region-api/29-RESEARCH.md
@db/services/regions/builder.go
@db/routes/regions_get.go
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of polygon extraction, table-driven cron, and hierarchy API</name>
  <files>none (verification checkpoint — no source changes)</files>
  <read_first>
    - .planning/phases/29-polygon-based-extraction-region-api/29-RESEARCH.md ("Pattern 1" verified `--region` clipping shape and the exact `pmtiles show`/`pmtiles tile` commands to reuse)
    - db/services/regions/builder.go (post-29-01: the cron query + `--region=` invocation being verified)
    - db/routes/regions_get.go (post-29-02: the hierarchy fields being verified)
  </read_first>
  <what-built>
    - 29-01: the `region-archive-build` cron now queries `regions` where `kind='leaf' AND enabled=true` and extracts each leaf's vector + DEM archive with `pmtiles extract --region=<polygon file>` (canonical polygon, not bbox); the region-id allow-list accepts materialized `path` ids.
    - 29-02: `GET /api/v1/regions` returns the full group+leaf catalog with `parent`/`path`/`depth`/`kind` (+ leaf `bbox`/`enabled` and existing build-state); the `region_config.json` loader is deleted.
    - 29-03: the SvelteKit download proxies accept `.`/`'`-containing path ids in lockstep with the Go allow-list.
  </what-built>
  <action>
    This is a blocking human-verify checkpoint — no code is written. The executor pauses and the operator runs the six verification steps below against a dev instance whose `regions` collection is seeded (Phase 28), then reports the result. Record the observed evidence (cron log excerpt, tile-count comparison, and a `GET /api/v1/regions` response sample) into the plan summary.
  </action>
  <how-to-verify>
    Run against a dev instance whose `regions` collection is already seeded (Phase 28). All commands assume the repo root and the `db` working dir.

    1. **Pick a small enabled leaf.** In PocketBase admin (or SQL against `db/pb_data`), find a leaf with a small polygon and note its `path` (choose something modest, e.g. a small country/sub-region — NOT a whole continent, to keep the extract fast). Set that row's `enabled = true`. Confirm no other leaf is enabled (so the cron builds exactly one).

    2. **Trigger the cron once.** Set `REGION_ARCHIVE_CRON_SCHEDULE` to fire within the next minute (e.g. the next clock minute in cron syntax), start the backend (`go run . serve` from `db/`, or the normal compose stack), and watch the log for `[regions] building vector archive for region <path> ...` followed by `[regions] vector archive for region <path> ready (<N> bytes)`. Confirm the log names the chosen leaf's `path` and NO other region.

    3. **Prove polygon clipping (EXTRACT-01).** For the built archive `db/pb_data/region_archives/<path>/vector.pmtiles`:
       - Run `pmtiles show db/pb_data/region_archives/<path>/vector.pmtiles` and note the tile count and bounds.
       - Extract a bbox-only baseline for comparison: `pmtiles extract db/pb_data/region_archives/<path>/vector.pmtiles /tmp/bbox_baseline.pmtiles --bbox=<the leaf's bbox from the regions table> --maxzoom=14`, then `pmtiles show /tmp/bbox_baseline.pmtiles`.
       - Confirm the region archive's tile count is LESS than the bbox baseline's for any leaf whose polygon is not itself a rectangle (an irregular boundary yields fewer tiles — the polygon-clipping win). For a spot-check, pick a `z14 x y` tile that lies inside the leaf's bbox but clearly outside its polygon and confirm `pmtiles tile db/pb_data/region_archives/<path>/vector.pmtiles 14 <x> <y>` reports "Tile not found in archive", while the same tile IS present in `/tmp/bbox_baseline.pmtiles`.

    4. **Prove table-driven targets + no config read (EXTRACT-02).** Confirm `grep -rn "region_config.json\|REGION_CATALOG_CONFIG_PATH\|LoadRegionCatalog" db --include='*.go' | grep -v '_test'` returns nothing, and that with zero enabled leaves the cron builds nothing (flip the leaf back to `enabled=false`, re-trigger, confirm the log shows no build).

    5. **Prove the hierarchy API (EXTRACT-03).** As a logged-in user, `GET /api/v1/regions` (through the app's SvelteKit path or the internal Go route with auth). Confirm: entries include `parent`, `path`, `depth`, `kind`; at least one `kind:"group"` row is present (no `bbox`/`status`); leaf rows still carry `id`, `name`, `bbox`, `status` (existing shape intact); and the enabled leaf's entry shows a `vector_url` once built.

    6. **Prove the relaxed download route (Pitfall 1 lockstep).** If the enabled leaf's `path` contains a `.` or apostrophe, request its `vector_url` and confirm it does NOT 400 at the SvelteKit proxy (streams the archive or returns a 200/206). If your chosen leaf's path has no `.`, additionally pick any `.`-containing leaf path and confirm `GET /api/v1/regions/<that.path>/download` returns 404 "not ready" (built-file-missing) rather than 400 "Invalid region id" — a 400 would mean the regex lockstep failed.
  </how-to-verify>
  <verify>
    <human-check>All six steps confirmed: polygon-clipped archive (fewer tiles than bbox baseline + a bbox-inside/polygon-outside tile absent), single enabled leaf built, no config-file read, hierarchy fields present in the API, and a `.`-path region downloads without a 400.</human-check>
  </verify>
  <acceptance_criteria>
    - Step 3: the region archive reports fewer tiles than the bbox baseline, and a chosen bbox-inside/polygon-outside `z14` tile is "not found" in the region archive but present in the baseline (EXTRACT-01).
    - Step 4: `grep -rn "region_config.json|REGION_CATALOG_CONFIG_PATH|LoadRegionCatalog" db --include='*.go' | grep -v '_test'` returns nothing, and a zero-enabled cron run logs no build (EXTRACT-02).
    - Step 5: `GET /api/v1/regions` includes `parent`/`path`/`depth`/`kind` on rows, at least one `group` row present, and leaf rows retain `id`/`name`/`bbox`/`status` (EXTRACT-03 + backward-compat).
    - Step 6: a `.`-containing region path returns 404-not-ready or 200/206, never 400 "Invalid region id" (regex lockstep holds).
  </acceptance_criteria>
  <resume-signal>Type "approved" once polygon clipping, table-driven target selection, the hierarchy fields, and the `.`-path download are all confirmed; otherwise describe which step failed (e.g. "step 3: region archive tile count equals bbox baseline" or "step 6: 400 Invalid region id").</resume-signal>
  <done>An operator has confirmed all six steps against a real seeded instance; EXTRACT-01/02/03 hold end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-run cron → `pmtiles` subprocess → on-disk archive | Verification exercises the same trusted-but-defensive path built in 29-01; no new boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Tampering | path-traversal id reaching `filepath.Join` during a real build/download | mitigate | This checkpoint's step 6 actively confirms the relaxed-but-bounded allow-list still rejects `..`/traversal while accepting real `.`-paths — a live check of the T-29-01/T-29-07 mitigations |
| T-29-SC | Tampering | package-manager installs | accept | N/A — verification-only plan, no code or dependency changes |
</threat_model>

<verification>
- Human confirms all six steps; the checkpoint blocks until "approved".
- This plan changes no source; its evidence is the cron log, `pmtiles show`/`pmtiles tile` output, and the `GET /api/v1/regions` response.
</verification>

<success_criteria>
- A real enabled leaf's archive follows its polygon boundary (fewer tiles than a bbox baseline; a bbox-inside/polygon-outside tile is absent) — EXTRACT-01.
- The cron builds only enabled leaves and no `region_config.json` read remains — EXTRACT-02.
- `GET /api/v1/regions` exposes `parent`/`path`/`depth`/`kind` and a `.`-path region downloads without a 400 — EXTRACT-03.
</success_criteria>

## Artifacts this phase produces (plan 29-04)

No source artifacts — this is a blocking human-verify checkpoint. Its output is the recorded verification evidence in `29-04-SUMMARY.md` (cron log excerpt, `pmtiles show`/`pmtiles tile` tile-count comparison, and a `GET /api/v1/regions` response sample showing `parent`/`path`/`depth`/`kind`).

<output>
Create `.planning/phases/29-polygon-based-extraction-region-api/29-04-SUMMARY.md` when done.
</output>
