---
phase: 16-list-map-screens-on-maplibre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/components/base/search_map.dart
  - app/lib/routes/list_detail_map_screen.dart
  - app/lib/routes/list_detail_screen.dart
autonomous: true
requirements: [CORE-08]
must_haves:
  truths:
    - "Opening a list's full map draws every trail in the list as a polyline on one MapLibreMap"
    - "The camera animates to fit all of the list's trails on load"
    - "The list detail screen's inline map shows the same trails, is non-interactive, and tapping it navigates to the full list map"
    - "Tapping a trail marker on the full list map fits the camera to that trail"
    - "flutter analyze reports no issues for all three files"
  artifacts:
    - path: "app/lib/components/base/search_map.dart"
      provides: "Lightweight trail-agnostic MapLibreMap host (style load + live theme swap only)"
      contains: "class SearchMap"
    - path: "app/lib/routes/list_detail_map_screen.dart"
      provides: "Full list map on MapLibreMap with fit-to-all-trails + per-marker tap"
      contains: "SearchMap"
    - path: "app/lib/routes/list_detail_screen.dart"
      provides: "Inline non-interactive list map on MapLibreMap (_ListMap)"
      contains: "SearchMap"
  key_links:
    - from: "app/lib/components/base/search_map.dart"
      to: "mapStyleJsonProvider"
      via: "ref.watch + ref.listen live theme swap"
      pattern: "mapStyleJsonProvider"
    - from: "app/lib/routes/list_detail_map_screen.dart"
      to: "ml.MapController.fitBounds"
      via: "onStyleLoaded imperative bounds fit"
      pattern: "fitBounds"
    - from: "app/lib/routes/list_detail_screen.dart"
      to: "/list/{id}/map"
      via: "MapEventClick tap-through navigation"
      pattern: "context\\.push"
---

<objective>
Port the two multi-trail list maps (`list_detail_map_screen.dart` and `list_detail_screen.dart`'s inline `_ListMap`) from `flutter_map` to `MapLibreMap`, and create the shared lightweight `SearchMap` host they and `map_screen` (16-03) build on. This delivers CORE-08.

Purpose: The list browse surfaces are two of the three flutter_map holdouts this phase retires. They render N trails on one map fitted to a combined bounds — no single-trail track, no offline branch — so they need a trail-agnostic MapLibre host, not Phase 15's single-trail `WandererMap`.
Output: `SearchMap` (new component) + both list screens rendering trails via `ml.PolylineLayer` + category-icon `ml.WidgetLayer` markers with an imperative camera fit.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-list-map-screens-on-maplibre/16-CONTEXT.md
@.planning/phases/16-list-map-screens-on-maplibre/16-RESEARCH.md
@.planning/phases/16-list-map-screens-on-maplibre/16-PATTERNS.md
@app/lib/components/base/wanderer_map.dart
@app/lib/routes/list_detail_map_screen.dart
@app/lib/routes/list_detail_screen.dart
</context>

## Artifacts this phase produces (Plan 16-01)

New file `app/lib/components/base/search_map.dart`:
- `SearchMap` — `ConsumerStatefulWidget` host. Fields: `initCenter` (`ml.Geographic?`), `initZoom` (`double?`), `disabled` (`bool`, default `false`), `onMapCreated` (`void Function(ml.MapController)?`), `onStyleLoaded` (`void Function(ml.StyleController)?`), `onMapEvent` (`void Function(ml.MapEvent)?`), `layers` (`List<Widget>?`), `children` (`List<Widget>?`).

Modified `app/lib/routes/list_detail_map_screen.dart`:
- `_ListDetailMapScreenState._controller` (`ml.MapController?`), `_onMarkerTap(Trail)`, retained `_combinedBounds(List<Trail>)` returning `ml.LngLatBounds?`.

Modified `app/lib/routes/list_detail_screen.dart`:
- `_ListMap` widget rebuilt on `SearchMap`; retained `_combinedBounds(List<Trail>)`.

<tasks>

<task type="auto">
  <name>Task 1: Create the lightweight SearchMap host</name>
  <files>app/lib/components/base/search_map.dart</files>
  <read_first>
    - app/lib/components/base/wanderer_map.dart (the file to strip down — style-load + live-swap skeleton only)
    - app/lib/provider/map_style_json_provider.dart (the style source SearchMap watches)
    - .planning/phases/16-list-map-screens-on-maplibre/16-PATTERNS.md (section "app/lib/components/base/search_map.dart" — lines 20-89: which parts to keep verbatim and which to drop)
    - .planning/phases/16-list-map-screens-on-maplibre/16-RESEARCH.md (Summary + Recommended Project Structure: why a new thin host, not WandererMap)
    - .planning/phases/16-list-map-screens-on-maplibre/16-CONTEXT.md (D-05's "no Trail param" discretion note)
  </read_first>
  <action>
    Create `app/lib/components/base/search_map.dart` as `SearchMap`, a `ConsumerStatefulWidget`, by taking ONLY the style-loading + live-theme-swap skeleton from `wanderer_map.dart` (PATTERNS.md lines 26-89) and dropping everything trail/offline-specific.

    Imports: `package:flutter/material.dart`, `package:flutter_riverpod/flutter_riverpod.dart`, `package:maplibre/maplibre.dart` as `ml`, `package:wanderer/provider/map_style_json_provider.dart`. Do NOT import `offline_style_rewriter`, `glyph_sprite_cache_provider`, `GlyphSpriteCachePaths`, `LocationMarkerPosition`, or `dart:convert` — all offline/trail-only.

    Widget fields (all optional, no `Trail`): `initCenter` (`ml.Geographic?`), `initZoom` (`double?`), `disabled` (`bool`, default `false`), `onMapCreated` (`void Function(ml.MapController)?`), `onStyleLoaded` (`void Function(ml.StyleController)?`), `onMapEvent` (`void Function(ml.MapEvent)?`), `layers` (`List<Widget>?`), `children` (`List<Widget>?`).

    State: keep `ml.MapController? _controller` and `String? _lastStyleJson` (the cache that prevents a loading-state remount on theme toggle — CORE-02 rationale in wanderer_map). In `build`, `ref.listen(mapStyleJsonProvider, (_, _) => _swapStyle())`, then resolve `ref.watch(mapStyleJsonProvider)`: if `baseJson == null` return the error text (when `error != null`) or a `ColoredBox(color: Theme.of(context).colorScheme.surface)` placeholder, exactly the loading/error passthrough at wanderer_map lines 104-131 (there is NO offline `_composeStyle` branch here — `baseJson` IS the style).

    `_swapStyle()`: read `mapStyleJsonProvider.value`; if non-null and `!= _lastStyleJson`, set `_lastStyleJson` and call `controller.setStyle(json)` (verbatim from wanderer_map lines 160-172).

    Build the `ml.MapLibreMap` per PATTERNS.md lines 67-86: `initStyle: styleJson`, `initCenter: widget.initCenter ?? const ml.Geographic(lat: 0, lon: 0)`, `initZoom: widget.initZoom ?? 3`, `gestures: widget.disabled ? const ml.MapGestures.none() : const ml.MapGestures.all()`, `androidForegroundLoadColor: Theme.of(context).colorScheme.surface`. `onMapCreated`: capture `_controller = controller` AND forward `widget.onMapCreated?.call(controller)`. `onStyleLoaded`: forward `widget.onStyleLoaded?.call(style)`. `onEvent`: forward `widget.onMapEvent?.call(event)`. `layers: widget.layers ?? const []`. `children: widget.children ?? const [ml.MapScalebar(), ml.SourceAttribution()]`.

    Do NOT add: `_composeStyle`, `_cacheWarmed`/glyph-sprite warm-up, `_buildElevationMarker`, `_buildLocationLayer`, `_fitInitialCamera`, or any `trail`/`offline`/`showTrail`/`selectedWaypoint` field. Bounds/camera work stays imperative in each caller's `onStyleLoaded` (see Tasks 2-3), so `SearchMap` itself is bounds-agnostic.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/components/base/search_map.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/components/base/search_map.dart` prints "No issues found!"
    - `grep -c "offline_style_rewriter\|glyph_sprite_cache_provider\|class Trail\| trail\b" app/lib/components/base/search_map.dart` returns 0 (no trail/offline surface)
    - `grep -q "controller.setStyle" app/lib/components/base/search_map.dart` succeeds (live theme swap wired)
    - `grep -q "widget.layers ?? const \[\]" app/lib/components/base/search_map.dart` succeeds (caller-supplied layers seam)
    - `SearchMap` exposes `onStyleLoaded`, `onMapEvent`, `onMapCreated`, `layers`, `children`, `disabled` fields
  </acceptance_criteria>
  <done>`SearchMap` renders the basemap from `mapStyleJsonProvider`, swaps light/dark styles live, and exposes layers/children/callback seams with no Trail or offline dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Port list_detail_map_screen to SearchMap (full list map, interactive)</name>
  <files>app/lib/routes/list_detail_map_screen.dart</files>
  <read_first>
    - app/lib/routes/list_detail_map_screen.dart (the file being ported in place — read in full)
    - app/lib/components/base/search_map.dart (the host created in Task 1)
    - app/lib/components/base/wanderer_map.dart (lines 239-264: `_fitInitialCamera` — the imperative bounds-fit pattern to mirror)
    - app/lib/components/map/trail_layer.dart (lines 145-237: `TrailMarkerLayer` — the GestureDetector-wrapped `ml.Marker` shape for category-icon markers)
    - app/lib/util/polyline_util.dart (polyline decode helper) and app/lib/util/category_icon_util.dart (`trailCategoryIcon`)
    - .planning/phases/16-list-map-screens-on-maplibre/16-PATTERNS.md (section "list_detail_map_screen.dart" — lines 313-364, plus Shared Patterns lines 401-424)
    - .planning/phases/16-list-map-screens-on-maplibre/16-RESEARCH.md (Pattern 3 lines 266-284; "Don't Hand-Roll" `ml.PolylineLayer` row line 297; Pitfall 3 lines 318-322)
  </read_first>
  <action>
    Replace the `FlutterMap` host and `AnimatedMapController` in `list_detail_map_screen.dart` with the new `SearchMap`. Remove the `flutter_map`, `flutter_map_animations`, `vector_map_tiles`, and `map_style_provider` imports; remove `map_coordinate_adapter` if now unused (verify with a build). Replace the `AnimatedMapController _animatedMapController` field with `ml.MapController? _controller` captured via `SearchMap.onMapCreated`.

    Keep `_combinedBounds(List<Trail>)` unchanged — it already returns `ml.LngLatBounds?` and is pure Dart math (PATTERNS.md lines 318-333).

    Imperative fit on load: in `SearchMap.onStyleLoaded`, call `_controller?.fitBounds(bounds: combinedBounds, padding: const EdgeInsets.all(40), nativeDuration: Duration.zero)` when `combinedBounds != null` (RESEARCH Pattern 3 / PATTERNS lines 336-347). There is no declarative bounds-fit in `MapOptions` (RESEARCH Pitfall 3), so this MUST be imperative in `onStyleLoaded`, exactly like `WandererMap._fitInitialCamera`.

    Render trails as polylines via `ml.PolylineLayer(polylines: <List<Feature<LineString>>>)` passed to `SearchMap.layers` — decode each trail's polyline with the existing `polyline_util` helper and build `Feature<LineString>` (geobase, already re-exported by `maplibre.dart`) from the decoded `Geographic` points. Do NOT hand-roll a GeoJSON source/layer — `ml.PolylineLayer` ships in the installed package and matches today's uniform-color `PolylineLayer` (RESEARCH "Don't Hand-Roll").

    Render markers as an `ml.WidgetLayer` passed to `SearchMap.children`: one `ml.Marker` per trail at `ml.Geographic(lat: trail.lat, lon: trail.lon)`, `size: const Size(36, 36)`, child a `GestureDetector(onTap: () => _onMarkerTap(trail))` wrapping the same `Container`/`trailCategoryIcon` decoration used at current lines ~100-146 (Shared Pattern lines 414-424) — category resolved via `firstWhereOrNull` from the category/subcategory providers exactly as today.

    Port `_onMarkerTap(Trail trail)` (PATTERNS lines 352-359): set `_selectedTrail = trail`, then `_controller?.fitBounds(bounds: ml.LngLatBounds(longitudeEast: trail.maxLon, longitudeWest: trail.minLon, latitudeNorth: trail.maxLat, latitudeSouth: trail.minLat), padding: const EdgeInsets.fromLTRB(40, 56, 40, 120), nativeDuration: <~750ms>)` — confirm the animated-duration param name against the installed `ml.MapController.fitBounds` signature. Port the existing deselect logic to reset `_selectedTrail`.

    Host widget is `SearchMap` (this screen shows N trails, no single trail), NOT `WandererMap`.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/list_detail_map_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/routes/list_detail_map_screen.dart` prints "No issues found!"
    - `grep -c "package:flutter_map/\|flutter_map_animations\|vector_map_tiles" app/lib/routes/list_detail_map_screen.dart` returns 0
    - `grep -q "SearchMap(" app/lib/routes/list_detail_map_screen.dart` succeeds
    - `grep -q "ml.PolylineLayer" app/lib/routes/list_detail_map_screen.dart` succeeds
    - `grep -q "fitBounds" app/lib/routes/list_detail_map_screen.dart` succeeds (imperative camera fit present)
    - `grep -q "trailCategoryIcon" app/lib/routes/list_detail_map_screen.dart` succeeds (category-icon markers retained)
  </acceptance_criteria>
  <done>The full list map opens fitted to every trail in the list, draws each trail as a polyline with a tappable category-icon marker, and tapping a marker fits the camera to that trail — all on `MapLibreMap`.</done>
</task>

<task type="auto">
  <name>Task 3: Port list_detail_screen's inline _ListMap to SearchMap (non-interactive)</name>
  <files>app/lib/routes/list_detail_screen.dart</files>
  <read_first>
    - app/lib/routes/list_detail_screen.dart (the `_ListMap` widget, lines ~320-436 — read in full)
    - app/lib/routes/list_detail_map_screen.dart (the sibling port from Task 2 — same rendering, minus interactivity)
    - app/lib/components/base/search_map.dart (host created in Task 1)
    - .planning/phases/16-list-map-screens-on-maplibre/16-PATTERNS.md (section "list_detail_screen.dart" lines 368-386)
    - .planning/phases/16-list-map-screens-on-maplibre/16-RESEARCH.md (Open Question 2 lines 415-418: preserve today's non-interactive/tap-through split exactly)
  </read_first>
  <action>
    In `_ListMap`, replace the `FlutterMap` (`data: (style) => FlutterMap(...)`) with `SearchMap(disabled: true, ...)` — `disabled: true` maps to `ml.MapGestures.none()`, matching today's `InteractionOptions(flags: InteractiveFlag.none)`. Since `SearchMap` resolves the style itself, drop the `map_style_provider` `AsyncLoader`/`data:` wrapper for this widget.

    Tap-through navigation (preserve today's behavior exactly, RESEARCH Open Question 2): `SearchMap.onMapEvent: (event) { if (event is ml.MapEventClick) context.push('/list/${list.id}/map'); }` (PATTERNS lines 374-382).

    Imperative bounds fit on load: identical `onStyleLoaded` → `_controller?.fitBounds(bounds: _combinedBounds(trails), padding: const EdgeInsets.all(40), nativeDuration: Duration.zero)` pattern as Task 2. Capture `ml.MapController? _controller` via `onMapCreated`.

    Keep `_combinedBounds(List<Trail>)` unchanged (PATTERNS lines 372, identical to the sibling screen's helper).

    Render trails as `ml.PolylineLayer` (via `polyline_util` → `Feature<LineString>`) in `SearchMap.layers` and category-icon markers as an `ml.WidgetLayer` in `SearchMap.children` — but the markers are NON-tappable here (NO `GestureDetector` wrapper), matching today's plain `MarkerLayer` markers (PATTERNS lines 382 confirms no per-marker tap in the current `_ListMap`).

    Remove `flutter_map`/`map_style_provider` imports from `list_detail_screen.dart` only if `_ListMap` was their sole user — verify the rest of the file does not still reference them before deleting the imports.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/list_detail_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/routes/list_detail_screen.dart` prints "No issues found!"
    - `grep -q "SearchMap(" app/lib/routes/list_detail_screen.dart` succeeds and `grep -q "disabled: true" app/lib/routes/list_detail_screen.dart` succeeds
    - `grep -q "context.push('/list/\${list.id}/map')" app/lib/routes/list_detail_screen.dart` succeeds (tap-through preserved)
    - `grep -q "FlutterMap" app/lib/routes/list_detail_screen.dart` fails inside `_ListMap` (no flutter_map host remains for the inline map)
  </acceptance_criteria>
  <done>The inline list-detail map renders all trails on a non-interactive `MapLibreMap` fitted to the combined bounds, and tapping anywhere navigates to the full list map — preserving today's exact behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server API → client render | `list_provider` returns already-authenticated, already-expanded list/trail data; it crosses into the MapLibre render pipeline. No new client→server input surface in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-AC | Information Disclosure | list rendering | accept | Renders only data the existing authenticated `listProvider` already returns; no new fetch, no privacy-relevant field added. Web/app already gate list visibility server-side. |
| T-16-01-DEC | Tampering | trail polyline decode | mitigate | Decode via the existing `polyline_util` helper (already handles malformed/empty polylines); a trail with no bounds is filtered out by `_combinedBounds`'s `withBounds` guard before any fit. |
| T-16-SC | Tampering | pub/npm installs | accept | No new packages added this phase (RESEARCH "Package Legitimacy Audit: Not applicable"). No install task exists; nothing to gate. |
</threat_model>

<verification>
- `cd app && flutter analyze` reports no issues across the three files.
- Manual (folded into 16-03's end-of-phase checkpoint): open a list → inline map shows all trails and taps through to the full map; the full list map fits all trails and a marker tap fits that trail.
</verification>

<success_criteria>
- `SearchMap` exists as a trail-agnostic MapLibre host with no offline/trail surface.
- `list_detail_map_screen` and `list_detail_screen`'s `_ListMap` both render their trails on `MapLibreMap`, fitted to the combined bounds, with category-icon markers.
- No `flutter_map`/`flutter_map_animations`/`vector_map_tiles` import remains in either list screen.
- `flutter analyze` is clean for all three files (CORE-08).
</success_criteria>

<output>
Create `.planning/phases/16-list-map-screens-on-maplibre/16-01-SUMMARY.md` when done.
</output>
