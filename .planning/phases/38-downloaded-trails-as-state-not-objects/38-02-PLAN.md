---
phase: 38-downloaded-trails-as-state-not-objects
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/i18n/app_en.arb
  - app/lib/i18n/untranslated_messages.json
  - app/lib/i18n/app_localizations.dart
  - app/lib/i18n/app_localizations_en.dart
  - .planning/todos/pending/2026-08-03-destructive-action-strings-untranslated.md
autonomous: true
requirements: [DL-01, DL-02]

must_haves:
  truths:
    - "A generated AppLocalizations getter named remove_download_confirm_body exists and returns the un-download confirm copy"
    - "A generated AppLocalizations getter named edit_needs_connection exists and returns the edit-refusal copy"
    - "Neither new string claims the action cannot be undone"
    - "The two new keys are listed for all 13 non-English locales in untranslated_messages.json"
    - "The pending translation todo names both new keys"
  artifacts:
    - path: "app/lib/i18n/app_en.arb"
      provides: "Source copy for the two new keys"
      contains: "remove_download_confirm_body"
    - path: "app/lib/i18n/app_localizations.dart"
      provides: "Generated getters"
      contains: "get edit_needs_connection"
    - path: "app/lib/i18n/untranslated_messages.json"
      provides: "The committed English-only backlog, regenerated"
      contains: "remove_download_confirm_body"
  key_links:
    - from: "app/lib/i18n/app_en.arb"
      to: "app/lib/i18n/app_localizations_en.dart"
      via: "flutter gen-l10n"
      pattern: "remove_download_confirm_body"
    - from: ".planning/todos/pending/2026-08-03-destructive-action-strings-untranslated.md"
      to: "app/lib/i18n/untranslated_messages.json"
      via: "backlog entry per new key"
      pattern: "edit_needs_connection"
---

<objective>
Mint the two — and only two — new user-facing strings this phase needs, and register them in the
pending human-translation backlog.

Purpose: D-06 is explicit that already-translated keys are reused rather than re-minted, so the
Remove-download menu label reuses `remove` and the offline-status text reuses `available_offline`
(both present in all 14 locales, both absent from `untranslated_messages.json`). Only two strings
genuinely have no existing equivalent: the un-download confirm body (D-04) and the edit-refusal
copy (D-17). Both ship English-only in 13 locales exactly as their sibling `delete_needs_connection`
already does — machine translation is explicitly rejected for destructive-action copy — so both
must be added to the pending todo's backlog.

D-04 is emphatic on content: the body names what is removed and states the recovery, and
deliberately makes NO "cannot be undone" claim — that claim is false for an un-download, which is
exactly what the comment at `trail_dropdown.dart:239-244` flags about the current reuse of
`delete_trail_confirm`.

Output: two new ARB keys, regenerated l10n, a grown `untranslated_messages.json`, and an updated
todo.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-downloaded-trails-as-state-not-objects/38-CONTEXT.md
@.planning/phases/38-downloaded-trails-as-state-not-objects/38-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two new keys to app_en.arb and regenerate l10n</name>
  <files>app/lib/i18n/app_en.arb, app/lib/i18n/untranslated_messages.json, app/lib/i18n/app_localizations.dart, app/lib/i18n/app_localizations_en.dart</files>
  <read_first>
    - app/lib/i18n/app_en.arb lines 60-95 (the delete/download/edit block, and `delete_needs_connection` at :71 which D-17 models the new refusal on)
    - app/lib/i18n/app_en.arb lines 355-375 (`remove` at :361, `offline` at :370, `available_offline` at :371 — the keys D-06 reuses rather than re-mints)
    - app/lib/i18n/app_en.arb lines 445-460 (`regions_delete_confirm_title` / `_body` / `_action`, the shape D-04 mirrors)
    - app/l10n.yaml (the `untranslated-messages-file` setting and its stated regeneration workflow)
  </read_first>
  <action>
Add exactly two keys to `app/lib/i18n/app_en.arb`. Neither takes a placeholder, so neither needs an
`@key` metadata block.

Key 1, inserted immediately after the existing `remove` entry (currently line 361). Key name:
remove_download_confirm_body. English value, verbatim:
This removes the downloaded copy from this device. The trail itself is not deleted — you'll need to download it again to use it offline.

Key 2, inserted immediately after the existing `edit` entry (currently line 81). Key name:
edit_needs_connection. English value, verbatim:
Editing works on the server copy of this trail. Connect to the internet to edit it.

Constraints on the copy, all load-bearing:
- Key 1 must NOT contain the phrase "cannot be undone" or any equivalent permanence claim (D-04).
  An un-download is genuinely undoable; the false claim is the specific defect being corrected.
- Key 1 mirrors `regions_delete_confirm_body`'s structure: what is removed, then the recovery.
- Key 2 mirrors `delete_needs_connection`'s structure: state the fact about the server copy, then
  the action the hiker can take.
- Both use a normal apostrophe in "you'll". That is safe because neither key carries an ICU
  placeholder, exactly like `regions_delete_confirm_body` which already contains "You'll".
- Do NOT add these keys to any of the 13 non-English ARB files. English-only in 13 locales is the
  accepted, deliberate outcome (D-17), identical to `delete_needs_connection` today.

Do not touch `remove` or `available_offline` — D-06 reuses both unchanged.

Then run `flutter gen-l10n` from the `app/` directory. It regenerates `app_localizations*.dart` and
rewrites `lib/i18n/untranslated_messages.json`. Never hand-edit `untranslated_messages.json`; the
whole point of committing it is that gen-l10n grows it and the growth shows in the diff.
  </action>
  <verify>
    <automated>cd app && flutter gen-l10n && grep -q "get remove_download_confirm_body" lib/i18n/app_localizations.dart && grep -q "get edit_needs_connection" lib/i18n/app_localizations.dart && test $(grep -c remove_download_confirm_body lib/i18n/untranslated_messages.json) -eq 13</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/i18n/app_en.arb` contains the key `remove_download_confirm_body` with the exact value `This removes the downloaded copy from this device. The trail itself is not deleted — you'll need to download it again to use it offline.`
    - `app/lib/i18n/app_en.arb` contains the key `edit_needs_connection` with the exact value `Editing works on the server copy of this trail. Connect to the internet to edit it.`
    - `grep -c "cannot be undone" app/lib/i18n/app_en.arb` outputs 1 (only the pre-existing `delete_trail_confirm`)
    - `app/lib/i18n/app_localizations.dart` contains `get remove_download_confirm_body` and `get edit_needs_connection`
    - `grep -c remove_download_confirm_body app/lib/i18n/untranslated_messages.json` outputs 13
    - `grep -c edit_needs_connection app/lib/i18n/untranslated_messages.json` outputs 13
    - `grep -rc remove_download_confirm_body app/lib/i18n/app_de.arb` outputs 0
  </acceptance_criteria>
  <done>Both keys exist in English, both generated getters exist, and both are listed as untranslated for all 13 non-English locales.</done>
</task>

<task type="auto">
  <name>Task 2: Register both keys in the pending human-translation backlog</name>
  <files>.planning/todos/pending/2026-08-03-destructive-action-strings-untranslated.md</files>
  <read_first>
    - .planning/todos/pending/2026-08-03-destructive-action-strings-untranslated.md (whole file — the priority-ordered key list, the count claims in the "Locales affected" section, and the Verification section)
  </read_first>
  <action>
Add both new keys to the todo's priority-ordered list so the backlog stays the live work list this
phase's CONTEXT (canonical_refs) requires it to be.

Place `remove_download_confirm_body` in "Priority 2 — destructive/blocking flow copy", since it is
the confirm body of a destructive action, and note inline that it deliberately makes no
irreversibility claim (an un-download is recoverable) so a translator does not "helpfully" add one.
Place `edit_needs_connection` alongside `trail_uploaded_reopen_to_edit`, which it is the sibling of
— both are edit-path refusals. Renumber the list so the ordinals stay contiguous.

Update the two count claims that go stale: the sentence in "Locales affected" stating how many keys
each locale is missing, and the total implied by the key list. Add one line under the existing
"Regenerated from ..." note recording that Phase 38 added these two keys and why they ship
English-only (D-17: machine translation is rejected for destructive-action copy).

Leave the Problem, Fix and Verification sections' reasoning intact — the Verification section's
"the file is empty ({})" full-closure criterion still holds, it just now covers two more keys.
  </action>
  <verify>
    <automated>grep -q remove_download_confirm_body .planning/todos/pending/2026-08-03-destructive-action-strings-untranslated.md && grep -q edit_needs_connection .planning/todos/pending/2026-08-03-destructive-action-strings-untranslated.md</automated>
  </verify>
  <acceptance_criteria>
    - The todo file contains the literal string `remove_download_confirm_body` inside the Priority 2 list
    - The todo file contains the literal string `edit_needs_connection`
    - The todo's numbered key list has contiguous ordinals with no duplicates
    - The todo file records that Phase 38 added the two keys and that they are English-only by decision, not by oversight
  </acceptance_criteria>
  <done>Both new destructive-action strings are tracked in the backlog that exists to stop exactly this class of string from being silently forgotten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy shown on a destructive action → hiker's decision | The dialog text is the only thing standing between "free some space" and "lose data" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Spoofing (misleading UI) | `remove_download_confirm_body` | mitigate | The body states the trail itself is not deleted and names the recovery; the acceptance criteria assert the absence of any "cannot be undone" claim, which would misrepresent a reversible action |
| T-38-02-02 | Repudiation | 13 locales showing English destructive copy | accept | Deliberate: machine translation of irreversible-action copy is rejected outright (see the pending todo); both keys are registered in the human-translation backlog so the gap is tracked rather than silent |
| T-38-02-SC | Tampering | package installs | accept | No package-manager install occurs in this plan; `pubspec.yaml` is untouched |
</threat_model>

<verification>
- `cd app && flutter gen-l10n` succeeds with no ICU syntax errors
- `cd app && flutter analyze --no-pub lib` shows no new errors or warnings
- Both keys appear for all 13 non-English locales in `untranslated_messages.json`
- Both keys appear in the pending translation todo
</verification>

<success_criteria>
- Exactly two new ARB keys exist; `remove` and `available_offline` are untouched and still absent
  from `untranslated_messages.json`
- Neither new string makes a permanence claim
- The English-only backlog grew by exactly these two keys and the growth is visible in the diff
</success_criteria>

## Artifacts this phase produces

**Created by this plan:**
- l10n key + generated getter `remove_download_confirm_body` (`app/lib/i18n/app_en.arb`, `app/lib/i18n/app_localizations*.dart`)
- l10n key + generated getter `edit_needs_connection` (same files)
- 26 new entries in `app/lib/i18n/untranslated_messages.json` (2 keys x 13 locales)

**Removed by this plan:** none.

**Explicitly NOT created (reused instead, per D-06):** `remove`, `available_offline`,
`regions_update_action`, `cancel` — all already translated in all 14 locales.

<output>
Create `.planning/phases/38-downloaded-trails-as-state-not-objects/38-02-SUMMARY.md` when done
</output>
