---
phase: 21.5-region-catalog-archive-pre-build-backend
plan: 01
subsystem: database
tags: [go, pocketbase, region-catalog, path-safety, migration]

# Dependency graph
requires: []
provides:
  - "regions.Region struct — shared id/name/bbox type for downstream plans"
  - "regions.LoadRegionCatalog() — tolerant JSON config loader (BACK-01)"
  - "regions.ValidateRegion / regions.IsValidRegionID — path-traversal-safe id allow-list"
  - "regions.RegionArchivePath / regions.RegionDemPath — validated archive path builders"
  - "region_archives PocketBase collection — per-region build/status persistence (D-08 status set, D-10 built-date, DEM-independent fields)"
affects: [21.5-02-PLAN.md, 21.5-03-PLAN.md, phase-22-region-package-data-model]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Config loader mirrors pluginsystem.LoadLocalPlugin's os.ReadFile+json.Unmarshal shape, but treats missing env var / missing file / empty file as an empty catalog, never an error (D-05)"
    - "Region id allow-list regex (^[a-z0-9][a-z0-9_-]*$) mirrors pluginsystem's pluginIDPattern exactly — validated before any filepath.Join to prevent path traversal"
    - "PocketBase collection migration mirrors tile_cells' m.Register(up,down) shape with all five *Rule keys null (superuser-only, no public collection API)"

key-files:
  created:
    - db/services/regions/config.go
    - db/services/regions/config_test.go
    - db/migrations/1784658610_created_region_archives.go
  modified: []

key-decisions:
  - "Bbox stored as [4]float64 in [minLon, minLat, maxLon, maxLat] order, matching generator.go's %f,%f,%f,%f argument order and the pmtiles extract --bbox= convention — documented in a doc comment since Phase 22's client manifest depends on this exact order"
  - "status/dem_status select values are building/ready/error (not pending) per D-08, with dem_status independently nullable so a vector-ready region isn't blocked by DEM status"
  - "Collection id pbc_1784658610 (derived from the migration's own unix-ms timestamp) verified unique against every existing migration file"

patterns-established:
  - "Region catalog config loading: env-var-gated path, os.IsNotExist and zero-byte tolerance, per-entry validation with skip-and-log rather than fail-fast"
  - "Region id used in any filesystem path must first pass IsValidRegionID — this is the sanctioned single point of truth for path-traversal defense in this new package"

requirements-completed: [BACK-01]

# Metrics
duration: 4min
completed: 2026-07-21
---

# Phase 21.5 Plan 01: Region Catalog Config Loader & Archive Schema Summary

**JSON region-catalog loader (tolerant of missing/empty config) plus path-traversal-safe id validation and the `region_archives` PocketBase collection tracking per-region vector/DEM build state.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-07-21T18:57:13Z
- **Completed:** 2026-07-21T19:00:35Z
- **Tasks:** 2 completed
- **Files modified:** 3 (all new)

## Accomplishments
- `regions.LoadRegionCatalog()` reads `REGION_CATALOG_CONFIG_PATH`, tolerating unset env var, missing file, and empty file as an empty catalog (never an error) — foundation for Success Criterion 1
- `regions.ValidateRegion` / `regions.IsValidRegionID` enforce a strict `^[a-z0-9][a-z0-9_-]*$` allow-list on region ids before they can ever reach `filepath.Join`, closing the path-traversal class flagged in RESEARCH.md Pitfall 7 / threat T-21.5-01
- `region_archives` PocketBase collection created (migration `1784658610_created_region_archives.go`), mirroring `tile_cells`' superuser-only lockdown, with `status`/`dem_status` using the `building/ready/error` set (D-08) and `vector_built_date` for D-10's staleness comparison

## Task Commits

Each task followed a full TDD RED→GREEN cycle:

1. **Task 1: Region config loader + path-safety validation (BACK-01)**
   - `366e4b2f` (test) — failing tests for loader tolerance, id validation, bbox range validation, path-traversal rejection
   - `1ca66555` (feat) — `LoadRegionCatalog`, `ValidateRegion`, `IsValidRegionID`, `RegionArchivePath`, `RegionDemPath` implementation; all tests pass
2. **Task 2: region_archives collection migration** - `45fa4bee` (feat)

**Plan metadata:** (this commit, docs)

## Files Created/Modified
- `db/services/regions/config.go` - `Region` struct, `LoadRegionCatalog`, `ValidateRegion`, `IsValidRegionID`, `RegionArchivePath`, `RegionDemPath`, `RegionCacheDir` const, `regionIDPattern` regex
- `db/services/regions/config_test.go` - table tests for every loader-tolerance/validation/path-traversal case in the plan's behavior block
- `db/migrations/1784658610_created_region_archives.go` - new `region_archives` collection: `region_id`, `name`, bbox fields, `status`/`dem_status` selects, `vector_built_date`, size fields, error-message fields, `created`/`updated`, superuser-only rules, indexed on `region_id`

## Decisions Made
- Followed the plan's TDD instruction literally: wrote the test file against a temporarily-removed implementation to confirm a genuine compile-failure RED state, then restored the implementation for GREEN — both committed separately (`366e4b2f` test, `1ca66555` feat) per the plan's `tdd="true"` task attribute
- No deviations from the plan's specified field set, regex, or path shapes — all acceptance criteria greps (region_archives, building, vector_built_date, dem_status, unique pbc_1784658610 id) verified before commit

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required. (Note: the Docker-volume mount + `REGION_CATALOG_CONFIG_PATH` env var wiring flagged in RESEARCH.md Pitfall 6 is infrastructure config for a later plan in this phase, not this plan's scope — this plan only implements the Go-side loader contract.)

## Next Phase Readiness
- `regions.Region`, `LoadRegionCatalog`, `IsValidRegionID`, `RegionArchivePath`, `RegionDemPath` and the `region_archives` collection are ready for Plan 02 (builder/cron) and Plan 03 (API endpoint) to consume
- No blockers — `cd db && go build ./...`, `go test ./services/regions/...`, and `go vet ./services/regions/... ./migrations/...` all pass clean

---
*Phase: 21.5-region-catalog-archive-pre-build-backend*
*Completed: 2026-07-21*

## Self-Check: PASSED

- FOUND: db/services/regions/config.go
- FOUND: db/services/regions/config_test.go
- FOUND: db/migrations/1784658610_created_region_archives.go
- FOUND commit: 366e4b2f
- FOUND commit: 1ca66555
- FOUND commit: 45fa4bee
