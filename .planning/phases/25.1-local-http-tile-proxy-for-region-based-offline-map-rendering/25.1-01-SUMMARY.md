---
phase: 25.1-local-http-tile-proxy-for-region-based-offline-map-rendering
plan: 01
subsystem: infra
tags: [android, ios, network-security-config, ats, cleartext, maplibre]

# Dependency graph
requires:
  - phase: 25
    provides: "trail_map.dart/navigation_screen.dart incremental region-composition pipeline (RENDER-01/02/03), the piece this phase's loopback tile proxy will eventually replace"
provides:
  - "127.0.0.1-scoped Android cleartext exception (network_security_config.xml + AndroidManifest.xml wiring)"
  - "127.0.0.1/localhost-scoped iOS ATS loopback exception (Info.plist NSAppTransportSecurity)"
affects: [25.1-02, 25.1-03, 25.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Platform network exceptions scoped to the exact literal (127.0.0.1 / localhost) rather than a blanket cleartext/insecure-loads allowance, with negative greps in the same verification gate proving the narrow scope"

key-files:
  created:
    - app/android/app/src/main/res/xml/network_security_config.xml
  modified:
    - app/android/app/src/main/AndroidManifest.xml
    - app/ios/Runner/Info.plist

key-decisions:
  - "Doc comments explaining the scoping rationale were reworded to avoid containing the literal forbidden strings (usesCleartextTraffic, base-config, NSAllowsArbitraryLoads, NSLocalNetworkUsageDescription) their own negative-grep verification gates check for, so the automated verify commands pass without weakening the comments' intent"

patterns-established:
  - "Loopback-only platform network exceptions: Android <domain-config> scoped to a single <domain>127.0.0.1</domain>, no <base-config>; iOS NSAppTransportSecurity with NSExceptionDomains scoped to 127.0.0.1 and localhost only, no NSAllowsArbitraryLoads"

requirements-completed: [PROXY-03]

# Metrics
duration: 4min
completed: 2026-07-23
---

# Phase 25.1 Plan 01: Platform Network Exceptions for Loopback Tile Proxy Summary

**Android network_security_config.xml scoped to 127.0.0.1 + iOS Info.plist NSAppTransportSecurity loopback exception, both narrowly bounded with negative greps proving no blanket cleartext/insecure-loads allowance was introduced**

## Performance

- **Duration:** 4 min
- **Started:** 2026-07-23T15:28:xxZ
- **Completed:** 2026-07-23T15:32:21Z
- **Tasks:** 2 completed
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- Android: new `network_security_config.xml` permits cleartext HTTP only to `127.0.0.1`, wired via `android:networkSecurityConfig` on `<application>`
- iOS: `Info.plist` gained a scoped `NSAppTransportSecurity` exception (`NSAllowsLocalNetworking` + `NSExceptionDomains` for `127.0.0.1`/`localhost`)
- Both platforms verified narrow-scoped by negative grep (no `usesCleartextTraffic`, no `<base-config>`, no `NSAllowsArbitraryLoads`, no `NSLocalNetworkUsageDescription`)

## Task Commits

Each task was committed atomically:

1. **Task 1: Android — 127.0.0.1-scoped cleartext exception** - `02db6c50` (feat)
2. **Task 2: iOS — ATS loopback exception in Info.plist** - `a565d811` (feat)

**Plan metadata:** pending (this commit)

## Files Created/Modified
- `app/android/app/src/main/res/xml/network_security_config.xml` - New: single `<domain-config cleartextTrafficPermitted="true">` scoped to `<domain>127.0.0.1</domain>`
- `app/android/app/src/main/AndroidManifest.xml` - Added `android:networkSecurityConfig="@xml/network_security_config"` to `<application>`; nothing else changed
- `app/ios/Runner/Info.plist` - Added top-level `NSAppTransportSecurity` dict with `NSAllowsLocalNetworking` + `NSExceptionDomains` for `127.0.0.1` and `localhost`, each `NSExceptionAllowsInsecureHTTPLoads = true`

## Decisions Made
- Kept explanatory XML/plist comments but reworded them so they don't contain the literal substrings their own negative-grep verification gates check for (e.g. wrote "app-wide cleartext allowance" instead of literal `android:usesCleartextTraffic`, "app-wide insecure-loads allowance" instead of literal `NSAllowsArbitraryLoads`). This was discovered mid-execution: the first draft of both files failed their own `verify` block because the doc comments explaining *why something was NOT added* ironically contained the forbidden string. Not a plan deviation — same intent, adjusted phrasing.

## Deviations from Plan

None - plan executed exactly as written. (The comment-wording adjustment above is not a scope/behavior deviation — it does not change what functionality was added, only how the doc comments phrase the rationale.)

## Issues Encountered
- Both `verify` blocks initially failed because the doc comments added for context (per this codebase's doc-comment doctrine of naming the bug class prevented) contained the literal strings the same block's negative greps check for (`base-config`, `NSAllowsArbitraryLoads`, `NSLocalNetworkUsageDescription`). Fixed by rewording the comments to describe the same rationale without using the literal disallowed identifiers. Re-ran both verify blocks after the fix — both passed.

## User Setup Required

None - no external service configuration required. These are static manifest/plist changes; no build or device testing was performed as part of this plan (that's Plan 03's on-device spike).

## Next Phase Readiness
- Plan 03 (on-device spike validating the loopback tile proxy over `http://127.0.0.1:<port>`) can now proceed without failing on a platform cleartext/ATS block — this plan's exceptions are the confirmed prerequisite.
- Plan 04 (real proxy rewiring in `trail_map.dart`/`navigation_screen.dart`) is unblocked on the network-policy side.
- No Dart files were touched; no packages installed; `git diff` for this plan touches exactly the three manifest/config files specified in `files_modified`.

---
*Phase: 25.1-local-http-tile-proxy-for-region-based-offline-map-rendering*
*Completed: 2026-07-23*

## Self-Check: PASSED

- FOUND: app/android/app/src/main/res/xml/network_security_config.xml
- FOUND: app/android/app/src/main/AndroidManifest.xml
- FOUND: app/ios/Runner/Info.plist
- FOUND commit: 02db6c50
- FOUND commit: a565d811
