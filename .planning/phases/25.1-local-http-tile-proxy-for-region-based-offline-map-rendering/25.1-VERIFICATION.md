---
phase: 25.1-local-http-tile-proxy-for-region-based-offline-map-rendering
verified: 2026-07-24T00:00:00Z
status: verified
score: 14/14 must-haves verified (T14 confirmed on-device Android after commit 9e164aab)
overrides_applied: 0
on_device_result: "PASS on Android — offline basemap, hillshade, glyphs, and sprite icons render through the loopback proxy on both TrailMap and navigation_screen. Required a post-UAT fix (commit 9e164aab): MapLibre.setConnected(true) to defeat MapLibre Native's airplane-mode connectivity gate, plus a network-free offline style path and the sprite @2x.json cache fix. iOS unverified — same connectivity gate applies and needs a package fork (see deferred-items.md)."
human_verification:
  - test: "On a physical device, open a trail with a downloaded region and view the trail detail map (TrailMap) offline (Wi-Fi + cellular off, then full airplane mode). Confirm basemap tiles (and hillshade, if the region's DEM is downloaded) render through the loopback proxy on the ACTUAL production screen — not the 25.1-03 spike harness."
    expected: "Basemap (and hillshade) renders offline exactly as it did through the harness in 25.1-03-SUMMARY.md, confirming main.dart's TileProxyServer startup + TrailMap's rewired _composeStyle work end-to-end on real hardware, not just in code review/flutter analyze."
    why_human: "The 25.1-03 spike validated the proxy mechanism (TileProxyServer, rewriteStyleForProxy, resolveRegionForTile) through a standalone throwaway harness with its own bootstrap. Plan 04 wired those same pieces into the real main.dart/TrailMap, but that specific wiring (openStore -> TileProxyServer.start -> ProviderScope override -> TrailMap._composeStyle) has only been checked via flutter analyze, grep, and unit tests — never run on a physical device. Rendering behavior cannot be confirmed by static analysis."
  - test: "On a physical device, start active navigation (navigation_screen) on a trail whose region is downloaded, then engage full airplane mode while panning/following GPS. Confirm the basemap renders without the Phase 25 UAT Test 4 reentrancy symptoms (stalled/blank tiles, crashes) now that _reconcileRegionComposition and the MapEventCameraIdle trigger are deleted."
    expected: "navigation_screen renders offline region tiles reliably during active GPS-follow, with no regression relative to the 25.1-03 spike's in-session airplane-mode PASS result, confirming the deletion of the reconcile machinery does not reintroduce or newly expose a rendering gap on the real screen (which has camera-follow/heading logic and breadcrumb layers the harness does not)."
    why_human: "The spike harness has no GPS-follow, no camera-follow/heading logic, and no breadcrumb layer — it is a simplified proxy of navigation_screen, not navigation_screen itself. The specific bug this phase exists to fix (a reentrancy race tied to navigation_screen's continuous programmatic camera-follow) can only be exercised by running the real screen during active navigation on real hardware."
  - test: "While navigation_screen (or TrailMap) is open and offline-rendering an already-downloaded region, finish downloading a second, previously-uncovered region and pan/fly to it. Confirm the newly-downloaded region's tiles appear without restarting or remounting the screen."
    expected: "Tiles for the newly-covered area appear automatically once the camera reaches them, matching the 25.1-03 spike's test-case-(d) finding ('no screen remount needed') and Plan 04's SUMMARY rationale for deleting navigation_screen's regionListNotifierProvider listener outright (the proxy re-queries RegionEntity fresh per HTTP request, so no Dart-side signal should be structurally required)."
    why_human: "This is architecturally sound per code review (the proxy handler queries `_store.box<RegionEntity>().getAll()` fresh on every request, no caching), but the spike's test case (d) was exercised through the harness's own 'Reload regions' button-driven dropdown, not through navigation_screen's real widget lifecycle with the listener now fully removed. Confirming the no-signal-needed architecture holds on the actual screen requires an on-device check, not a logic trace."
---

# Phase 25.1: Local HTTP Tile Proxy for Region-Based Offline Map Rendering Verification Report

**Phase Goal:** Replace navigation_screen.dart's (and trail_map.dart's, per D-01) incremental addSource/removeSource region-swap reconcile with a loopback HTTP tile proxy — a static XYZ vector/DEM source served by an in-app HttpServer that resolves each tile request against the downloaded region archives — so MapLibre Native's own viewport tracking handles which regions contribute tiles instead of hand-rolled Dart source/layer diffing.
**Verified:** 2026-07-23T17:14:04Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Android grants a cleartext exception scoped to 127.0.0.1 only (PROXY-03 prereq) | VERIFIED | `app/android/app/src/main/res/xml/network_security_config.xml` contains only `<domain-config cleartextTrafficPermitted="true"><domain includeSubdomains="false">127.0.0.1</domain></domain-config>`; `AndroidManifest.xml` `<application>` carries `android:networkSecurityConfig="@xml/network_security_config"`; no `usesCleartextTraffic`/`<base-config>` present |
| 2 | iOS ATS permits loopback HTTP to 127.0.0.1/localhost, no blanket allowance | VERIFIED | `plutil -lint` OK; `NSAppTransportSecurity` dict confirmed via `PlistBuddy`: `NSAllowsLocalNetworking=true`, `NSExceptionDomains` for `127.0.0.1` and `localhost`, each `NSExceptionAllowsInsecureHTTPLoads=true`; zero occurrences of `NSAllowsArbitraryLoads`/`NSLocalNetworkUsageDescription` |
| 3 | No LAN/hotspot host gains a cleartext exception; no local-network prompt introduced | VERIFIED | Negative greps confirmed above (T1/T2) |
| 4 | `resolveRegionForTile` picks smallest-bbox-area overlapping region, ties broken by most-recent download (D-02/D-03) | VERIFIED | Code in `tile_repository_manager.dart:102-138` implements exact area-then-recency sort; 6 dedicated unit tests pass (`resolveRegionForTile smallest-bbox-area...`, `...equal-area...D-03`, `...null downloadedAtUtc sorts as epoch-zero`, `...dem:true/false...`) |
| 5 | A tile inside no downloaded region resolves to HTTP 404, empty body | VERIFIED | `tile_proxy_server.dart:133-136` — `region == null` → `HttpStatus.notFound`, closes response with no body written |
| 6 | Loopback `HttpServer` binds 127.0.0.1 on ephemeral port, serves `/vector/{z}/{x}/{y}.pbf` and `/dem/{z}/{x}/{y}.png` via `.pmtiles` random access | VERIFIED | `HttpServer.bind(InternetAddress.loopbackIPv4, 0)` at line 50; route parsing + `archive.tile(ZXY(...).toTileId())` at lines 81-176; explicit z/x/y bounds check before `ZXY` construction (lines 101-112) |
| 7 | Composed offline style points at static loopback XYZ template, not per-cell `pmtiles://file://` | VERIFIED | `offline_style_rewriter.dart:264-309` `rewriteStyleForProxy` emits exactly one `tiles: ['<baseUrl>/vector/{z}/{x}/{y}.pbf']` and one `.../dem/...` template; 9 dedicated unit tests pass including "no https:// or pmtiles:// survives...exactly one vector and one dem source" |
| 8 | Human can launch harness, observe rendering online/radios-off/airplane mode on real hardware | VERIFIED | `25.1-03-SUMMARY.md` records all 5 test cases PASS on a physical Pixel 6, including case (c) full airplane mode — "tiles kept rendering while panning in airplane mode" |
| 9 | Harness exercises REAL production pieces (not a bespoke test path) | VERIFIED | `tile_proxy_spike_harness.dart` greps confirm `TileProxyServer.start`, `rewriteStyleForProxy`, `tileProxyBaseUrlProvider.overrideWithValue` all present; file lives under `app/test/`, not referenced by `router_provider.dart`; `flutter analyze` clean |
| 10 | Proceed/stop decision is human-gated; failure does not auto-trigger a fallback (D-04) | VERIFIED | `25.1-03-SUMMARY.md` Task 2 is decision-recording only, verdict PROCEED, commit `2f181fab`; D-04 honored (no fallback code exists in the repo) |
| 11 | Proxy starts at launch (after openStore, before runApp); base URL available via `tileProxyBaseUrlProvider` before first `_composeStyle` | VERIFIED | `main.dart:37-38,57` — `TileProxyServer.start(store)` called immediately after `openStore`, `tileProxyBaseUrlProvider.overrideWithValue(proxyServer.baseUrl)` in the same `ProviderScope.overrides` list, both before `runApp` |
| 12 | TrailMap and navigation_screen compose offline style via `rewriteStyleForProxy` against the single static loopback source; no per-viewport tile enumeration | VERIFIED | Both files' `_composeStyle` call `rewriteStyleForProxy(... proxyBaseUrl: ref.read(tileProxyBaseUrlProvider) ...)`; `grep -c 'localTilePathsForBounds\|rewriteStyleForOffline'` = 0 in both files; navigation_screen's `_composeStyle` signature dropped the `viewportBounds` parameter entirely |
| 13 | No `_reconcileRegionComposition`/`_addRegionComposition`/`_sourceFromJson`/`_layerFromJson`/`_seedRegionTracking`/tracking-set code remains; `MapEventCameraIdle` reconcile trigger gone | VERIFIED | `grep -c` for the full machinery-name set returns 0 in both `trail_map.dart` and `navigation_screen.dart`; `grep -c 'MapEventCameraIdle'` returns 0 in `navigation_screen.dart` |
| 14 | A downloaded region's basemap (and hillshade when DEM downloaded) renders offline on both the trail detail map and navigation screen via the proxy | VERIFIED (on-device Android, after commit 9e164aab) | Confirmed on a physical Android device: offline basemap + hillshade (`served dem ... -> 200`), street-name glyphs, and sprite icons render through the loopback proxy on both TrailMap and navigation_screen. The initial on-device run failed (grey basemap) because MapLibre Native suppresses all online-file-source HTTP requests in airplane mode (ConnectivityReceiver reports offline) — not a proxy/style-wiring defect. Fixed via `MapLibre.setConnected(true)` (MainActivity), a network-free offline style path, and the sprite `@2x.json` cache addition. |

**Score:** 14/14 truths VERIFIED — T13 at code level, T14 confirmed on-device (Android) after the connectivity-gate fix. **iOS caveat:** the fix is Android-only; iOS has the same gate and remains unverified pending a maplibre_ios fork (see deferred-items.md).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/android/app/src/main/res/xml/network_security_config.xml` | 127.0.0.1-scoped cleartext exception | VERIFIED | Exists, contains exact expected XML, no blanket allowance |
| `app/android/app/src/main/AndroidManifest.xml` | `android:networkSecurityConfig` on `<application>` | VERIFIED | Attribute present, wired correctly |
| `app/ios/Runner/Info.plist` | `NSAppTransportSecurity` loopback exception | VERIFIED | Plist lints clean, exact expected keys present |
| `app/lib/util/xyz_tile_bounds.dart` | Pure `tileToBounds` z/x/y → bbox | VERIFIED | Exists, pure `dart:math`-only, 3 unit tests pass |
| `app/lib/services/tile_repository_manager.dart` | `resolveRegionForTile` added | VERIFIED | Function present, unchanged siblings (`bboxOverlaps`/`splitRegionTilePaths`) confirmed byte-stable by grep |
| `app/lib/services/tile_proxy_server.dart` | `class TileProxyServer` | VERIFIED | Full implementation: loopback bind, routing, bounded archive LRU cache, error handling |
| `app/lib/provider/region/tile_proxy_provider.dart` | `tileProxyBaseUrlProvider` (keepAlive) | VERIFIED | Matches `objectbox_store_provider.dart`'s exact shape; `.g.dart` generated |
| `app/lib/util/offline_style_rewriter.dart` | `rewriteStyleForProxy` | VERIFIED | Function present, legacy `rewriteStyleForOffline` untouched |
| `app/test/services/tile_proxy_spike_harness.dart` | Standalone on-device PROXY-03 harness | VERIFIED | Exists under `app/test/`, uses real production pieces, not referenced by router |
| `app/lib/main.dart` | `TileProxyServer.start` + provider override | VERIFIED | Wired exactly per plan, before `runApp` |
| `app/lib/components/base/trail_map.dart` | Static-proxy `_composeStyle`; reconcile deleted | VERIFIED | Confirmed via grep + full read of `_composeStyle`/`_swapStyle` |
| `app/lib/routes/navigation_screen.dart` | Static-proxy `_composeStyle`; reconcile + camera-idle trigger deleted | VERIFIED | Confirmed via grep + full read; breadcrumb/trail-layer logic intact |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `AndroidManifest.xml` | `network_security_config.xml` | `android:networkSecurityConfig="@xml/network_security_config"` | WIRED | Confirmed by grep, attribute present on `<application>` |
| `tile_proxy_server.dart` | `resolveRegionForTile` | per-request winning-region resolution | WIRED | Handler calls it with live `_store.box<RegionEntity>().getAll()` and computed `tileBounds` |
| `tile_proxy_server.dart` | `PmTilesArchive.tile` | per-tile random access via `ZXY(z,x,y).toTileId()` | WIRED | Confirmed at line 159, archive resolved via bounded `_ArchiveCache` |
| `offline_style_rewriter.dart` | 127.0.0.1 proxy base URL | `tiles:` templates | WIRED | `rewriteStyleForProxy` asserts `proxyBaseUrl.startsWith('http://127.0.0.1:')` and injects it into both vector/dem `tiles:` arrays |
| `main.dart` | `TileProxyServer` | `TileProxyServer.start(store)` then `tileProxyBaseUrlProvider.overrideWithValue` | WIRED | Confirmed sequential ordering before `runApp` |
| `trail_map.dart` | `tileProxyBaseUrlProvider` | `_composeStyle` reads base URL, calls `rewriteStyleForProxy` | WIRED | Confirmed at lines ~147-152 |
| `navigation_screen.dart` | `tileProxyBaseUrlProvider` | `_composeStyle` reads base URL, calls `rewriteStyleForProxy` | WIRED | Confirmed at lines ~886-891 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `main.dart` | `store` passed to `TileProxyServer.start` | `await openStore(directory: dbPath)` — real ObjectBox store, same instance overridden into `objectBoxProvider` | Yes | FLOWING |
| `tile_proxy_server.dart` handler | `_store.box<RegionEntity>().getAll()` | Live ObjectBox query, no caching/staleness — re-queried fresh on every HTTP request | Yes | FLOWING |
| `trail_map.dart` / `navigation_screen.dart` `_composeStyle` | `ref.read(tileProxyBaseUrlProvider)` | Overridden in `main.dart` from the real `proxyServer.baseUrl` (not a stub/throw path in production) | Yes | FLOWING |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PROXY-01 | 25.1-02 (core), 25.1-04 (rewiring) | Both screens serve offline tiles through a single static XYZ source, replacing the incremental reconcile | SATISFIED (code-level; on-device confirmation pending — see human_verification) | `rewriteStyleForProxy`/`tileProxyBaseUrlProvider` wired in both screens; reconcile machinery fully deleted from both files |
| PROXY-02 | 25.1-02 | Overlapping regions resolve deterministically — smallest bbox wins, most-recent tie-break | SATISFIED | `resolveRegionForTile` implementation + passing unit tests match D-02/D-03 exactly |
| PROXY-03 | 25.1-01 (prereq), 25.1-03 (spike) | On-device spike confirms MapLibre Native reliably loads loopback-HTTP tiles offline before the full build is committed | SATISFIED | Physical-device spike (Pixel 6) confirmed PASS for online, radios-off, and full airplane mode; platform manifest exceptions confirmed sufficient via `adb logcat` (zero cleartext-blocked errors) |

No orphaned requirements — REQUIREMENTS.md maps exactly PROXY-01/02/03 to Phase 25.1, and all three appear in the `requirements:` frontmatter of at least one plan (01: PROXY-03, 02: PROXY-01/02, 03: PROXY-03, 04: PROXY-01).

### Anti-Patterns Found

None. Scanned all phase-modified files (`main.dart`, `trail_map.dart`, `navigation_screen.dart`, `tile_proxy_server.dart`, `tile_proxy_provider.dart`, `offline_style_rewriter.dart`, `xyz_tile_bounds.dart`, `network_security_config.xml`, `AndroidManifest.xml`, `Info.plist`, `tile_proxy_spike_harness.dart`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|coming soon|not yet implemented|not available` — zero matches. `flutter analyze` is clean for every phase-modified file individually and for the whole app (the 37 pre-existing whole-app `flutter analyze` infos are all unrelated `icon_util.dart` deprecated-icon warnings, confirmed by grep to not touch any phase file).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Proxy core + rewriter unit tests pass | `flutter test test/util/xyz_tile_bounds_test.dart test/services/tile_repository_manager_test.dart test/util/offline_style_rewriter_test.dart` | 45/45 tests passed, including all `resolveRegionForTile` and `rewriteStyleForProxy` cases | PASS |
| All phase-touched files analyze clean | `flutter analyze <8 phase files>` | "No issues found!" | PASS |
| Whole-app analyze shows no orphaned references from deletions | `flutter analyze` (repo root) | 37 pre-existing, unrelated `icon_util.dart` deprecation infos only; zero issues in any phase file | PASS |
| Commits claimed in SUMMARYs actually exist | `git log --oneline \| grep -E '<10 hashes>'` | All 10 commit hashes (02db6c50, a565d811, 259dced8, 71643353, 876951db, d9bec784, 2f181fab, 37275b4a, 3125bb9e, 172c7455) found in git log | PASS |

### Human Verification Required

See `human_verification` in frontmatter — 3 items, all centered on the same gap: the 25.1-03 spike validated the proxy mechanism through a throwaway harness, but Plan 04's actual rewiring of the two production screens (`TrailMap`, `navigation_screen`) — including `main.dart`'s real bootstrap sequence, and specifically `navigation_screen`'s camera-follow/GPS-follow logic that caused the original Phase 25 UAT Test 4 reentrancy bug — has not been exercised on a physical device. This is explicitly flagged as outstanding by the phase's own artifacts: `25.1-04-SUMMARY.md` states "On-device UAT re-verification of both screens (trail detail map + active navigation) rendering offline region tiles through the proxy is recommended before this milestone's `/gsd-complete-milestone` pass" and `.planning/STATE.md` repeats the same recommendation verbatim.

### Gaps Summary

No code-level gaps. All must-have truths, artifacts, and key links from all 4 plans (25.1-01 through 25.1-04) are present, substantive, and correctly wired — confirmed by direct file reads, greps, `flutter analyze`, passing unit tests, and git-log commit verification. Requirements PROXY-01/02/03 are all satisfied at the code level with no orphaned requirement IDs.

The phase is NOT marked `passed` because one observable truth (offline basemap/hillshade rendering on the actual production screens) has only been validated indirectly: the on-device spike (25.1-03) proved the underlying mechanism (TileProxyServer/rewriteStyleForProxy/resolveRegionForTile) works in airplane mode, and Plan 04 wired that exact mechanism into TrailMap and navigation_screen — but the rewired production screens themselves were never run on a physical device. Given this phase exists specifically to fix an on-device-only reentrancy bug in navigation_screen's camera-follow logic, and the project's own SUMMARY/STATE.md flag this same gap, routing to human_needed (rather than passed) is the correct, non-optimistic classification per this project's end-of-phase human-verify convention.

---

*Verified: 2026-07-23T17:14:04Z*
*Verifier: Claude (gsd-verifier)*
