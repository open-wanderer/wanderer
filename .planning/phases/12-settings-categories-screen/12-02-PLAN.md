---
phase: 12-settings-categories-screen
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - app/lib/routes/settings_categories_screen.dart
autonomous: false
requirements: [SETCAT-06, SETCAT-07, SETCAT-09, SETCAT-11]
must_haves:
  truths:
    - "SettingsCategoriesScreen lists categories sorted by priority ascending, alphabetical (locale name) for ties, each row showing drag handle, category icon, locale-resolved name, and a visibility Switch"
    - "Tapping a category row body navigates to the subcategories route passing the category; tapping the switch or drag handle does not navigate"
    - "Toggling a switch auto-saves via CategoryPreferenceNotifier.upsert with an error toast on failure and no success toast"
    - "Dragging via the handle reorders and POSTs the ordered category IDs; a failed reorder reverts the list to pre-drag order with an error toast"
    - "Turning a switch OFF when the user has own trails in that category shows a confirm dialog with the count and a view-trails action that pushes the user's own profile trail list (handle carries the '@' sigil) pre-filtered to that category; cancel reverts to ON, confirm saves"
  artifacts:
    - path: "app/lib/routes/settings_categories_screen.dart"
      provides: "Category visibility + priority-reorder settings screen"
      min_lines: 120
      contains: "class SettingsCategoriesScreen"
  key_links:
    - from: "app/lib/routes/settings_categories_screen.dart"
      to: "categoryProvider + categoryPreferenceProvider"
      via: "ref.watch"
      pattern: "categoryPreferenceProvider"
    - from: "app/lib/routes/settings_categories_screen.dart"
      to: "CategoryPreferenceNotifier.reorder"
      via: "ref.read(...).reorder"
      pattern: "\\.reorder\\("
    - from: "app/lib/routes/settings_categories_screen.dart"
      to: "ownTrailCount"
      via: "own-trail count on OFF-toggle"
      pattern: "ownTrailCount"
    - from: "app/lib/routes/settings_categories_screen.dart"
      to: "/profile/@{ownHandle}/trails (pre-filtered to category)"
      via: "view-trails button resolves '@'-prefixed handle, seeds trailFilterProvider then context.push"
      pattern: "context\\.push\\('/profile/"
---

<objective>
Build `SettingsCategoriesScreen` — the category list with priority sort, per-row visibility toggle (auto-save), drag-handle reorder, and the own-trail confirm-before-disable dialog. Consumes the Plan 01 `reorder` method, sort helpers, own-trail count helper, and l10n keys.

Purpose: Deliver SETCAT-06 (sorted list + icon + name), SETCAT-07 (toggle auto-save), SETCAT-09 (drag-handle reorder POST), and the category half of SETCAT-11 (own-trail confirm dialog).

Output: `app/lib/routes/settings_categories_screen.dart` (new `ConsumerStatefulWidget`).
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-settings-categories-screen/12-CONTEXT.md
@.planning/phases/12-settings-categories-screen/12-RESEARCH.md
@.planning/phases/12-settings-categories-screen/12-PATTERNS.md
@.planning/phases/12-settings-categories-screen/12-UI-SPEC.md
</context>

## Artifacts this phase produces

See `12-01-PLAN.md` "Artifacts this phase produces" for the full Phase 12 symbol list. This plan creates:
- `SettingsCategoriesScreen` — class in `app/lib/routes/settings_categories_screen.dart`

It consumes (created in Plan 01, do not re-implement): `CategoryPreferenceNotifier.reorder`, `sortedCategoriesByPreference`, `categoryVisible`, `ownTrailCount`, and the new l10n keys.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the category list — load, sort, row layout, toggle</name>
  <files>app/lib/routes/settings_categories_screen.dart</files>
  <read_first>
    - app/lib/routes/settings_notifications_screen.dart (analog: `_save` try/catch + `ToastMessage(type: ToastType.error, icon: FontAwesomeIcons.circleExclamation, text: l10n.error_saving_settings)`; `activeThumbColor` derivation lines 90-93; Scaffold+AppBar with `Icons.arrow_back` + `context.pop()` lines 154-162)
    - app/lib/components/async_loader.dart (AsyncLoader&lt;T&gt;(asyncValue:, mockData:, builder:) — D-14 loading wrapper)
    - app/lib/util/category_preference_sort.dart (Plan 01 — `sortedCategoriesByPreference`, `categoryVisible`)
    - app/lib/util/category_icon_util.dart (`categoryFilterAvatar(Category)` line 72 — row leading icon)
    - app/lib/provider/trail/category_provider.dart (`categoryProvider` — List&lt;Category&gt;)
    - app/lib/provider/category_preference_provider.dart (`categoryPreferenceProvider`, `.upsert(id, visible)`)
    - app/lib/models/category.dart (`CategoryDisplay.displayName(Locale?)`; Category has `id`)
    - .planning/phases/12-settings-categories-screen/12-UI-SPEC.md (Interaction & State Contract table — every state to implement)
    - .planning/phases/12-settings-categories-screen/12-RESEARCH.md (Pattern 2 separate hit targets, Pattern 3 toggle+toast, Pitfall 4 visibility default, Pitfall 5 mounted guards)
  </read_first>
  <action>
    Create `SettingsCategoriesScreen` as a `ConsumerStatefulWidget` (needs local drag state + `mounted` guards). Scaffold with an AppBar whose leading is `IconButton(icon: Icon(Icons.arrow_back), onPressed: () => context.pop())` and title `Text(l10n.categories)`, copying the notifications-screen AppBar shape.

    In the State's `build`: `ref.watch(categoryProvider)` and `ref.watch(categoryPreferenceProvider)`. Wrap the body in `AsyncLoader<...>` with `mockData: const []` (D-14) — no bespoke spinner. Once loaded, compute the sorted list via `sortedCategoriesByPreference(categories, prefs, Localizations.localeOf(context))` (Plan 01). Seed a local `List<String> _orderedIds` from the sorted list's ids (used by the reorder working copy in Task 2); on the non-drag path render directly from the sorted list.

    Each row (a `ListTile`-free explicit `Row`, NOT a `SwitchListTile` — Pattern 2, because the body must navigate independently of the switch): leading `ReorderableDragStartListener(index: index, child: Padding(padding: EdgeInsets.symmetric(horizontal: 8), child: Icon(Icons.drag_handle)))` (built in Task 2); then an `Expanded(child: InkWell(onTap: <navigate — Task 2>, child: Row([categoryFilterAvatar(category), SizedBox(width: 12), Expanded(child: Text(category.displayName(locale), overflow: TextOverflow.ellipsis))])))`; then a trailing `Switch(value: isVisible, activeThumbColor: activeColor, onChanged: (v) => _onToggle(category, v))`. Compute `isVisible = categoryVisible(category.id, prefs)` (visible unless a pref row is explicitly false — Pitfall 4). Derive `activeColor` exactly as the notifications screen: `Theme.of(context).brightness == Brightness.dark ? colorScheme.onSurface : colorScheme.primary`.

    Implement `_onToggle(Category category, bool value)`: for `value == true` (ON) call `_save(() => ref.read(categoryPreferenceProvider.notifier).upsert(category.id, true))` immediately (D-11, no check). For `value == false` (OFF) delegate to the own-trail-guarded flow (Task 2). Implement `_save(Future<void> Function() op)` copying the notifications-screen pattern: try `await op()`, catch any error and add a `ToastMessage(type: ToastType.error, icon: FontAwesomeIcons.circleExclamation, text: l10n.error_saving_settings)` via `toastProvider.notifier` — no success toast (D-08). The watched provider drives optimistic UI via `invalidateSelf`. Do NOT add any warning when a toggle leaves zero categories visible — hiding all categories is allowed silently (D-15), consistent with no cross-field validation in the settings suite. Guard with `if (!mounted) return;` after awaits (Pitfall 5).
  </action>
  <verify>
    <automated>cd app && grep -q "class SettingsCategoriesScreen extends ConsumerStatefulWidget" lib/routes/settings_categories_screen.dart && grep -q "sortedCategoriesByPreference" lib/routes/settings_categories_screen.dart && grep -q "categoryVisible" lib/routes/settings_categories_screen.dart && grep -q "AsyncLoader" lib/routes/settings_categories_screen.dart && grep -q "activeThumbColor" lib/routes/settings_categories_screen.dart && grep -q "categoryFilterAvatar" lib/routes/settings_categories_screen.dart && ! grep -q "SwitchListTile" lib/routes/settings_categories_screen.dart && dart analyze lib/routes/settings_categories_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsCategoriesScreen` is a `ConsumerStatefulWidget` with an AppBar (`Icons.arrow_back` + `context.pop()`, title `l10n.categories`).
    - Body is wrapped in `AsyncLoader` (no raw `CircularProgressIndicator`).
    - Rows are sorted via `sortedCategoriesByPreference` and render drag handle + `categoryFilterAvatar` + `displayName` + a trailing `Switch` (not `SwitchListTile`).
    - Switch `value` uses `categoryVisible(id, prefs)` (visible-by-default); ON toggle calls `upsert(id, true)`; OFF toggle routes to the guarded flow (Task 2).
    - `_save` shows only an error toast (`error_saving_settings`) on failure, no success toast; `dart analyze` clean.
  </acceptance_criteria>
  <done>Screen loads, sorts, and renders category rows with working ON-toggle auto-save and error toast; analyzer clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire reorder gesture, body-tap navigation, and own-trail confirm dialog (with concrete view-trails destination)</name>
  <files>app/lib/routes/settings_categories_screen.dart</files>
  <read_first>
    - app/lib/routes/settings_categories_screen.dart (the file from Task 1 — extend it)
    - app/lib/routes/settings_account_screen.dart (analog: `showDialog<bool>` + `AlertDialog` returning bool, lines 74-97; `if (!context.mounted) return;` guards)
    - app/lib/util/own_trail_count.dart (Plan 01 — `ownTrailCount(ref, isSubcategory:, id:)`; note it also resolves the own handle as `'@${user.preferredUsername}'` — this screen MUST resolve the handle the same '@'-prefixed way for the view-trails push)
    - app/lib/provider/category_preference_provider.dart (Plan 01 — `.reorder(List<String>)`)
    - app/lib/routes/profile_screen.dart (line 38 — CANONICAL own-handle resolution: `widget.handle ?? "@${ref.read(authProvider).value?.preferredUsername}"`; the app-wide convention for a self `/profile/{handle}/...` route is the `@`-prefixed handle, NOT a bare username)
    - app/lib/provider/auth_provider.dart (`authProvider` → `UserEntity?`; read `.value?.preferredUsername` for the own handle, then prefix with `@` — same source `profile_screen.dart` uses at line 38 and `own_trail_count.dart` uses in Plan 01)
    - app/lib/provider/trail/trail_filter_provider.dart (`trailFilterProvider(String filterId)`; `TrailFilterNotifier.updateFilter((f) => f.copyWith(...))` — used to seed the profile-trails filter before pushing; filterId is `'profile_trail_$handle'` where `$handle` is the `@`-prefixed handle)
    - app/lib/routes/profile_trail_screen.dart (destination screen — reads `trailFilterProvider('profile_trail_$widget.handle')` at build; `widget.handle` here is the `@`-prefixed handle from the `/profile/{handle}/trails` route, so the seeded filterId must use the SAME `@`-prefixed handle)
    - app/lib/provider/profile/profile_trails_provider.dart (confirms `profileTrailsProvider(handle)` watches `trailFilterProvider('profile_trail_$handle')` — so seeding the filter under the `@`-prefixed handle then pushing `/profile/$handle/trails` yields the pre-filtered list)
    - app/lib/models/trail.dart (`TrailFilter` freezed model: `category` is `List<Category>`, `subcategory` is `List<Subcategory>`, `copyWith` available)
    - .planning/phases/12-settings-categories-screen/12-RESEARCH.md (Pattern 1 ReorderableListView + custom handle; Pitfall 1 index-shift; Pitfall 2 snap-back; "Confirm dialog" code example; Open Question #2 "View trails" destination — resolved concretely below; A6)
    - .planning/phases/12-settings-categories-screen/12-UI-SPEC.md (Copywriting Contract for exact dialog copy keys; Interaction & State Contract rows for reorder-failure + toggle-off states)
  </read_first>
  <action>
    Render the list with `ReorderableListView.builder(buildDefaultDragHandles: false, itemCount: ..., itemBuilder: ..., onReorder: ...)` driven by the local `_orderedIds` working copy (Pitfall 2 — never render reorder from the re-sorting provider mid-drag). Every `itemBuilder` child MUST have `key: ValueKey(category.id)` (Pitfall 3). The drag handle from Task 1 is the `ReorderableDragStartListener`; `buildDefaultDragHandles: false` disables whole-row/long-press drag (D-01).

    `onReorder(oldIndex, newIndex)`: apply `if (newIndex > oldIndex) newIndex -= 1;` (Pitfall 1), snapshot the pre-drag `_orderedIds`, optimistically `setState` the reordered `_orderedIds`, then `await ref.read(categoryPreferenceProvider.notifier).reorder(_orderedIds)` (POSTs the full ordered category ID list, D-02/SETCAT-09). On any error, `setState` back to the pre-drag snapshot and show the `error_saving_settings` error toast (D-04/D-09). Guard `if (!mounted) return;` after the await.

    Body-tap navigation (Pattern 2, D-05): the `InkWell.onTap` from Task 1 calls `context.push('/settings/categories/subcategories', extra: category)` — pass the `Category` object via `extra` (route registered in Plan 04; A3 acceptable since only reached by in-app push). This must NOT fire from the switch or drag handle (they are separate hit targets).

    Own-trail confirm (SETCAT-11 category half — implements D-10, including its required "link/action to view those trails"): implement the OFF path of `_onToggle`. On `value == false`, call `final count = await ownTrailCount(ref, isSubcategory: false, id: category.id)` (lazy, D-12 — never preloaded). If `count == 0`, proceed to `_save(() => upsert(category.id, false))`. If `count > 0`, `showDialog<bool>` with an `AlertDialog`: title `l10n.settings_categories_confirm_disable_title`, body `l10n.settings_categories_confirm_disable_body(count)`, and actions `TextButton(pop(false), l10n.cancel)` + `TextButton(pop(true), l10n.settings_categories_confirm_disable_confirm)`. Include a `TextButton` labelled `l10n.settings_categories_confirm_view_trails` whose `onPressed` performs a REAL, non-empty navigation to the user's own profile trail list pre-filtered to this category (resolves RESEARCH.md Open Question #2 / A6):
      1. Resolve the own handle WITH the `@` sigil, exactly matching `profile_screen.dart:38` and Plan 01's `own_trail_count.dart`. First read the raw username: `final username = ref.read(authProvider).value?.preferredUsername;`. If `username == null` (not signed in — should not happen on this screen), do NOT build a bogus `'@null'` handle: show the `error_saving_settings` error toast via `toastProvider.notifier` and `return` early (warning-1 fix — no silent no-op). Otherwise build `final handle = '@$username';` so the handle carries the `@` prefix that every self `/profile/{handle}/...` call site uses.
      2. Seed the destination screen's filter by category so the pushed list is pre-filtered: `ref.read(trailFilterProvider('profile_trail_$handle').notifier).updateFilter((f) => f.copyWith(category: [category], subcategory: const []));` (`$handle` here is the `@`-prefixed handle; `TrailFilter.category` is `List<Category>` — pass the current `category` object; profile_trail_screen keys its filter by exactly `'profile_trail_$handle'` with the same `@`-prefixed handle).
      3. Dismiss the dialog and navigate: `Navigator.of(dialogContext).pop(); context.push('/profile/$handle/trails');` — `$handle` is `@`-prefixed, so the pushed `ProfileTrailScreen` receives `widget.handle == '@username'` (matching how the server resolves `/profile/{handle}` via `acct:` webfinger) and reads the seeded `trailFilterProvider('profile_trail_$handle')`, listing only this user's trails in the selected category. Do NOT leave `onPressed` as a `/* navigate to filtered trail list */` stub or a no-op — it MUST call `context.push('/profile/...')` with the `@`-prefixed handle and seed `trailFilterProvider`. Guard `context.mounted` before the push if the dialog await interleaves.
    If the `showDialog` future returns `true`, `_save(() => upsert(category.id, false))`; otherwise do nothing — the switch reverts automatically because provider state was never changed (D-10). Tapping "View trails" does not itself save or disable — it only navigates; the user returns to an unchanged switch. Guard `if (!context.mounted) return;` after the dialog await. ON-toggle never runs this check (D-11). Do NOT port any plugin-mapping messaging (D-13).
  </action>
  <verify>
    <automated>cd app && grep -q "ReorderableListView.builder" lib/routes/settings_categories_screen.dart && grep -q "buildDefaultDragHandles: false" lib/routes/settings_categories_screen.dart && grep -q "ReorderableDragStartListener" lib/routes/settings_categories_screen.dart && grep -q "if (newIndex > oldIndex) newIndex -= 1" lib/routes/settings_categories_screen.dart && grep -q "ValueKey(" lib/routes/settings_categories_screen.dart && grep -q "\.reorder(" lib/routes/settings_categories_screen.dart && grep -q "ownTrailCount" lib/routes/settings_categories_screen.dart && grep -q "showDialog<bool>" lib/routes/settings_categories_screen.dart && grep -q "settings_categories_confirm_disable_title" lib/routes/settings_categories_screen.dart && grep -q "settings_categories_confirm_view_trails" lib/routes/settings_categories_screen.dart && grep -q "context.push('/settings/categories/subcategories'" lib/routes/settings_categories_screen.dart && grep -Eq "'@\\\$?\{?.*preferredUsername" lib/routes/settings_categories_screen.dart && grep -Eq "final handle = '@" lib/routes/settings_categories_screen.dart && grep -Fq "context.push('/profile/$handle/trails')" lib/routes/settings_categories_screen.dart && grep -Fq "trailFilterProvider('profile_trail_$handle')" lib/routes/settings_categories_screen.dart && grep -Eq "updateFilter\(\(f\) => f\.copyWith\(category: \[category\]" lib/routes/settings_categories_screen.dart && ! grep -q "'@null'" lib/routes/settings_categories_screen.dart && ! grep -qi "plugin" lib/routes/settings_categories_screen.dart && ! grep -q "navigate to filtered trail list" lib/routes/settings_categories_screen.dart && dart analyze lib/routes/settings_categories_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - List uses `ReorderableListView.builder(buildDefaultDragHandles: false, ...)` with `ValueKey(category.id)` on each child and a `ReorderableDragStartListener` handle.
    - `onReorder` applies `if (newIndex > oldIndex) newIndex -= 1`, reorders a local `_orderedIds`, calls `.reorder(_orderedIds)`, and on error reverts to the pre-drag snapshot + shows `error_saving_settings` toast.
    - Body tap calls `context.push('/settings/categories/subcategories', extra: category)`; switch/handle do not navigate.
    - OFF-toggle calls `ownTrailCount(ref, isSubcategory: false, id: category.id)` and shows an `AlertDialog` (title/body/view-trails/confirm/cancel keys) only when count > 0; confirm saves `upsert(id, false)`, cancel is a no-op reverting the switch; ON-toggle skips the check.
    - The "View trails" `TextButton.onPressed` resolves the own handle WITH the `@` sigil (`final handle = '@$username'` from `authProvider`'s `preferredUsername`, matching `profile_screen.dart:38` and Plan 01's `own_trail_count.dart`), and on a null username shows the `error_saving_settings` toast and returns early (never constructs `'@null'`). It then seeds `trailFilterProvider('profile_trail_$handle')` via `updateFilter((f) => f.copyWith(category: [category], subcategory: const []))` and calls `context.push('/profile/$handle/trails')` with the `@`-prefixed handle. It is NOT a no-op and contains no `navigate to filtered trail list` stub comment (grep gates enforce the `@`-prefixed handle assignment near `preferredUsername`, the `context.push('/profile/$handle/trails')` and `trailFilterProvider('profile_trail_$handle')`/`copyWith(category: [category]` seed, the absence of the stub comment, and the absence of a literal `'@null'`).
    - No `plugin` string appears (D-13); `dart analyze` clean.
  </acceptance_criteria>
  <done>Reorder, body-tap navigation, and own-trail confirm dialog all function with revert-on-failure and lazy count; the "View trails" action resolves the `@`-prefixed own handle and really navigates to the own profile trail list pre-filtered to the category; analyzer clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify SettingsCategoriesScreen (sort, reorder, confirm dialog)</name>
  <action>Human verification only — no code changes. The operator runs the app and confirms the behaviors in <how-to-verify>, then approves or reports issues.</action>
  <what-built>SettingsCategoriesScreen with sorted category rows, per-row visibility switch (auto-save), drag-handle reordering, and the own-trail confirm-before-disable dialog whose "View trails" action opens the own profile trail list (via the `@`-prefixed own handle) pre-filtered to the category. (Reached via the tile+route wired in Plan 04 — if Plan 04 has not run, navigate by temporarily pushing `/settings/categories` or via the wired tile once available.)</what-built>
  <how-to-verify>
    Run the app against a live backend (`flutter run` in `app/`) signed in as a user who owns at least one trail in some category.
    1. Open Settings → Categories. Confirm categories are listed sorted by priority (ascending) with alphabetical tie-break, each showing a drag handle, category icon, locale-resolved name, and a switch.
    2. Drag a row by the handle (not the body) to a new position; release. Confirm the new order persists after leaving and re-entering the screen. Simulate a failure (airplane mode) and confirm the list reverts to the prior order with an error toast.
    3. Toggle a category OFF that has your own trails; confirm a dialog appears stating the count with a "View trails" action, "Disable anyway", and "Cancel". Tap "View trails" — confirm it opens your own profile trail list showing ONLY your trails in that category (the switch behind is unchanged, nothing was saved). Return, toggle OFF again, and Cancel — confirm the switch returns to ON and nothing saved. Toggle OFF again and confirm "Disable anyway" — confirm it saves. Toggle a category ON — confirm NO dialog appears.
    4. Tap a row body (not switch, not handle) — confirm it navigates toward the subcategories screen (blank/empty is fine if Plan 03/04 not yet complete).
    5. Verify reduced-motion: the drag uses only Flutter's built-in drag-elevation affordance, no extra gratuitous animation.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (wrong sort, snap-back on drag, dialog on ON-toggle, navigation firing from switch/handle, "View trails" not opening the filtered own-trail list or 404-ing because the handle lost its `@` prefix, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → PocketBase API | Visibility upserts and reorder POSTs cross here; server enforces session-owned writes |
| OFF-toggle intent → own-trail count query | Category id flows into an author-scoped Meilisearch filter |
| View-trails navigation → profile-trails query | Own `@`-prefixed handle + category filter seed the author-scoped profile-trails endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | `upsert` / `reorder` calls from this screen | mitigate | Screen calls the Plan 01 notifier methods which never send `user`; server injects owner from session. No client-trusted owner. |
| T-12-04 | Tampering | reorder `_orderedIds` list posted to server | accept | IDs originate from `categoryProvider` (server data), not user text; server re-validates ownership of each id in the posted list before persisting (server-side reorder endpoint). |
| T-12-03 | Information Disclosure | own-trail count on OFF-toggle | mitigate | `ownTrailCount` queries `/profile/@{ownHandle}/trails`, author-scoped server-side; only the user's own trail count is returned. |
| T-12-06 | Information Disclosure | view-trails push to `/profile/@{ownHandle}/trails` | accept | `@`-prefixed handle comes from `authProvider` (own session), category from server-fetched `categoryProvider`; the destination endpoint is author-scoped server-side, so only the user's own trails are shown. |
| T-12-SC | Tampering | pub installs | accept | No packages installed; all widgets first-party Flutter Material. |
</threat_model>

<verification>
- `dart analyze lib/routes/settings_categories_screen.dart` clean.
- Grep gates confirm: ReorderableListView + custom handle + index-shift fix + ValueKey; `.reorder(` call; `ownTrailCount`; `showDialog<bool>` with confirm keys; body-tap push to the subcategories route; the "View trails" action really navigates with an `@`-prefixed handle — grep for the `final handle = '@'`-prefixed assignment near `preferredUsername`, `context.push('/profile/$handle/trails')` + `trailFilterProvider('profile_trail_$handle')` seed + `copyWith(category: [category]`, the ABSENCE of the `navigate to filtered trail list` stub comment, and the ABSENCE of a literal `'@null'`; no `plugin` string; no `SwitchListTile`.
- Human-verify checkpoint confirms sort, reorder persistence + revert, confirm-dialog behavior on OFF (and its absence on ON), that "View trails" opens the pre-filtered own-trail list (handle carries the `@` prefix, no 404), and independent hit targets.
</verification>

<success_criteria>
- SETCAT-06: categories sorted by priority asc / alphabetical tie-break, each row shows icon + locale name.
- SETCAT-07: visibility switch auto-saves via `upsert` with error toast, no success toast.
- SETCAT-09: drag-handle reorder POSTs ordered category IDs; failure reverts + toast.
- SETCAT-11 (category half): OFF-toggle with own trails shows count + a working view-trails link that opens the own profile trail list (via the `@`-prefixed own handle) pre-filtered to the category (D-10); cancel reverts, confirm saves; ON never checks; no plugin messaging.
</success_criteria>

<output>
Create `.planning/phases/12-settings-categories-screen/12-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
