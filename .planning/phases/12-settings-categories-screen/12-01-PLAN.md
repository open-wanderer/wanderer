---
phase: 12-settings-categories-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/provider/category_preference_provider.dart
  - app/lib/provider/subcategory_preference_provider.dart
  - app/lib/util/category_preference_sort.dart
  - app/lib/util/own_trail_count.dart
  - app/lib/i18n/app_en.arb
autonomous: true
requirements: [SETCAT-09, SETCAT-10, SETCAT-11]
must_haves:
  truths:
    - "CategoryPreferenceNotifier exposes a reorder(List<String>) method that POSTs the ordered category IDs and invalidates itself"
    - "SubcategoryPreferenceNotifier exposes a reorder(String, List<String>) method that POSTs the parent category id + ordered subcategory IDs and invalidates itself"
    - "sortedCategoriesByPreference / sortedSubcategoriesByPreference sort by priority ascending then locale-name alphabetical for ties, treating no-preference and visible!=false as visible"
    - "ownTrailCount returns the count of the current user's own trails using a given category or subcategory id"
    - "New confirm-dialog and empty-state l10n keys exist and AppLocalizations is regenerated"
  artifacts:
    - path: "app/lib/provider/category_preference_provider.dart"
      provides: "reorder method on CategoryPreferenceNotifier"
      contains: "reorder"
    - path: "app/lib/provider/subcategory_preference_provider.dart"
      provides: "reorder method on SubcategoryPreferenceNotifier"
      contains: "reorder"
    - path: "app/lib/util/category_preference_sort.dart"
      provides: "sortedCategoriesByPreference + sortedSubcategoriesByPreference + visibility helpers"
      min_lines: 30
    - path: "app/lib/util/own_trail_count.dart"
      provides: "ownTrailCount lazy author-scoped trail count"
      min_lines: 15
    - path: "app/lib/i18n/app_en.arb"
      provides: "confirm-dialog + empty-state l10n keys"
      contains: "settings_categories_confirm_disable_title"
  key_links:
    - from: "app/lib/provider/category_preference_provider.dart"
      to: "/user-category-preference/reorder"
      via: "apiProvider.post"
      pattern: "user-category-preference/reorder"
    - from: "app/lib/provider/subcategory_preference_provider.dart"
      to: "/user-subcategory-preference/reorder"
      via: "apiProvider.post"
      pattern: "user-subcategory-preference/reorder"
    - from: "app/lib/util/own_trail_count.dart"
      to: "/profile/{handle}/trails"
      via: "apiProvider.post with filter"
      pattern: "profile/.*/trails"
---

<objective>
Build the shared, screen-independent foundation for Phase 12: the two provider `reorder` methods (SETCAT-09, SETCAT-10), the priority/visibility sort helpers, the lazy own-trail count helper (SETCAT-11), and the new l10n keys. This is the contract layer — the two settings screens (Plans 02, 03) consume these symbols directly and must not re-implement them.

Purpose: Isolate the two genuinely new provider methods and two new util files (the only parts with no in-repo Dart analog beyond the reorder gesture) into one plan so the screen plans are pure assembly against defined contracts.

Output: `reorder` on both preference notifiers, `category_preference_sort.dart`, `own_trail_count.dart`, and regenerated `.g.dart` + `AppLocalizations`.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-settings-categories-screen/12-CONTEXT.md
@.planning/phases/12-settings-categories-screen/12-RESEARCH.md
@.planning/phases/12-settings-categories-screen/12-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created across Phase 12 (exclude from drift verification — they do not exist before this phase):

- `reorder(List<String> orderedCategoryIds)` — method on `CategoryPreferenceNotifier`
- `reorder(String categoryId, List<String> orderedSubcategoryIds)` — method on `SubcategoryPreferenceNotifier`
- `app/lib/util/category_preference_sort.dart` — file, with `sortedCategoriesByPreference`, `sortedSubcategoriesByPreference`, `categoryVisible`, `subcategoryVisible`
- `app/lib/util/own_trail_count.dart` — file, with `ownTrailCount`
- `SettingsCategoriesScreen` — class (`app/lib/routes/settings_categories_screen.dart`, Plan 02)
- `SettingsSubcategoriesScreen` — class (`app/lib/routes/settings_subcategories_screen.dart`, Plan 03)
- `/settings/categories` and `/settings/categories/subcategories` go_router paths (Plan 04)
- l10n keys: `settings_categories_confirm_disable_title`, `settings_categories_confirm_disable_subcategory_title`, `settings_categories_confirm_disable_body`, `settings_categories_confirm_view_trails`, `settings_categories_confirm_disable_confirm`, `settings_categories_empty_title`, `settings_categories_empty_body`

<tasks>

<task type="auto">
  <name>Task 1: Add reorder methods to both preference notifiers</name>
  <files>app/lib/provider/category_preference_provider.dart, app/lib/provider/subcategory_preference_provider.dart</files>
  <read_first>
    - app/lib/provider/category_preference_provider.dart (existing `build` + `upsert`; the `reorder` method sits alongside `upsert`)
    - app/lib/provider/subcategory_preference_provider.dart (existing `build` + `upsert`)
    - .planning/phases/12-settings-categories-screen/12-RESEARCH.md ("Code Examples" section — verified payload shapes and method bodies)
    - .planning/phases/12-settings-categories-screen/12-PATTERNS.md (provider sections — analog is each file's own `upsert`)
  </read_first>
  <action>
    Add a `Future<void> reorder(List<String> orderedCategoryIds)` method to `CategoryPreferenceNotifier` that calls `ref.read(apiProvider).post('/user-category-preference/reorder', data: {'categories': orderedCategoryIds})` then `ref.invalidateSelf()`. The JSON key is exactly `categories` (verified against the server Zod schema, D-02). Do NOT send a `user` field — the server injects it from session (Security V4 / T-10-05 precedent), mirroring the existing `upsert`.

    Add a `Future<void> reorder(String categoryId, List<String> orderedSubcategoryIds)` method to `SubcategoryPreferenceNotifier` that calls `ref.read(apiProvider).post('/user-subcategory-preference/reorder', data: {'category': categoryId, 'subcategories': orderedSubcategoryIds})` then `ref.invalidateSelf()`. The JSON keys are exactly `category` (the parent id) and `subcategories`. No `user` field.

    Mirror the exact call/return shape of each file's existing `upsert` (apiProvider read, await post, invalidateSelf). Do not add error handling in the provider — callers wrap in try/catch + toast (Plans 02/03). After editing, run `dart run build_runner build --delete-conflicting-outputs` in `app/` to regenerate `category_preference_provider.g.dart` and `subcategory_preference_provider.g.dart`.
  </action>
  <verify>
    <automated>cd app && grep -q "user-category-preference/reorder" lib/provider/category_preference_provider.dart && grep -q "user-subcategory-preference/reorder" lib/provider/subcategory_preference_provider.dart && grep -q "'categories': orderedCategoryIds" lib/provider/category_preference_provider.dart && grep -q "'subcategories': orderedSubcategoryIds" lib/provider/subcategory_preference_provider.dart && ! grep -n "'user'" lib/provider/category_preference_provider.dart lib/provider/subcategory_preference_provider.dart && dart analyze lib/provider/category_preference_provider.dart lib/provider/subcategory_preference_provider.dart</automated>
  </verify>
  <acceptance_criteria>
    - `category_preference_provider.dart` contains a `reorder(List<String> orderedCategoryIds)` method posting to `/user-category-preference/reorder` with body key `categories` and calling `ref.invalidateSelf()`.
    - `subcategory_preference_provider.dart` contains a `reorder(String categoryId, List<String> orderedSubcategoryIds)` method posting to `/user-subcategory-preference/reorder` with body keys `category` and `subcategories` and calling `ref.invalidateSelf()`.
    - Neither `reorder` method includes a `user` key in its payload (grep for `'user'` in both files returns no reorder-payload match).
    - `dart analyze` on both provider files reports no errors; both `.g.dart` files regenerated.
  </acceptance_criteria>
  <done>Both notifiers expose a `reorder` method with the verified payload shapes; codegen passes; analyzer clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create sort helper + own-trail count helper</name>
  <files>app/lib/util/category_preference_sort.dart, app/lib/util/own_trail_count.dart</files>
  <read_first>
    - app/lib/models/category.dart (CategoryDisplay.displayName(Locale?) extension; Category has `id`, `name`)
    - app/lib/models/subcategory.dart (SubcategoryDisplay.displayName(Locale?); Subcategory has `id`, `category` parent-id field)
    - app/lib/models/category_preference.dart (fields: `id?`, `user`, `category`, `visible?`, `priority?`)
    - app/lib/models/subcategory_preference.dart (analogous fields; `subcategory` parent-id field)
    - app/lib/provider/profile/profile_trails_provider.dart (lines 80-124 — exact `POST /profile/{handle}/trails` call shape; reads `data['hits']` and `data['totalPages']`)
    - app/lib/provider/auth_provider.dart (authProvider returns UserEntity?; UserEntity exposes `preferredUsername`)
    - .planning/phases/12-settings-categories-screen/12-RESEARCH.md (Pattern 4 sort logic; Pattern 5 own-trail count; Pitfall 4 visibility default)
  </read_first>
  <action>
    Create `app/lib/util/category_preference_sort.dart` as a pure-function util (no Riverpod dependency — keeps the Phase-10 read-only-provider boundary). Port `sortedCategoriesByPreference(List<Category> categories, List<CategoryPreference> prefs, Locale? locale)` and `sortedSubcategoriesByPreference(List<Subcategory> subs, List<SubcategoryPreference> prefs, Locale? locale)` from `web/src/lib/util/category_util.ts` per RESEARCH Pattern 4: prioritized items (priority != null && priority > 0) sort ascending by priority; non-prioritized items sort after prioritized ones; ties break on `displayName(locale).toLowerCase().compareTo(...)`. Do NOT add an intl-collation package (A4). Also export `bool categoryVisible(String categoryId, List<CategoryPreference> prefs)` and `bool subcategoryVisible(String subcategoryId, List<SubcategoryPreference> prefs)` computed as "visible unless a preference row exists with `visible == false`" — i.e. `prefs.where((p) => p.category == id).map((p) => p.visible).firstOrNull != false` (Pitfall 4: no-preference and null both mean visible). Match the parent-id field name on each preference model (`category` on CategoryPreference, `subcategory` on SubcategoryPreference — confirm in read_first).

    Create `app/lib/util/own_trail_count.dart` with `Future<int> ownTrailCount(Ref ref, {required bool isSubcategory, required String id})` (accept whatever ref type the callers pass — use `Ref` or a `WidgetRef`; callers are ConsumerStatefulWidget so pass `ref`). Read `ref.read(authProvider).value`; if null return 0. Resolve the handle as `'@${user.preferredUsername}'` (matches `profile_screen.dart:38`). Build the Meilisearch filter string `"$field IN ['$id']"` where `field` is `subcategory_id` when `isSubcategory` else `category_id` (same field names used by `TrailFilter.toFilterText`, A2). POST to `/profile/$handle/trails` with body `{'q': '', 'options': {'filter': <filter>, 'hitsPerPage': 1, 'page': 1}}` mirroring `profile_trails_provider.dart`. Read the count as `(data['totalHits'] ?? data['estimatedTotalHits'] ?? (data['hits'] as List?)?.length ?? 0) as int` (A1 fallback chain). Guard the response is a `Map<String, dynamic>` before casting.
  </action>
  <verify>
    <automated>cd app && grep -q "sortedCategoriesByPreference" lib/util/category_preference_sort.dart && grep -q "sortedSubcategoriesByPreference" lib/util/category_preference_sort.dart && grep -Eq "categoryVisible|subcategoryVisible" lib/util/category_preference_sort.dart && grep -q "profile/\$handle/trails" lib/util/own_trail_count.dart && grep -q "subcategory_id" lib/util/own_trail_count.dart && grep -q "category_id" lib/util/own_trail_count.dart && grep -q "totalHits" lib/util/own_trail_count.dart && dart analyze lib/util/category_preference_sort.dart lib/util/own_trail_count.dart</automated>
  </verify>
  <acceptance_criteria>
    - `category_preference_sort.dart` exports `sortedCategoriesByPreference`, `sortedSubcategoriesByPreference`, `categoryVisible`, `subcategoryVisible`; sort places prioritized (priority > 0) items first ascending, non-prioritized after, ties broken by lowercased `displayName(locale)`.
    - `categoryVisible`/`subcategoryVisible` return `true` when no preference row exists or `visible` is null; return `false` only when a row has `visible == false`.
    - `own_trail_count.dart` exports `ownTrailCount` that returns 0 for a null user, POSTs to `/profile/@{preferredUsername}/trails` with filter `category_id IN ['id']` or `subcategory_id IN ['id']` and `hitsPerPage: 1`, and reads `totalHits ?? estimatedTotalHits ?? hits.length ?? 0`.
    - `dart analyze` on both files reports no errors.
  </acceptance_criteria>
  <done>Both util files exist, export the named functions with the verified logic, and pass the analyzer.</done>
</task>

<task type="auto">
  <name>Task 3: Add l10n keys and regenerate AppLocalizations</name>
  <files>app/lib/i18n/app_en.arb</files>
  <read_first>
    - app/lib/i18n/app_en.arb (canonical ARB with @metadata; existing keys `categories`, `cancel`, `error_saving_settings`)
    - app/l10n.yaml (arb-dir lib/i18n, template app_en.arb, generate on build)
    - .planning/phases/12-settings-categories-screen/12-UI-SPEC.md (Copywriting Contract — exact en copy strings and suggested key names)
  </read_first>
  <action>
    Add the following keys to `app/lib/i18n/app_en.arb` (canonical) with matching `@`-metadata entries following the file's existing ICU-placeholder convention, exact en copy from the UI-SPEC Copywriting Contract:
    - `settings_categories_confirm_disable_title` = "Hide this category?"
    - `settings_categories_confirm_disable_subcategory_title` = "Hide this subcategory?"
    - `settings_categories_confirm_disable_body` = "{count} of your trails use this category. They will stay published but this filter will be hidden." with a `{count}` placeholder of type `int` (@metadata placeholders block). Provide a subcategory-worded variant is NOT required — the body reads "category" generically; if a distinct subcategory wording is desired reuse the same key (planner's discretion is to keep one body key with `{count}`).
    - `settings_categories_confirm_view_trails` = "View trails"
    - `settings_categories_confirm_disable_confirm` = "Disable anyway"
    - `settings_categories_empty_title` = "No subcategories"
    - `settings_categories_empty_body` = "This category has no subcategories to configure."

    Reuse existing keys `categories` (settings tile + AppBar title), `cancel` (dialog cancel), and `error_saving_settings` (toast) — do NOT duplicate them. Add the new keys ONLY to `app_en.arb`; the other 13 ARBs may keep English fallback (matches the Phase 11 `subcategories` key process where en is canonical with @metadata). Run `flutter gen-l10n` (or `flutter pub get` / a build) in `app/` to regenerate `app_localizations.dart` so `AppLocalizations` exposes the new getters.
  </action>
  <verify>
    <automated>cd app && grep -q '"settings_categories_confirm_disable_title"' lib/i18n/app_en.arb && grep -q '"settings_categories_confirm_disable_body"' lib/i18n/app_en.arb && grep -q '"settings_categories_confirm_disable_confirm"' lib/i18n/app_en.arb && grep -q '"settings_categories_empty_title"' lib/i18n/app_en.arb && grep -q '"settings_categories_confirm_view_trails"' lib/i18n/app_en.arb && flutter gen-l10n && grep -q "settings_categories_confirm_disable_title" lib/i18n/app_localizations.dart</automated>
  </verify>
  <acceptance_criteria>
    - `app_en.arb` contains all seven new keys with their exact UI-SPEC en copy and `@`-metadata (including a `{count}` int placeholder on the body key).
    - No duplicate definitions of `categories`, `cancel`, or `error_saving_settings` are introduced.
    - `flutter gen-l10n` succeeds and `app_localizations.dart` exposes getters for the new keys (grep finds `settings_categories_confirm_disable_title`).
  </acceptance_criteria>
  <done>All new l10n keys present in canonical ARB with metadata; AppLocalizations regenerated and exposes them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → PocketBase API | Preference reorder writes and profile-trails reads cross here; server enforces auth/session |
| Filter string → Meilisearch | Category/subcategory IDs interpolated into a Meilisearch filter clause |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | `reorder` payloads on both notifiers | mitigate | Never send a client `user` field; server injects owner from session (mirrors existing `upsert`). Verified by grep gate in Task 1 (no `'user'` key). |
| T-12-02 | Tampering | Meilisearch filter in `own_trail_count.dart` | mitigate | `id` comes from server-fetched provider data (15-char PB IDs), never free-text user input; interpolated into `IN ['<id>']` exactly as the vetted `TrailFilter.toFilterText`. |
| T-12-03 | Information Disclosure | `/profile/{handle}/trails` own-trail count | mitigate | Endpoint is author-scoped server-side; querying own `preferredUsername` returns only the requesting user's trails (V4). |
| T-12-SC | Tampering | pub/npm installs | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); no supply-chain surface. |
</threat_model>

<verification>
- `dart analyze lib/provider lib/util` reports no new errors.
- `dart run build_runner build --delete-conflicting-outputs` regenerates provider `.g.dart` without conflicts.
- `flutter gen-l10n` succeeds; new keys resolvable on `AppLocalizations`.
- Grep gates confirm reorder endpoints, payload keys, absence of `user` field, and filter field names.
</verification>

<success_criteria>
- Both preference notifiers have a `reorder` method with verified payloads and no `user` field.
- `category_preference_sort.dart` and `own_trail_count.dart` exist with the named exported functions and pass the analyzer.
- Seven new l10n keys exist in canonical ARB with metadata; AppLocalizations regenerated.
- SETCAT-09/10 provider halves and SETCAT-11 count halves are in place for Plans 02/03 to consume.
</success_criteria>

<output>
Create `.planning/phases/12-settings-categories-screen/12-01-SUMMARY.md` when done.
</output>
