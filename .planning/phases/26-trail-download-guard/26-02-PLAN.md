---
phase: 26-trail-download-guard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/components/trail/missing_coverage_sheet.dart
  - app/lib/services/download_notification_service.dart
autonomous: true
requirements: [GUARD-02, GUARD-03]
must_haves:
  truths:
    - "The bottom sheet lists every missing region as a bordered card with the region name, a Vector checkbox with size, and (only when the region has a DEM archive) an Elevation-data checkbox with size"
    - "Vector checkboxes default checked, DEM checkboxes default unchecked (D-07)"
    - "A combined-size summary above the Download button recomputes on every toggle and reads 'Downloading trail only' when nothing is checked (D-08 escape hatch made visible)"
    - "The Download button is always enabled — 0 checked and N checked are both valid (D-08); dismissing the sheet returns null, tapping Download returns a selection"
    - "DownloadNotificationService can render an aggregate title/body without changing its existing single-source showProgress copy (D-10 prerequisite, Pitfall 1)"
  artifacts:
    - path: "app/lib/components/trail/missing_coverage_sheet.dart"
      provides: "MissingCoverageSelection model + showMissingCoverageSheet() + _MissingCoverageSheetContent widget"
      contains: "showMissingCoverageSheet"
    - path: "app/lib/services/download_notification_service.dart"
      provides: "showAggregateProgress(title, body, done, total) alongside the unchanged showProgress"
      contains: "showAggregateProgress"
  key_links:
    - from: "app/lib/components/trail/missing_coverage_sheet.dart"
      to: "app/lib/util/byte_format_util.dart"
      via: "formatBytes() for every size string"
      pattern: "formatBytes"
    - from: "app/lib/components/trail/missing_coverage_sheet.dart"
      to: "app/lib/provider/region/tile_repository_provider.dart"
      via: "ref.watch(tileRepositoryStatusProvider) for live in-flight/error row state"
      pattern: "tileRepositoryStatusProvider"
---

<objective>
Build the two downstream building blocks the guard wiring (Plan 03) consumes: the missing-coverage bottom modal sheet (the GUARD-02/GUARD-03 user surface) and the notification-service extension that lets one notification show aggregate download copy (the D-10 prerequisite). Neither block wires itself into the download flow — that is Plan 03. Both are self-contained and defined here so Plan 03 receives finished contracts, not a scavenger hunt.

Purpose: The sheet is the only genuinely new UI in this phase; isolating it (plus its returned selection contract) and the notification signature gap here keeps Plan 03 focused purely on orchestration.
Output: `app/lib/components/trail/missing_coverage_sheet.dart` (new) + a `showAggregateProgress` method added to `app/lib/services/download_notification_service.dart`.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-trail-download-guard/26-CONTEXT.md
@.planning/phases/26-trail-download-guard/26-UI-SPEC.md
@.planning/phases/26-trail-download-guard/26-PATTERNS.md
@.planning/phases/26-trail-download-guard/26-RESEARCH.md
</context>

## Artifacts this phase produces (Plan 02 scope)

New symbols this plan creates — plan-review source-grounding should treat these as newly-introduced, not drift:

- `app/lib/components/trail/missing_coverage_sheet.dart` (new file)
  - `class MissingCoverageSelection` with `final List<RegionEntity> vectorRegions;` and `final List<RegionEntity> demRegions;`
  - `Future<MissingCoverageSelection?> showMissingCoverageSheet(BuildContext context, Trail trail, List<RegionEntity> missingRegions)` — top-level function
  - `class _MissingCoverageSheetContent extends ConsumerStatefulWidget` (private) + its State
- `app/lib/services/download_notification_service.dart` (modified)
  - `Future<void> showAggregateProgress(String title, String body, int done, int total)` — new method on `DownloadNotificationService`

<tasks>

<task type="auto">
  <name>Task 1: DownloadNotificationService.showAggregateProgress</name>
  <files>app/lib/services/download_notification_service.dart</files>
  <read_first>
    - app/lib/services/download_notification_service.dart (the existing `showProgress` at lines 69-100 — copy its `AndroidNotificationDetails`/`DarwinNotificationDetails`/`_plugin.show(_notificationId, ...)` body verbatim; note fixed `_notificationId = 42`, channel `trail_download`)
    - app/lib/provider/download_notification_provider.dart (the provider exposing this service)
    - .planning/phases/26-trail-download-guard/26-UI-SPEC.md (Copywriting Contract → unified notification title/body copy)
  </read_first>
  <action>
    Add a new method `Future<void> showAggregateProgress(String title, String body, int done, int total)` to `DownloadNotificationService`. It must reuse `showProgress`'s exact `AndroidNotificationDetails` construction (same `_channelId`/`_channelName`, `importance: Importance.low`, `priority: Priority.low`, `showProgress: true`, `maxProgress: total > 0 ? total : 1`, `progress: done`, `indeterminate: total == 0`, `ongoing: true`, `autoCancel: false`, `onlyAlertOnce: true`) and the same `DarwinNotificationDetails`, and call `_plugin.show(_notificationId, title, body, ...)` on the SAME fixed `_notificationId` (42) — one notification, no new channel or id. The ONLY difference from `showProgress` is that `title` and `body` are caller-supplied instead of hardcoded to `trailName`/`'Downloading trail... {pct}%'`.

    Do NOT modify `showProgress`, `showGenerating`, `showSuccess`, or `showError` — the 0-region GUARD-01 path (Plan 03) must keep calling the untouched `showProgress(trail.name, ...)` so today's behavior is byte-for-byte identical (Pitfall 1). This is deliberately a sibling method, not a signature change, so the invariant is obvious in a diff.

    Call `_ensureInitialized()` first, exactly as the other methods do.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/services/download_notification_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'Future<void> showAggregateProgress(String title, String body, int done, int total)' app/lib/services/download_notification_service.dart` returns 1.
    - Source: `showProgress`'s existing signature `Future<void> showProgress(String trailName, int done, int total)` is unchanged (`grep -c 'showProgress(String trailName, int done, int total)' app/lib/services/download_notification_service.dart` returns 1).
    - Source: `showAggregateProgress` calls `_plugin.show(_notificationId,` (same fixed id 42, no new id/channel introduced — `grep -c "static const _notificationId = 42" app/lib/services/download_notification_service.dart` returns 1 and no second notification id constant is added).
    - `flutter analyze` on the file reports no errors.
  </acceptance_criteria>
  <done>
    A new `showAggregateProgress(title, body, done, total)` exists reusing the id-42 notification with caller-supplied copy; all four existing methods are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Missing-coverage bottom sheet + selection contract</name>
  <files>app/lib/components/trail/missing_coverage_sheet.dart</files>
  <read_first>
    - app/lib/components/navigation/track_save_options_sheet.dart (FULL file — copy the `showModalBottomSheet` config, drag handle 30×5 / `secondaryContainer`, bold `titleMedium` title, `SingleChildScrollView` padded `EdgeInsets.fromLTRB(16,16,16,24+viewInsets.bottom)`, `crossAxisAlignment: stretch`, flat bordered `_ToggleCard` (`elevation: 0` + `BorderSide(colorScheme.outline)`), full-width bottom `FilledButton`)
    - app/lib/routes/settings_offline_regions_screen.dart (region-row precedent lines 300-407: region-name header bold `bodyLarge`, `if (region.demUrl != null)` DEM gating, `ListTile(dense: true)` per package, size subtitle via `formatBytes`, `resolveVectorTileStatus`/`resolveDemTileStatus` usage, red `regions_download_failed` / orange convention)
    - app/lib/util/byte_format_util.dart (`formatBytes(int)` — the ONLY size formatter; reuse verbatim)
    - app/lib/util/region_tile_status_util.dart (`resolveVectorTileStatus(RegionStatus, double?)`, `resolveDemTileStatus(PackageStatus, double?)` — reuse to detect an in-flight/error row)
    - app/lib/entities/region_entity.dart (`RegionEntity`: `name`, `demUrl`, `vectorSize`, `demSize`, `status`, `vectorPackage.target?.status`, `demPackage.target?.status`)
    - app/lib/provider/region/tile_repository_provider.dart (`tileRepositoryStatusProvider` → `Map<String, RegionDownloadState>`; `RegionDownloadState.vectorProgress`/`demProgress`)
    - app/lib/models/trail.dart (`Trail.name`)
    - .planning/phases/26-trail-download-guard/26-UI-SPEC.md (Copywriting Contract + Component & Pattern Inventory — the visual/copy source of truth)
  </read_first>
  <action>
    Create `missing_coverage_sheet.dart` defining, in this order (interface-first):

    1. A plain immutable class `MissingCoverageSelection` with `final List<RegionEntity> vectorRegions;` and `final List<RegionEntity> demRegions;` (const constructor, both required). This is the contract Plan 03 consumes: the regions whose Vector package the user checked, and the regions whose DEM package the user checked. Keep it a plain class (NOT freezed) so no build_runner run is needed.

    2. `Future<MissingCoverageSelection?> showMissingCoverageSheet(BuildContext context, Trail trail, List<RegionEntity> missingRegions)` returning `showModalBottomSheet<MissingCoverageSelection>` with `isDismissible: true`, `enableDrag: true`, `isScrollControlled: true` (this list can be longer than the 2-toggle precedent), `shape: RoundedRectangleBorder(borderRadius: BorderRadius.vertical(top: Radius.circular(20)))`, `builder: (_) => _MissingCoverageSheetContent(trail: trail, missingRegions: missingRegions)`. A dismissed sheet resolves to `null` (abort); a Download tap resolves to a non-null `MissingCoverageSelection` (even when both lists are empty).

    3. `_MissingCoverageSheetContent` as a `ConsumerStatefulWidget` (needs `ref.watch(tileRepositoryStatusProvider)` for live per-package in-flight/error state). Its State holds two local selection maps keyed by region id: `_vectorChecked` (init `true` for every listed region — D-07) and `_demChecked` (init `false` for every listed region — D-07). Toggling a checkbox only mutates local state via `setState`; it starts NO download (D-06).

    Sheet body (mirror `track_save_options_sheet.dart`'s scroll/padding/handle/title layout):
    - Drag handle + bold `titleMedium` title with the literal "Missing offline map coverage" (GUARD-02).
    - Intro `bodyLarge` text: "{trail.name} isn't fully covered by your downloaded regions yet. Select any regions below to download them together with the trail — or tap Download to get the trail without them." (interpolate `trail.name`).
    - One flat bordered card per region in `missingRegions` (elevation 0, `BorderSide(colorScheme.outline)`, radius 16): region name bold (`bodyLarge`), then a Vector `CheckboxListTile` (label `l10n.regions_vector_tile_title`, subtitle `formatBytes(region.vectorSize ?? 0)`), and — ONLY when `region.demUrl != null` — a DEM `CheckboxListTile` (label `l10n.regions_dem_tile_title`, subtitle `formatBytes(region.demSize ?? 0)`). Checkbox `value` binds to the local maps.
    - Per-package live-state edge cases (reuse the Settings precedent, read via `ref.watch(tileRepositoryStatusProvider)[region.id]`): resolve the Vector row's effective status with `resolveVectorTileStatus(region.status, downloadState?.vectorProgress)` and the DEM row's with `resolveDemTileStatus(region.demPackage.target?.status ?? PackageStatus.notDownloaded, downloadState?.demProgress)`. If a package is `downloading` → replace its checkbox with a `LinearProgressIndicator` + a Label-role "Downloading…" caption (it is already running; do not add it to the selection and do not count it in the combined size). If a package is `error` → keep the checkbox but show the red `l10n.regions_download_failed` caption (checking it re-triggers the download in Plan 03).
    - A combined-size summary Label-role `Text` directly above the button, recomputed on every toggle: sum `region.vectorSize` for each checked-and-not-already-downloading vector package + `region.demSize` for each checked DEM package; when at least one package is checked show "{N} region(s) selected · {formatBytes(total)}" (N = count of regions with ≥1 checked package); when nothing is checked show "Downloading trail only" (this makes the D-08 escape hatch visible).
    - A full-width `FilledButton` labelled `l10n.download`, ALWAYS enabled (never gate `onPressed` on the selection — Pitfall 5 / D-08). On press: build `MissingCoverageSelection(vectorRegions: [regions whose _vectorChecked is true], demRegions: [regions whose _demChecked is true])` and `Navigator.pop(context, selection)`.

    Use hardcoded English string literals for the new copy (sheet title, intro, combined-size summary, "Downloading…" caption) — this matches the Phase 24 precedent ("[24-02] size-breakdown row text is a hardcoded English literal per the plan's own l10n scope, matching UI-SPEC copy verbatim"). Reuse the EXISTING l10n keys where they already exist: `regions_vector_tile_title`, `regions_dem_tile_title`, `download`, `regions_download_failed`. Do not add new ARB keys or touch the 14 locale files in this plan.

    Do NOT import `trail_download_state_provider.dart`, call `downloadVector`/`downloadDem`, or trigger any download from this file — the sheet only returns a selection (D-06/D-09). Wiring is Plan 03.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/components/trail/missing_coverage_sheet.dart</automated>
    <human-check>End-of-phase on-device (deferred to phase UAT): open the sheet on a trail with ≥1 missing region — confirm Vector pre-checked, DEM unchecked, sizes shown, DEM row absent when the region has no DEM archive, Download button enabled with 0 checked, and the combined-size line updates live and reads "Downloading trail only" at 0 selected.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'class MissingCoverageSelection' app/lib/components/trail/missing_coverage_sheet.dart` returns 1, and the class declares both `vectorRegions` and `demRegions` (`grep -c 'vectorRegions\|demRegions' ...` ≥ 2).
    - Source: `grep -c 'Future<MissingCoverageSelection?> showMissingCoverageSheet' app/lib/components/trail/missing_coverage_sheet.dart` returns 1.
    - Source: `grep -c 'ConsumerStatefulWidget' app/lib/components/trail/missing_coverage_sheet.dart` returns 1 (sheet watches `tileRepositoryStatusProvider`).
    - Source: DEM checkbox is gated — `grep -c 'region.demUrl != null' app/lib/components/trail/missing_coverage_sheet.dart` ≥ 1.
    - Source: the `FilledButton` onPressed is not gated on selection — `grep -c 'formatBytes' app/lib/components/trail/missing_coverage_sheet.dart` ≥ 1 and the file contains no `onPressed: .*isNotEmpty` selection guard on the download button.
    - Source: the sheet does NOT import or call the download engines — `grep -c 'downloadVector\|downloadDem\|trail_download_state_provider' app/lib/components/trail/missing_coverage_sheet.dart` returns 0.
    - `flutter analyze` on the file reports no errors.
  </acceptance_criteria>
  <done>
    `showMissingCoverageSheet` returns a `MissingCoverageSelection?` (null on dismiss), lists each missing region with Vector (+ gated DEM) checkboxes and sizes, defaults Vector-on/DEM-off, keeps Download always enabled, and shows a live combined-size summary — with no download side effects. GUARD-02/GUARD-03's UI surface is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local region catalog snapshot → sheet render | The sheet only reads already-validated local `RegionEntity` data and renders it; it introduces no new external input path and performs no download side effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 (surfaced here, mitigated in Plan 03) | Information disclosure (false confidence) | Missing-coverage sheet | accept | The sheet is presentational; the D-04 "no region at all" non-blocking warning is a Plan 03 concern. The sheet itself makes the escape hatch explicit ("Downloading trail only" at 0 selected) so the user is never misled into thinking a subset selection equals full coverage. |

No new auth/session/crypto surface (ASVS V2/V3/V6 N/A). No package installs → no supply-chain threat. No high-severity findings.
</threat_model>

<verification>
- `cd app && flutter analyze lib/components/trail/missing_coverage_sheet.dart lib/services/download_notification_service.dart` — no errors.
- Runtime sheet behavior verified at end-of-phase on-device UAT (human-verify_mode is end-of-phase).
</verification>

<success_criteria>
- `MissingCoverageSelection` + `showMissingCoverageSheet` exist and compile; the sheet lists regions with Vector/DEM checkboxes, sizes, Vector-on/DEM-off defaults, an always-enabled Download button, and a live combined-size summary (GUARD-02/GUARD-03).
- `showAggregateProgress` exists on the notification service without altering the existing `showProgress` path (D-10 prerequisite).
- Neither file wires into the download flow (that is Plan 03).
</success_criteria>

<output>
Create `.planning/phases/26-trail-download-guard/26-02-SUMMARY.md` when done.
</output>
