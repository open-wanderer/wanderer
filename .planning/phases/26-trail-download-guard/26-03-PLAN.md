---
phase: 26-trail-download-guard
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - app/lib/provider/trail/trail_download_state_provider.dart
autonomous: true
requirements: [GUARD-01, GUARD-02, GUARD-03, GUARD-04]
must_haves:
  truths:
    - "Tapping download on a trail whose overlapping regions are all downloaded/updateAvailable starts the download immediately, unchanged from today (GUARD-01)"
    - "When overlapping regions are missing, the guard shows the missing-coverage sheet; dismissing it aborts with no download at all; tapping Download proceeds"
    - "Selected region Vector/DEM packages start downloading in parallel with the trail, fire-and-forget, from one Download tap (D-08/D-09)"
    - "A trail bbox that overlaps NO catalog region surfaces a non-blocking warning toast and still lets the trail download proceed (D-04)"
    - "When 1+ region packages are included, one unified id-42 notification aggregates the trail and every selected package into a single progress bar (D-10); the 0-region path uses the unchanged per-trail notification (GUARD-01)"
  artifacts:
    - path: "app/lib/provider/trail/trail_download_state_provider.dart"
      provides: "Coverage guard + parallel region-download orchestration + unified aggregate notification inside DownloadingTrailIds.download()"
      contains: "missingCoverageRegions"
  key_links:
    - from: "app/lib/provider/trail/trail_download_state_provider.dart"
      to: "app/lib/provider/region/region_provider.dart"
      via: "ref.read(regionListNotifierProvider) — local snapshot only, no refreshCatalog (D-11)"
      pattern: "regionListNotifierProvider"
    - from: "app/lib/provider/trail/trail_download_state_provider.dart"
      to: "app/lib/components/trail/missing_coverage_sheet.dart"
      via: "showMissingCoverageSheet via navigatorKey.currentContext"
      pattern: "showMissingCoverageSheet"
    - from: "app/lib/provider/trail/trail_download_state_provider.dart"
      to: "app/lib/provider/region/tile_repository_provider.dart"
      via: "downloadVector/downloadDem per selected package"
      pattern: "downloadVector|downloadDem"
    - from: "app/lib/provider/trail/trail_download_state_provider.dart"
      to: "app/lib/services/download_notification_service.dart"
      via: "showAggregateProgress for the unified bar"
      pattern: "showAggregateProgress"
---

<objective>
Wire the coverage guard into the single shared trail-download entry point `DownloadingTrailIds.download(trail)`, so both existing call sites (detail-screen button, dropdown item) inherit it with no call-site change. This plan ties Plan 01's coverage decision and Plan 02's sheet + aggregate notification into the real download flow: check coverage locally, branch (unchanged / warn-and-proceed / show sheet), start the trail plus any selected region packages together, and drive one unified progress notification.

Purpose: This is the phase's integration — GUARD-01/02/03/04 only become user-visible here. Keeping the guard inside the one shared notifier method (per CONTEXT discretion) guarantees both call sites are covered without duplication.
Output: A modified `DownloadingTrailIds.download()` implementing the full guard + parallel-download + unified-notification behavior.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-trail-download-guard/26-CONTEXT.md
@.planning/phases/26-trail-download-guard/26-RESEARCH.md
@.planning/phases/26-trail-download-guard/26-PATTERNS.md
@.planning/phases/26-trail-download-guard/26-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 03 scope + phase-wide consumed symbols)

This plan creates no new files. It CONSUMES symbols produced earlier this phase — plan-review source-grounding should treat these as this-phase artifacts, not external drift:

- From Plan 01: `overlappingRegions(...)`, `missingCoverageRegions(...)` (in `app/lib/util/trail_coverage_util.dart`)
- From Plan 02: `MissingCoverageSelection`, `showMissingCoverageSheet(...)` (in `app/lib/components/trail/missing_coverage_sheet.dart`); `DownloadNotificationService.showAggregateProgress(...)`
- Existing (pre-phase): `navigatorKey` (`app/lib/provider/router_provider.dart`), `regionListNotifierProvider` (`app/lib/provider/region/region_provider.dart`), `tileRepositoryStatusProvider` + `downloadVector`/`downloadDem` (`app/lib/provider/region/tile_repository_provider.dart`)

<tasks>

<task type="auto">
  <name>Task 1: Coverage guard entry, conditional sheet, D-04 warning</name>
  <files>app/lib/provider/trail/trail_download_state_provider.dart</files>
  <read_first>
    - app/lib/provider/trail/trail_download_state_provider.dart (FULL current file — the existing `download(Trail trail)` method, lines 22-86: the re-entry guard, the toast, `showProgress(trail.name, 0, 0)`, the try/catch/finally trail-download body, the trailing glyph-cache await)
    - app/lib/util/trail_coverage_util.dart (Plan 01 output — `overlappingRegions`, `missingCoverageRegions`)
    - app/lib/components/trail/missing_coverage_sheet.dart (Plan 02 output — `showMissingCoverageSheet`, `MissingCoverageSelection`)
    - app/lib/provider/region/region_provider.dart (lines 165-174 — `RegionListNotifier`/`regionListNotifierProvider`, the synchronous local snapshot; NEVER call `RegionRepository.refreshCatalog` here — D-11)
    - app/lib/main.dart (lines 202-241 — the `navigatorKey.currentContext` non-widget dialog-trigger precedent; reuse the null-check-then-use shape)
    - app/lib/provider/router_provider.dart (line 56 — `final navigatorKey = GlobalKey<NavigatorState>();`)
    - app/lib/provider/toast_provider.dart (ToastMessage/ToastType — for the D-04 warning toast)
  </read_first>
  <action>
    Modify `DownloadingTrailIds.download(Trail trail)` to insert the coverage guard immediately after the re-entry guard sets `state = {...state, trail.id};` and BEFORE the existing `trailDownloadService`/`notificationService`/`toastNotifier` reads.

    Guard logic (synchronous, D-11 local-only):
    1. Read the local catalog snapshot: `final regions = ref.read(regionListNotifierProvider);` — this is `RegionListNotifier`'s synchronous `getAll()` list. Never call `refreshCatalog()` / no network fetch on tap.
    2. Compute BOTH sets (Pitfall 4 / D-04): `final overlapping = overlappingRegions(trail, regions);` and `final missing = missingCoverageRegions(trail, regions);`.
    3. Branch:
       - `overlapping.isEmpty` → a genuine no-region gap (D-04): add a non-blocking warning toast via `toastProvider.notifier` — `ToastType.info`, `FontAwesomeIcons.triangleExclamation` (or the codebase's existing warning icon), text "Part of this trail isn't covered by any offered region." Then FALL THROUGH to start the trail download unchanged (never block). No sheet.
       - `overlapping.isNotEmpty && missing.isEmpty` → fully covered (GUARD-01): fall straight through to the existing trail-download body, no sheet, no extra toast.
       - `missing.isNotEmpty` → show the sheet: read `final ctx = navigatorKey.currentContext;`. If `ctx == null`, proceed with a trail-only download (never strand the user). Otherwise `final selection = await showMissingCoverageSheet(ctx, trail, missing);`. If `selection == null` (dismissed), abort the whole download: run `state = {...state}..remove(trail.id);` and `return;` — start NOTHING (matches the RESEARCH architecture diagram: dismiss → download() ABORTS). If `selection` is non-null (Download tapped, possibly with 0 checked), continue into the download body, passing the `selection` to Task 2's orchestration.

    Keep the sheet `await` OUTSIDE the existing `try/catch/finally` — a dismissed sheet is a clean abort, not an "error" (no notification, no error toast). The existing re-entry guard, the trail-download body, the success/error toasts, and the trailing glyph-cache await all remain; you are inserting the guard ahead of them and threading `selection` through to Task 2.

    Add the necessary imports (`trail_coverage_util.dart`, `missing_coverage_sheet.dart`, `region_provider.dart`, `router_provider.dart`). Do NOT store `navigatorKey.currentContext` on the notifier as a field — read it transiently at point of use only (anti-pattern flagged in RESEARCH).

    For this task, wire the branch skeleton so a fully-covered or no-region trail downloads exactly as today, and a missing-coverage trail shows the sheet and aborts cleanly on dismiss. Task 2 fills in what happens to `selection`'s checked packages and the unified notification.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/provider/trail/trail_download_state_provider.dart</automated>
    <human-check>End-of-phase on-device UAT: (a) a trail fully inside a downloaded region → tapping download starts immediately, no sheet (GUARD-01/GUARD-04, verify a merely-updateAvailable region also passes without a sheet); (b) a trail overlapping a not-downloaded region → sheet appears; swipe it down → nothing downloads; (c) a trail outside every configured region → orange/info warning toast, trail still downloads.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'regionListNotifierProvider' app/lib/provider/trail/trail_download_state_provider.dart` ≥ 1 and `grep -c 'refreshCatalog' app/lib/provider/trail/trail_download_state_provider.dart` returns 0 (D-11 local-only).
    - Source: both `overlappingRegions(` and `missingCoverageRegions(` are called (`grep -c 'overlappingRegions(\|missingCoverageRegions(' ...` ≥ 2) — the two-set distinction for D-04.
    - Source: `grep -c 'navigatorKey.currentContext' app/lib/provider/trail/trail_download_state_provider.dart` ≥ 1 and `showMissingCoverageSheet(` is called.
    - Source: a dismissed sheet aborts — the method contains a `return;` guarded by the sheet result being null, and it does NOT run `trailDownloadService.downloadTrail` in that branch.
    - Source: `navigatorKey.currentContext` is never assigned to an instance field (`grep -c '_context\s*=\|this.context\s*=' ...` returns 0).
    - `flutter analyze` on the file reports no errors.
  </acceptance_criteria>
  <done>
    `download()` checks local coverage before proceeding, distinguishes fully-covered / no-region-gap / missing, shows the sheet only when regions are missing, warns non-blockingly on a no-region gap, and aborts cleanly (nothing started) when the sheet is dismissed. Both call sites inherit this with no change. GUARD-01/GUARD-04 and the GUARD-02 trigger are in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parallel region downloads + unified aggregate notification</name>
  <files>app/lib/provider/trail/trail_download_state_provider.dart</files>
  <read_first>
    - app/lib/provider/trail/trail_download_state_provider.dart (the Task-1-modified method — you are now filling in `selection` handling and the notification aggregation)
    - app/lib/provider/region/tile_repository_provider.dart (`TileRepositoryStatus.downloadVector(regionId)`/`downloadDem(regionId)` — both `Future<void>`, already idempotent/re-entry-guarded; `tileRepositoryStatusProvider` state map + `RegionDownloadState.vectorProgress`/`demProgress` fractions)
    - app/lib/services/download_notification_service.dart (Plan 02's `showAggregateProgress(title, body, done, total)` + the untouched `showProgress`)
    - app/lib/models/region_download_state.dart (`RegionDownloadState` — `vectorProgress`/`demProgress` are nullable 0.0–1.0 fractions)
    - app/lib/entities/region_entity.dart (`vectorSize`/`demSize` catalog sizes for the aggregation weights)
    - .planning/phases/26-trail-download-guard/26-UI-SPEC.md (Copywriting Contract → unified notification title/body)
  </read_first>
  <action>
    Extend the `missing.isNotEmpty` (Download-tapped) branch to start the selected region packages and drive one unified notification (D-08/D-09/D-10).

    Starting selected packages (fire-and-forget, D-09 — never await before the trail download starts): for each region in `selection.vectorRegions` call `ref.read(tileRepositoryStatusProvider.notifier).downloadVector(region.id)`; for each region in `selection.demRegions` call `downloadDem(region.id)`. Capture each returned `Future<void>` in a list `regionFutures` (do not await them here). `downloadVector`/`downloadDem` are already idempotent and re-entry-guarded — do not add a second guard, and a package already downloading (checked-out edge case) is a safe no-op.

    Unified notification (D-10) — only when at least one package was selected (`selection.vectorRegions` or `selection.demRegions` non-empty):
    - Compute `itemCount = 1 + selection.vectorRegions.length + selection.demRegions.length` (the trail plus each selected package).
    - Keep a mutable `double lastTrailFraction = 0` in the method scope. In the trail download's `onProgress: (done, total)` callback, set `lastTrailFraction = total > 0 ? (done / total).clamp(0, 1) : 0` and then call an `updateAggregate()` closure (defined below) INSTEAD of the plain `showProgress(trail.name, ...)`.
    - `updateAggregate()` reads the live region progress: `final st = ref.read(tileRepositoryStatusProvider);` then for each selected vector region take `st[region.id]?.vectorProgress ?? 0.0`, and for each selected DEM region take `st[region.id]?.demProgress ?? 0.0`. `combined = (lastTrailFraction + sumOfSelectedFractions) / itemCount`. Call `notificationService.showAggregateProgress("Downloading offline content", "Downloading… ${(combined*100).round()}% · $itemCount items", (combined*100).round(), 100)`. (Fraction-normalized average, one unit per source — avoids mixing the trail's point-count with the regions' byte-counts; this is the aggregation approach D-10 left to the planner. Use the exact aggregate title/body copy from UI-SPEC's Copywriting Contract.)
    - Also drive `updateAggregate()` from region progress so the bar advances even between trail ticks: before starting the trail download, subscribe with `final sub = ref.listenManual(tileRepositoryStatusProvider, (_, __) => updateAggregate());` and close it (`sub.close()`) in the method's `finally`/terminal path. (This codebase already uses `ref.listenManual` — see the Phase quick-260712-pac note in STATE.md.)
    - Replace the initial `await notificationService.showProgress(trail.name, 0, 0);` with an initial `updateAggregate()` call for the aggregate path; keep `showProgress(trail.name, ...)` verbatim for the 0-region path.

    0-region / no-region / fully-covered paths (GUARD-01): leave the existing notification calls (`showProgress(trail.name, ...)`, `showGenerating`, `showSuccess`, `showError`) completely unchanged — no aggregation, no listener, byte-for-byte today's behavior. Gate all aggregation on "a package was selected."

    Terminal state (aggregate path): after the existing trail-download `try/catch/finally` body completes, best-effort `await Future.wait(regionFutures)` wrapped in its own `try/catch` (a region download failure must NOT fail or error the trail download — it surfaces through the region engine's own Settings/Regions status). Then close the `listenManual` subscription. Keep the existing `showSuccess(trail.name)`/`showError(trail.name)` terminal notification and the trail toasts as-is (UI-SPEC specifies aggregate copy only for the in-progress state; terminal copy is unchanged).

    Preserve the existing re-entry `state` bookkeeping and the trailing glyph-cache warm await exactly as they are.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/provider/trail/trail_download_state_provider.dart</automated>
    <human-check>End-of-phase on-device UAT: on a multi-region trail, check Vector for two regions + DEM for one, tap Download once → the trail and all three packages start downloading in parallel; the single system notification (id 42) shows "Downloading offline content" with one combined progress bar advancing (not one notification per download); Settings → Offline Maps shows the same regions downloading. Also confirm the 0-checked Download path starts the trail with the unchanged per-trail notification.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'downloadVector(\|downloadDem(' app/lib/provider/trail/trail_download_state_provider.dart` ≥ 2 (both selected-package triggers wired).
    - Source: `grep -c 'showAggregateProgress' app/lib/provider/trail/trail_download_state_provider.dart` ≥ 1 and the exact literal title `Downloading offline content` appears (`grep -c 'Downloading offline content' ...` ≥ 1).
    - Source: the aggregation is gated on a non-empty selection — the aggregate/notification-listener code is NOT reached on the 0-region path, and `showProgress(trail.name` still appears for the unchanged path (`grep -c 'showProgress(trail.name' ...` ≥ 1).
    - Source: `grep -c 'listenManual' app/lib/provider/trail/trail_download_state_provider.dart` ≥ 1 and the subscription is closed (`grep -c '.close()' ...` ≥ 1).
    - Source: region-download failures are isolated — `Future.wait(regionFutures)` (or equivalent) is wrapped so it cannot throw into the trail-download error path (`grep -c 'Future.wait' ...` ≥ 1, inside a try/catch).
    - `flutter analyze` on the file reports no errors.
  </acceptance_criteria>
  <done>
    One Download tap starts the trail plus every selected Vector/DEM package in parallel (fire-and-forget), and — only when packages were selected — a single id-42 notification aggregates all of them into one advancing progress bar with the UI-SPEC aggregate copy. The 0-region and fully-covered paths remain byte-for-byte today's behavior. GUARD-02/GUARD-03/D-08/D-09/D-10 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guard decision → user's offline-safety expectation | The guard's verdict shapes whether a user believes a trail is fully usable offline; a trail spanning a no-region gap must not create false confidence |
| Local catalog snapshot → download orchestration | `ref.read(regionListNotifierProvider)` returns already-validated local rows; no new network/external input path is added (D-11 local-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Information disclosure (false offline-safety confidence) | `download()` no-region-gap branch | mitigate | D-04 non-blocking warning toast ("Part of this trail isn't covered by any offered region") fires whenever `overlappingRegions.isEmpty`, and the download still proceeds — never a silent full-offline claim, never a hard block. This is a UX-correctness control, not a security vuln (ASVS out of scope), addressed by design. |
| T-26-01 (mitigated in Plan 01) | Tampering | coverage read consumed here | mitigate | Consumes Plan 01's degenerate-bbox-safe `missingCoverageRegions`; a corrupted region row drops out rather than corrupting the verdict. |

No new auth/session/crypto surface. No package installs → no supply-chain threat. No high-severity findings.
</threat_model>

<verification>
- `cd app && flutter analyze lib/provider/trail/trail_download_state_provider.dart` — no errors.
- Full guard behavior (fully-covered / no-region-warn / sheet / parallel downloads / unified notification) verified at end-of-phase on-device UAT (human-verify_mode is end-of-phase).
</verification>

<success_criteria>
- GUARD-01: fully-covered trail downloads immediately, unchanged; no-region-gap trail warns and proceeds.
- GUARD-02/GUARD-03: missing coverage shows the sheet; the user can download any subset (including none — trail-only) from one Download tap; multi-region trails list all missing regions.
- GUARD-04: `updateAvailable` never re-fires the guard (enforced by Plan 01's filter, consumed here).
- D-08/D-09: one tap starts the trail + selected packages in parallel, fire-and-forget; dismiss aborts everything.
- D-10: one unified id-42 notification aggregates trail + selected packages when any package is selected; the 0-region path is unchanged.
- D-11: coverage read is local-only; no `refreshCatalog`/network on the download tap.
</success_criteria>

<output>
Create `.planning/phases/26-trail-download-guard/26-03-SUMMARY.md` when done.
</output>
