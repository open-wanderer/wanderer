---
phase: 02-follow-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/1782290001_add_rejected_to_follows_status.go
autonomous: true
requirements: [FLCL-04]
must_haves:
  truths:
    - "The follows.status select field accepts the value \"rejected\" without a validation error"
    - "Saving a follows record with status=\"rejected\" succeeds"
  artifacts:
    - path: "db/migrations/1782290001_add_rejected_to_follows_status.go"
      provides: "Migration adding \"rejected\" to follows.status select values"
      contains: "8obn1ukumze565i"
  key_links:
    - from: "db/migrations/1782290001_add_rejected_to_follows_status.go"
      to: "follows collection status field (select2063623452)"
      via: "SelectField.Values append"
      pattern: "rejected"
---

<objective>
Add the `"rejected"` value to the `follows.status` select field via a PocketBase migration. This is the schema prerequisite for FLCL-04: without it, any code that calls `Set("status", "rejected")` and saves will fail PocketBase select-field validation, silently breaking the Reject{Follow} delivery path implemented in Plan 03.

Purpose: The `follows.status` select field currently allows only `["pending", "accepted"]` (verified in `db/migrations/1747064968_collections_snapshot.go` field `select2063623452`). The Reject lifecycle (D-08, mandatory) requires a third allowed value.
Output: A new timestamp-prefixed migration file that appends `"rejected"` to the status select field's allowed values, with a reversible down-migration.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-follow-lifecycle/02-RESEARCH.md
@.planning/phases/02-follow-lifecycle/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration adding "rejected" to follows.status</name>
  <files>db/migrations/1782290001_add_rejected_to_follows_status.go</files>
  <read_first>
    - db/migrations/1782290000_add_actor_type_to_activitypub_actors.go (analog: m.Register up/down structure, package name, imports)
    - db/migrations/1747064968_collections_snapshot.go lines 100-162 (follows collection id `8obn1ukumze565i`, status field id `select2063623452`, current values `["pending","accepted"]`)
  </read_first>
  <action>
    Create a new migration file `db/migrations/1782290001_add_rejected_to_follows_status.go` in package `migrations`. Use timestamp `1782290001` (one greater than the latest existing migration `1782290000_add_actor_type_to_activitypub_actors.go`).

    Follow the exact `m.Register(upFunc, downFunc)` structure from the analog migration. Import `"github.com/pocketbase/pocketbase/core"`, `m "github.com/pocketbase/pocketbase/migrations"`, and `"fmt"`.

    UP function: call `app.FindCollectionByNameOrId("8obn1ukumze565i")` to load the follows collection. Get the status field via `collection.Fields.GetByName("status")`; if nil, return `fmt.Errorf("status field not found in follows collection")`. Type-assert it to `*core.SelectField`; if the assertion fails, return `fmt.Errorf("status field is not a SelectField")`. Append `"rejected"` to `selectField.Values` only if not already present (guard against double-apply: iterate existing values, skip append if `"rejected"` found). Then `return app.Save(collection)`.

    DOWN function: load the same collection, get the status field, type-assert to `*core.SelectField`, and rebuild `selectField.Values` excluding `"rejected"`, then `return app.Save(collection)`. Return nil early if the field or assertion is not found (rollback must be tolerant).

    Do NOT modify the snapshot migration `1747064968_collections_snapshot.go`. Do NOT change the field id (`select2063623452`) or any other field property — only the `Values` slice changes.
  </action>
  <verify>
    <automated>cd db && go build ./... && go vet ./migrations/</automated>
    <automated>cd db && grep -v '^[[:space:]]*//' migrations/1782290001_add_rejected_to_follows_status.go | grep -c '8obn1ukumze565i'</automated>
  </verify>
  <acceptance_criteria>
    - File `db/migrations/1782290001_add_rejected_to_follows_status.go` exists and declares `package migrations`
    - File contains `m.Register(` with both an up and a down function argument
    - File references the follows collection id `8obn1ukumze565i`
    - File contains the string `"rejected"` appended to `selectField.Values`
    - File type-asserts the status field to `*core.SelectField`
    - `cd db && go build ./...` exits 0
    - `cd db && go vet ./migrations/` exits 0
  </acceptance_criteria>
  <done>The migration compiles, references the correct collection id and status field, appends "rejected" idempotently in the up function, and removes it in the down function.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols / files created by this plan:
- `db/migrations/1782290001_add_rejected_to_follows_status.go` — new migration file
- An anonymous `func init()` calling `m.Register(...)` (PocketBase migration registration; not exported)

Schema change:
- `follows.status` select field (id `select2063623452`, collection `8obn1ukumze565i`) gains the allowed value `"rejected"` (values become `["pending","accepted","rejected"]`)

No new exported Go functions, types, routes, or hooks are introduced by this plan.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → SQLite schema | Migration mutates the follows collection schema at startup; runs with full DB privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | follows.status select migration | mitigate | Migration only appends a value to an existing select field; does not drop columns or data. Idempotent guard prevents duplicate values on re-run. Down-migration is reversible. |
| T-02-02 | Denial of Service | startup migration | accept | If the migration fails, PocketBase startup aborts (fail-closed). Acceptable: a broken migration is caught at deploy time, not at request time. |
| T-02-SC | Tampering | go module installs | mitigate | No new packages installed. All imports (`pocketbase/core`, `pocketbase/migrations`) already in go.mod per RESEARCH.md Package Legitimacy Audit. |
</threat_model>

<verification>
- `cd db && go build ./...` passes
- `cd db && go vet ./migrations/` passes
- The migration file exists at the expected path and references collection `8obn1ukumze565i` and the value `"rejected"`
</verification>

<success_criteria>
- A new migration adds `"rejected"` to the `follows.status` select field's allowed values
- After this migration runs, `app.Save()` of a follows record with `status="rejected"` will not fail select-field validation (validated end-to-end in Plan 03)
- The down-migration removes `"rejected"`, restoring `["pending","accepted"]`
</success_criteria>

<output>
Create `.planning/phases/02-follow-lifecycle/02-01-SUMMARY.md` when done
</output>
