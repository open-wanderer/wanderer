---
phase: 21-route-planner-handoff-entry-point
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/models/settings.dart
  - app/lib/entities/settings_entity.dart
autonomous: true
requirements: [HANDOFF-03]
must_haves:
  truths:
    - "Settings exposes a nullable behavior field carrying allowAutoGeolocate, mirroring web's Settings.behavior (D-03)"
    - "allowAutoGeolocate persists across app restarts via a SettingsEntity behaviorJson blob, using the same encode/decode strategy as privacyJson (Pitfall 4)"
    - "A Settings round-trip through fromJson/toModel/toModel preserves behavior — absent behavior resolves to a null field, so an unset allowAutoGeolocate never reads as true (privacy default-to-false, A2)"
  artifacts:
    - path: "app/lib/models/settings.dart"
      provides: "Behavior freezed class + Settings.behavior field"
      contains: "class Behavior"
    - path: "app/lib/entities/settings_entity.dart"
      provides: "behaviorJson persistence field + fromModel encode + toModel decode"
      contains: "behaviorJson"
  key_links:
    - from: "app/lib/models/settings.dart"
      to: "Behavior.allowAutoGeolocate"
      via: "nullable bool field consumed by Plan 03's GPS gate"
      pattern: "allowAutoGeolocate"
    - from: "app/lib/entities/settings_entity.dart"
      to: "SettingsEntity.behaviorJson"
      via: "jsonEncode on fromModel / jsonDecode on toModel, mirroring privacyJson"
      pattern: "behaviorJson"
---

<objective>
Port the `Behavior` nested type (with `allowAutoGeolocate`) from the web `Settings`
model onto the Flutter `Settings` freezed model and its ObjectBox `SettingsEntity`,
closing the standing gap flagged in the 2026-07-16 memory note (D-03, SCOPE ADD).
This is the data-layer prerequisite for Plan 03's GPS gate at planner entry — the
planner only auto-centers on a GPS fix when `Settings.behavior?.allowAutoGeolocate`
is `true`, otherwise it uses a non-GPS fallback.

Scope is strictly the model field + entity persistence + a round-trip regen — NO
settings-screen toggle UI (D-03 explicitly: "no new toggle UI required").

Purpose: make the opt-in geolocation preference readable and persistable on the
Flutter side, matching web's behavior exactly.
Output: `Behavior` freezed class + `Settings.behavior` field; `SettingsEntity.behaviorJson`
encode/decode; regenerated codegen.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-route-planner-handoff-entry-point/21-CONTEXT.md
@.planning/phases/21-route-planner-handoff-entry-point/21-RESEARCH.md
@.planning/phases/21-route-planner-handoff-entry-point/21-PATTERNS.md
</context>

<artifacts_produced>
Symbols this plan creates (exclude from source-grounding drift checks):
- `Behavior` — new freezed class in `app/lib/models/settings.dart` with `bool? allowAutoGeolocate`, `int? mapClusteringMaxZoom`, `bool? showTrailStartMarker`
- `Settings.behavior` — new `Behavior? behavior` field on the `Settings` freezed class
- `SettingsEntity.behaviorJson` — new `String? behaviorJson` field + constructor param
- Regenerated: `app/lib/models/settings.freezed.dart`, `app/lib/models/settings.g.dart`, `app/lib/objectbox.g.dart`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add Behavior freezed class + Settings.behavior field + SettingsEntity.behaviorJson, then regenerate codegen (D-03)</name>
  <files>app/lib/models/settings.dart, app/lib/entities/settings_entity.dart</files>
  <read_first>
    - app/lib/models/settings.dart (the file being modified — the `SettingsPrivacy` freezed class at lines 70-80 is the exact shape template for the new `Behavior` class: `@freezed abstract class ... with _$... { const factory ...; factory ...fromJson(...) => _$...FromJson(json); }`; and the `Settings` class at lines 93-108 where the `behavior` field is added alongside `privacy`/`notifications`; note the `part 'settings.freezed.dart';` + `part 'settings.g.dart';` directives at the top)
    - app/lib/entities/settings_entity.dart (the file being modified — `privacyJson` is the exact template: the `String? privacyJson` field (line 20), the constructor param (line 30), the `fromModel` encode `privacyJson: settings.privacy != null ? jsonEncode(settings.privacy!.toJson()) : null` (lines 51-53), and the `toModel` decode `SettingsPrivacy? priv; if (privacyJson != null) { priv = SettingsPrivacy.fromJson(jsonDecode(privacyJson!) as Map<String, dynamic>); }` (lines 79-84) plus its inclusion in the returned `Settings(...)`)
    - web/src/lib/models/settings.ts (lines 55-59 — the `Behavior` type: `allowAutoGeolocate: boolean`, `mapClusteringMaxZoom?: number`, `showTrailStartMarker?: boolean` — the exact field set to mirror so a settings JSON round-trip through the Flutter model never drops the two optional siblings)
    - web/src/lib/models/api/settings_schema.ts (lines 25-29 — confirms `behavior` is `.optional().nullable()` and its three fields; `allowAutoGeolocate` is required within the object, the other two optional)
    - .planning/phases/21-route-planner-handoff-entry-point/21-RESEARCH.md (Pitfall 4 — the freezed+objectbox regen requirement and the exact JSON-blob-per-field storage strategy to replicate; A2 — default-to-false privacy convention)
  </read_first>
  <action>
    Add a `Behavior` freezed class to `settings.dart`, mirroring `SettingsPrivacy`'s exact structure. Fields (mirroring web's `Behavior` type verbatim so a round-trip never silently drops a sibling): `bool? allowAutoGeolocate`, `int? mapClusteringMaxZoom`, `bool? showTrailStartMarker` — all nullable (Dart has no non-null-with-default freezed factory need here; a null `allowAutoGeolocate` reads as "not opted in", matching web's `?? false`, A2). Add its `factory Behavior.fromJson(Map<String, dynamic> json) => _$BehaviorFromJson(json);`.

    Add `Behavior? behavior` to the `Settings` factory constructor (place it next to `privacy`/`notifications` in the field list). Do not reorder or change any existing field.

    In `settings_entity.dart`: add `String? behaviorJson` as a field (next to `privacyJson`) and as a named constructor param (`this.behaviorJson`). In `fromModel`, add `behaviorJson: settings.behavior != null ? jsonEncode(settings.behavior!.toJson()) : null` (identical shape to `privacyJson`). In the `toModel` extension, decode it exactly like `priv`: declare `Behavior? behav; if (behaviorJson != null) { behav = Behavior.fromJson(jsonDecode(behaviorJson!) as Map<String, dynamic>); }` and pass `behavior: behav` into the returned `Settings(...)`. Do NOT use a flattened boolean column — the JSON-blob-per-field strategy is required (Pitfall 4).

    After editing both files, regenerate all codegen with `cd app && dart run build_runner build --delete-conflicting-outputs` — this refreshes `settings.freezed.dart`, `settings.g.dart` (freezed + json_serializable for `Behavior`/`Settings`), and `objectbox.g.dart` (the new `behaviorJson` column). Do not hand-edit the generated files.
  </action>
  <verify>
    <automated>cd app && dart run build_runner build --delete-conflicting-outputs && flutter analyze lib/models/settings.dart lib/entities/settings_entity.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class Behavior" app/lib/models/settings.dart` returns a match, and `grep -n "allowAutoGeolocate" app/lib/models/settings.dart` returns a match (nullable bool field).
    - `grep -n "Behavior? behavior" app/lib/models/settings.dart` returns a match (field on Settings).
    - `grep -c "behaviorJson" app/lib/entities/settings_entity.dart` returns at least 3 (field declaration + fromModel encode + toModel decode).
    - Source assertion: `settings_entity.dart` decodes via `Behavior.fromJson(jsonDecode(behaviorJson!)` (mirrors privacyJson), NOT a flattened bool column — `grep -n "Behavior.fromJson(jsonDecode(behaviorJson" app/lib/entities/settings_entity.dart` returns a match.
    - `grep -n "_\$BehaviorFromJson\|allowAutoGeolocate" app/lib/models/settings.g.dart` confirms codegen picked up the new class (regen ran).
    - `cd app && flutter analyze lib/models/settings.dart lib/entities/settings_entity.dart` reports no errors (no missing `behavior` getter / stale generated file).
  </acceptance_criteria>
  <done>Settings.behavior?.allowAutoGeolocate exists, round-trips through JSON and ObjectBox via a behaviorJson blob, and the whole app analyzes clean after regen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ObjectBox local store → app | `behaviorJson` is read from the device's local SettingsEntity row on cold start; the gate value it carries governs whether GPS auto-resolves (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Information Disclosure | privacy default of the new geolocation gate | mitigate | `allowAutoGeolocate` is nullable and read as opt-in — a null/absent value never resolves to `true`, so GPS never auto-centers for a user who never opted in on web (A2 mirrors web's `?? false`). This is the exact ASVS V8 mitigation for silent GPS auto-centering (RESEARCH Known Threat Patterns) |
| T-21-02-02 | Tampering | `behaviorJson` blob in the local ObjectBox store | accept | Single-user on-device store; the value is a typed `bool?` after freezed decode (V5 input validation is compile-time), and it only gates a local map-camera decision — no cross-user or server-trust surface |
| T-21-02-SC | Tampering | package installs | accept | No installs — reuses `freezed_annotation`, `json_annotation`, `objectbox`/`objectbox_generator`, all already in `pubspec.yaml` (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd app && dart run build_runner build --delete-conflicting-outputs` completes with no errors.
- `cd app && flutter analyze` (whole-app) reports no NEW errors — the new `behavior` field and `behaviorJson` column resolve everywhere the generated `Settings`/`SettingsEntity` are used.
- `cd app && flutter test` shows no NEW failures beyond the 3 pre-existing unrelated failures logged in Phase 18's deferred-items.md.
</verification>

<success_criteria>
- `Settings.behavior?.allowAutoGeolocate` is a readable nullable bool mirroring web's `Behavior` type (D-03).
- The value persists across restarts via `SettingsEntity.behaviorJson`, using privacyJson's exact encode/decode strategy.
- No settings-screen toggle UI is added (D-03 scope respected); codegen is regenerated and the app builds clean.
</success_criteria>

<output>
Create `.planning/phases/21-route-planner-handoff-entry-point/21-02-SUMMARY.md` when done.
</output>
