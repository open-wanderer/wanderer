---
phase: 21-route-planner-handoff-entry-point
plan: 04
type: execute
wave: 3
depends_on: [21-01, 21-03]
files_modified:
  - app/lib/routes/route_planner_screen.dart
autonomous: true
requirements: [HANDOFF-01]
must_haves:
  truths:
    - "The app bar shows a single Finish action (check icon) in the slot undo/redo previously occupied; undo/redo relocate into the top-right map controls Column below the auto-routing toggle (D-04)"
    - "Finish is enabled only with >=2 route anchors; below that it is disabled and its long-press tooltip explains why (D-05)"
    - "Tapping Finish runs finishPlanning — the one-time elevation merge + GPX-track-only draft-Trail handoff to /trail/create/edit (HANDOFF-01)"
  artifacts:
    - path: "app/lib/routes/route_planner_screen.dart"
      provides: "app-bar Finish action + undo/redo pills relocated to controls Column + _onFinish handler"
      contains: "finishPlanning"
  key_links:
    - from: "app/lib/routes/route_planner_screen.dart"
      to: "finishPlanning(ref, navContext, travelProfile)"
      via: "Finish IconButton onPressed (gated on anchors.length >= 2)"
      pattern: "finishPlanning"
    - from: "app/lib/routes/route_planner_screen.dart"
      to: "notifier.undo / notifier.redo"
      via: "undo/redo pills in the top-right controls Column"
      pattern: "FontAwesomeIcons.arrowRotateLeft"
---

<objective>
Complete HANDOFF-01's user-facing trigger: add a "Finish planning" app-bar action
to `RoutePlannerScreen` and relocate undo/redo out of the app bar into the top-right
map controls Column (D-04). Finish is gated on ≥2 route anchors (D-05) and, when
tapped, invokes Plan 01's `finishPlanning` to hand the route off as a draft Trail.

This is the final wire of the v1.5 milestone — the planner can now leave with a
result instead of being a dead-end. It touches exactly one file and adds no new
gesture/segment logic (Phase 19/20 wiring is untouched).

Purpose: Give the user the button that closes the plan→create loop.
Output: modified `route_planner_screen.dart` (Finish action, undo/redo relocation,
`_onFinish` handler).
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-route-planner-handoff-entry-point/21-CONTEXT.md
@.planning/phases/21-route-planner-handoff-entry-point/21-RESEARCH.md
@.planning/phases/21-route-planner-handoff-entry-point/21-PATTERNS.md
@.planning/phases/21-route-planner-handoff-entry-point/21-UI-SPEC.md
</context>

<artifacts_produced>
Symbols this plan creates (exclude from source-grounding drift checks):
- `_onFinish() → Future<void>` — new async handler in `_RoutePlannerScreenState` (re-entrancy-guarded via a new `bool _finishing` field)
- `_buildFinishAction(RouteAnchorsState state, AppLocalizations l10n) → Widget` — new app-bar action builder
- `_buildUndoButton(RouteAnchorsState state) → Widget`, `_buildRedoButton(RouteAnchorsState state) → Widget` — new pill builders in the controls Column (mirroring `_buildAutoRoutingToggle`'s styling)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add Finish app-bar action, relocate undo/redo to the controls Column, wire finishPlanning (D-04/D-05, HANDOFF-01)</name>
  <files>app/lib/routes/route_planner_screen.dart</files>
  <read_first>
    - app/lib/routes/route_planner_screen.dart (the file being modified — the `AppBar.actions` list at lines 144-162 currently holds the two undo/redo `IconButton`s reading `state.undoStack`/`state.redoStack` and calling `notifier.undo`/`notifier.redo` (this is what Finish replaces + these move); `_buildMap`'s top-right controls `Positioned(top: 128, right: 0)` at lines 265-269 currently holds only `_buildAutoRoutingToggle(state.autoRoutingEnabled)`; `_buildAutoRoutingToggle` at lines 340-366 is the exact pill styling to reuse for undo/redo — `Padding(all: 8.0)` → `Container(color: canvasColor, borderRadius: circular(24))` → `IconButton(visualDensity: compact, ...)` with accent-primary enabled tint (`Color(0xff3E435B)` dark / `Color(0xff242734)` light) and `.4`-alpha disabled tint; `build()` already computes `state` and `notifier`; the class-level non-static-field caution comment at lines 51-60)
    - app/lib/util/route_planner_handoff_util.dart (Plan 01 — `finishPlanning({required WidgetRef ref, required BuildContext navContext, required String travelProfile})`)
    - .planning/phases/21-route-planner-handoff-entry-point/21-UI-SPEC.md (App Bar Finish Action & Undo/Redo Relocation Contract — Finish is `FontAwesomeIcons.check` size 18, `IconButton.styleFrom(backgroundColor: colorScheme.surface)`, enabled tint accent-primary, disabled = standard ~38% opacity with `onPressed: null`; undo/redo keep their exact Phase 19 pill spec unchanged, only position moves, stacking Auto-routing → Undo → Redo top-to-bottom; Copywriting Contract — enabled tooltip `l10n.finish`, disabled tooltip `l10n.finish_disabled_hint`)
    - .planning/phases/21-route-planner-handoff-entry-point/21-CONTEXT.md (D-04 undo/redo relocation was the user's own request; D-05 ≥2-anchor gate; RESEARCH Pitfall 3 — keep any new wrapper widget's fields non-static)
  </read_first>
  <action>
    Four additive edits to `route_planner_screen.dart`, none touching the existing gesture/segment/`onEvent` wiring:

    (1) Add `import 'package:wanderer/util/route_planner_handoff_util.dart';` and `import 'package:wanderer/i18n/app_localizations.dart';`. Add a `bool _finishing = false;` instance field (non-static — Pitfall 3). In `build()`, add `final l10n = AppLocalizations.of(context)!;`.

    (2) Replace the two undo/redo `IconButton`s in `AppBar.actions` with a single Finish action built by `_buildFinishAction(state, l10n)`: an `IconButton(icon: const FaIcon(FontAwesomeIcons.check, size: 18), style: IconButton.styleFrom(backgroundColor: Theme.of(context).colorScheme.surface), tooltip: state.anchors.length >= 2 ? l10n.finish : l10n.finish_disabled_hint, onPressed: (state.anchors.length >= 2 && !_finishing) ? _onFinish : null)`. The disabled state (null `onPressed`) uses Flutter's standard ~38%-opacity treatment; the `finish_disabled_hint` tooltip is available on long-press even while disabled (D-05, UI-SPEC — no toast/snackbar).

    (3) Add `Future<void> _onFinish()`: guard `if (_finishing) return;`, `setState(() => _finishing = true);`, then `try { await finishPlanning(ref: ref, navContext: context, travelProfile: widget.travelProfile); } finally { if (mounted) setState(() => _finishing = false); }`. The `_finishing` guard prevents a double-tap from firing two `/valhalla/height` fetches or two navigations while the fetch is in flight.

    (4) In `_buildMap`, change the top-right controls `Positioned(top: 128, right: 0)` child from the single `_buildAutoRoutingToggle(...)` into a `Column(mainAxisSize: MainAxisSize.min, children: [ _buildAutoRoutingToggle(state.autoRoutingEnabled), _buildUndoButton(state), _buildRedoButton(state) ])` (stacking order Auto-routing → Undo → Redo, D-04). Add `_buildUndoButton(RouteAnchorsState state)` and `_buildRedoButton(RouteAnchorsState state)` that return the identical pill container as `_buildAutoRoutingToggle` (Padding all 8 → canvasColor Container radius 24 → `IconButton(visualDensity: compact)`) with `FaIcon(FontAwesomeIcons.arrowRotateLeft, size: 18)` / `arrowRotateRight`, accent-primary enabled tint / `.4`-alpha disabled tint, tooltip `'Undo'`/`'Redo'`, and `onPressed: state.undoStack.isNotEmpty ? ref.read(routeAnchorsProvider(widget.travelProfile).notifier).undo : null` (redo analogously on `state.redoStack` / `.redo`). Update the stale controls-Column comment (lines 260-264) to note undo/redo now live here per D-04 (Finish took the app-bar slot), replacing the "only the auto-routing toggle remains here" wording.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/route_planner_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FontAwesomeIcons.check" app/lib/routes/route_planner_screen.dart` returns a match (Finish icon).
    - `grep -n "finishPlanning(ref: ref, navContext: context, travelProfile: widget.travelProfile)" app/lib/routes/route_planner_screen.dart` returns a match.
    - `grep -c "anchors.length >= 2" app/lib/routes/route_planner_screen.dart` returns at least 2 (tooltip choice + onPressed gate — D-05).
    - `grep -n "l10n.finish_disabled_hint" app/lib/routes/route_planner_screen.dart` returns a match (disabled tooltip).
    - Source assertion: undo/redo relocated into the controls Column — `grep -n "FontAwesomeIcons.arrowRotateLeft" app/lib/routes/route_planner_screen.dart` and `grep -n "FontAwesomeIcons.arrowRotateRight" app/lib/routes/route_planner_screen.dart` both return a match, and a read confirms they appear inside the `Positioned(top: 128, right: 0)` Column (not in `AppBar.actions`), with `_buildFinishAction`/the check icon in `AppBar.actions`.
    - `grep -n "bool _finishing" app/lib/routes/route_planner_screen.dart` returns a match, and `grep -c "static " app/lib/routes/route_planner_screen.dart` remains 0 (Pitfall 3 — no new static fields).
    - `cd app && flutter analyze lib/routes/route_planner_screen.dart` reports no errors.
  </acceptance_criteria>
  <done>The app bar shows a ≥2-anchor-gated Finish action wired to finishPlanning; undo/redo work identically from their new home in the top-right controls Column; the file analyzes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finish tap → handoff sequence | A user gesture triggers the (Plan 01) network fetch + navigation; the only new surface here is the trigger + its re-entrancy control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Denial of Service | double-tap Finish firing multiple `/valhalla/height` fetches / navigations | mitigate | The `_finishing` re-entrancy guard plus the `!_finishing` `onPressed` condition disable the button while a handoff is in flight, so exactly one fetch/navigation runs per completed tap |
| T-21-04-02 | Tampering | handoff to the create/edit screen | accept | Reuses Plan 01's `pendingImportedTrail` + the existing `/trail/create/edit` route builder (unchanged); actual persistence is server-validated via the existing `/trail/form` path — no new bypass introduced by adding the trigger |
| T-21-04-03 | Information Disclosure | Finish enabled below the intended anchor count | mitigate | The `anchors.length >= 2` gate (D-05) is enforced both in the tooltip choice and the `onPressed` condition, and `finishPlanning` itself re-checks `points.length < 2` (Plan 01) — a disabled Finish cannot hand off a degenerate single-point route |
| T-21-04-SC | Tampering | package installs | accept | No installs — reuses `font_awesome_flutter`, `flutter_riverpod`, `go_router`, all already in `pubspec.yaml` (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd app && flutter analyze lib/routes/route_planner_screen.dart` — no errors.
- `cd app && flutter analyze` (whole-app) reports no NEW errors from this plan.
- HUMAN (end-of-phase, on device): open the planner via the entry point; with <2 anchors confirm Finish is disabled and long-press shows "Add at least 2 anchors to finish your route."; add 2+ anchors, confirm undo/redo work from the top-right controls column, then tap Finish → confirm the trail create/edit screen opens pre-filled with the planned track (map preview renders), the category is pre-selected to a hike/bike category, and the waypoints section shows its normal empty state; save and confirm the saved trail has a real track (not zero-distance — Pitfall 1 guard).
</verification>

<success_criteria>
- A Finish app-bar action (check icon) appears where undo/redo used to be; undo/redo now live in the top-right controls Column and still function (D-04).
- Finish is disabled below 2 anchors with an explanatory tooltip and enabled at ≥2 (D-05).
- Tapping Finish hands the route off as a draft Trail to the create/edit screen (HANDOFF-01).
</success_criteria>

<output>
Create `.planning/phases/21-route-planner-handoff-entry-point/21-04-SUMMARY.md` when done.
</output>
