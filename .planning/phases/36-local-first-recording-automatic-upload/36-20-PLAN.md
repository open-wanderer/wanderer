---
phase: 36-local-first-recording-automatic-upload
plan: 20
type: execute
wave: 5
depends_on: [36-17, 36-18, 36-19]
gap_closure: true
autonomous: true
requirements: [REC-05, SYNC-04, SYNC-05]
files_modified:
  - app/test/routes/trail_create_screen_local_save_gate_test.dart
  - app/test/store/local_trail_retirement_gate_test.dart
  - app/test/routes/trail_detail_screen_retired_redirect_test.dart

must_haves:
  truths:
    - "A gate that would pass against an empty guard body no longer exists in this phase's test suite"
    - "Every gate test's name describes the property it actually asserts"
    - "A hiker whose trail uploads while its detail screen is open is proven — by an executable test, not a source grep — to be taken to the uploaded trail"
  artifacts:
    - path: "app/test/routes/trail_create_screen_local_save_gate_test.dart"
      provides: "effect-asserting slices for the CR-01/CR-02/CR-03 guards; correctly-titled three-outcome guard test"
      contains: "return;"
    - path: "app/test/routes/trail_detail_screen_retired_redirect_test.dart"
      provides: "behavioural widget test for the post-retirement redirect, on a real GoRouter"
      min_lines: 80
  key_links:
    - from: "app/test/routes/trail_detail_screen_retired_redirect_test.dart"
      to: "TrailDetailScreen"
      via: "mounted inside a real GoRouter with localTrailProvider and trailSyncProvider overridden"
      pattern: "GoRouter\\("
    - from: "app/test/routes/trail_create_screen_local_save_gate_test.dart"
      to: "_saveViaNetwork"
      via: "slices assert what is PASSED to it and that the refusal returns, not token order"
      pattern: "_saveViaNetwork\\("
---

<objective>
Close WR-05: the three gates the previous fix pass added assert token *presence* and *ordering*
and nothing else. The CR-01 gate at `trail_create_screen_local_save_gate_test.dart:465-503`
checks only that the substring `trailHasServerId(` appears before the substring
`trailSaveProvider.notifier)` — replace the guard block with
`if (!trailHasServerId(updatedTrail.id)) { /* TODO */ }` and the gate stays green while the
blank-id POST is fully restored. The CR-03 gate at `:505-555` asserts an outcome name appears
before `_finishLocalSave(`, so moving it into an unrelated `debugPrint` would satisfy it. And
`:197-243` is factually mis-titled: it is called *"the updateLocal branch reaches the network
ONLY on the alreadySynced escape hatch"* while the guard is now
`alreadySynced || missing || alreadyUploaded`, so it passes for a reason unrelated to the
property it names. Crucially, **no gate in that file constrains what is passed to
`_saveViaNetwork`** — which is where CR-01, CR-03 and WR-13 all live.

This plan runs last on purpose: it pins the shape the four preceding plans produce, so it can
assert effects rather than tokens.

Purpose: this phase has already shipped one real defect behind exactly this pattern — the
unsynced-tap-to-edit divert hid behind a source-grep gate for a whole UAT round. A green suite
over a broken primary flow is the failure mode being removed.
Output: gates whose only way to pass is for the guard to do its job, an honest set of test names,
and one genuinely behavioural test where the widget tree is actually mountable.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-local-first-recording-automatic-upload/36-REVIEW.md
@.planning/phases/36-local-first-recording-automatic-upload/36-17-SUMMARY.md
@.planning/phases/36-local-first-recording-automatic-upload/36-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The create-screen gates assert the guard's effect, and say what they check</name>
  <files>app/test/routes/trail_create_screen_local_save_gate_test.dart</files>
  <read_first>
    - `app/test/routes/trail_create_screen_local_save_gate_test.dart` — the whole file, especially
      the `methodEnd(...)` slicing helper, the mis-titled test at `:197-243`, the CR-02 ordering
      gate at `:414-463` (the review calls this one acceptable — keep it), the CR-01 gate at
      `:465-503` and the CR-03 gate at `:505-555`.
    - `app/lib/routes/trail_create_screen.dart` as it stands after 36-16, 36-17 and 36-18 —
      `_onSave`'s `networkUpdate` branch (now resolving through `resolveNetworkSaveTarget`), the
      `alreadySynced || missing || alreadyUploaded` branch, `_saveViaNetwork`'s
      `reconcileLocalId` parameter and its `trailHasServerId` backstop, and
      `_invalidateOwnTrailsList`'s null guard.
    - `.planning/phases/36-local-first-recording-automatic-upload/36-REVIEW.md` §WR-05 — the
      gate-by-gate verdict and the specific falsifying rewrites it names.
  </read_first>
  <action>
    Rewrite the weak gates in `app/test/routes/trail_create_screen_local_save_gate_test.dart`.
    Every replacement must fail against the falsifying rewrite the review names for it; add that
    falsifier to each test's `reason:` string so the next reader can check the gate is still
    load-bearing.

    **1. Fix the mis-titled test (`:197-243`).** Rename it to name the actual guard — the
    `updateLocal` branch reaches the network on `alreadySynced`, `missing` OR `alreadyUploaded`,
    and on nothing else. Assert the guard expression contains all three outcome names and that
    each appears before the `_saveViaNetwork(` call it gates. Correct the `networkCalls == 1`
    `reason:` so it describes the slice actually taken (it deliberately excludes `_onSave`'s
    `networkUpdate` branch).

    **2. Strengthen the CR-01 gate (`:465-503`).** Keep the `trailHasServerId(` presence check,
    then take the slice from the guard's `if (` to the first `_saveViaNetwork`/network call and
    assert that slice contains `return;`. A guard with an empty body, or one that falls through,
    then fails. Additionally assert the slice does NOT contain `error_saving_trail` and DOES
    contain `trail_uploaded_reopen_to_edit` — the refusal must name the recoverable state, which
    is the half of the previous review's minimum that did not ship.

    **3. Add the missing "what is passed to `_saveViaNetwork`" gates** — the review's central
    finding. Slice `_onSave`'s `networkUpdate` branch and assert:
    - it contains `resolveNetworkSaveTarget(`;
    - every `_saveViaNetwork(` occurrence in the slice is preceded by
      `updatedTrail.copyWith(id: targetId` (count equality), so a blank id cannot be handed to
      the network path;
    - the slice contains `localPhotos: const []` and `existsSync()`, the two guards against
      reading photo files retirement already deleted.
    Slice `_saveViaNetwork` and assert it contains `reconcileLocalId` and that
    `applyNetworkEditToLocalRow(` appears after `trail = result.trail` and before
    `_invalidateOwnTrailsList()` — the ordering CR-03's fix depends on.

    **4. Strengthen the CR-03 outcome gate (`:505-555`).** Instead of asserting
    `LocalUpdateOutcome.alreadyUploaded` appears before `_finishLocalSave(`, take the slice from
    `final outcome = updateLocalTrail(` to the first `_finishLocalSave(` and assert the
    `alreadyUploaded` token appears inside an `if (` condition in that slice — i.e. the slice
    between the nearest preceding `if (` and the following `) {` contains it. Moving the name into
    a `debugPrint` then fails.

    **5. Add a WR-13 gate.** Assert `_onSave` contains `photosNotYetOnServer(` and that the
    `createLocal` branch slice does NOT — the filter belongs on the network payload only.

    Keep the CR-02 `_localId`-ordering gate (`:414-463`) as-is; the review judged it acceptable.
    Do not delete any existing test — re-point it or strengthen it, matching this file's own
    stated discipline ("Re-point this gate rather than deleting it — the invariant still
    matters").
  </action>
  <verify>
    <automated>cd app && flutter test test/routes/trail_create_screen_local_save_gate_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The CR-01 gate fails when the guard body is emptied: temporarily replace the
      `if (!trailHasServerId(...)) { ... return; }` block in a scratch copy with
      `if (!trailHasServerId(updatedTrail.id)) { }` and confirm
      `cd app && flutter test test/routes/trail_create_screen_local_save_gate_test.dart` reports a
      failure; then restore. Record the observed failure message in the SUMMARY. This is the
      task's primary proof — a gate that cannot fail is not a gate.
    - The CR-03 gate fails when `LocalUpdateOutcome.alreadyUploaded` is moved out of the `if`
      condition into a `debugPrint` in a scratch copy — same falsification procedure, same
      restore, same SUMMARY record.
    - No test in the file asserts a bare two-token ordering as its only assertion:
      every `expect(` on an `indexOf` comparison is accompanied in the same `test(` body by at
      least one `contains(` or `allMatches(` assertion about the guarded region.
    - The test previously named "the updateLocal branch reaches the network ONLY on the
      alreadySynced escape hatch" no longer exists under that name, and its replacement names all
      three outcomes.
    - `cd app && flutter test test/routes/trail_create_screen_local_save_gate_test.dart` passes
      against the real source.
  </acceptance_criteria>
  <done>Each gate in the file can be made to fail by the exact rewrite the review said it would tolerate, and each gate's name matches what it asserts.</done>
</task>

<task type="auto">
  <name>Task 2: The drain gates pin the retired-id carry-forward as an effect, not a mention</name>
  <files>app/test/store/local_trail_retirement_gate_test.dart</files>
  <read_first>
    - `app/test/store/local_trail_retirement_gate_test.dart` — the whole file as 36-15, 36-16 and
      36-18 left it: `retirementBody()` (whose signature literal 36-16 already re-pointed to
      `'String? retireUploadedLocalTrail(Store store, String localId) {'`, so this task must NOT
      re-point it again), `drainOneBody()`, `deleteUnsyncedBody()` (whose literal 36-15 already
      re-pointed to the `UnsyncedDeleteResult` return type), and the header doc explaining why
      source slicing is the only available technique for these functions (`flutter test` cannot
      open an ObjectBox `Store`).
    - `app/lib/provider/trail/trail_sync_provider.dart` as it stands after 36-16 and 36-18 —
      `_drainOne` step 4's `final retiredServerId = retireUploadedLocalTrail(...)`,
      `_rememberRetiredServerId`, `ref.invalidate(localTrailProvider(localId))`, and
      `serverIdForRetired`'s account check.
    - `app/lib/store/local_trail_store.dart` — `retireUploadedLocalTrail`'s `String?` return.
  </read_first>
  <action>
    Add a group to `app/test/store/local_trail_retirement_gate_test.dart` pinning CR-01/WR-01's
    carry-forward, using the existing `drainOneBody()` helper and a new
    `serverIdForRetiredBody()` helper built the same way:

    - `drainOneBody()` contains `= retireUploadedLocalTrail(` — the return value is **assigned**,
      not discarded. `reason:` must state that discarding it re-opens CR-01: the screen's
      `trail.id` stays `''` forever and every post-upload save fails.
    - `drainOneBody()` contains `_rememberRetiredServerId(` and its index is greater than the
      index of `= retireUploadedLocalTrail(`.
    - `drainOneBody()` contains `invalidate(localTrailProvider(localId))` and its index is greater
      than the index of `retireUploadedLocalTrail(`. `reason:` must state WR-01's consequence: a
      hiker on `/trail/local/<localId>` keeps rendering a row that no longer exists, with a live
      Edit button.
    - `serverIdForRetiredBody()` contains `currentAccountId(` and contains no `return` of the
      memoized value that is not inside an account comparison — assert by slicing from the
      `currentAccountId(` occurrence to the end of the body and requiring the remaining text to
      contain `accountId` on the same statement as the returned value. `reason:` must state the
      threat: `trailSyncProvider` is deliberately absent from `accountScopedProviders`, so without
      the check account B could resolve account A's server id and post an edit to A's trail.

    Also extend the existing `retirementBody()` group: the body must return the captured server id
    from BOTH exits — assert the comment-stripped body contains at least two `return` statements
    referencing the captured id variable, and that the capture appears before both
    `entity.owner = null` and `trailBox.remove(`.

    Update the file's header doc comment to state plainly which of its assertions are effect
    assertions (a value is assigned and consumed; a call is ordered relative to a state
    transition) and which remain presence/absence facts, and that the end-to-end proof for all of
    them is the device UAT named in each plan's `<verification>`.
  </action>
  <verify>
    <automated>cd app && flutter test test/store/local_trail_retirement_gate_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The new gate fails when `final retiredServerId =` is removed from the retirement call in a
      scratch copy (i.e. the return value is discarded) — falsify, observe the failure, restore,
      and record the failure message in the SUMMARY.
    - The new gate fails when `ref.invalidate(localTrailProvider(localId))` is moved above the
      `retireUploadedLocalTrail(` call in a scratch copy — same procedure.
    - `serverIdForRetiredBody()`'s assertion fails when the account comparison is removed in a
      scratch copy — same procedure.
    - `retirementBody()` contains at least two `return` statements referencing the captured id.
    - `cd app && flutter test test/store/local_trail_retirement_gate_test.dart` passes against the
      real source.
  </acceptance_criteria>
  <done>The retired-id carry-forward and its account scoping cannot be removed without a red suite.</done>
</task>

<task type="auto">
  <name>Task 3: A real widget test for the post-retirement redirect</name>
  <files>app/test/routes/trail_detail_screen_retired_redirect_test.dart</files>
  <read_first>
    - `app/test/routes/profile_trail_screen_navigation_test.dart` — the precedent this file
      copies: a real `GoRouter`, a real screen, and an assertion on the actual pushed location.
      Reuse its router-construction and location-capture technique.
    - `app/test/components/trail/trail_dropdown_menu_test.dart` — the stub-notifier conventions
      (`_StubAuth`, `_StubTrailSync`) and the `MaterialApp` localization delegate list.
    - `app/lib/routes/trail_detail_screen.dart` as 36-16 left it — the `widget.localId != null`
      branch, the `trail == null` sub-branch, its `serverIdForRetired` lookup and the post-frame
      `pushReplacement('/trail/$retiredServerId')`.
    - `app/lib/provider/trail/local_trail_provider.dart` — `localTrailProvider` is a synchronous
      `@riverpod` family returning `Trail?`, so it can be overridden with a plain value.
    - `app/lib/provider/trail/trail_sync_provider.dart` — `TrailSync` is a `keepAlive` Notifier;
      subclass it in the test to return a fixed `serverIdForRetired` result.
  </read_first>
  <action>
    Create `app/test/routes/trail_detail_screen_retired_redirect_test.dart`.

    This branch IS mountable, unlike the rest of the screen: when `localTrailProvider` returns
    null the widget never builds `TrailPanel`, so no `maplibre` platform view and no network
    providers are reached. Say so in the file's header comment, together with why the sibling
    create-screen path is not mountable (it embeds `TrailMap` and needs a live `Store` and
    `path_provider`).

    Build a harness with:
    - a real `GoRouter` declaring `/trail/local/:localId` → `TrailDetailScreen(id: '', localId: ...)`
      and `/trail/:id` → a trivial placeholder widget carrying a findable `Key`;
    - `localTrailProvider('local-1-0').overrideWithValue(null)`;
    - a `_StubTrailSync extends TrailSync` whose `serverIdForRetired` returns a caller-supplied
      `String?` (and whose `build()` returns `{}`), installed via
      `trailSyncProvider.overrideWith(...)`;
    - the same `MaterialApp.router` localization delegates as
      `trail_dropdown_menu_test.dart`.

    Three test cases, all asserting on rendered widgets and the router's current location:
    1. **Redirect.** `serverIdForRetired` returns `'server-1'`. Pump, then `pumpAndSettle` so the
       post-frame callback runs. Assert the router's current location is `/trail/server-1` (or
       that the placeholder widget's `Key` is present), and that the
       `trail_not_on_this_device` text is NOT found.
    2. **No retired id — the honest dead end stays.** `serverIdForRetired` returns `null`. Assert
       the `trail_not_on_this_device` text IS found and the location is still
       `/trail/local/local-1-0`.
    3. **Control — a present local trail renders normally and does not redirect.** Override
       `localTrailProvider('local-1-0')` with a `Trail.empty().copyWith(localId: 'local-1-0', syncState: TrailSyncState.pending, ...)`
       fixture and assert the location is unchanged. If mounting the full detail body proves
       impossible in the test environment (it reaches `TrailPanel` → `TrailMap`), replace this
       case with an equivalent that keeps `localTrailProvider` null but sets
       `serverIdForRetired` to the empty string, asserting the empty string is treated as "no
       target" and the dead-end text renders — and record the substitution and its reason in the
       SUMMARY rather than silently dropping the case.
  </action>
  <verify>
    <automated>cd app && flutter test test/routes/trail_detail_screen_retired_redirect_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - With `serverIdForRetired` returning `'server-1'`, the test asserts the router settles on
      `/trail/server-1` and that `trail_not_on_this_device`'s text is absent — both on rendered
      output, with no source-text assertion anywhere in the file.
    - With `serverIdForRetired` returning `null`, the test asserts `trail_not_on_this_device`'s
      text is present and the location is unchanged.
    - `grep -c "readAsStringSync\|File(" app/test/routes/trail_detail_screen_retired_redirect_test.dart` returns 0 — this file must contain no source-slicing at all.
    - The file is at least 80 lines including its header comment explaining what is and is not
      mountable.
    - `cd app && flutter test` reports 0 failures.
  </acceptance_criteria>
  <done>The one place in this gap-closure batch where a real widget test can prove a real behaviour has one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → release confidence | A green suite is the signal a human uses to decide the phase is safe; a gate that cannot fail transfers false confidence |
| Route parameter → mounted screen | The redirect test exercises `/trail/local/:localId`, the attacker-supplied deep-link surface `readOwnLocalTrail` is scoped for |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-20-01 | Repudiation | gate tests that pass against a disabled guard | mitigate | Each rewritten gate must be demonstrated to FAIL against the exact falsifying rewrite the review named, with the observed failure recorded in the SUMMARY; token-order-only assertions are removed (WR-05) |
| T-36-20-02 | Repudiation | test names that misdescribe what they assert | mitigate | The mis-titled three-outcome test is renamed and its assertions widened to cover all three outcomes; every `reason:` names the falsifier it defends against |
| T-36-20-03 | Elevation of Privilege | `serverIdForRetired` losing its account check | mitigate | A gate asserts the account comparison is present and is demonstrated to fail without it, so 36-16's T-36-16-01 control cannot be silently removed |
| T-36-20-04 | Information Disclosure | the redirect test exercising a deep-link route | accept | The test overrides `localTrailProvider` directly and never opens a `Store`, so it exercises routing only; the owner-scoping control lives in `readOwnLocalTrail` and is covered by 36-11's existing coverage plus device UAT |
| T-36-20-SC | Tampering | package-manager installs | accept | No dependency is added or changed; `app/pubspec.yaml` is not in `files_modified` |
</threat_model>

## Artifacts this phase produces

**New files**
- `app/test/routes/trail_detail_screen_retired_redirect_test.dart` — behavioural widget test for
  the post-retirement redirect (real `GoRouter`, stubbed `localTrailProvider` and `TrailSync`)

**New test-file symbols**
- `_StubTrailSync` (in the new file) — a `TrailSync` subclass with a caller-supplied
  `serverIdForRetired` result
- `serverIdForRetiredBody()` (in `local_trail_retirement_gate_test.dart`) — a source-slicing
  helper matching the file's existing `drainOneBody()` / `deleteUnsyncedBody()` shape

**Changed tests**
- `app/test/routes/trail_create_screen_local_save_gate_test.dart` — the three weak gates rewritten
  to assert effects, the mis-titled test renamed, and new gates covering what is passed to
  `_saveViaNetwork`
- `app/test/store/local_trail_retirement_gate_test.dart` — a new group pinning the retired-id
  carry-forward, its ordering, and its account check

**No lib changes. No new l10n keys. No dependency changes.**

<verification>
- `cd app && flutter test` → 0 failures.
- `cd app && flutter analyze --no-pub` → 0 errors.
- Each falsification listed in the acceptance criteria was performed, observed to fail, and
  restored; the SUMMARY records the failure message for each.

**Explicitly not claimed.** These gates do not make the phase's ObjectBox and PocketBase paths
behaviourally tested — they cannot, and pretending otherwise is the exact failure this plan
exists to remove. The end-to-end proof remains the five device items in 36-VERIFICATION.md
`human_verification`, extended by 36-15 (corrupt-GPX delete, 404 delete, offline delete), 36-16
(post-upload edit and the mounted-screen redirect), 36-17 (`alreadyUploaded` edit visibility and
photo-count stability), 36-18 (waypoint photos after a mid-loop failure; no spurious parking) and
36-19 (downloaded-trail tabs).
</verification>

<success_criteria>
- No gate in this phase's test suite passes against the falsifying rewrite the review named for
  it.
- Every gate test's name matches its assertions.
- The post-retirement redirect has an executable behavioural test.
- The suite is green and the analyzer is clean.
</success_criteria>

<output>
Create `.planning/phases/36-local-first-recording-automatic-upload/36-20-SUMMARY.md` when done.
</output>
