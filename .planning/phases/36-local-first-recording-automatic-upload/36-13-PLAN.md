---
phase: 36-local-first-recording-automatic-upload
plan: 13
type: execute
wave: 4
depends_on: ["36-10", "36-11", "36-12"]
gap_closure: true
autonomous: true
requirements: [REC-03, SYNC-02]
files_modified:
  - app/lib/components/trail/trail_dropdown.dart
  - app/lib/provider/profile/profile_trails_provider.g.dart
  - app/lib/provider/trail/trail_sync_provider.g.dart
  - app/lib/provider/trail/trail_filter_provider.g.dart
  - app/lib/provider/router_provider.g.dart
  - app/test/components/trail/trail_dropdown_menu_test.dart
  - app/test/components/trail/trail_dropdown_delete_gate_test.dart

must_haves:
  truths:
    - "The trail dropdown's Show-on-map entry opens an unsynced trail's map instead of a no-route error page"
    - "Editing from the dropdown refreshes the screen it returns to, for a local trail as well as a server one"
    - "An automated widget test opens the real menu and proves Download is absent for an unsynced trail"
    - "An automated widget test proves the Delete confirmation says the deletion cannot be undone, and that Delete is disabled mid-upload"
    - "Every generated .g.dart in app/lib matches its source at phase end -- a second build_runner run produces no diff"
  artifacts:
    - path: "app/test/components/trail/trail_dropdown_menu_test.dart"
      provides: "Behavioural coverage of D-14/D-17 dropdown gating -- opens the menu, reads the rendered items"
      min_lines: 120
  key_links:
    - from: "app/lib/components/trail/trail_dropdown.dart"
      to: "trailMapLocation"
      via: "the show_on_map menu entry"
      pattern: "trailMapLocation\\(trail\\)"
    - from: "app/lib/components/trail/trail_dropdown.dart"
      to: "localTrailProvider"
      via: "post-edit invalidation for an unsynced trail"
      pattern: "invalidate\\(localTrailProvider\\("
---

<objective>
Close UAT's test-quality finding, finish unblocking UAT Test 3, and leave the phase's
generated code consistent with its sources.

`app/test/components/trail/trail_dropdown_delete_gate_test.dart` asserts by grepping the
*source text* of `trail_dropdown.dart`. It stayed green for the whole phase while
`TrailDropdown` — instantiated in exactly one place, `trail_detail_screen.dart:98` — was
unreachable in the running app for every unsynced trail, because the own-trails list
diverted those taps to the edit screen. All of D-14's delete gating and D-17's download
hiding shipped with no automated signal that it was live. 36-11/36-12 make the menu
reachable; this plan proves it behaves, and fixes the two entries in it that still assume a
server id.

This plan is also the last one in the phase and the only plan in its wave, which makes it
the one safe place to run a whole-tree `build_runner` pass. Four `@riverpod` sources were
edited across 36-09/36-10/36-12 by plans that deliberately did not run codegen (to avoid
racing each other for `.dart_tool/build`'s exclusive lock), so their generated source
hashes are stale. Task 3 reconciles them once.

Purpose: REC-03/SYNC-02 make the sync state actionable through this menu. UAT Test 3 is
`blocked` solely because the menu could not be opened on an unsynced trail; the phase must
end in a state where that test is re-runnable and where its expectations are pinned by
something that fails when they break.

Output: `Show on map` retargeted through the shared route helper, post-edit invalidation
that works for a local trail, a widget test that opens the real `PopupMenuButton` and reads
the real rendered items, and a codegen tree at a fixpoint.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-local-first-recording-automatic-upload/36-UAT.md
@.planning/phases/36-local-first-recording-automatic-upload/36-08-SUMMARY.md
@.planning/debug/unsynced-trail-skips-detail-screen.md

@app/lib/components/trail/trail_dropdown.dart
@app/test/components/trail/trail_dropdown_delete_gate_test.dart
@app/test/components/trail/sync_status_chip_test.dart
@app/test/routes/settings_account_screen_test.dart

Interface facts the executor needs (already verified, do not re-derive):

- 36-11-PLAN provides `String? trailMapLocation(TrailSummary)` in
  `app/lib/util/trail_route_location.dart` (null = not addressable) and
  `localTrailProvider(String localId)` returning `Trail?` in
  `app/lib/provider/trail/local_trail_provider.dart`.
- 36-12-PLAN provides the `map` sub-route under `/trail/local/:localId`. Without it,
  `trailMapLocation` on an unsynced trail returns a *non-null* path that resolves to
  nothing — the Show-on-map entry would render enabled and still land on an error page.
  That is why `36-12` is in `depends_on`: the "Show-on-map opens an unsynced trail's map"
  truth needs 36-12's route, not just 36-11's helper. (The declared order used to omit it
  and happened to run correctly anyway; it would have broken under any re-wave.)
- 36-10-PLAN provides `ownTrailsHandle(String preferredUsername)` in
  `app/lib/util/own_trails_handle.dart` and has already switched
  `trail_dropdown.dart:318` to use it. Do not revert that, and do not reintroduce an
  inline `'@$...'` — 36-10's lib-wide gate test will fail if you do.
- `TrailDropdown.build` watches exactly three providers: `downloadingTrailIdsProvider`
  (`Set<String>`, `@Riverpod(keepAlive: true)` class notifier), `trailSyncProvider`
  (`Set<String>`), and `authProvider` (transitively, via `_canEditTrail` and
  `_allowDelete`). `trailLibraryProvider` and `trailSaveProvider` are only `ref.read` inside
  `_deleteTrail`, so they do not need overriding for a menu-rendering test.
- Current menu structure: `open` (Show on map) -> `directions` -> `if (_canEditTrail(ref))
  [divider, edit]` -> `if (!isUnsynced) [divider, download]` -> `if (_allowDelete(ref))
  [divider, delete with enabled: !isDraining]`.
- `_canEditTrail` returns true when `trail.author == user.actorId`. A local capture DOES
  carry its author actor id (`saveNewLocalTrail` sets it), so Edit is present for an
  unsynced trail — the fixture must set `author` to the stub user's `actorId` for the Edit
  entry to render.
- `_allowDelete` returns true unconditionally when `trail.isLocal`, and
  `TrailEntity.toModel()` hardcodes `isLocal: true`, so Delete is always present for both
  fixture shapes below.
- Exact English strings (`app/lib/i18n/app_en.arb`) for `find.text`:
  - `show_on_map` -> `"Show on map"`
  - `edit` -> `"Edit"`
  - `download` -> `"Download"`
  - `delete` -> `"Delete"`
  - `delete_trail_confirm` -> `"Do you really want to delete this trail? This action cannot be undone."`
  - `delete_unsynced_trail_confirm` -> `"Delete this trail? It hasn't been uploaded yet, so this can't be undone."`
  Note the download item renders the literal `'Available offline'` (not an l10n key) when
  `availableOffline` is true — pass `availableOffline: false` in the tests.
- Test doubles to copy rather than invent:
  - `_StubAuth extends Auth` — `app/test/routes/settings_account_screen_test.dart:14-30`
    (its fixture already has `actorId: 'actor-id'`).
  - `_StubTrailSync extends TrailSync` with a caller-supplied in-flight `Set<String>` —
    `app/test/components/trail/sync_status_chip_test.dart:92-105`.
  - The `MaterialApp` + l10n delegate harness — same two files.
  - `Trail.empty().copyWith(...)` fixtures — `app/test/util/own_trails_merge_test.dart`.
- `CircularProgressIndicator`'s indeterminate animation never settles: use `tester.pump()`
  rather than `pumpAndSettle()` for any state that renders one (36-08's own finding).
- Stale generated hashes to be reconciled by Task 3, and which plan left them stale:
  `profile_trails_provider.g.dart` (36-09 Task 2, 36-10 Task 1),
  `trail_sync_provider.g.dart` (36-10 Task 1), `router_provider.g.dart` (36-12 Task 1),
  and `trail_filter_provider.g.dart` if anything downstream shifted it. Task 3 does not
  need to know which — it regenerates the whole package.

Verification conventions for this plan:

- Comment stripping in shell greps uses POSIX `grep -Ev '^[[:space:]]*//'`, not
  `grep -v '^\s*//'`. Adopted for portability; see 36-09-PLAN's `<context>` for the full
  note including the recorded disagreement with the premise.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget the dropdown's map entry and its post-edit invalidation</name>
  <files>app/lib/components/trail/trail_dropdown.dart</files>
  <action>
    1. Compute once in `build`, alongside the existing `isUnsynced` / `isDownloading` /
       `isDraining` locals:

           // D-06 blanks a local-sentinel id, so '/trail/${trail.id}/map' is
           // '/trail//map' for a not-yet-uploaded trail -- go_router
           // canonicalizes that to '/trail/map', which matches no route.
           final String? mapLocation = trailMapLocation(trail);

       Change the `TrailAction.open` `PopupMenuItem`:
       - `onTap: mapLocation == null ? null : () => context.push(mapLocation)`
       - `enabled: mapLocation != null`
       - grey the leading `FaIcon` and the title `Text` when `mapLocation == null`,
         following the `directions` entry's existing disabled treatment (which greys on
         `trail.lat == null`) so the two disabled states look the same.

    2. Change the `TrailAction.edit` `PopupMenuItem`'s `onTap` so the post-edit refresh
       targets the right provider:

           onTap: () async {
             await context.push('/trail/create/edit', extra: trail);
             // The edit screen invalidates the LIST providers itself (36-10);
             // refreshing the single-trail provider stays a caller
             // responsibility, as it has been since this call site was
             // written. Which provider that is depends on where the trail
             // lives: an unsynced trail is keyed on its localId, and
             // `trailProvider('')` would be a meaningless family instance that
             // refreshes nothing.
             if (isUnsynced) {
               final localId = trail.localId;
               if (localId != null) ref.invalidate(localTrailProvider(localId));
             } else {
               ref.invalidate(trailProvider(trail.id));
             }
           },

       `isUnsynced` is already computed in `build` and is in scope for the item builder.

    3. Add imports: `app/lib/util/trail_route_location.dart`,
       `app/lib/provider/trail/local_trail_provider.dart`.

    Change nothing else. The `if (!isUnsynced)` download gate, the `enabled: !isDraining`
    delete gate, the three-way `_deleteTrail` branch order, the per-state confirm copy and
    36-10's `ownTrailsHandle` call at `:318` all stay exactly as they are — this plan tests
    them, it does not rewrite them.
  </action>
  <verify>
    <automated>cd app &amp;&amp; flutter analyze --no-pub lib/components/trail/trail_dropdown.dart &amp;&amp; ! grep -Ev '^[[:space:]]*//' lib/components/trail/trail_dropdown.dart | grep -q "/trail/\${trail.id}/map" &amp;&amp; grep -Ev '^[[:space:]]*//' lib/components/trail/trail_dropdown.dart | grep -c 'invalidate(localTrailProvider('</automated>
  </verify>
  <done>`flutter analyze` reports zero errors, no inline `/trail/${trail.id}/map` interpolation remains outside comments, and the local post-edit invalidation is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Behavioural coverage for the D-14/D-17 menu gating</name>
  <files>app/test/components/trail/trail_dropdown_menu_test.dart, app/test/components/trail/trail_dropdown_delete_gate_test.dart</files>
  <behavior>
    `app/test/components/trail/trail_dropdown_menu_test.dart` — mounts a real
    `TrailDropdown`, opens the real `PopupMenuButton` and asserts on rendered widgets:

    - **D-17, unsynced:** for a pending trail (`id: ''`, `localId: 'local-1-0'`,
      `syncState: TrailSyncState.pending`), the opened menu shows no `"Download"` text and
      no `"Available offline"` text. `"Show on map"`, `"Edit"` and `"Delete"` are present.
    - **D-17, downloaded:** for a synced trail (`id: 'server-1'`,
      `syncState: TrailSyncState.synced`), the opened menu DOES show `"Download"`. This is
      the control that proves the previous assertion is about the sync state and not about
      the harness failing to render the item at all.
    - **D-14, confirm copy:** tapping `"Delete"` on the unsynced fixture shows a dialog
      containing `"Delete this trail? It hasn't been uploaded yet, so this can't be undone."`
      and not the reversible `delete_trail_confirm` copy.
    - **D-14, confirm copy, control:** tapping `"Delete"` on the synced fixture shows
      `"Do you really want to delete this trail? This action cannot be undone."`
    - **D-14, blocked mid-drain:** with `'local-1-0'` in the stubbed in-flight set, the
      opened menu's delete `PopupMenuItem<TrailAction>` has `enabled == false`. Read it with
      `tester.widget<PopupMenuItem<TrailAction>>(find.widgetWithText(PopupMenuItem<TrailAction>, 'Delete'))`
      rather than asserting on colours.
    - **Show on map is live:** the `"Show on map"` item's `PopupMenuItem.enabled` is true for
      the unsynced fixture — `trailMapLocation` resolves it to `/trail/local/local-1-0/map`,
      which is a real route as of 36-12.

    No assertion in this file reads the source text of any lib file.
  </behavior>
  <action>
    1. Create `app/test/components/trail/trail_dropdown_menu_test.dart`.

       Harness: a `_harness(Trail trail, {Set<String> inFlight = const {}, Set<String> downloading = const {}})`
       returning a `ProviderScope` with `authProvider.overrideWith(_StubAuth.new)`,
       `trailSyncProvider.overrideWith(() => _StubTrailSync(inFlight))` and a stub for
       `downloadingTrailIdsProvider`, wrapping a `MaterialApp` (l10n delegates + `Locale('en')`)
       whose `home` is `Scaffold(appBar: AppBar(actions: [TrailDropdown(trail: trail, availableOffline: false)]))`.
       Mounting it in an `AppBar` matches its only real call site
       (`trail_detail_screen.dart:98`) and gives `PopupMenuButton` the anchor it expects.

       Fixtures — both must set `author: 'actor-id'` so `_canEditTrail` is true against
       `_StubAuth`'s user:

           final unsynced = Trail.empty().copyWith(
             id: '', localId: 'local-1-0', author: 'actor-id',
             name: 'Unsynced Trail', syncState: TrailSyncState.pending,
             lat: 1, lon: 1,
           );
           final synced = Trail.empty().copyWith(
             id: 'server-1', author: 'actor-id', name: 'Synced Trail',
             syncState: TrailSyncState.synced, lat: 1, lon: 1,
           );

       (`lat`/`lon` set so the `directions` entry renders enabled and does not skew a
       disabled-item assertion. Verify `Trail.author` is a plain `String?`/`String` before
       using `copyWith(author:)`; if it is not, set it through whatever field
       `_canEditTrail` reads.)

       Open the menu with
       `await tester.tap(find.byType(PopupMenuButton<TrailAction>)); await tester.pumpAndSettle();`
       Cover the six behaviours listed above.

       Head the file with a comment recording why it exists: UAT found that the existing
       delete gate test greps source text, so it passed for the entire phase while the menu
       it described could not be opened at all in the running app (`TrailDropdown` is
       instantiated only in `trail_detail_screen.dart`, which was unreachable for an
       unsynced trail). This file is the missing signal — it fails if the item is absent,
       disabled wrongly, or shows the wrong confirmation copy.

    2. Amend `app/test/components/trail/trail_dropdown_delete_gate_test.dart`: keep every
       existing assertion (they pin branch ORDER inside `_deleteTrail`, which no widget test
       can observe — the unsynced branch must precede and return before the `isLocal`
       un-download branch, and getting that wrong silently destroys the hiker's only copy).
       Add a header note stating that the reachability half of what this file appears to
       assert is now covered behaviourally by `trail_dropdown_menu_test.dart`, and that this
       file's remaining scope is branch order only.
  </action>
  <verify>
    <automated>cd app &amp;&amp; flutter test test/components/trail/trail_dropdown_menu_test.dart test/components/trail/trail_dropdown_delete_gate_test.dart</automated>
  </verify>
  <done>All six new behavioural tests pass, every pre-existing assertion in the delete gate test still passes, and no assertion was deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile generated code with its sources, once, at the end of the phase</name>
  <files>app/lib/provider/profile/profile_trails_provider.g.dart, app/lib/provider/trail/trail_sync_provider.g.dart, app/lib/provider/trail/trail_filter_provider.g.dart, app/lib/provider/router_provider.g.dart</files>
  <action>
    Run, from the repo root:

        cd app && dart run build_runner build --delete-conflicting-outputs

    Then commit every `.g.dart` it changes under `app/lib`, including any file not listed in
    `files_modified` above — the list is the known set, not a bound. `build_runner` holds an
    exclusive lock on `app/.dart_tool/build` and regenerates the whole package regardless of
    which file triggered the run, which is exactly why this is the only place in the phase
    where it can run safely: 36-13 is alone in wave 4.

    Why the phase needs this at all, written down so nobody deletes the task as redundant:

    - 36-09 Task 1 runs codegen, then 36-09 Task 2 edits `profile_trails_provider.dart`
      (a `@riverpod` class) — leaving that file's `_$...Hash()` describing the pre-edit
      source.
    - 36-10 edits `profile_trails_provider.dart` and `trail_sync_provider.dart` and
      deliberately runs no codegen, because 36-11 holds the lock in the same wave.
    - 36-11 runs codegen from a worktree that does not contain 36-10's concurrent edits.
    - 36-12 edits `router_provider.dart` and runs no codegen for the same reason.

    None of this is a runtime fault — `debugGetCreateSourceHash` feeds riverpod's
    hot-reload staleness detection, not provider behaviour — but a tree whose generated
    output does not match its sources is a tree where the next person's `build_runner` run
    produces an unexplained diff. This phase already produced one such commit
    ("chore(36): regenerate riverpod provider hashes"); this task is what stops the next one.

    After the regeneration, run the full suite (see `<verification>`) to confirm nothing
    else moved.
  </action>
  <verify>
    <automated>cd app &amp;&amp; dart run build_runner build --delete-conflicting-outputs &amp;&amp; find lib -name '*.g.dart' -exec shasum {} \; | sort | shasum &gt; /tmp/gsd-gen-1.txt &amp;&amp; dart run build_runner build --delete-conflicting-outputs &amp;&amp; find lib -name '*.g.dart' -exec shasum {} \; | sort | shasum &gt; /tmp/gsd-gen-2.txt &amp;&amp; diff /tmp/gsd-gen-1.txt /tmp/gsd-gen-2.txt &amp;&amp; flutter analyze --no-pub</automated>
  </verify>
  <done>Two consecutive `build_runner` runs produce a byte-identical set of `.g.dart` files (the tree is at a codegen fixpoint), and `flutter analyze --no-pub` reports zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| menu action -> destructive local operation | Delete on an unsynced trail destroys the only copy that exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-13-01 | Denial of service (data loss) | Delete racing an in-flight drain | mitigate | Unchanged from 36-08: `TrailSync.deleteUnsynced` refuses while the localId is in the in-flight set, and the menu's `enabled: !isDraining` mirrors it. This plan adds the first automated proof that the mirror actually renders disabled — previously asserted only by source grep. |
| T-36-13-02 | Repudiation | delete confirmation understating irreversibility | mitigate | The unsynced fixture's confirm copy is now asserted by opening the real dialog and matching the rendered string, with the reversible-copy case as an explicit control. |
| T-36-13-03 | Elevation of privilege | Download offered for a trail with no server record | mitigate | The `if (!isUnsynced)` gate is unchanged (D-17); the new test asserts the item's absence for an unsynced fixture and its presence for a synced one, so a regression cannot pass as "the harness rendered nothing". |
| T-36-13-04 | Tampering | generated code silently diverging from its sources | mitigate | Task 3 regenerates the whole package once, serialised in the phase's only single-plan wave, and proves a fixpoint by running `build_runner` twice and diffing the hash of every `.g.dart`. Closes the drift the wave-2 concurrency constraint deliberately accepted. |
| T-36-13-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan adds no package-manager dependency. |
</threat_model>

<verification>
Full-suite regression, run once after all three tasks:

    cd app && flutter analyze --no-pub && flutter test

This is also the phase's final gate: every plan from 36-09 onward has landed, so this run
covers the whole gap-closure set, not just this plan.

UAT Test 3 re-run (device, not run by this plan — it was `blocked` and is now unblocked):
open the dropdown on an unsynced trail from its detail screen and confirm Download is
absent and the Delete confirmation states the deletion is unrecoverable; start or wait for
that trail's upload and reopen the menu to confirm Delete is greyed out; then open the same
menu on an ordinary downloaded trail and confirm it is unchanged.
</verification>

<success_criteria>
- The dropdown's Show-on-map entry resolves through `trailMapLocation` and disables itself
  when the trail is not addressable.
- Post-edit invalidation targets `localTrailProvider` for an unsynced trail and
  `trailProvider` otherwise.
- `trail_dropdown_menu_test.dart` covers all six behaviours by opening the real menu.
- `trail_dropdown_delete_gate_test.dart` keeps every existing assertion, scoped explicitly
  to branch order.
- Two consecutive `build_runner` runs produce identical output; no `.g.dart` in `app/lib`
  is stale relative to its source.
- `flutter analyze --no-pub` clean; `flutter test` green.
</success_criteria>

<output>
Create `.planning/phases/36-local-first-recording-automatic-upload/36-13-SUMMARY.md` when done.
</output>
