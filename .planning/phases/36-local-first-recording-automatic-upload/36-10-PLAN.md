---
phase: 36-local-first-recording-automatic-upload
plan: 10
type: execute
wave: 2
depends_on: ["36-09"]
gap_closure: true
autonomous: true
requirements: [REC-05, REC-06]
files_modified:
  - app/lib/routes/trail_create_screen.dart
  - app/test/routes/trail_create_screen_local_save_gate_test.dart

must_haves:
  truths:
    - "Saving an edit to an unsynced trail while offline updates the own-trails list on pop-back, with no pull-to-refresh"
    - "Saving an edit to an already-uploaded trail also updates the own-trails list"
    - "Both save tails invalidate the same pair the drain invalidates, so a local save and an upload produce the same refresh"
  artifacts:
    - path: "app/test/routes/trail_create_screen_local_save_gate_test.dart"
      provides: "Call-site placement gate for _invalidateOwnTrailsList in both save tails"
      contains: "_invalidateOwnTrailsList"
  key_links:
    - from: "app/lib/routes/trail_create_screen.dart"
      to: "profileTrailsProvider"
      via: "_invalidateOwnTrailsList() called from _finishLocalSave and _saveViaNetwork"
      pattern: "invalidate\\(profileTrailsProvider\\("
---

<objective>
Close UAT gap 3 (major): "After saving edits on a non synced trail the 'own trail' list
needs a manual reload before edits are shown."

`trail_create_screen.dart` contains zero `invalidate`/`refresh` calls. Its shared
local-first save tail `_finishLocalSave` (714-760) commits to ObjectBox, drains, toasts
and `setState`s its own `trail` field — and notifies nobody else. The app has no ObjectBox
`Query.watch()` streams anywhere, so a row mutation emits no signal at all, and the
own-trails list stays mounted beneath the pushed edit route (`maintainState: true`),
keeping its auto-dispose provider alive with pre-edit state. The three existing
`invalidate(profileTrailsProvider(...))` sites cover successful drain upload,
`deleteUnsynced` and server-side delete — none of which fires on an offline edit.

Purpose: REC-05 promises the hiker can edit an unsynced trail offline. The edit lands in
ObjectBox but is invisible until a manual pull-to-refresh, which reads as data loss.

Output: one private method on `_TrailCreateScreenState` that invalidates the same pair
the drain does, called from both save tails.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-local-first-recording-automatic-upload/36-UAT.md
@.planning/debug/unsynced-trail-edit-not-reflected-in-list.md

@app/lib/routes/trail_create_screen.dart
@app/lib/provider/trail/trail_sync_provider.dart
@app/lib/provider/profile/profile_trails_provider.dart
@app/test/routes/trail_create_screen_local_save_gate_test.dart

Interface facts the executor needs (already verified, do not re-derive):

- `_finishLocalSave(Store store, AppLocalizations l10n, int failedPhotoCount, {required String localId})`
  at `:714` is the shared tail of BOTH local branches: `createLocal` (~451-506) and
  `updateLocal` (~508-569). Fixing it once covers first save and re-save. Its first
  statement today is `unawaited(ref.read(trailSyncProvider.notifier).drainIfOnline());`
  and its `if (!mounted) return;` early-out sits at `:737`.
- `_saveViaNetwork(AppLocalizations l10n, Trail updatedTrail, {required String authorId, required List<File> newPhotoFiles})`
  at `:583` is the equally-silent server path, reached by `LocalSaveMode.networkUpdate` and
  by `LocalUpdateOutcome.alreadySynced`. Its success `setState` assigning
  `trail = result.trail` is at `:601-605`.
- `TrailCreateScreen`'s State is a `ConsumerState`, so `ref` is available.
  `package:wanderer/provider/auth_provider.dart` is ALREADY imported (`:33`).
  `trail_library_provider.dart` and `profile_trails_provider.dart` are NOT — add both.
- The pair to invalidate is `trailLibraryProvider` + `profileTrailsProvider('@<username>')`.
  That is exactly what `trail_sync_provider.dart:303` and `:377` invalidate after a
  successful drain; a local save must produce the same refresh or the two paths diverge.
  Match their spelling verbatim.
- `ref.invalidate` defaults to `asReload: false`, which riverpod treats as a *seamless
  refresh*; `AsyncValue.when`'s `skipLoadingOnRefresh` defaults to `true`. So these
  invalidations refresh the list without flashing a spinner — they do not reintroduce the
  defect 36-09 fixes.
- D-13: the account must be read fresh at the moment of use, never from a cached field.

Recorded decisions — do NOT reopen these during execution:

- **No `ownTrailsHandle` / `ownTrailsInvalidationTargets` helpers.** An earlier draft of
  this plan extracted the `@handle` producer and the invalidation target list into
  `app/lib/util/`, with a lib-wide gate across ten call sites and a `ProviderContainer`
  test. Dropped by explicit decision: the drift it guarded against is future-tense (the
  invalidation and `profile_screen.dart:44` spell the handle identically today, so the
  inline form works), and the phase is verified on device. Do not reintroduce either file.
- **No null guard on the interpolation.** `'@${ref.read(authProvider).value?.preferredUsername}'`
  interpolates a nullable and would yield `'@null'` signed-out. Considered and rejected as
  unreachable: every profile route and `/trail/create` sits inside the `ShellRoute` at
  `router_provider.dart:144-194`, none appears in `authRoutes` (`:109-114`), so the
  redirect at `:119-125` bounces a logged-out visit to `/welcome` before the screen builds.
  The only null window is inside `logout()` (`auth_provider.dart:220-233`), between its
  `state = const AsyncLoading()` and the `invalidateSelf()` that fires the redirect —
  reachable only by a save resuming from an `await` during those three awaits, and even
  then the result is a no-op invalidate on a key nothing watches. Keeping the spelling
  identical to `trail_sync_provider`'s is worth more than the guard.
- **No `ownHandleProvider`.** A `@Riverpod(keepAlive: true)` derived handle was considered.
  It would force a `build_runner` run, which this wave cannot take (36-11 runs
  concurrently and the lock is package-wide), and no call site watches — all of them read
  at a point. If a future consumer needs reactivity, add it in its own wave.

Verification conventions for this plan:

- Comment stripping in shell greps uses POSIX `grep -Ev '^[[:space:]]*//'`, not
  `grep -v '^\s*//'`. Adopted for portability; see 36-09-PLAN's `<context>` for the full
  note, including the recorded disagreement with the premise that `\s` is broken on macOS
  (it is not, on either `/usr/bin/grep` or the shell's `ugrep`). The Dart-side
  `RegExp(r'^\s*//')` in the gate test is unaffected and stays.
- This plan runs NO codegen and edits no `@riverpod`-annotated source, so no `.g.dart`
  hash goes stale. Do not add a `build_runner` invocation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Both save tails notify the own-trails list</name>
  <files>app/lib/routes/trail_create_screen.dart, app/test/routes/trail_create_screen_local_save_gate_test.dart</files>
  <action>
    1. Add the two missing imports to `trail_create_screen.dart`, in the existing
       alphabetical block: `package:wanderer/provider/profile/profile_trails_provider.dart`
       and `package:wanderer/provider/trail/trail_library_provider.dart`.
       `auth_provider.dart` is already there at `:33` — do not duplicate it.

    2. Add a private method to `_TrailCreateScreenState`, placed directly above
       `_finishLocalSave`:

           /// Notifies every other consumer of the row this screen just wrote.
           ///
           /// The local-first save path commits to ObjectBox, and this app has
           /// no ObjectBox `Query.watch()` streams anywhere, so an explicit
           /// invalidate is the ONLY propagation mechanism that exists. Without
           /// it the own-trails list -- which stays mounted beneath this pushed
           /// edit route (`maintainState: true`), keeping its auto-dispose
           /// provider alive -- holds its pre-edit snapshot until the hiker
           /// pull-to-refreshes, which reads as the edit having been lost.
           ///
           /// This is the same PAIR `trail_sync_provider` invalidates after a
           /// successful drain upload (`:303`, `:377`), spelled identically on
           /// purpose: a local save and an upload must produce the same
           /// refresh, and the family key must match the one
           /// `profile_trail_screen` watches or the invalidation is a silent
           /// no-op. The username is read FRESH here, never from a cached
           /// field (D-13).
           ///
           /// `asReload` defaults to false, i.e. a seamless refresh --
           /// `AsyncValue.when`'s `skipLoadingOnRefresh` defaults to true, so
           /// the list updates without flashing a spinner (36-09).
           void _invalidateOwnTrailsList() {
             ref.invalidate(trailLibraryProvider);
             ref.invalidate(
               profileTrailsProvider(
                 '@${ref.read(authProvider).value?.preferredUsername}',
               ),
             );
           }

    3. Call it from both tails:

       - `_finishLocalSave`: as the FIRST statement of the method body, wrapped in
         `if (mounted)`, i.e. above the existing `unawaited(...drainIfOnline())`. It must
         run before the method's `if (!mounted) return;` at `:737` so a save that completes
         while a toast is settling still propagates. The ObjectBox write has already
         committed by the time either caller reaches this tail (`saveNewLocalTrail` /
         `updateLocalTrail` are both synchronous and complete before the `await`), so
         there is no ordering hazard. If the screen is already unmounted the `ref` is dead
         and nothing can be invalidated — accepted, and not reachable from `_onSave`, which
         awaits this tail while mounted.
       - `_saveViaNetwork`: immediately after the successful `setState` at `:601-605` that
         assigns `trail = result.trail`, inside the same `try` (i.e. only on success). The
         `if (!mounted) return;` on `:600` already guards it.

    4. Extend (do not replace) `app/test/routes/trail_create_screen_local_save_gate_test.dart`
       with three assertions, reusing the file's existing `source` string and its
       comment-stripping approach:

       a. `_finishLocalSave`'s body contains `_invalidateOwnTrailsList()`.
       b. `_saveViaNetwork`'s body contains `_invalidateOwnTrailsList()`.
       c. `_invalidateOwnTrailsList`'s body contains both `invalidate(trailLibraryProvider)`
          and `invalidate(` + `profileTrailsProvider(`.

       Slice each method body the way the file already does: find the method signature's
       index, then find the next `'\n  }\n'` from there. Generalise the existing
       `onSaveEnd` helper into a reusable `int methodEnd(int start)` (or
       `String methodBody(String signature)`) rather than copying the index arithmetic
       three more times — keep `onSaveEnd`'s doc comment, which records why anchoring on
       the next member was wrong.

       Extend the file's header comment with the new invariant (3), and state its scope
       honestly: this gate asserts call-site PLACEMENT only, not that the invalidation
       propagates. `trail_create_screen.dart` needs auth, a router, a live ObjectBox store,
       an image picker and a map controller to drive, and `flutter test` cannot load
       `libobjectbox.dylib` at all, so the propagation itself is verified on device (see
       `<verification>`). Note explicitly how this differs from the failure mode UAT
       flagged in `trail_dropdown_delete_gate_test.dart`: that gate described the shape of
       UI nothing could reach, whereas this one pins placement on a code path whose
       reachability is proven by UAT Test 1 and Test 2 both passing.
  </action>
  <verify>
    <automated>cd app &amp;&amp; flutter analyze --no-pub lib/routes/trail_create_screen.dart &amp;&amp; flutter test test/routes/trail_create_screen_local_save_gate_test.dart &amp;&amp; grep -Ev '^[[:space:]]*//' lib/routes/trail_create_screen.dart | grep -c '_invalidateOwnTrailsList()'</automated>
  </verify>
  <done>`flutter analyze` reports zero errors, every assertion in the gate test passes (three new, none pre-existing removed), and the grep returns 3 — one declaration plus two call sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| account A -> account B (ObjectBox rows survive logout) | The invalidated list re-reads owner-scoped rows for whoever is signed in *now* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10-01 | Information disclosure | `_invalidateOwnTrailsList` deriving the wrong account's handle | mitigate | The username is read fresh from `authProvider` inside the method at the moment of use (D-13), never captured into a field. The invalidated provider then re-derives `accountId`/`actorId` fresh in `_readOwnLocal`, which WR-06 already hardened — so even a stale handle would surface an empty list, not another account's rows. |
| T-36-10-02 | Spoofing | family-key drift making an invalidation a silent no-op | accept | The invalidation and `profile_screen.dart:44` (which produces the `/profile/<handle>/trails` route segment the screen is keyed on) spell the handle identically today, and this plan adds a third site with the same spelling as `trail_sync_provider.dart:303`/`:377`. A future edit to one spelling and not the others would silently break the refresh with no test failure. Accepted rather than mitigated: the extraction that would have prevented it (a single `ownTrailsHandle` producer plus a lib-wide gate) was dropped by explicit decision in favour of device verification. If this recurs, that extraction is the fix. |
| T-36-10-03 | Denial of Service | invalidation loop between save and drain | accept | `_finishLocalSave` already calls `drainIfOnline()`, whose success path invalidates the same pair. A second invalidation is idempotent — `ProfileTrailsNotifier.build` has no write side effects, and `TrailSync._drainOne` holds an in-flight guard, so the two cannot ping-pong. |
| T-36-10-04 | Repudiation | a structural gate passing green while the behaviour is broken | accept | The gate pins call-site placement only and cannot observe propagation; `flutter test` cannot mount this screen. The behaviour is verified on device per `<verification>`, on a path whose reachability UAT Tests 1 and 2 already prove. Scoped explicitly in the test file's header so no future reader mistakes green for working. |
| T-36-10-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan adds no package-manager dependency. |
</threat_model>

<verification>
Full-suite regression, run once after Task 1:

    cd app && flutter analyze --no-pub && flutter test

Device re-test — this is the PRIMARY signal for this plan, not a supplement. The automated
gate cannot observe propagation, so gap 3 is not closed until this passes:

1. In airplane mode, open an unsynced trail from `/profile/<handle>/trails`, change its
   title, save, pop back. The list row must show the new title with no pull-to-refresh,
   and without the list flashing a loading spinner.
2. Online, repeat on an already-uploaded trail (exercises `_saveViaNetwork`). Same
   expectation.
3. Confirm the own-trails list does not flash a spinner on either refresh — that would
   mean `asReload` semantics regressed and 36-09's fix is undone.
</verification>

<success_criteria>
- `_finishLocalSave` and `_saveViaNetwork` both call `_invalidateOwnTrailsList()`.
- `_invalidateOwnTrailsList` invalidates `trailLibraryProvider` and
  `profileTrailsProvider('@<username>')` — the same pair, spelled the same way, as
  `trail_sync_provider.dart:303`/`:377`.
- The extended gate test pins call-site placement and is scoped to that in its header.
- No new files under `app/lib/util/`; no `build_runner` run.
- `flutter analyze --no-pub` clean; `flutter test` green.
- Device re-test 1 and 2 pass with no manual reload and no spinner flash.
</success_criteria>

<output>
Create `.planning/phases/36-local-first-recording-automatic-upload/36-10-SUMMARY.md` when done.
</output>
