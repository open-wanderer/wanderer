---
phase: 36-local-first-recording-automatic-upload
plan: 10
type: execute
wave: 2
depends_on: ["36-09"]
gap_closure: true
autonomous: true
requirements: [REC-05, REC-06]
files_modified:
  - app/lib/util/own_trails_handle.dart
  - app/lib/util/own_trails_refresh.dart
  - app/lib/util/own_trail_count.dart
  - app/lib/provider/profile/profile_trails_provider.dart
  - app/lib/provider/trail/trail_sync_provider.dart
  - app/lib/components/trail/trail_dropdown.dart
  - app/lib/routes/profile_screen.dart
  - app/lib/routes/profile_share_screen.dart
  - app/lib/routes/settings_categories_screen.dart
  - app/lib/routes/settings_subcategories_screen.dart
  - app/lib/routes/trail_create_screen.dart
  - app/test/util/own_trails_handle_test.dart
  - app/test/util/own_trails_refresh_test.dart
  - app/test/routes/trail_create_screen_local_save_gate_test.dart

must_haves:
  truths:
    - "Saving an edit to an unsynced trail while offline updates the own-trails list on pop-back, with no pull-to-refresh"
    - "Saving an edit to an already-uploaded trail also updates the own-trails list"
    - "Every '@handle' string built for the SIGNED-IN account anywhere in app/lib comes from ownTrailsHandle -- including the profile_screen getter that produces the route segment the own-trails screen is keyed on -- so an invalidation and the screen it targets cannot drift apart"
    - "Invalidating the propagation function's targets provably rebuilds the exact profileTrailsProvider family instance the own-trails screen watches, asserted in a live ProviderContainer rather than by grep"
  artifacts:
    - path: "app/lib/util/own_trails_handle.dart"
      provides: "ownTrailsHandle(preferredUsername) -- the single producer of the signed-in account's '@handle'"
      contains: "String ownTrailsHandle"
    - path: "app/lib/util/own_trails_refresh.dart"
      provides: "ownTrailsInvalidationTargets(preferredUsername) -- the providers a local write must refresh, as data"
      contains: "ownTrailsInvalidationTargets"
    - path: "app/test/util/own_trails_handle_test.dart"
      provides: "Format assertion plus a lib-wide gate that no other call site builds the own-account handle inline"
      min_lines: 50
    - path: "app/test/util/own_trails_refresh_test.dart"
      provides: "ProviderContainer test proving the targets actually rebuild the provider the screen watches"
      min_lines: 60
  key_links:
    - from: "app/lib/routes/trail_create_screen.dart"
      to: "ownTrailsInvalidationTargets"
      via: "_invalidateOwnTrailsList() called from _finishLocalSave and _saveViaNetwork"
      pattern: "ownTrailsInvalidationTargets\\("
    - from: "app/lib/util/own_trails_refresh.dart"
      to: "profileTrailsProvider"
      via: "ownTrailsHandle-keyed family instance in the returned target list"
      pattern: "profileTrailsProvider\\(ownTrailsHandle\\("
    - from: "app/lib/routes/profile_screen.dart"
      to: "ownTrailsHandle"
      via: "_handle getter -- the producer of the /profile/<handle>/trails route segment"
      pattern: "ownTrailsHandle\\("
---

<objective>
Close UAT gap 3 (major): "After saving edits on a non synced trail the 'own trail' list
needs a manual reload before edits are shown."

`trail_create_screen.dart` contains zero `invalidate`/`refresh` calls. Its shared
local-first save tail `_finishLocalSave` (714-762) commits to ObjectBox, drains, toasts
and `setState`s its own `trail` field — and notifies nobody else. The app has no ObjectBox
`Query.watch()` streams anywhere, so a row mutation emits no signal at all, and the
own-trails list stays mounted beneath the pushed edit route (`maintainState: true`),
keeping its auto-dispose provider alive with pre-edit state. The three existing
`invalidate(profileTrailsProvider(...))` sites cover successful drain upload,
`deleteUnsynced` and server-side delete — none of which fires on an offline edit.

Purpose: REC-05 promises the hiker can edit an unsynced trail offline. The edit lands in
ObjectBox but is invisible until a manual pull-to-refresh, which reads as data loss.

Output: one canonical producer of the signed-in account's `@handle` used by every
own-account call site in `app/lib` (including the route-segment producer the own-trails
screen is ultimately keyed on), a propagation function that is real code rather than an
inlined method body, and both save tails wired to it.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-local-first-recording-automatic-upload/36-UAT.md
@.planning/debug/unsynced-trail-edit-not-reflected-in-list.md

@app/lib/routes/trail_create_screen.dart
@app/lib/provider/trail/trail_sync_provider.dart
@app/lib/provider/profile/profile_trails_provider.dart
@app/lib/routes/profile_screen.dart
@app/test/routes/trail_create_screen_local_save_gate_test.dart

Interface facts the executor needs (already verified, do not re-derive):

- There are exactly **ten** lines in `app/lib` that build or compare an `@handle` for the
  SIGNED-IN account. Task 1 enumerates all ten with their exact current text. The earlier
  claim that there were five was wrong: it counted only the lines that mention
  `profileTrailsProvider` on the same line, and missed the four producers that assign the
  string to a local `handle` variable first — including
  `profile_screen.dart:44`, which produces the `/profile/<handle>/trails` path segment
  that becomes `ProfileTrailScreen.handle` and therefore the family key the screen watches.
  That is precisely the reconciliation UAT gap 3's third `missing:` bullet asks for.
- The other ~20 `'@$...'` string literals in `app/lib` build display handles for an
  ARBITRARY actor (`actor.preferredUsername`, `author.preferredUsername`,
  `trail.expand!.author!...`) or a bare domain suffix (`"@${trail.domain}"` in
  `trail_list_item.dart:148` and `trail_card.dart:219`). Those are out of scope and the
  Task 1 gate is calibrated to exclude them — the exclusion rule was validated against the
  real corpus and produces exactly the ten in-scope lines and zero false positives.
- `_finishLocalSave(Store store, AppLocalizations l10n, int failedPhotoCount, {required String localId})`
  is the shared tail of BOTH local branches: `createLocal` (~451-506) and `updateLocal`
  (~508-569). Fixing it once covers first save and re-save.
- `_saveViaNetwork(AppLocalizations l10n, Trail updatedTrail, {required String authorId, required List<File> newPhotoFiles})`
  is the equally-silent server path, reached by `LocalSaveMode.networkUpdate` and by
  `LocalUpdateOutcome.alreadySynced`.
- `TrailCreateScreen`'s State is a `ConsumerState`, so `ref` is available; `authProvider`
  is not yet imported in this file — add it.
- `ref.invalidate` defaults to `asReload: false`, which riverpod treats as a *seamless
  refresh*; `AsyncValue.when`'s `skipLoadingOnRefresh` defaults to `true`. So these
  invalidations refresh the list without flashing a spinner — they do not reintroduce the
  defect 36-09 fixes.
- `ProviderOrFamily` is NOT exported by `flutter_riverpod.dart` or `riverpod.dart`. It is
  exported by `package:riverpod_annotation/riverpod_annotation.dart`, which is already a
  direct dependency (`pubspec.yaml:52`, `^4.0.2`). Import that in
  `own_trails_refresh.dart` and in its test.
- `$ClassProvider.overrideWith(NotifierT Function() create)` (riverpod 3.2.1,
  `notifier_provider.dart:425`) is available on BOTH `trailLibraryProvider` (a
  `$NotifierProvider`) and `profileTrailsProvider('<key>')` (an `$AsyncNotifierProvider`
  family instance). Do NOT use `trailLibraryProvider.overrideWithValue(...)` for the
  container test — that swaps in a `$SyncValueProvider` constant, which never rebuilds and
  would make the test vacuous.
- `ProfileTrailsNotifierProvider` overrides `operator ==` on its `argument`
  (`profile_trails_provider.g.dart:42-49`), so `profileTrailsProvider('@tester')` compares
  equal across calls. That is what makes the container test's key-drift assertion sound.
- `profile_screen.dart`'s three consumers of `_handle` already null-check it
  (`:65` `if (h == null) return;`, `:139`/`:223` `if (h != null)`, `:509`/`:520`
  `handle != null ? ...`). Today the getter can never return null for the own-profile case
  — it returns the string `"@null"` when there is no signed-in user. Returning a real
  `null` instead is strictly safer and needs no consumer changes.
- D-13: the account must be read fresh at the moment of use, never from a cached field.

Verification conventions for this plan:

- Comment stripping in shell greps uses POSIX `grep -Ev '^[[:space:]]*//'`, not
  `grep -v '^\s*//'`. Adopted for portability; see 36-09-PLAN's `<context>` for the full
  note, including the recorded disagreement with the premise that `\s` is broken on macOS
  (it is not, on either `/usr/bin/grep` or the shell's `ugrep`). The Dart-side
  `RegExp(r'^\s*//')` in the gate tests is unaffected and stays.
- This plan runs NO codegen, even though it edits three `@riverpod`-annotated sources
  (`profile_trails_provider.dart`, `trail_sync_provider.dart` — and the generated source
  hashes for those go stale as a result). That is deliberate: `build_runner` regenerates
  every `.g.dart` in the package under an exclusive lock, and 36-11 runs concurrently in
  this same wave. 36-13's final task reconciles the whole tree once. Do not add a
  `build_runner` invocation here.

Task sizing note: Task 1 touches nine lib files, which normally signals a split. It is
kept as one task because every edit is a single-line substitution whose exact before and
after text is spelled out below — there is no exploration, and the lib-wide gate can only
go green once ALL ten sites are switched, so splitting would leave a task that cannot
verify itself.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One canonical producer of the signed-in account's handle</name>
  <files>app/lib/util/own_trails_handle.dart, app/lib/provider/profile/profile_trails_provider.dart, app/lib/provider/trail/trail_sync_provider.dart, app/lib/components/trail/trail_dropdown.dart, app/lib/routes/profile_screen.dart, app/lib/routes/profile_share_screen.dart, app/lib/routes/settings_categories_screen.dart, app/lib/routes/settings_subcategories_screen.dart, app/lib/util/own_trail_count.dart, app/test/util/own_trails_handle_test.dart</files>
  <behavior>
    - `ownTrailsHandle('tester')` returns exactly `'@tester'`.
    - `ownTrailsHandle('')` returns `'@'` (no special-casing; the caller is responsible
      for having a signed-in user).
    - Lib-wide gate: over every `.dart` file under `app/lib` except
      `util/own_trails_handle.dart`, with lines matching `RegExp(r'^\s*//')` stripped, the
      set of lines satisfying ALL THREE of

          a) contains `@$`                                  (an '@' followed by an interpolation)
          b) contains `preferredUsername` OR `profileTrailsProvider`
          c) does NOT match `RegExp(r'(actor|author)[.!]')` (display handle for some other actor)

      is EMPTY. The gate must also assert the same emptiness for
      `RegExp(r"""['"]@['"]\s*\+""")` (the string-concatenation spelling, `'@' + name`).
  </behavior>
  <action>
    1. Create `app/lib/util/own_trails_handle.dart` with a single top-level function.
       It imports nothing — keep it dependency-free so `own_trails_refresh.dart` (Task 2)
       can import it without creating a cycle through `profile_trails_provider.dart`:

           /// The `@`-prefixed handle for the SIGNED-IN account.
           ///
           /// This one string is three things at once, and they must stay
           /// byte-identical or the app silently misbehaves:
           ///   1. the `<handle>` segment of `/profile/<handle>/trails`,
           ///   2. therefore `ProfileTrailScreen.handle`, therefore the
           ///      `profileTrailsProvider` family key the screen watches,
           ///   3. the `filterId` suffix `'profile_trail_<handle>'`.
           ///
           /// It was spelled out inline at TEN call sites -- three
           /// invalidations, two own-handle comparisons, four handle
           /// producers and one share URL -- and drift between any producer
           /// and any invalidation makes that invalidation a silent no-op
           /// with no error and no test failure. One function, so they
           /// cannot drift.
           ///
           /// NOT for display handles of other actors: those append a domain
           /// (`@name@example.org`) and are built from an `Actor`, not from
           /// the auth session.
           String ownTrailsHandle(String preferredUsername) => '@$preferredUsername';

    2. Switch all ten sites. Exact substitutions — do not go looking for others:

       | # | File:line | Replace | With |
       |---|-----------|---------|------|
       | 1 | `provider/trail/trail_sync_provider.dart:303` | `profileTrailsProvider('@${userEntity.preferredUsername}')` | `profileTrailsProvider(ownTrailsHandle(userEntity.preferredUsername))` |
       | 2 | `provider/trail/trail_sync_provider.dart:377` | same as #1 | same as #1 |
       | 3 | `components/trail/trail_dropdown.dart:318` | `profileTrailsProvider('@$handle')` | `profileTrailsProvider(ownTrailsHandle(handle))` |
       | 4 | `provider/profile/profile_trails_provider.dart:82` | `handle == '@$preferredUsername'` | `handle == ownTrailsHandle(preferredUsername)` |
       | 5 | `provider/profile/profile_trails_provider.dart:172` | `_handle != '@${user.preferredUsername}'` | `_handle != ownTrailsHandle(user.preferredUsername)` |
       | 6 | `routes/profile_screen.dart:43-44` | the `_handle` getter (see below) | see below |
       | 7 | `util/own_trail_count.dart:21` | `final handle = '@${user.preferredUsername}';` | `final handle = ownTrailsHandle(user.preferredUsername);` |
       | 8 | `routes/settings_categories_screen.dart:584` | `final handle = '@${ref.read(authProvider).value!.preferredUsername}';` | `final handle = ownTrailsHandle(ref.read(authProvider).value!.preferredUsername);` |
       | 9 | `routes/settings_subcategories_screen.dart:561` | same as #8 | same as #8 |
       | 10 | `routes/profile_share_screen.dart:28` | `'$serverUrl/profile/@${userEntity.preferredUsername}'` | `'$serverUrl/profile/${ownTrailsHandle(userEntity.preferredUsername)}'` |

       Note on #3: the local `handle` in `trail_dropdown.dart` is already the bare
       `preferredUsername` (assigned three lines above from
       `ref.read(authProvider).value?.preferredUsername`), NOT the `@`-prefixed form.
       Confirm that before editing.

       Note on #10: this is a share URL, not a provider family key, so it is not
       load-bearing for gap 3. It is included anyway so that the invariant is "the
       signed-in account's `@handle` has exactly one producer" with no exceptions — an
       invariant with a documented exception is one a future maintainer will widen.

       Site #6 in full. `profile_screen.dart:43-44` currently reads

           String? get _handle =>
               widget.handle ?? "@${ref.read(authProvider).value?.preferredUsername}";

       which interpolates a NULLABLE and therefore yields the literal string `"@null"`
       when nobody is signed in. Replace with:

           /// Resolves the handle to use for data providers.
           ///
           /// For a remote profile this is `widget.handle` verbatim. For the
           /// own profile it is `ownTrailsHandle` -- the same producer every
           /// own-trails invalidation uses, which is what makes
           /// `context.push('/profile/$handle/trails')` below land on the
           /// family key those invalidations target. Returns null rather than
           /// the old `"@null"` when there is no signed-in account; all three
           /// consumers already null-check.
           String? get _handle {
             final remote = widget.handle;
             if (remote != null) return remote;
             final preferredUsername =
                 ref.read(authProvider).value?.preferredUsername;
             return preferredUsername == null
                 ? null
                 : ownTrailsHandle(preferredUsername);
           }

       Add the `package:wanderer/util/own_trails_handle.dart` import to each of the nine
       edited files (check first — none of them has it yet).

    3. Create `app/test/util/own_trails_handle_test.dart` with the three behaviours above.
       Implement the lib-wide gate by walking `Directory('lib')` with
       `listSync(recursive: true)`, filtering to `.dart` files, skipping
       `own_trails_handle.dart`, and stripping lines matching `RegExp(r'^\s*//')` before
       applying the three-part predicate. On failure, report every offending
       `path:lineNumber: text` in the `reason:` so the fix is obvious.

       Head the file with a comment recording the calibration, because the exclusion rule
       is the part a future reader will distrust: predicate (c) exists because ~20 lines in
       `app/lib` legitimately build `@handle` display strings for an ARBITRARY actor
       (`profile_follow_screen`, `global_search_screen`, `wanderer_actor_search`,
       `list_detail_screen`, `trail_panel`, `comment_card`, `summit_log_card`), and
       predicate (b) exists because `trail_list_item.dart:148` and `trail_card.dart:219`
       build a bare domain suffix `"@${trail.domain}"`. Record that the predicate was run
       against the pre-fix tree and matched exactly the ten lines in the table above,
       with zero false positives — so a green gate means something.

       Record the one known blind spot honestly: the predicate is line-based, so a handle
       assembled across two lines (e.g. `'@' `+ newline + `'$name'`) would slip through.
       Dart string literals are atomic and `dart format` will not split one, so this is
       accepted rather than mitigated.

       Include the same working-directory reason string
       `trail_create_screen_local_save_gate_test.dart` uses ("must be run with `flutter
       test`'s working directory set to `app/`").
  </action>
  <verify>
    <automated>cd app &amp;&amp; flutter test test/util/own_trails_handle_test.dart &amp;&amp; flutter analyze --no-pub lib/util/own_trails_handle.dart lib/provider/profile/profile_trails_provider.dart lib/provider/trail/trail_sync_provider.dart lib/components/trail/trail_dropdown.dart lib/routes/profile_screen.dart lib/routes/profile_share_screen.dart lib/routes/settings_categories_screen.dart lib/routes/settings_subcategories_screen.dart lib/util/own_trail_count.dart</automated>
  </verify>
  <done>All tests pass — including the lib-wide gate finding zero inline own-account handles — and `flutter analyze` reports zero errors for all nine lib files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The propagation targets, as data and behaviourally tested</name>
  <files>app/lib/util/own_trails_refresh.dart, app/test/util/own_trails_refresh_test.dart</files>
  <behavior>
    `app/test/util/own_trails_refresh_test.dart` drives the real function in a real
    `ProviderContainer` with stub notifiers. No live Store, no widget pump.

    - **The invalidation actually lands.** Listen to `profileTrailsProvider('@tester')` and
      `trailLibraryProvider` (both overridden with build-counting stubs) so each has built
      once. Invalidate every element of `ownTrailsInvalidationTargets('tester')`, then read
      both providers again. Both build counts increment to 2. This is the behavioural
      assertion gap 3 actually needs: not "the string `_invalidateOwnTrailsList()` appears
      in the source" but "invalidating these targets rebuilds the provider the own-trails
      screen is watching".
    - **Key drift is caught.** `ownTrailsInvalidationTargets('tester')` contains
      `profileTrailsProvider('@tester')`; `ownTrailsInvalidationTargets('other')` does not.
      Sound because `ProfileTrailsNotifierProvider` compares by `argument`.
    - **Signed-out is not a crash and not a wrong key.** `ownTrailsInvalidationTargets(null)`
      equals `[trailLibraryProvider]` exactly — no profile entry, no `'@null'` key.
    - **The pair matches the drain's pair.** Assert the returned list length is 2 for a
      signed-in user, with a comment naming why: `trail_sync_provider` invalidates exactly
      `trailLibraryProvider` + `profileTrailsProvider(<own handle>)` after a successful
      drain, and a local save must produce the same refresh as an upload or the two paths
      diverge.
  </behavior>
  <action>
    Create `app/lib/util/own_trails_refresh.dart`:

        import 'package:riverpod_annotation/riverpod_annotation.dart';
        import 'package:wanderer/provider/profile/profile_trails_provider.dart';
        import 'package:wanderer/provider/trail/trail_library_provider.dart';
        import 'package:wanderer/util/own_trails_handle.dart';

        /// Every provider that must be refreshed after this device writes to
        /// one of the signed-in hiker's own trail rows.
        ///
        /// Returned as DATA rather than performed here so the propagation
        /// logic is drivable in a plain `ProviderContainer`: the screen that
        /// calls it (`trail_create_screen`) needs auth, a router, a live
        /// ObjectBox store, an image picker and a map controller to mount,
        /// and `flutter test` cannot load `libobjectbox.dylib` at all -- so
        /// an inlined method body could only ever be asserted by grepping
        /// source text, which is exactly the test shape UAT condemned.
        ///
        /// The local-first save path commits to ObjectBox, and this app has
        /// no ObjectBox `Query.watch()` streams anywhere, so an explicit
        /// invalidate is the ONLY propagation mechanism that exists. Without
        /// it the own-trails list -- which stays mounted beneath the pushed
        /// edit route (`maintainState: true`), keeping its auto-dispose
        /// provider alive -- holds its pre-edit snapshot until the hiker
        /// pull-to-refreshes, which reads as the edit having been lost.
        ///
        /// This is the same PAIR `trail_sync_provider` invalidates after a
        /// successful drain upload, keyed through [ownTrailsHandle] so the
        /// family key cannot drift from the one `profile_trail_screen`
        /// watches. Callers must pass a username read FRESH at the moment of
        /// use, never a cached field (D-13).
        ///
        /// Callers invalidate with riverpod's default `asReload: false`, i.e.
        /// a seamless refresh -- `AsyncValue.when`'s `skipLoadingOnRefresh`
        /// defaults to true, so the list updates without flashing a spinner.
        List<ProviderOrFamily> ownTrailsInvalidationTargets(
          String? preferredUsername,
        ) => [
          trailLibraryProvider,
          if (preferredUsername != null)
            profileTrailsProvider(ownTrailsHandle(preferredUsername)),
        ];

    Create `app/test/util/own_trails_refresh_test.dart` covering the four behaviours.

    Stub shape — do not try to construct real notifiers:

        class _StubProfileTrails extends ProfileTrailsNotifier {
          _StubProfileTrails(this._onBuild);
          final void Function() _onBuild;
          @override
          FutureOr<ProfileTrailsState> build(String handle) {
            _onBuild();
            return const ProfileTrailsState(
              trails: [], page: 1, perPage: 1, totalPages: 1,
            );
          }
        }

    and the same pattern for `TrailLibraryNotifier` returning `const <Trail>[]`.

    Wire them with `profileTrailsProvider('@tester').overrideWith(...)` and
    `trailLibraryProvider.overrideWith(...)` — NOT `overrideWithValue`, which installs a
    constant that never rebuilds and would make the whole test vacuous. Use
    `addTearDown(container.dispose)`.

    Both providers are auto-dispose, so hold subscriptions open for the duration:
    `container.listen(<provider>, (_, __) {})` before the first assertion, and keep the
    returned subscriptions alive until the end of the test.

    Head the file with a comment stating what it replaces: gap 3's truth is "saving an edit
    updates the list on pop-back", and the only automated signal for it used to be a source
    grep asserting a method-call string appeared in two method bodies. That is the same
    test shape as `trail_dropdown_delete_gate_test.dart`, which stayed green for a whole
    phase while the thing it described was unreachable. This file asserts the propagation
    itself; the remaining source gate (Task 3) asserts only the part no container can see —
    that the call sites exist inside the two save tails.
  </action>
  <verify>
    <automated>cd app &amp;&amp; flutter test test/util/own_trails_refresh_test.dart &amp;&amp; flutter analyze --no-pub lib/util/own_trails_refresh.dart</automated>
  </verify>
  <done>All four behavioural tests pass against a real `ProviderContainer`, and `flutter analyze` reports zero errors for the new file.</done>
</task>

<task type="auto">
  <name>Task 3: Both save tails notify the own-trails list</name>
  <files>app/lib/routes/trail_create_screen.dart, app/test/routes/trail_create_screen_local_save_gate_test.dart</files>
  <action>
    1. Add a private method to `_TrailCreateScreenState` (place it directly above
       `_finishLocalSave`):

           /// Notifies every other consumer of the row this screen just wrote.
           ///
           /// The WHAT lives in `ownTrailsInvalidationTargets` (and is tested
           /// against a real ProviderContainer there). All this method adds is
           /// the WHERE: a fresh `authProvider` read at the moment of use,
           /// never a cached field (D-13).
           void _invalidateOwnTrailsList() {
             final preferredUsername =
                 ref.read(authProvider).value?.preferredUsername;
             for (final target in ownTrailsInvalidationTargets(preferredUsername)) {
               ref.invalidate(target);
             }
           }

    2. Call it from both tails:

       - `_finishLocalSave`: as the first statement of the method body, guarded by
         `if (mounted)`. It must run before the method's existing `if (!mounted) return;`
         early-out, so a save that completes while the screen is being popped still
         propagates. The ObjectBox write has already committed by the time either caller
         reaches this tail (`saveNewLocalTrail` / `updateLocalTrail` are both synchronous
         and complete before the `await`), so there is no ordering hazard.
       - `_saveViaNetwork`: immediately after the successful `setState` that assigns
         `trail = result.trail`, inside the same `try` (i.e. only on success).

    3. Add the missing imports: `package:wanderer/provider/auth_provider.dart` and
       `package:wanderer/util/own_trails_refresh.dart`. Check first — auth may already be
       present. Note that `profileTrailsProvider` and `trailLibraryProvider` are
       deliberately NOT imported here: this screen names the intent, not the targets.

    4. Extend (do not replace) `app/test/routes/trail_create_screen_local_save_gate_test.dart`
       with four assertions, reusing the file's existing `source` string and its
       comment-stripping approach:

       a. `_finishLocalSave`'s body contains `_invalidateOwnTrailsList()`.
       b. `_saveViaNetwork`'s body contains `_invalidateOwnTrailsList()`.
       c. `_invalidateOwnTrailsList`'s body contains `ownTrailsInvalidationTargets(`
          and `ref.invalidate(`.
       d. `_invalidateOwnTrailsList`'s body contains neither the literal `'@$` nor
          `profileTrailsProvider` — the targets must come from the helper, never be
          rebuilt here.

       Slice each method body the way the file already does: find the method signature's
       index, then find the next `'\n  }\n'` from there (the existing `onSaveEnd` helper's
       technique — a closing brace at method indentation). Generalise `onSaveEnd` into a
       reusable `String methodBody(String signature)` helper rather than copying the index
       arithmetic four times.

       Update the file's header comment to state the NEW, narrower scope precisely. This
       gate no longer stands in for the propagation behaviour — `own_trails_refresh_test.dart`
       proves that against a real `ProviderContainer`. What remains here is the one fact no
       container test can observe: that the call sites exist inside these two specific
       method bodies. `trail_create_screen.dart` needs auth, a router, a live ObjectBox
       store, an image picker and a map controller to drive, and `flutter test` cannot load
       `libobjectbox.dylib` at all. State explicitly how this differs from the failure mode
       UAT flagged in `trail_dropdown_delete_gate_test.dart`: that gate asserted the shape
       of UI that was unreachable, whereas this one asserts call-site placement on a code
       path whose reachability is proven by UAT Test 1 and Test 2 both passing, and whose
       effect is proven by a separate behavioural test.
  </action>
  <verify>
    <automated>cd app &amp;&amp; flutter analyze --no-pub lib/routes/trail_create_screen.dart &amp;&amp; flutter test test/routes/trail_create_screen_local_save_gate_test.dart &amp;&amp; grep -Ev '^[[:space:]]*//' lib/routes/trail_create_screen.dart | grep -c '_invalidateOwnTrailsList()'</automated>
  </verify>
  <done>`flutter analyze` reports zero errors, every assertion in the gate test passes (four new, none pre-existing removed), and the grep returns 3 — one declaration plus two call sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| account A -> account B (ObjectBox rows survive logout) | The invalidated list re-reads owner-scoped rows for whoever is signed in *now* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10-01 | Information disclosure | `_invalidateOwnTrailsList` deriving the wrong account's handle | mitigate | The username is read fresh from `authProvider` inside the method at the moment of use (D-13), never captured into a field. The invalidated provider then re-derives `accountId`/`actorId` fresh in `_readOwnLocal`, which WR-06 already hardened — so even a stale handle would surface an empty list, not another account's rows. |
| T-36-10-02 | Spoofing | family-key drift making an invalidation a silent no-op | mitigate | `ownTrailsHandle` becomes the only producer of the signed-in account's `@handle` in `app/lib` — including `profile_screen.dart:44`, the producer of the route segment the own-trails screen is keyed on. Enforced two ways: a lib-wide gate calibrated against the real corpus (ten in-scope lines, zero false positives), and a `ProviderContainer` test that proves invalidating the targets rebuilds the exact family instance the screen watches. The previous plan's gate (`profileTrailsProvider('@`) could not see any of the four handle producers and would have gone green on a `profile_screen.dart:44` regression. |
| T-36-10-03 | Denial of Service | invalidation loop between save and drain | accept | `_finishLocalSave` already calls `drainIfOnline()`, whose success path invalidates the same pair. A second invalidation is idempotent — `ProfileTrailsNotifier.build` has no write side effects, and `TrailSync._drainOne` holds an in-flight guard, so the two cannot ping-pong. |
| T-36-10-04 | Repudiation | a structural gate passing green while the behaviour is broken | mitigate | The propagation logic is extracted to `ownTrailsInvalidationTargets` and asserted behaviourally in a live `ProviderContainer`. The residual source gate is scoped, in its own header, to call-site placement only — the single fact no container test can observe. |
| T-36-10-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan adds no package-manager dependency. |
</threat_model>

<verification>
Full-suite regression, run once after all three tasks:

    cd app && flutter analyze --no-pub && flutter test

Device re-test (feeds UAT Test 2, not run by this plan): in airplane mode, open an
unsynced trail from `/profile/<handle>/trails`, change its title, save, pop back. The list
row must show the new title with no pull-to-refresh. Then check the two surfaces whose
handle producers moved: tap the trail count card on your own profile (must still open the
own-trails list) and open Settings -> Categories -> toggle a category off (the pre-filtered
list must still open and be pre-filtered).
</verification>

<success_criteria>
- `ownTrailsHandle` exists and is the only producer of the signed-in account's `@handle`
  in `app/lib`, across all ten former inline sites.
- The lib-wide gate is calibrated (documented ten-match / zero-false-positive run) and green.
- `ownTrailsInvalidationTargets` exists and its effect is proven in a real
  `ProviderContainer`, not by grep.
- `_finishLocalSave` and `_saveViaNetwork` both call `_invalidateOwnTrailsList()`.
- The extended gate test pins call-site placement and is scoped to that in its header.
- `flutter analyze --no-pub` clean; `flutter test` green.
</success_criteria>

<output>
Create `.planning/phases/36-local-first-recording-automatic-upload/36-10-SUMMARY.md` when done.
</output>
