---
phase: 03-stats-sheet
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/lib/routes/navigation_screen.dart
  - app/lib/i18n/app_en.arb
  - app/lib/i18n/app_de.arb
autonomous: false
requirements: [STATS-01, STATS-02, STATS-03, STATS-04, STATS-05]
user_setup: []
must_haves:
  truths:
    - "A DraggableScrollableSheet is visible at the bottom of NavigationScreen and can be dragged between a collapsed and expanded snap point (STATS-01)"
    - "Collapsed sheet shows Time elapsed, Distance covered, and Elevation gain (STATS-02, STATS-04)"
    - "Expanded sheet additionally shows Elevation loss, Current speed, and Average speed (STATS-04, STATS-05)"
    - "A button row shows [Elevation profile | Pause/Resume | Exit]; Exit pops the route and Pause toggles navigationStatsProvider pause"
    - "The elevation-profile button switches the in-sheet PageView to the reused ElevationProfile widget; a back control returns to the stats page (STATS-01)"
    - "The old top-left X overlay exit button is removed from NavigationScreen"
    - "Stats values update live from the existing single GPS stream (D-13) — onPosition is called on navigationStatsProvider from the existing listener"
  artifacts:
    - path: "app/lib/routes/navigation_screen.dart"
      provides: "DraggableScrollableSheet + PageController-driven PageView (stats page + ElevationProfile page) + button row; stats fed from existing GPS listener"
      contains: "DraggableScrollableSheet"
    - path: "app/lib/i18n/app_en.arb"
      provides: "New stat/button labels (time, current_speed, pause, resume, exit_navigation)"
  key_links:
    - from: "app/lib/routes/navigation_screen.dart GPS listener (_sub)"
      to: "navigationStatsProvider(widget.response).notifier.onPosition"
      via: "method call inside existing _positionStream.listen callback (D-13 single stream)"
      pattern: "navigationStatsProvider.*onPosition"
    - from: "app/lib/routes/navigation_screen.dart PageView page 1"
      to: "ElevationProfile widget"
      via: "trailAsync.when data → ElevationProfile(trail:, gpx:)"
      pattern: "ElevationProfile\\("
    - from: "app/lib/routes/navigation_screen.dart"
      to: "navigationStatsProvider state"
      via: "ref.watch(navigationStatsProvider(widget.response))"
      pattern: "ref.watch\\(navigationStatsProvider"
---

<objective>
Wire the stats sheet into `NavigationScreen`: add a `DraggableScrollableSheet` to the outer `Stack`, a button-controlled `PageView` (page 0 = live stats, page 1 = the reused `ElevationProfile`), and the `[Elevation profile | Pause/Resume | Exit]` button row. Feed `navigationStatsProvider.onPosition(Position)` from the existing single GPS listener (D-13), watch the stats state to render values, remove the old top-left X overlay, and add the few missing i18n labels.

Purpose: Deliver the user-visible stats panel (STATS-01..05) using the Wave 1 provider/formatters as pure composition — no new chart code, no new GPS stream.

Output: Edited `navigation_screen.dart`, extended `app_en.arb`/`app_de.arb`, regenerated localizations.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stats-sheet/03-CONTEXT.md
@.planning/phases/03-stats-sheet/03-RESEARCH.md
@.planning/phases/03-stats-sheet/03-01-SUMMARY.md

# Files this plan edits / depends on:
@app/lib/routes/navigation_screen.dart
@app/lib/routes/trail_detail_map_screen.dart
@app/lib/components/trail/elevation_profile.dart
@app/lib/provider/navigation_stats_provider.dart
@app/lib/util/format_util.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n labels for stats sheet (en + de) and regenerate localizations</name>
  <files>app/lib/i18n/app_en.arb, app/lib/i18n/app_de.arb</files>
  <read_first>
    - app/lib/i18n/app_en.arb (existing keys; reuse distance, average_speed, elevation_gain, elevation_loss, elevation_profile, speed)
    - app/lib/i18n/app_de.arb (German parity — every new key must exist in both)
    - app/l10n.yaml (template-arb-file is app_en.arb; output is app_localizations.dart)
  </read_first>
  <action>
    Add ONLY the labels not already present. Already present (DO NOT re-add): distance, average_speed, elevation_gain, elevation_loss, elevation_profile, speed.
    Add to app_en.arb: "time": "Time", "current_speed": "Speed", "pause": "Pause", "resume": "Resume", "exit_navigation": "Exit". (Note: a generic "speed" key already exists meaning current GPS speed; reuse it for current speed if preferred and skip "current_speed" — pick one and use it consistently in Task 2.)
    Add the German equivalents to app_de.arb with matching keys: "time": "Zeit", "current_speed": "Tempo", "pause": "Pause", "resume": "Fortsetzen", "exit_navigation": "Beenden".
    Keep JSON valid (the .arb is a flat JSON object; preserve trailing-comma-free formatting and existing @-metadata convention if used).
    Run flutter gen-l10n (or flutter pub get which triggers gen) to regenerate app_localizations*.dart so the new getters compile.
  </action>
  <verify>
    <automated>cd app && flutter gen-l10n && grep -c '"pause"' lib/i18n/app_en.arb && grep -c '"pause"' lib/i18n/app_de.arb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"pause"' app/lib/i18n/app_en.arb` returns 1 and same for app_de.arb
    - `grep -c '"resume"' app/lib/i18n/app_en.arb` returns 1 and same for app_de.arb
    - `grep -c '"exit_navigation"' app/lib/i18n/app_en.arb` returns 1 and same for app_de.arb
    - en and de arb files have identical key sets for the newly added keys (no key present in one but missing in the other)
    - `cd app && flutter gen-l10n` exits 0 and app_localizations_en.dart contains a getter for each new key (e.g. `grep -c 'get pause' lib/i18n/app_localizations_en.dart` returns 1)
  </acceptance_criteria>
  <done>New stats/button labels exist in both en and de arb files, localizations regenerate cleanly, and getters are available for use in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add DraggableScrollableSheet + PageView stats UI to NavigationScreen, feed stats from GPS listener, remove old exit button</name>
  <files>app/lib/routes/navigation_screen.dart</files>
  <read_first>
    - app/lib/routes/navigation_screen.dart (FULL — the outer Stack, _positionStream listener at ~line 59, dispose at ~line 78, _buildExitButton at ~line 374, the bottom-right controls Positioned at ~line 205)
    - app/lib/routes/trail_detail_map_screen.dart lines 200-235 (DraggableScrollableSheet decoration + scrollController ListView pattern to copy)
    - app/lib/components/trail/elevation_profile.dart lines 13-29 (ElevationProfile constructor: required trail, required gpx, optional enableLineTouch)
    - app/lib/provider/navigation_stats_provider.dart (onPosition(Position) and togglePause() signatures; NavigationStats fields)
    - app/lib/util/format_util.dart (formatDistance, formatElevation, formatSpeed, formatElapsed)
    - .planning/phases/03-stats-sheet/03-RESEARCH.md (Pattern 2 sheet+PageView; Pitfalls 1 and 4 gesture/coverage)
  </read_first>
  <action>
    Feed stats from the SINGLE existing GPS stream (D-13). In the existing `_positionStream.listen` callback in initState (the one already calling navigationProvider.onPosition), add a call to `ref.read(navigationStatsProvider(widget.response).notifier).onPosition(pos)` passing the raw geolocator Position (the stats provider needs altitude+speed, so pass `pos`, NOT a LatLng). Do NOT add a second stream or a second listener.
    Add a `final PageController _pageController = PageController();` field and a `final DraggableScrollableController _sheetController = DraggableScrollableController();` field on _NavigationScreenState; dispose both in dispose() alongside the existing cleanup.
    In build(), add `final stats = ref.watch(navigationStatsProvider(widget.response));` next to the existing navState/trailAsync watches.
    Add a `DraggableScrollableSheet` as a direct child of the outer Stack (after the maneuver banner), NOT inside a full-screen wrapper (Pitfall 4 — sheet must only cover the bottom band so the map stays interactive). Use controller: _sheetController, initialChildSize 0.18, minChildSize 0.18, maxChildSize 0.45, snap: true, snapSizes const [0.18, 0.45] (A2 — tune if the map's bottom-right control column gets covered). Builder returns a Container with the canvasColor + top radius + shadow decoration copied from trail_detail_map_screen, child a ListView(controller: scrollController, physics: ClampingScrollPhysics) so vertical drag-to-expand works (Pitfall 1).
    ListView children: (a) a small centered drag handle; (b) a fixed-height SizedBox (e.g. height ~220) wrapping the PageView; (c) the button row.
    PageView(controller: _pageController, physics: const NeverScrollableScrollPhysics()) with two children:
      page 0 = stats content: a compact Row of three stat cells [Time → formatElapsed(stats.elapsed) labeled time; Distance → formatDistance(stats.distanceMeters) labeled distance; Elev. gain → formatElevation(stats.elevationGainMeters) labeled elevation_gain]; AND a second Row (the expanded stats) shown below for Elevation loss → formatElevation(stats.elevationLossMeters); Current speed → formatSpeed(stats.currentSpeedKmh); Average speed → formatSpeed(stats.averageSpeedKmh) labeled average_speed. Each stat cell: small subdued label above, value in TextStyle(fontSize: 24, fontWeight: FontWeight.bold) per CONTEXT specifics. The expanded row naturally becomes visible as the sheet expands (it is lower in the scrollable content); no conditional on snap extent is required, but a back-control/visual separation is fine.
      page 1 = elevation profile: trailAsync.when(data: (trail) => trail.expand?.gpx != null ? Column with a top-left back IconButton (FontAwesomeIcons.arrowLeft) calling `_pageController.animateToPage(0, duration: const Duration(milliseconds: 250), curve: Curves.easeInOut)` + ElevationProfile(trail: trail, gpx: trail.expand!.gpx!, enableLineTouch: false) : SizedBox.shrink(), loading: CircularProgressIndicator, error: SizedBox.shrink).
    Button row (always visible, below the PageView in the ListView): three items —
      Left: IconButton (FontAwesomeIcons.chartArea or mountainSun) → `_pageController.animateToPage(1, ...)` (elevation_profile).
      Center: a large FilledButton.icon (visually dominant per CONTEXT) showing pause vs resume — icon FontAwesomeIcons.pause/play, label localizations.pause / localizations.resume keyed on stats.isPaused; onPressed → `ref.read(navigationStatsProvider(widget.response).notifier).togglePause()`.
      Right: IconButton (FontAwesomeIcons.xmark) labeled exit_navigation → `context.pop()` (this replaces NAV-07's old overlay).
    REMOVE the old top-left exit overlay: delete the `SafeArea > Align(topLeft) > Padding > _buildExitButton(context)` block from the outer Stack (currently ~lines 259-267) AND delete the `_buildExitButton` helper method (~lines 372-392). Exit now lives only in the sheet button row.
    Keep untouched: the FlutterMap and all its children (including the bottom-right MapCompass + recenter Positioned), the maneuver banner, the follow/recenter logic, the navigationProvider wiring.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/navigation_screen.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DraggableScrollableSheet' app/lib/routes/navigation_screen.dart` returns at least 1 (STATS-01)
    - `grep -c 'NeverScrollableScrollPhysics' app/lib/routes/navigation_screen.dart` returns at least 1 (horizontal swipe disabled per CONTEXT)
    - `grep -c 'navigationStatsProvider' app/lib/routes/navigation_screen.dart` returns at least 2 (one notifier.onPosition feed, one ref.watch)
    - `grep -c 'ElevationProfile(' app/lib/routes/navigation_screen.dart` returns at least 1 (page 1 reuse)
    - `grep -c 'togglePause' app/lib/routes/navigation_screen.dart` returns at least 1
    - old exit overlay removed: `grep -v '^[[:space:]]*//' app/lib/routes/navigation_screen.dart | grep -c '_buildExitButton'` returns 0
    - single GPS stream preserved: `grep -c 'getPositionStream' app/lib/routes/navigation_screen.dart` returns exactly 1 (D-13 — no second stream added)
    - both _pageController and _sheetController are disposed: `grep -c '_pageController.dispose\|_sheetController.dispose' app/lib/routes/navigation_screen.dart` returns 2
    - `cd app && flutter analyze lib/routes/navigation_screen.dart` reports no errors
    - `cd app && flutter test` exits 0 (no regressions in existing tests)
  </acceptance_criteria>
  <done>NavigationScreen shows a draggable stats sheet (collapsed: time/distance/elev gain; expanded: + elev loss/current speed/avg speed) with a button-driven PageView to the reused ElevationProfile, a pause/resume button bound to the provider, and an exit button that pops the route. Stats are fed from the existing single GPS stream. The old top-left X overlay is gone. Analyze is clean and existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the stats sheet on device</name>
  <files>app/lib/routes/navigation_screen.dart</files>
  <what-built>The full stats sheet wired into NavigationScreen: a DraggableScrollableSheet with a two-page button-controlled PageView (live stats + reused elevation profile), a [Elevation profile | Pause/Resume | Exit] button row, live stat values fed from the existing GPS stream, and the old top-left exit button removed.</what-built>
  <action>
    Pause execution and ask the user to manually verify the stats sheet on a running device/emulator using the steps in how-to-verify. Do NOT auto-advance — this is a blocking gate. After the user responds, resume only when they reply "approved"; if they report issues, address them in navigation_screen.dart before re-presenting.
  </action>
  <how-to-verify>
    1. Run the app: `cd app && flutter run` on a device/emulator with location enabled (simulated GPS movement is fine).
    2. Open a trail and tap Navigate to reach the NavigationScreen.
    3. Confirm a bottom sheet is visible (collapsed) showing three stats: Time (counting up), Distance, Elevation gain. Confirm the map above the sheet is still pannable/zoomable and the bottom-right compass + recenter buttons are reachable (not covered by the collapsed sheet) — Pitfall 4.
    4. Drag the sheet up: confirm it snaps to the expanded size and now also shows Elevation loss, Current speed, and Average speed.
    5. Drag the sheet back down: confirm it snaps to collapsed.
    6. Tap the left Elevation-profile button: confirm the sheet content switches to the trail elevation chart, and the map behind it is still interactive. Tap the back arrow: confirm it returns to the stats page. Confirm horizontal swiping does NOT change pages (button-only).
    7. Tap the center Pause button: confirm the label toggles to Resume, the Time clock freezes, and Distance/Elevation stop increasing as simulated movement continues. Tap Resume: confirm Time resumes and there is no large jump in Distance/Elevation (Pitfall 6).
    8. Tap the right Exit button: confirm navigation closes and you return to the originating trail screen. Confirm there is NO longer a separate X button in the top-left corner.
    9. With simulated movement, confirm Distance and Elevation gain/loss increase plausibly (not wildly inflated while stationary — Pitfall 2) and Current/Average speed show sane km/h values (no NaN/negative — Pitfall 3).
  </how-to-verify>
  <verify>
    <human-check>User confirms the 9 verification steps pass on a running device.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any issues (e.g. sheet covers controls, swipe changes pages, stats inflate, pause jump, old exit button still present).</resume-signal>
  <done>User has run the app, walked through all 9 verification steps, and replied "approved" (or all reported issues were fixed and re-approved).</done>
</task>

</tasks>

<verification>
- `cd app && flutter analyze lib/routes/navigation_screen.dart` — no errors
- `cd app && flutter test` — existing test suite passes (no regressions)
- `cd app && flutter gen-l10n` — localizations regenerate with new keys
- D-13 honored: exactly one `getPositionStream` reference in navigation_screen.dart
- Old exit overlay + `_buildExitButton` helper removed
- Human verification checkpoint approved
</verification>

<success_criteria>
- DraggableScrollableSheet visible at bottom of NavigationScreen, draggable between collapsed (0.18) and expanded (0.45) snaps (STATS-01)
- Collapsed: Time, Distance, Elevation gain (STATS-02, STATS-04); Expanded adds Elevation loss, Current speed, Average speed (STATS-04, STATS-05); ETA/elapsed satisfies STATS-03 intent via Time elapsed + distance covered
- Button-driven PageView switches to the reused ElevationProfile and back (no horizontal swipe)
- Pause/Resume bound to navigationStatsProvider.togglePause(); Exit pops the route; old top-left X removed (NAV-07 consolidated)
- Stats fed from the single existing GPS stream (D-13); no second stream
- New i18n labels present in en + de; localizations regenerate
- Human verification checkpoint approved
</success_criteria>

<artifacts_this_phase_produces>
- `app/lib/routes/navigation_screen.dart` — stats sheet UI, PageView, button row, GPS-fed stats, old exit removed
- `app/lib/i18n/app_en.arb`, `app/lib/i18n/app_de.arb` (+ regenerated `app_localizations*.dart`) — new stat/button labels
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-stats-sheet/03-02-SUMMARY.md` when done. Record: final snap sizes chosen, whether "speed" or "current_speed" key was used for current speed, the PageView fixed height, and any UI tuning done after the human-verify checkpoint.
</output>
