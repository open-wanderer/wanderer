---
phase: 15-maplibre-core-trail-rendering-offline-parity
plan: 05
type: execute
wave: 4
depends_on: [15-04]
files_modified:
  - app/lib/components/map/trail_layer.dart
  - app/lib/components/base/wanderer_map.dart
autonomous: true
requirements: [TRAIL-01, TRAIL-02, TRAIL-03, TRAIL-04]
user_setup: []

must_haves:
  truths:
    - "The GPX track renders as a native 5px route-colored line (#3549bb, routeColor-overridable) over a 2px white casing (a 9px white line under a 5px colored line), line-cap/join round (TRAIL-01)"
    - "Directional arrows render statically along the track via a native symbol layer at zoom > 8, denser at higher zoom, rotated to the line — with NO animation loop (TRAIL-02, D-04/D-05)"
    - "Waypoints render as tappable WidgetLayer markers (32px) preserving the AnimatedScale 0.875→1.0 / 200ms / easeOutBack selection animation and the onWaypointTap callback (TRAIL-03)"
    - "Start and finish pins render as 28px WidgetLayer markers (green bullseye / red flag) nudged apart when within 36 screen pixels via MapController.toScreenLocations (TRAIL-04)"
  artifacts:
    - path: "app/lib/components/map/trail_layer.dart"
      provides: "Track/arrow style-layer builder (via StyleController) + waypoint/pin WidgetLayer markers; the dead AnimationController + _addArrowsAlongPath bearing loop deleted"
      min_lines: 80
  key_links:
    - from: "app/lib/components/base/wanderer_map.dart"
      to: "trail_layer.dart track/arrow style layers"
      via: "onStyleLoaded → addSource(GeoJsonSource trail) + addLayer(casing, route, arrows)"
      pattern: "addSource|addLayer|GeoJsonSource"
    - from: "app/lib/components/base/wanderer_map.dart"
      to: "trail_layer.dart waypoint/pin markers"
      via: "WidgetLayer(markers: ...) in children"
      pattern: "WidgetLayer"
    - from: "app/lib/components/map/trail_layer.dart"
      to: "MapController.toScreenLocations"
      via: "36px proximity nudge for start/finish"
      pattern: "toScreenLocations"
---

<objective>
Render the trail itself on the MapLibre `WandererMap` shell from 15-04: the GPX track as a native casing+route line pair (TRAIL-01), static directional arrows via a native symbol layer (TRAIL-02, D-04/D-05 — re-enable the currently-dead `showArrows = false` feature, statically, no animation), waypoints as tappable `WidgetLayer` markers preserving the `AnimatedScale` selection animation and `onWaypointTap` (TRAIL-03), and start/finish pins as `WidgetLayer` markers with the 36-screen-pixel proximity nudge (TRAIL-04). The rewrite is largely a DELETION: the `AnimationController` and the ~60-line `_addArrowsAlongPath` bearing loop collapse to one `SymbolStyleLayer` with `symbol-placement: line`; the `PolylineLayer` becomes two stacked `LineStyleLayer`s; only the interactive markers stay as Flutter widgets.

Track/arrow layers are added imperatively via the `StyleController.addSource`/`addLayer` provided in `onStyleLoaded` (they are `Source`/`StyleLayer`, not the declarative annotation `Layer` helpers); markers go through `WidgetLayer` in `children`.

Purpose: Complete the online trail render — success-criterion #1/#2 of the phase — on top of 15-04's basemap shell.
Output: rewritten `trail_layer.dart` (helpers) wired into `wanderer_map.dart`.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-CONTEXT.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-RESEARCH.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-PATTERNS.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-UI-SPEC.md
@app/lib/components/map/trail_layer.dart
@app/lib/components/base/wanderer_map.dart
@app/lib/models/trail.dart
@app/lib/models/waypoint.dart
</context>

<artifacts_this_phase_produces>
New / reshaped symbols in THIS plan (exact API is executor's discretion, but the split is fixed):
- `trail_layer.dart` — a track/arrow style-layer builder that, given a `ml.StyleController` + `Trail` + `routeColor`, calls `addSource(GeoJsonSource(id:'trail', data: <GeoJSON LineString of gpx.allPoints>))` then `addLayer(LineStyleLayer 'trail-casing')`, `addLayer(LineStyleLayer 'trail-route')`, `addLayer(SymbolStyleLayer 'trail-arrows')`
- `trail_layer.dart` — a marker builder returning the waypoint + start/finish `ml.Marker` list for a `ml.WidgetLayer`
- Deleted: `AnimationController`, `_addArrowsAlongPath`, `SphericalGreatCircle`/`initialBearingTo` bearing math, `PolylineLayer`/`MarkerLayer`, `flutter_map`/`map_coordinate_adapter` imports in `trail_layer.dart`
- `_buildCircularMarker` (the 32/28px circular marker widget) ports verbatim (font_awesome icons, shadow, border, selected/unselected states)

Consumes: 15-04's `ml.MapController`/`StyleController` seam (`// 15-05: trail track + markers`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Track casing+route lines and static directional arrows as native style layers (TRAIL-01/02)</name>
  <files>app/lib/components/map/trail_layer.dart, app/lib/components/base/wanderer_map.dart</files>
  <read_first>
    - app/lib/components/map/trail_layer.dart — current full impl: `AnimationController` (39-48), `_addArrowsAlongPath` bearing loop (56-107), the zoom→meters spacing table (187-198), `showArrows = false` dead flag (200-201), `PolylineLayer`/`Polyline` (204-215), `AnimatedBuilder` arrow block (217-232) — all being deleted/replaced
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-RESEARCH.md § "Pattern 1 (track line with casing: casing #ffffff width 9 UNDER route #3549bb width 5, both round)", § "Pattern 6 (arrows: SymbolStyleLayer symbol-placement:line, icon-image:arrow, icon-rotation-alignment:map, minZoom 8, zoom-interpolated symbol-spacing)", § "Common Pitfalls → Pitfall 5 (meter→pixel spacing is approximate; tune on device — D-05 accepts this)"
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-UI-SPEC.md § "Track line (TRAIL-01)" and § "Directional arrows (TRAIL-02)" (exact px/color/zoom values + STATIC requirement)
    - app/lib/components/base/wanderer_map.dart — the 15-04 `onStyleLoaded` seam where the track source/layers get added, and the `StyleController` handle
    - ~/.pub-cache/hosted/pub.dev/maplibre_platform_interface-0.3.5/lib/src/style_controller.dart (`addSource`, `addLayer`, `addImage`, `addImageFromIconData`, `updateGeoJsonSource`, `removeLayer`, `removeSource`); .../style/sources/geo_json_source.dart (`GeoJsonSource(id:, data:)`); .../style/layers/line_style_layer.dart and symbol_style_layer.dart (`id`, `sourceId`, `layout`, `paint`, `minZoom`)
  </read_first>
  <action>
    In `trail_layer.dart`, replace the widget with a builder that, given the 15-04 `ml.StyleController`, `Trail`, and `routeColor` (default `Color(0xff3549bb)`), adds ONE `GeoJsonSource(id: 'trail', data: <GeoJSON Feature LineString built from gpx.allPoints — ml.Geographic lon/lat pairs>)`, then adds in order: (1) `LineStyleLayer(id: 'trail-casing', sourceId: 'trail', layout: {'line-cap':'round','line-join':'round'}, paint: {'line-color':'#ffffff','line-width':9})`; (2) `LineStyleLayer(id: 'trail-route', sourceId: 'trail', layout: {'line-cap':'round','line-join':'round'}, paint: {'line-color': <routeColor hex>, 'line-width':5})`; (3) `SymbolStyleLayer(id: 'trail-arrows', sourceId: 'trail', minZoom: 8, layout: {'symbol-placement':'line','icon-image':'arrow','icon-rotation-alignment':'map','icon-allow-overlap':true,'icon-ignore-placement':true,'symbol-spacing': <zoom-interpolated pixel expression, denser at higher zoom per UI-SPEC>})`. Casing MUST be added before route (order = draw order). Static only — do NOT create any `AnimationController` (D-05).

    The `arrow` icon comes from the style sprite (STYLE-04). If on-device verification shows the Protomaps sprite does not provide an `arrow` image, register one via `styleController.addImageFromIconData('arrow', ...)` before adding the arrows layer (a small white directional mark, ~6px equivalent, per UI-SPEC) — note this fallback in the SUMMARY.

    Wire this builder into `wanderer_map.dart`'s `onStyleLoaded` at the `// 15-05` seam, gated by `widget.showTrail && trail.expand?.gpx != null`. On a subsequent `setStyle` (theme swap) the style is rebuilt, so re-run the track-layer add after each style load (register it inside `onStyleLoaded`, which fires again after `setStyle`). DELETE the `AnimationController`, `_addArrowsAlongPath`, the bearing math, `PolylineLayer`, and the `flutter_map`/`map_coordinate_adapter` imports from `trail_layer.dart`.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/components/map/trail_layer.dart lib/components/base/wanderer_map.dart 2>&1 | grep -Eq "No issues found" && grep -q "trail-casing" lib/components/map/trail_layer.dart && grep -q "symbol-placement" lib/components/map/trail_layer.dart && ! grep -q "AnimationController" lib/components/map/trail_layer.dart && ! grep -q "_addArrowsAlongPath" lib/components/map/trail_layer.dart</automated>
  </verify>
  <acceptance_criteria>
    - The track renders as a `trail-casing` `LineStyleLayer` (#ffffff, width 9) UNDER a `trail-route` `LineStyleLayer` (routeColor, width 5), both round cap/join (TRAIL-01).
    - A single `trail-arrows` `SymbolStyleLayer` with `symbol-placement: line`, `minZoom: 8`, and a zoom-interpolated `symbol-spacing` renders static arrows (TRAIL-02); no `AnimationController` exists anywhere in `trail_layer.dart` (D-05).
    - Track layers are (re)added in `onStyleLoaded` so they survive the CORE-02 theme swap.
    - `_addArrowsAlongPath`, the bearing math, `PolylineLayer`, and `flutter_map` imports are deleted from `trail_layer.dart`; `flutter analyze` clean.
  </acceptance_criteria>
  <done>Track casing+route + static arrows render as native style layers; all dead arrow/bearing code is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Waypoint, start, and finish markers as interactive WidgetLayer markers (TRAIL-03/04)</name>
  <files>app/lib/components/map/trail_layer.dart, app/lib/components/base/wanderer_map.dart</files>
  <read_first>
    - app/lib/components/map/trail_layer.dart — waypoint marker block (120-142), start/finish pins + 36px nudge (145-184), `_buildCircularMarker` (241-267) to port verbatim
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-RESEARCH.md § "Pattern 7 (WidgetLayer + Marker allowInteraction:true; toScreenLocations for the 36px nudge; elevation marker allowInteraction:false)"
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-UI-SPEC.md § "Waypoint markers (TRAIL-03)" and § "Start / finish pins (TRAIL-04)" (32px/28px, colors, icons, AnimatedScale 0.875→1.0 200ms easeOutBack, nudge alignments)
    - ~/.pub-cache/hosted/pub.dev/maplibre-0.3.5/lib/src/widget_layer.dart (`WidgetLayer(allowInteraction:, markers:)`, `Marker(point:, size:, alignment:, child:)`); ~/.pub-cache/hosted/pub.dev/maplibre_platform_interface-0.3.5/lib/src/map_controller.dart:42 (`toScreenLocations(List<Geographic>) → List<Offset>`)
    - app/lib/models/waypoint.dart — `wp.lon`/`wp.lat`/`wp.icon`/`wp.id`
  </read_first>
  <action>
    Add a marker builder in `trail_layer.dart` returning the `List<ml.Marker>` for a single `ml.WidgetLayer(allowInteraction: true)` placed in `wanderer_map.dart`'s `children`. For each waypoint (when `showWaypoints` and `trail.expand?.waypointsViaTrail != null`): `ml.Marker(point: ml.Geographic(lon: wp.lon, lat: wp.lat), size: const Size(32,32), child: GestureDetector(onTap: () => onWaypointTap?.call(wp), child: AnimatedScale(scale: isSelected ? 1.0 : 0.875, duration: const Duration(milliseconds:200), curve: Curves.easeOutBack, child: _buildCircularMarker(wp.icon, color: Theme primaryColor, selected: isSelected))))` where `isSelected = selectedWaypoint?.id == wp.id` (TRAIL-03). Port `_buildCircularMarker` verbatim (selected → white bg / primaryColor icon+border / 16px icon; unselected → primaryColor bg / white icon+border / 14px icon; 2px border; black@20% blur-4 offset(0,2) shadow).

    For start/finish pins (TRAIL-04): a 28×28 `_buildCircularMarker(FontAwesomeIcons.bullseye, color: Colors.greenAccent)` at `gpx.allPoints.first` and a 28×28 `_buildCircularMarker(FontAwesomeIcons.flagCheckered, color: Colors.redAccent)` at `.last`. Compute the 36-pixel proximity nudge using `mapController.toScreenLocations([first, last])`: when `sqrt(dx²+dy²) < 36`, set start `Marker.alignment = Alignment(1,0)` and finish `Alignment(-1,0)`, else `Alignment.center` — replacing today's `camera.latLngToScreenOffset`. Since `toScreenLocations` needs the live controller, recompute the nudge on camera-move (or on build with the current controller) as the current code does per-frame.

    Wire the resulting `WidgetLayer` into `wanderer_map.dart` `children` (alongside the elevation/interim-location markers from 15-04). Delete the remaining `flutter_map` `Marker`/`MarkerLayer`/`Stack` scaffolding from `trail_layer.dart`.
  </action>
  <verify>
    <automated>cd app && flutter analyze 2>&1 | grep -Eq "No issues found" && grep -q "WidgetLayer" lib/components/base/wanderer_map.dart && grep -q "toScreenLocations" lib/components/map/trail_layer.dart && grep -q "easeOutBack" lib/components/map/trail_layer.dart && grep -q "flagCheckered" lib/components/map/trail_layer.dart</automated>
  </verify>
  <acceptance_criteria>
    - Waypoints render as 32px tappable `WidgetLayer` markers firing `onWaypointTap`, with the `AnimatedScale` 0.875→1.0 / 200ms / easeOutBack selection animation preserved (TRAIL-03).
    - Start (green bullseye) and finish (red flag) 28px pins render and nudge apart (`Alignment(1,0)`/`Alignment(-1,0)`) when within 36 screen pixels via `toScreenLocations`, else centered (TRAIL-04).
    - `_buildCircularMarker` visual spec is preserved verbatim (colors, border, shadow, icon sizes).
    - Whole-app `flutter analyze` clean; no `flutter_map` `MarkerLayer` remains in `trail_layer.dart`.
  </acceptance_criteria>
  <done>Waypoints, start, and finish markers render interactively via WidgetLayer with the selection animation and 36px nudge preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trail GPX data → GeoJSON source | Trail geometry (owner content) is serialized into a style source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | GeoJSON built from gpx.allPoints | accept | Geometry is the app's own parsed GPX (Phase 14 typed `ml.Geographic`), not free-form user string injected into the style structure; serialized via `jsonEncode`, not string concatenation |
| T-15-05-02 | DoS | very large tracks in one GeoJSON source | accept | Native GL renders line geometry on the GPU; `is_large` full-polyline rendering is explicitly deferred (FUT-01, STATE.md), so track sizes here match today's rendered set |
| T-15-05-SC | Tampering | package installs | accept | No new packages — `maplibre`, `font_awesome_flutter` already resolved (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- Track = casing (#fff, 9) under route (routeColor, 5), round caps; static arrows via `symbol-placement: line`, minZoom 8.
- Waypoints/pins render via `WidgetLayer`; selection animation + 36px nudge preserved.
- No `AnimationController`/`_addArrowsAlongPath`/`flutter_map` markers remain in `trail_layer.dart`; whole-app `flutter analyze` clean.
</verification>

<success_criteria>
TRAIL-01/02/03/04 satisfied: opening a trail shows the GPX track with white-cased route line, static directional arrows, tappable animated waypoints, and nudged start/finish pins — all on native GL over the 15-04 basemap shell.
</success_criteria>

<output>
Create `.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-05-SUMMARY.md` when done, recording the final `symbol-spacing` pixel values chosen and whether the `arrow` sprite icon resolved from the style or needed `addImageFromIconData`.
</output>
