---
phase: 15-maplibre-core-trail-rendering-offline-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/routes/spike_glyph_file_screen.dart
  - app/lib/util/spike_glyph_seed.dart
  - app/lib/main.dart
autonomous: false
requirements: [OFFL-04]
user_setup: []

must_haves:
  truths:
    - "A throwaway spike screen renders a MapLibreMap from a hand-built minimal style whose glyphs and sprite keys point at file:// paths under the app documents directory"
    - "The spike pre-seeds a few glyph ranges and the sprite files into the app documents directory while online, before the device goes offline"
    - "Christian runs the spike build on a PHYSICAL device in airplane mode and reports whether the text label renders (PASS) or is blank/tofu (FAIL) with the captured native log"
  artifacts:
    - path: "app/lib/routes/spike_glyph_file_screen.dart"
      provides: "Minimal MapLibreMap spike screen with a file:// glyph + sprite style and one symbol layer"
      min_lines: 40
    - path: "app/lib/util/spike_glyph_seed.dart"
      provides: "One-off pre-seed helper fetching glyph ranges + sprite files into app-docs before airplane mode"
      min_lines: 20
  key_links:
    - from: "app/lib/routes/spike_glyph_file_screen.dart"
      to: "getApplicationDocumentsDirectory()/spike_glyphs"
      via: "file:// glyph + sprite URLs in the style JSON"
      pattern: "file://"
    - from: "app/lib/util/spike_glyph_seed.dart"
      to: "mapStyleSourcesProvider.glyphUrl"
      via: "dio download of {fontstack}/{range}.pbf + sprite files"
      pattern: "glyphUrl|spriteUrl"
---

<objective>
Prove — or disprove — that MapLibre GL Native (Android 13.0 / iOS 6.25, via the `maplibre` 0.3.5 binding) resolves a `file://`-scheme glyph URL TEMPLATE and a `file://`-scheme sprite base at runtime, rendering place-name labels and sprite icons from files cached in the app documents directory, on a PHYSICAL device in airplane mode. This is the milestone's single highest-risk unknown (RESEARCH.md Assumptions A1/A2, ROADMAP.md "Risk gate", CONTEXT.md D-01/D-02/D-03) and it gates OFFL-04.

This plan is a THROWAWAY spike. It must NOT attempt real trail rendering, real style extraction, the full glyph cache, or the download flow — those are later plans (15-02..15-06), every one of which declares `depends_on` on this plan and none of which is safe to execute until this spike returns PASS.

Purpose: Convert RESEARCH.md A1/A2 from ASSUMED to VERIFIED (or capture the concrete failure mode for a fresh decision per D-03) before the phase invests in the real rewrite.
Output: A minimal spike screen + seed helper, and a human-verified PASS/FAIL verdict from Christian's physical device.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-CONTEXT.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-RESEARCH.md
@app/lib/provider/map_style_sources_provider.dart
@app/lib/services/trail_download_service.dart
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (all throwaway, deleted after the gate resolves):
- `app/lib/routes/spike_glyph_file_screen.dart` — `SpikeGlyphFileScreen` (ConsumerStatefulWidget)
- `app/lib/util/spike_glyph_seed.dart` — `seedSpikeGlyphCache()` top-level async function
- A debug-only navigation entry point to reach the spike screen (temporary route or button in `app/lib/main.dart` or the debug menu)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the throwaway file:// glyph + sprite spike screen and seed helper</name>
  <files>app/lib/routes/spike_glyph_file_screen.dart, app/lib/util/spike_glyph_seed.dart, app/lib/main.dart</files>
  <read_first>
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-RESEARCH.md section "Spike Design (Plan 1)" — the exact minimal build (steps 1-4), pass/fail signals, and the explicit "Do NOT include" list
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-CONTEXT.md section "Risk-gate spike" (D-01/D-02/D-03)
    - app/lib/provider/map_style_sources_provider.dart — the `mapStyleSourcesProvider` returning `{tileUrl, glyphUrl, spriteUrl}` (glyphUrl is the `{fontstack}/{range}.pbf` template; spriteUrl is the sprite base)
    - app/lib/services/trail_download_service.dart:32-33,158-178 — the `getApplicationDocumentsDirectory()` + `Directory.create(recursive:true)` + `File(path).exists()` idempotent + `_api.download(url, localPath)` convention to mirror for the seed fetch
    - ~/.pub-cache/hosted/pub.dev/maplibre_platform_interface-0.3.5/lib/src/widget/map.dart — `MapLibreMap(options, onStyleLoaded, children)` and `MapOptions(initStyle: <raw json string>)`
  </read_first>
  <action>
    Create `seedSpikeGlyphCache()` in `app/lib/util/spike_glyph_seed.dart`: resolve `getApplicationDocumentsDirectory()`, create `<docs>/spike_glyphs/`, read `mapStyleSourcesProvider` for `glyphUrl` and `spriteUrl`, and (while online) fetch a SMALL set of glyph ranges for `Noto Sans Regular` — at minimum `0-255.pbf` and `256-511.pbf` — into `<docs>/spike_glyphs/Noto Sans Regular/{range}.pbf`, plus `sprite.json`, `sprite.png`, and `sprite@2x.png` into `<docs>/spike_glyphs/sprite*`. Substitute the `{fontstack}` and `{range}` tokens in `glyphUrl` to build each fetch URL. Use the existing dio api client (mirror `trail_download_service.dart`'s `_api.download`); skip files already present. Do NOT sanitize-optimize or generalize — this is throwaway.

    Create `SpikeGlyphFileScreen` in `app/lib/routes/spike_glyph_file_screen.dart` as a `ConsumerStatefulWidget` whose `initState` calls `seedSpikeGlyphCache()` and, once complete, builds a single `MapLibreMap`. Hand-build a MINIMAL style JSON String (NOT the 7,677-line theme) containing: (a) one GeoJSON source holding a single Point Feature with a `name` property placed at a fixed lat/lon; (b) `"glyphs": "file://<docs>/spike_glyphs/{fontstack}/{range}.pbf"` using the absolute app-docs path resolved at runtime; (c) `"sprite": "file://<docs>/spike_glyphs/sprite"`; (d) one `symbol` layer with `text-field: ["get","name"]`, `text-font: ["Noto Sans Regular"]`, and `text-size: 20`. Pass this string to `MapOptions(initStyle:)`. No basemap tile source is required — a labelled point over a blank background is sufficient to prove glyph resolution. Optionally add a second symbol layer using `icon-image` from the sprite to prove A2 (sprite file:// resolution).

    Add a DEBUG-ONLY entry point to reach `SpikeGlyphFileScreen` (a temporary route registered behind `kDebugMode` in `app/lib/main.dart`, or a temporary debug button) so Christian can open the spike without a real trail. Clearly comment every spike file with `// SPIKE 15-01 — THROWAWAY, delete after the file:// glyph gate resolves`.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/spike_glyph_file_screen.dart lib/util/spike_glyph_seed.dart 2>&1 | grep -Eq "No issues found|^Analyzing" && grep -q "file://" lib/routes/spike_glyph_file_screen.dart && grep -q "spike_glyphs" lib/util/spike_glyph_seed.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` reports no errors for the two spike files.
    - The spike style JSON contains a top-level `glyphs` key with a `file://` value and a `symbol` layer using `text-font: ["Noto Sans Regular"]`.
    - `seedSpikeGlyphCache()` fetches at least `Noto Sans Regular/0-255.pbf` and the sprite files into `<app-docs>/spike_glyphs/` using the `mapStyleSourcesProvider` glyph/sprite URLs, and is idempotent (skips existing files).
    - A debug-only path to open `SpikeGlyphFileScreen` exists.
    - Every spike file carries the `// SPIKE 15-01 — THROWAWAY` comment.
  </acceptance_criteria>
  <done>Spike screen + seed helper compile, build a minimal file:// glyph/sprite style, pre-seed the cache online, and are reachable in debug builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Physical-device airplane-mode verification of file:// glyph + sprite resolution (Christian runs)</name>
  <what-built>A throwaway MapLibreMap spike screen that renders a place label (and optionally a sprite icon) from `file://`-scheme glyphs and sprite files pre-seeded into the app documents directory. This is the milestone's risk gate: nothing downstream in Phase 15 is safe to execute until it returns PASS on a physical device (D-01/D-02).</what-built>
  <read_first>
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-RESEARCH.md section "Spike Design" (Pass signal / Fail signals to capture) and section "Common Pitfalls -> Pitfall 1"
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-CONTEXT.md D-01/D-02/D-03
  </read_first>
  <how-to-verify>
    1. Build and run the app on a PHYSICAL iOS or Android device (NOT a simulator/emulator — D-01: simulators sandbox the filesystem differently and can give a false PASS).
    2. Open the app WITH network first and navigate to the debug spike screen so `seedSpikeGlyphCache()` completes and the glyph/sprite files land in the app documents directory.
    3. Enable AIRPLANE MODE (fully offline — no wifi, no cellular).
    4. Close and reopen the spike screen (or hot-restart the app while offline, then reopen).
    5. Observe the map:
       - PASS = the place-name text label renders correctly (real letters), and — if the sprite layer was added — the sprite icon renders.
       - FAIL = the label is blank, shows tofu/box glyphs, the style fails to load, OR it only worked because it silently reached the network.
    6. On FAIL: capture the native resource-load error — Android `adb logcat` (look for glyph/resource 404 or "unsupported scheme") or the iOS device console — and paste the exact log lines back. Do NOT pick a fallback direction; per D-03 the failure mode is returned to the developer for a fresh decision.
  </how-to-verify>
  <action>No autonomous action — this is a human-only physical-device gate (D-01/D-02). Hand the debug build to Christian, follow the steps in `<how-to-verify>`, and HALT: do not touch any downstream Phase 15 plan until the resume signal is received.</action>
  <verify>
    <automated>MISSING — human-only physical-device airplane-mode gate; verified by Christian's PASS/FAIL report per `<how-to-verify>` (Claude cannot access a physical device, D-02).</automated>
  </verify>
  <acceptance_criteria>
    - Christian reports an explicit PASS or FAIL for BOTH glyph labels (A1) and sprite icons (A2), tested on a physical device in airplane mode.
    - On PASS: plans 15-02..15-06 are cleared to execute; OFFL-02 (`file://` rewrite) is safe to build.
    - On FAIL: the captured native log is recorded in the SUMMARY and returned to the user; the phase STOPS and the offline-label strategy is re-decided (roadmap revision per ROADMAP.md Risk gate) before any further Phase 15 execution.
  </acceptance_criteria>
  <done>Christian reports an explicit PASS (glyphs + sprite render offline on a physical device) or FAIL with the captured native log.</done>
  <resume-signal>Reply "PASS" (glyphs + sprite render offline) or "FAIL: <captured native log>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> /map/style-sources | Operator-controlled glyph/sprite URLs enter the app (Phase 13 endpoint, defaults to Protomaps) |
| app -> remote glyph/sprite host | The seed fetch downloads `.pbf`/sprite bytes over TLS |
| constructed file:// paths -> local filesystem | Spike builds `file://` paths under the app documents directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | `file://` path built in the spike style | mitigate | Root every `file://` path at `getApplicationDocumentsDirectory()`; the fontstack is a single hard-coded literal ("Noto Sans Regular") and ranges are hard-coded — no external input reaches the path in this throwaway spike |
| T-15-01-02 | Info disclosure | seed fetch over network | accept | Uses the existing authenticated dio client + TLS to the Wanderer-controlled endpoint; throwaway, no new secrets |
| T-15-01-SC | Tampering | package installs | accept | No new packages — `maplibre` 0.3.5, `path_provider`, `dio` all already resolved in `pubspec.lock` (RESEARCH Package Legitimacy Audit); nothing to slopcheck |
</threat_model>

<verification>
- `flutter analyze` clean for the spike files.
- Spike style JSON has a `file://` `glyphs` key and a `Noto Sans Regular` symbol layer.
- Human gate: PASS/FAIL verdict recorded from a physical-device airplane-mode run (D-01/D-02).
</verification>

<success_criteria>
The risk gate is resolved: either A1/A2 are VERIFIED (labels + sprite render from `file://` offline on a physical device — plans 15-02..15-06 unblocked), or the concrete failure mode is captured and returned to the user for a fresh offline-label decision (D-03), halting the phase.
</success_criteria>

<output>
Create `.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-01-SUMMARY.md` when done. Record the PASS/FAIL verdict verbatim, and on FAIL include the captured native log and an explicit STOP recommendation.
</output>
