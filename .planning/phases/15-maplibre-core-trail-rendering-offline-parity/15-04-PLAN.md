---
phase: 15-maplibre-core-trail-rendering-offline-parity
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified:
  - app/lib/components/base/wanderer_map.dart
  - app/lib/routes/trail_detail_map_screen.dart
  - app/lib/components/trail/trail_panel.dart
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, TRAIL-05]
user_setup: []

must_haves:
  truths:
    - "WandererMap renders via MapLibreMap and preserves its public widget contract (trail, controls, disabled, offline, elevationMarkerPosition, showLocation, onTap, onMapEvent, onWaypointTap, selectedWaypoint) so its two call sites compile with only the mapController type changed (CORE-01)"
    - "The Protomaps basemap renders through native GL from mapStyleJsonProvider; changing the app light/dark theme swaps the style live via MapController.setStyle with no widget remount and no flash (CORE-02)"
    - "The initial camera fits trail.bounds with the caller's padding in onStyleLoaded, falling back to center-on-lat/lon zoom 18 when bounds are absent (CORE-03)"
    - "Every map shows a scale bar (bottom-left) and the Protomaps/OpenStreetMap source attribution (bottom-right) — the app's first-ever ODbL attribution (CORE-04)"
    - "trail_detail_map_screen still shows a location dot: WandererMap provides an interim location marker when showLocation is true, without the Phase-17 native follow/heading behavior (A5)"
    - "The elevation-profile scrub marker tracks elevationMarkerPosition as a 12px white/black WidgetLayer marker (TRAIL-05)"
  artifacts:
    - path: "app/lib/components/base/wanderer_map.dart"
      provides: "MapLibreMap-based WandererMap: style, camera fit, live theme swap, scalebar+attribution, controls, interim location, elevation marker"
      min_lines: 120
  key_links:
    - from: "app/lib/components/base/wanderer_map.dart"
      to: "mapStyleJsonProvider"
      via: "MapOptions.initStyle + MapController.setStyle on provider change"
      pattern: "mapStyleJsonProvider|setStyle"
    - from: "app/lib/components/base/wanderer_map.dart"
      to: "MapController.fitBounds"
      via: "onStyleLoaded fits trail.bounds with padding"
      pattern: "fitBounds"
    - from: "app/lib/components/base/wanderer_map.dart"
      to: "glyphSpriteCacheProvider"
      via: "ref.read(glyphSpriteCacheProvider.future) on first map open (D-09)"
      pattern: "glyphSpriteCacheProvider"
    - from: "app/lib/routes/trail_detail_map_screen.dart"
      to: "ml.MapController"
      via: "field type change + fitBounds replacing fitCamera/CameraFit.bounds"
      pattern: "ml.MapController|fitBounds"
---

<objective>
Rewrite `WandererMap` from `FlutterMap` to `MapLibreMap` (CORE-01), rendering the Protomaps basemap through native GL from `mapStyleJsonProvider` (15-02), with: live light/dark style swap via `MapController.setStyle` (CORE-02), initial camera fit to `trail.bounds` with the caller's padding in `onStyleLoaded` (CORE-03), the built-in `MapScalebar` + `SourceAttribution` controls for the app's first-ever ODbL attribution (CORE-04), the elevation-profile scrub marker as a `WidgetLayer` marker (TRAIL-05), and an interim location marker so `trail_detail_map_screen` (which passes `showLocation: true`) does not lose its location dot (A5 correction — the full native `enableLocation`/`trackLocation` puck is Phase-17 CORE-07). Update the two consumers for the `mapController` type change, wire the D-09 map-open glyph-cache trigger, and fix the pre-existing `onMapEvent` bug.

This plan does NOT yet draw the trail track/arrows/waypoints/pins — that is 15-05, which builds on this shell. At this plan's boundary the map renders the basemap + chrome + camera + elevation/location markers, and the app still builds with the 4 flutter_map screens untouched.

Purpose: Establish the MapLibre host that 15-05 (trail rendering) and 15-06 (offline) extend.
Output: `wanderer_map.dart` on `MapLibreMap`, two updated consumers.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-CONTEXT.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-RESEARCH.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-PATTERNS.md
@.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-UI-SPEC.md
@app/lib/components/base/wanderer_map.dart
@app/lib/routes/trail_detail_map_screen.dart
@app/lib/components/trail/trail_panel.dart
@app/lib/provider/foreground_position_stream_provider.dart
</context>

<artifacts_this_phase_produces>
Modified / newly-shaped symbols in THIS plan:
- `WandererMap` — same constructor params (CORE-01 contract preserved), `mapController` field retyped from `flutter_map` `MapController` to `ml.MapController`
- WandererMap internal: `onStyleLoaded` → `fitBounds`; `setStyle` live swap; `MapScalebar` + `SourceAttribution` children; interim-location `WidgetLayer` marker; elevation `WidgetLayer` marker
- `trail_detail_map_screen.dart`: `_mapController` retyped to `ml.MapController`; `_buildMapControls` fit uses `ml.MapController.fitBounds`
- `trail_panel.dart`: `disabled: true` → `MapGestures.none()`; `onTap` mapped to the new `onEvent`

Interfaces consumed (defined upstream): `mapStyleJsonProvider` (15-02), `glyphSpriteCacheProvider` (15-03).
Removed here: `MultiPmTilesVectorTileProvider`/`_initOffline`/`_buildTileLayer` and `map_coordinate_adapter` imports drop from `wanderer_map.dart` (native uses `ml.Geographic`/`ml.LngLatBounds` directly). The offline branch is re-added in 15-06.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite WandererMap onto MapLibreMap — style, camera, live theme swap, chrome, markers</name>
  <files>app/lib/components/base/wanderer_map.dart</files>
  <read_first>
    - app/lib/components/base/wanderer_map.dart — the full current widget: constructor params (16-48), offline provider init (55-95), style `.when` loading/error passthrough (101-112), initial camera fit (123-135), elevation marker (152-175), controls stack (177-183), and the `onMapEvent: (e) => widget.onMapEvent` bug (119)
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-RESEARCH.md § "Pattern 2 (live theme swap / setStyle)", § "Pattern 3 (fitBounds in onStyleLoaded)", § "Component Responsibilities" param→MapLibre table, § "Code Examples → Built-in controls (SourceAttribution, MapScalebar)"
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-PATTERNS.md § "components/base/wanderer_map.dart" (widget contract to preserve, loading/error to port verbatim, deletions, bug to fix)
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-UI-SPEC.md § "Camera (CORE-03)", § "Controls / attribution / scale bar (CORE-04)", § "Elevation-profile position marker (TRAIL-05)", § "Copywriting Contract" (loading/error copy ported verbatim)
    - app/lib/provider/foreground_position_stream_provider.dart — `foregroundPositionStreamProvider` emits `Stream<LocationMarkerPosition?>` (latitude/longitude/accuracy) for the interim location marker
    - ~/.pub-cache/hosted/pub.dev/maplibre_platform_interface-0.3.5/lib/src/map_controller.dart:70 (`fitBounds`), :172 (`setStyle`); ~/.pub-cache/hosted/pub.dev/maplibre_platform_interface-0.3.5/lib/src/widget/map.dart (`MapLibreMap` params `options`/`onMapCreated`/`onStyleLoaded`/`onEvent`/`children`); ~/.pub-cache/hosted/pub.dev/maplibre_platform_interface-0.3.5/lib/src/options/map_options.dart (`initStyle`, `gestures`, `MapGestures.none()`/`all()`, `initCenter`, `initZoom`)
  </read_first>
  <action>
    Rewrite `wanderer_map.dart` to build a `MapLibreMap`. Keep the WandererMap constructor param names/defaults verbatim (CORE-01 contract) except retype `mapController` to `ml.MapController?`. Watch `mapStyleJsonProvider` (15-02) and pass its String to `MapOptions(initStyle:)`; keep the `.when` loading (`ColoredBox(surface)`) / error (`Center(child: Text(e.toString()))`) passthrough exactly as today (UI-SPEC Copywriting Contract — no new copy). Capture the native `ml.MapController` via `onMapCreated` (or the provided controller) so `setStyle` and `fitBounds` can be called. On `themeMode`/`mapStyleJsonProvider` change, call `controller.setStyle(newJson)` in place — do NOT remount with `ObjectKey(style)` (RESEARCH Pattern 2 / Pitfall 3: CORE-02 requires no flash). Map `disabled` → `MapOptions(gestures: MapGestures.none())` else `MapGestures.all()`.

    In `onStyleLoaded`, if `trail.bounds` is non-null call `controller.fitBounds(bounds: trail.bounds!, padding: initialCameraFitPadding, nativeDuration: Duration.zero)` (instant initial fit, CORE-03); else `controller.moveCamera(center: ml.Geographic(lat: trail.lat ?? 0, lon: trail.lon ?? 0), zoom: 18)`. `trail.bounds` is already `ml.LngLatBounds` (Phase 14) — drop the `toLatLngBounds` adapter here.

    Add to `children`: `const MapScalebar()` (default bottom-left) and `const SourceAttribution()` (default bottom-right) for CORE-04 (the class is `SourceAttribution`, NOT `AttributionButton`); the caller `controls` `Column` at top-right (port verbatim); the elevation marker as a `WidgetLayer` with one non-interactive `Marker` (12×12, white fill, 2px black border, black@20% blur-4 offset(0,2) shadow) driven by `elevationMarkerPosition` (TRAIL-05); and — when `showLocation` is true — an interim location `WidgetLayer` marker driven by `foregroundPositionStreamProvider` (a simple puck; NO heading-up/follow — that is Phase-17 CORE-07). Wire `onEvent` to actually invoke `widget.onMapEvent` and route click events to `widget.onTap` (fixing the `(e) => widget.onMapEvent` bug at line 119).

    On first build, `ref.read(glyphSpriteCacheProvider.future)` to warm the shared cache on map open (D-09) — idempotent against the 15-03 download trigger. DELETE `MultiPmTilesVectorTileProvider`/`_offlineTileProvider`/`_initOffline`/`_buildTileLayer` and the `vector_map_tiles`/`pm_tile_provider`/`map_coordinate_adapter`/`flutter_map` imports from this file (the offline `pmtiles://file://` path is re-introduced declaratively in 15-06). Do NOT add the trail track/waypoint layers yet — leave a clearly-commented `// 15-05: trail track + markers wired here` seam and remove the `TrailLayer(...)` child and its import for now.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/components/base/wanderer_map.dart 2>&1 | grep -Eq "No issues found" && grep -q "MapLibreMap" lib/components/base/wanderer_map.dart && grep -q "fitBounds" lib/components/base/wanderer_map.dart && grep -q "SourceAttribution" lib/components/base/wanderer_map.dart && grep -q "setStyle" lib/components/base/wanderer_map.dart && ! grep -q "MultiPmTilesVectorTileProvider" lib/components/base/wanderer_map.dart</automated>
  </verify>
  <acceptance_criteria>
    - `wanderer_map.dart` builds a `MapLibreMap` fed by `mapStyleJsonProvider`; `flutter analyze` clean for the file.
    - Theme change calls `controller.setStyle(...)` (no `ObjectKey` remount); `onStyleLoaded` calls `fitBounds(bounds, padding, nativeDuration: Duration.zero)` with the moveCamera fallback.
    - `MapScalebar` + `SourceAttribution` are in `children`; the caller `controls` remain top-right.
    - The elevation marker (12px, TRAIL-05) and — when `showLocation` — an interim location marker render via `WidgetLayer`.
    - `onEvent` actually invokes `widget.onMapEvent`/`widget.onTap` (bug fixed).
    - `MultiPmTilesVectorTileProvider` and the `flutter_map`/`map_coordinate_adapter` imports are gone from this file; the map-open glyph-cache warm is wired.
  </acceptance_criteria>
  <done>WandererMap renders basemap + camera fit + live theme swap + scalebar/attribution + elevation/interim-location markers on MapLibreMap.</done>
</task>

<task type="auto">
  <name>Task 2: Update the two WandererMap consumers for the ml.MapController signature change</name>
  <files>app/lib/routes/trail_detail_map_screen.dart, app/lib/components/trail/trail_panel.dart</files>
  <read_first>
    - .planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-PATTERNS.md § "Consumer updates (signature-break, CORE-01)" — the exact lines to change in both consumers
    - app/lib/routes/trail_detail_map_screen.dart:39 (`final MapController _mapController = MapController();`), :82-102 (WandererMap construction with `showLocation: true`, `offline: trail.isOffline`), :261-266 (`_mapController.fitCamera(CameraFit.bounds(bounds: toLatLngBounds(bounds), padding: padding))`)
    - app/lib/components/trail/trail_panel.dart:249-255 (WandererMap with `disabled: true`, `offline: trail.isOffline`, `onTap:`)
    - ~/.pub-cache/hosted/pub.dev/maplibre_platform_interface-0.3.5/lib/src/map_controller.dart:70 — `fitBounds({required LngLatBounds bounds, EdgeInsets padding, ...})`
  </read_first>
  <action>
    In `trail_detail_map_screen.dart`: retype `_mapController` from `flutter_map` `MapController` to `ml.MapController` (construct the maplibre controller as its API requires — via the WandererMap `onMapCreated`/controller hand-off if the maplibre controller cannot be free-standing; if it must be created by the map, hold it as a nullable field set from the map). Replace the `_buildMapControls` expand button's `_mapController.fitCamera(CameraFit.bounds(bounds: toLatLngBounds(bounds), padding: padding))` with `_mapController.fitBounds(bounds: bounds, padding: padding)` — `bounds` is already `ml.LngLatBounds`, so drop `toLatLngBounds` and the `map_coordinate_adapter` import if now unused. Keep `showLocation: true` (the interim location marker from Task 1 serves it).

    In `trail_panel.dart`: WandererMap is constructed with `disabled: true` and `onTap:` and no `mapController` — confirm it still compiles against the new WandererMap contract; `disabled: true` now routes to `MapGestures.none()` inside WandererMap, and `onTap` maps to the new `onEvent`. Make only the minimal edits required to compile.

    Confirm the 4 not-yet-migrated screens (`list_detail_map_screen.dart`, `list_detail_screen.dart`, `map_screen.dart`, `navigation_screen.dart`) are NOT touched and still build on `flutter_map` (phase success criterion #5).
  </action>
  <verify>
    <automated>cd app && flutter analyze 2>&1 | grep -Eq "No issues found" && grep -q "ml.MapController" lib/routes/trail_detail_map_screen.dart && grep -q "fitBounds" lib/routes/trail_detail_map_screen.dart && ! grep -q "toLatLngBounds" lib/routes/trail_detail_map_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - Whole-app `flutter analyze` is clean (both consumers + the 4 untouched flutter_map screens compile).
    - `trail_detail_map_screen.dart` uses `ml.MapController` and `fitBounds(bounds, padding)` with no `toLatLngBounds`.
    - `trail_panel.dart` compiles with `disabled: true` → gestures-none and `onTap` → `onEvent`.
    - `git diff` shows no changes to `list_detail_map_screen.dart`, `list_detail_screen.dart`, `map_screen.dart`, `navigation_screen.dart`.
  </acceptance_criteria>
  <done>Both WandererMap consumers compile on the ml.MapController contract; the 4 flutter_map screens are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → mapStyleJsonProvider | Operator tile/glyph/sprite URLs already injected upstream (15-02) |
| device GPS → interim location marker | Foreground position stream drives an on-map puck |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | DoS | style swap on every theme toggle | mitigate | `setStyle` in place (no remount) bounds the work to a native style reload; provider is keepAlive so the JSON is not re-fetched per toggle |
| T-15-04-02 | Info disclosure | interim location marker exposes device position on-map | accept | Same foreground GPS stream already shown by today's `CurrentLocationLayer`; no new data leaves the device (RESEARCH § A5) |
| T-15-04-SC | Tampering | package installs | accept | No new packages — `maplibre` 0.3.5 already resolved (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `wanderer_map.dart` renders `MapLibreMap` with `setStyle` swap, `fitBounds`, `MapScalebar`+`SourceAttribution`, elevation + interim-location markers.
- Whole-app `flutter analyze` clean; the 4 flutter_map screens untouched.
- `MultiPmTilesVectorTileProvider` and `flutter_map`/`map_coordinate_adapter` imports removed from `wanderer_map.dart`.
</verification>

<success_criteria>
CORE-01/02/03/04 + TRAIL-05 satisfied at the shell level: WandererMap renders the basemap via native GL from the operator tile URL, swaps light/dark live with no flash, fits the trail bounds with padding, shows scale bar + ODbL attribution + elevation/interim-location markers, and keeps its widget contract so both consumers compile while the flutter_map screens keep working.
</success_criteria>

<output>
Create `.planning/phases/15-maplibre-core-trail-rendering-offline-parity/15-04-SUMMARY.md` when done, documenting the `ml.MapController` acquisition approach (onMapCreated vs. field) so 15-05/15-06 reuse it, and confirming the `// 15-05: trail track + markers` seam location.
</output>
