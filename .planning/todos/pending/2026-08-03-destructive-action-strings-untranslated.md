---
created: 2026-08-03T00:00:00.000Z
title: Human-translate the sync/destructive-action l10n backlog into 13 locales
area: app
resolves_phase: 36
files:
  - app/lib/i18n/app_cs.arb
  - app/lib/i18n/app_de.arb
  - app/lib/i18n/app_es.arb
  - app/lib/i18n/app_eu.arb
  - app/lib/i18n/app_fr.arb
  - app/lib/i18n/app_hu.arb
  - app/lib/i18n/app_it.arb
  - app/lib/i18n/app_nl.arb
  - app/lib/i18n/app_no.arb
  - app/lib/i18n/app_pl.arb
  - app/lib/i18n/app_pt.arb
  - app/lib/i18n/app_ru.arb
  - app/lib/i18n/app_zh.arb
  - app/lib/i18n/untranslated_messages.json
---

## Problem

`app/lib/i18n/untranslated_messages.json` lists strings that are English-only in 13 of the
app's 14 supported locales. Two of these are the copy shown on irreversible actions — a
non-English hiker sees English text on the one dialog that destroys data.

This is deliberately **not fixed** by this plan (36-19). `36-REVIEW.md` §WR-06 raised it, and
the reasoning stands: closing this gap needs real human translation into 13 languages, and no
autonomous agent can supply correct destructive-action copy in 13 languages. Machine
translation is explicitly out of scope — a machine translation that reads slightly wrong on an
irreversible action ("this cannot be undone") is worse than English a hiker can recognise as
untranslated, because English is at least honestly foreign rather than confidently wrong. The
app's existing fallback-to-English behaviour for a missing key means shipping is not blocked by
leaving this untranslated; it is blocked only from being silently forgotten, which is what this
todo prevents.

Note: the correctness half of the original WR-06 finding is already resolved (`b9ee66ca`) —
`_confirmDelete` now keys its dialog text on `trailHasServerId(trail.id)`, so a trail with a
server id no longer claims its deletion is device-only and irreversible. What remains is
coverage only: the (now-correct) English copy has no translation in 13 locales.

## Locales affected

cs, de, es, eu, fr, hu, it, nl, no, pl, pt, ru, zh (all supported locales except en).

`de` already carries translations for the `trail_source_*`/`library_empty_*` group below from
an earlier batch; it is missing the same 17 keys every other locale is missing. All 12 other
locales are missing all 24 keys.

## Key list and priority order

Regenerated from `app/lib/i18n/untranslated_messages.json` as of this todo's creation (after
this plan's Task 2 deleted the dead `retry_upload` key and re-ran `flutter gen-l10n`, and after
accounting for the destructive-action keys `36-15`/`36-16` added since `36-REVIEW.md` was
written). Phase 38's plan 38-02 added `remove_download_confirm_body` and
`edit_needs_connection` on 2026-08-04, English-only in all 13 non-English locales by decision
(D-17: machine translation is rejected for destructive-action copy), not by oversight.

**Priority 1 — copy on irreversible actions (translate first):**
1. `delete_unsynced_trail_confirm`
2. `signout_unsynced_warning`

**Priority 2 — destructive/blocking flow copy added by 36-15:**
3. `delete_blocked_while_uploading`
4. `trail_not_on_this_device`
5. `error_deleting_trail`
6. `delete_needs_connection`
7. `remove_download_confirm_body` — the un-download confirm body. Deliberately makes NO
   irreversibility claim (an un-download is recoverable — the trail itself is not deleted); do
   not "helpfully" add a "cannot be undone" phrase when translating.

**Priority 3 — copy added by 36-16:**
8. `trail_uploaded_reopen_to_edit`
9. `edit_needs_connection` — sibling edit-path refusal added by Phase 38, modelled on
   `delete_needs_connection`.

**Priority 4 — informational sync-status strings (non-destructive, lower urgency):**
10. `sync_pending`
11. `sync_uploading`
12. `sync_failed`
13. `photo_copy_failed_toast`
14. `own_trails_offline_banner`
15. `own_trails_empty_title`
16. `own_trails_empty_body`
17. `trails_on_device`

**Priority 5 — additional keys present in the current backlog, unrelated to sync/delete
(discovered while regenerating this list; not part of the original WR-06 finding but real
English-only strings, so tracked here rather than silently dropped):**
18. `trail_source_planner_description`
19. `trail_source_record_description`
20. `trail_source_import_description`
21. `trail_source_offline_import_error`
22. `library_empty_title`
23. `library_empty_body`
24. `library_empty_search_body`

## Fix

Ship real human translations for `signout_unsynced_warning` and
`delete_unsynced_trail_confirm` to every locale before release, then work down the priority
list. `app/lib/i18n/untranslated_messages.json` is the live, committed work list — it is
regenerated by `flutter gen-l10n` and shows up in a diff when it grows, which is exactly how
this backlog was caught growing unnoticed in the first place. Do not hand-edit it; run
`flutter gen-l10n` after adding real translations to the locale ARBs and let it shrink on its
own.

## Verification

- `app/lib/i18n/untranslated_messages.json` no longer lists `delete_unsynced_trail_confirm` or
  `signout_unsynced_warning` for any locale.
- Full closure: the file is empty (`{}`) or contains no entries for any of the 13 locales
  above.
- `flutter gen-l10n && flutter analyze --no-pub && flutter test` all pass after translations
  land.

## Context

See `.planning/phases/36-local-first-recording-automatic-upload/36-REVIEW.md` §WR-06 and
`.planning/phases/36-local-first-recording-automatic-upload/36-19-PLAN.md` (this todo's origin,
Task 2).
