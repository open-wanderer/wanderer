import 'package:flutter_test/flutter_test.dart';

void main() {
  test('settings language screen picker (LANG-01)', () {
    markTestSkipped(
      'MISSING — Plan 02 must implement 14-tile picker + selection-persists assertions',
    );
  });
}
